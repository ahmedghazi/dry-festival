import { i as __toESM } from "./rolldown-runtime-B-lAHAz2.js";
import { t as require_react } from "./react.js";
import { t as require_jsx_runtime } from "./react_jsx-runtime.js";
//#region node_modules/.pnpm/@sanity+telemetry@1.1.0_react@19.2.8/node_modules/@sanity/telemetry/dist/_chunks/createDeferredStore-5760b8dc.js
var import_react = /* @__PURE__ */ __toESM(require_react());
var import_jsx_runtime = require_jsx_runtime();
var capture = { capture: true };
function listen(target, type, cb) {
	target.addEventListener(type, cb, capture);
	return () => target.removeEventListener(type, cb, capture);
}
function onPageHide(listener) {
	if ("onpagehide" in window) return listen(window, "pagehide", listener);
	const cleanupUnload = listen(window, "unload", listener);
	const cleanupBeforeUnload = listen(window, "unload", listener);
	return () => {
		cleanupUnload();
		cleanupBeforeUnload();
	};
}
function onVisibilityHidden(cb) {
	return listen(document, "visibilitychange", () => {
		if (document.visibilityState === "hidden") cb();
	});
}
function createNoopLogger() {
	const logger = {
		updateUserProperties() {},
		trace,
		resume,
		log
	};
	function trace(telemetryTrace) {
		return {
			start() {},
			log(data) {},
			complete() {},
			newContext(name) {
				return logger;
			},
			error(error) {},
			await: (promise) => promise
		};
	}
	function log(event, data) {}
	function resume(events) {}
	return logger;
}
var noopLogger = createNoopLogger();
function createDeferredStore() {
	let buffer = [];
	const logger = {
		log(event, data) {
			buffer.push({
				createdAt: (/* @__PURE__ */ new Date()).toISOString(),
				event,
				data
			});
		},
		trace(event) {
			return {
				start() {},
				log() {},
				complete() {},
				error() {},
				newContext() {
					return logger;
				},
				await: (promise) => promise
			};
		},
		updateUserProperties() {},
		resume(events) {
			buffer.push(...events);
		}
	};
	function consumeEvents() {
		const events = buffer;
		buffer = [];
		return events;
	}
	return {
		logger,
		consumeEvents
	};
}
//#endregion
//#region node_modules/.pnpm/@sanity+telemetry@1.1.0_react@19.2.8/node_modules/@sanity/telemetry/dist/react/index.js
function useTelemetryStoreLifeCycleEvents(store) {
	(0, import_react.useEffect)(() => onVisibilityHidden(store.flush), [store.flush]);
	(0, import_react.useEffect)(() => onPageHide(store.endWithBeacon), [store.endWithBeacon]);
}
var DeferredEventsContext = (0, import_react.createContext)(null);
function useDeferredEvents() {
	return (0, import_react.useContext)(DeferredEventsContext);
}
function DeferredTelemetryProvider(_ref) {
	let { children } = _ref;
	const [deferredStore] = (0, import_react.useState)(() => createDeferredStore());
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(DeferredEventsContext.Provider, {
		value: deferredStore.consumeEvents,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(TelemetryLoggerContext.Provider, {
			value: deferredStore.logger,
			children
		})
	});
}
var TelemetryLoggerContext = (0, import_react.createContext)(noopLogger);
function TelemetryProvider(_ref2) {
	let { children, store } = _ref2;
	const consumeDeferredEvents = useDeferredEvents();
	(0, import_react.useEffect)(() => {
		if (consumeDeferredEvents) store.logger.resume(consumeDeferredEvents());
	}, [store, consumeDeferredEvents]);
	useTelemetryStoreLifeCycleEvents(store);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(TelemetryLoggerContext.Provider, {
		value: store.logger,
		children
	});
}
function useTelemetry() {
	return (0, import_react.useContext)(TelemetryLoggerContext);
}
//#endregion
export { TelemetryProvider as n, useTelemetry as r, DeferredTelemetryProvider as t };
