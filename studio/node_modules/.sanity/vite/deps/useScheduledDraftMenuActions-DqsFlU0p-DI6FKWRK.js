import { i as __toESM, t as __commonJSMin } from "./rolldown-runtime-B-lAHAz2.js";
import { t as require_react } from "./react.js";
import { t as require_compiler_runtime } from "./compiler-runtime-gazgokOh.js";
import { t as require_jsx_runtime } from "./react_jsx-runtime.js";
import { $ as Grid, At as TextInput, Ct as useClickOutsideEvent, Ht as Stack, Jt as LayerProvider, Kt as Box, Ut as Text, Vt as Flex, dn as ft, fn as gt, nt as Inline, o as Checkbox, qt as Card, tn as useLayer, x as Select } from "./dist-MhpRzy4S.js";
import { An as normalizeDates, Dn as isValid, En as format$1, Fn as getWeek, Hi as switchMap, In as startOfWeek, Kn as TZDate, Kt as useDocumentPreviewStore, Ln as toDate, Mn as parse, Pn as addDays, Q as getReleaseIdFromReleaseDocumentId, Sn as format, Yi as distinctUntilChanged, i as RELEASES_STUDIO_CLIENT_OPTIONS, kn as startOfDay, ta as reduce, tn as useSchema, tt as getVariantId, zn as constructFrom } from "./datastores-D4iZNSXV-CqhbcdwE.js";
import { r as useTranslation$1 } from "./useTranslation-Cqs6Itwv-DwNRrRK4.js";
import { t as Dialog$1 } from "./Dialog-wGP8Pfhq-DLQPsnhJ.js";
import { h as __assign } from "./BehaviorSubject-Bt_vJCli.js";
import { o as operate, t as map } from "./map-pLAxL0_r.js";
import { E as of, _ as finalize, h as shareReplay, l as useClient, m as startWith, o as DEFAULT_STUDIO_CLIENT_OPTIONS, t as useObservable, u as useSource, v as catchError, w as mergeInternals } from "./dist-DPi9e2VS.js";
import { T as EMPTY, h as useToast, p as defineEvent } from "./dist-B2gx505F.js";
import { t as filter } from "./filter-CWG4UsI5.js";
import { $ as isPausedCardinalityOneRelease, Et as useDocumentVersions, Nt as useReleaseOperations, O as createReleaseId, P as getBundleDocumentsObservable, Pt as useReleasesStore, V as getReleaseDocumentIdFromReleaseId, Z as isGoingToUnpublish, an as delay, ht as unstable_useValuePreview, in as isAgentBundleName, rt as isReleaseScheduledOrScheduling, t as Autocomplete, x as Preview, yt as useAllReleases } from "./autocomplete-Bd8zBMuI.js";
import { t as Button$1 } from "./Button-CGZ3ETwD-CQ5Daaas.js";
import { a as Popover$1, s as TooltipDelayGroupProvider$1, t as LoadingBlock } from "./LoadingBlock-Cy2Pckab-CaBiwwPj.js";
import { t as ChevronRightIcon } from "./ChevronRight-DdTArvZr.js";
import { b as getVersionFromId, v as getDraftId, x as getVersionId, y as getPublishedId } from "./draftUtils-D3HJGQaW-xgf4xoyc.js";
import { r as useTelemetry } from "./react-LEHxSjkU.js";
import { n as v4 } from "./partition-pdpBl5Pv.js";
import { t as Translate } from "./Translate-0HIWr1eF-BpZMq3Gv.js";
import { t as useCurrentLocale } from "./useLocale-ChRJXddd-BGcnWIcZ.js";
import { t as CalendarIcon } from "./Calendar-BejaREJU.js";
import { r as useTimeZone } from "./useTimeZone-DnxQlCGS-Nj-5hv3Z.js";
import { t as SearchIcon } from "./Search-DfKrAg0p.js";
import { t as TrashIcon } from "./Trash-EPEt-_oE.js";
import { t as PublishIcon } from "./Publish-DsDmema0.js";
import { t as ChevronLeftIcon } from "./ChevronLeft-BDiORTih.js";
import { t as EditIcon } from "./Edit-B9_1oTEB.js";
import { t as EarthGlobeIcon } from "./EarthGlobe-BhjJoKlT.js";
//#region node_modules/.pnpm/rxjs@7.8.2/node_modules/rxjs/dist/esm5/internal/operators/expand.js
function expand(project, concurrent, scheduler) {
	if (concurrent === void 0) concurrent = Infinity;
	concurrent = (concurrent || 0) < 1 ? Infinity : concurrent;
	return operate(function(source, subscriber) {
		return mergeInternals(source, subscriber, project, concurrent, void 0, true, scheduler);
	});
}
//#endregion
//#region node_modules/.pnpm/date-fns@4.4.0/node_modules/date-fns/isBefore.js
var import_react = /* @__PURE__ */ __toESM(require_react());
var import_compiler_runtime = require_compiler_runtime();
var import_jsx_runtime = require_jsx_runtime();
/**
* @name isBefore
* @category Common Helpers
* @summary Is the first date before the second one?
*
* @description
* Is the first date before the second one?
*
* @param date - The date that should be before the other one to return true
* @param dateToCompare - The date to compare with
*
* @returns The first date is before the second date
*
* @example
* // Is 10 July 1989 before 11 February 1987?
* const result = isBefore(new Date(1989, 6, 10), new Date(1987, 1, 11))
* //=> false
*/
function isBefore(date, dateToCompare) {
	return +toDate(date) < +toDate(dateToCompare);
}
//#endregion
//#region node_modules/.pnpm/@babel+runtime@7.29.7/node_modules/@babel/runtime/helpers/esm/objectWithoutPropertiesLoose.js
function _objectWithoutPropertiesLoose(r, e) {
	if (null == r) return {};
	var t = {};
	for (var n in r) if ({}.hasOwnProperty.call(r, n)) {
		if (-1 !== e.indexOf(n)) continue;
		t[n] = r[n];
	}
	return t;
}
//#endregion
//#region node_modules/.pnpm/@babel+runtime@7.29.7/node_modules/@babel/runtime/helpers/esm/extends.js
function _extends() {
	return _extends = Object.assign ? Object.assign.bind() : function(n) {
		for (var e = 1; e < arguments.length; e++) {
			var t = arguments[e];
			for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]);
		}
		return n;
	}, _extends.apply(null, arguments);
}
//#endregion
//#region node_modules/.pnpm/react-is@16.13.1/node_modules/react-is/cjs/react-is.development.js
/** @license React v16.13.1
* react-is.development.js
*
* Copyright (c) Facebook, Inc. and its affiliates.
*
* This source code is licensed under the MIT license found in the
* LICENSE file in the root directory of this source tree.
*/
var require_react_is_development = /* @__PURE__ */ __commonJSMin(((exports) => {
	(function() {
		"use strict";
		var hasSymbol = typeof Symbol === "function" && Symbol.for;
		var REACT_ELEMENT_TYPE = hasSymbol ? Symbol.for("react.element") : 60103;
		var REACT_PORTAL_TYPE = hasSymbol ? Symbol.for("react.portal") : 60106;
		var REACT_FRAGMENT_TYPE = hasSymbol ? Symbol.for("react.fragment") : 60107;
		var REACT_STRICT_MODE_TYPE = hasSymbol ? Symbol.for("react.strict_mode") : 60108;
		var REACT_PROFILER_TYPE = hasSymbol ? Symbol.for("react.profiler") : 60114;
		var REACT_PROVIDER_TYPE = hasSymbol ? Symbol.for("react.provider") : 60109;
		var REACT_CONTEXT_TYPE = hasSymbol ? Symbol.for("react.context") : 60110;
		var REACT_ASYNC_MODE_TYPE = hasSymbol ? Symbol.for("react.async_mode") : 60111;
		var REACT_CONCURRENT_MODE_TYPE = hasSymbol ? Symbol.for("react.concurrent_mode") : 60111;
		var REACT_FORWARD_REF_TYPE = hasSymbol ? Symbol.for("react.forward_ref") : 60112;
		var REACT_SUSPENSE_TYPE = hasSymbol ? Symbol.for("react.suspense") : 60113;
		var REACT_SUSPENSE_LIST_TYPE = hasSymbol ? Symbol.for("react.suspense_list") : 60120;
		var REACT_MEMO_TYPE = hasSymbol ? Symbol.for("react.memo") : 60115;
		var REACT_LAZY_TYPE = hasSymbol ? Symbol.for("react.lazy") : 60116;
		var REACT_BLOCK_TYPE = hasSymbol ? Symbol.for("react.block") : 60121;
		var REACT_FUNDAMENTAL_TYPE = hasSymbol ? Symbol.for("react.fundamental") : 60117;
		var REACT_RESPONDER_TYPE = hasSymbol ? Symbol.for("react.responder") : 60118;
		var REACT_SCOPE_TYPE = hasSymbol ? Symbol.for("react.scope") : 60119;
		function isValidElementType(type) {
			return typeof type === "string" || typeof type === "function" || type === REACT_FRAGMENT_TYPE || type === REACT_CONCURRENT_MODE_TYPE || type === REACT_PROFILER_TYPE || type === REACT_STRICT_MODE_TYPE || type === REACT_SUSPENSE_TYPE || type === REACT_SUSPENSE_LIST_TYPE || typeof type === "object" && type !== null && (type.$$typeof === REACT_LAZY_TYPE || type.$$typeof === REACT_MEMO_TYPE || type.$$typeof === REACT_PROVIDER_TYPE || type.$$typeof === REACT_CONTEXT_TYPE || type.$$typeof === REACT_FORWARD_REF_TYPE || type.$$typeof === REACT_FUNDAMENTAL_TYPE || type.$$typeof === REACT_RESPONDER_TYPE || type.$$typeof === REACT_SCOPE_TYPE || type.$$typeof === REACT_BLOCK_TYPE);
		}
		function typeOf(object) {
			if (typeof object === "object" && object !== null) {
				var $$typeof = object.$$typeof;
				switch ($$typeof) {
					case REACT_ELEMENT_TYPE:
						var type = object.type;
						switch (type) {
							case REACT_ASYNC_MODE_TYPE:
							case REACT_CONCURRENT_MODE_TYPE:
							case REACT_FRAGMENT_TYPE:
							case REACT_PROFILER_TYPE:
							case REACT_STRICT_MODE_TYPE:
							case REACT_SUSPENSE_TYPE: return type;
							default:
								var $$typeofType = type && type.$$typeof;
								switch ($$typeofType) {
									case REACT_CONTEXT_TYPE:
									case REACT_FORWARD_REF_TYPE:
									case REACT_LAZY_TYPE:
									case REACT_MEMO_TYPE:
									case REACT_PROVIDER_TYPE: return $$typeofType;
									default: return $$typeof;
								}
						}
					case REACT_PORTAL_TYPE: return $$typeof;
				}
			}
		}
		var AsyncMode = REACT_ASYNC_MODE_TYPE;
		var ConcurrentMode = REACT_CONCURRENT_MODE_TYPE;
		var ContextConsumer = REACT_CONTEXT_TYPE;
		var ContextProvider = REACT_PROVIDER_TYPE;
		var Element = REACT_ELEMENT_TYPE;
		var ForwardRef = REACT_FORWARD_REF_TYPE;
		var Fragment = REACT_FRAGMENT_TYPE;
		var Lazy = REACT_LAZY_TYPE;
		var Memo = REACT_MEMO_TYPE;
		var Portal = REACT_PORTAL_TYPE;
		var Profiler = REACT_PROFILER_TYPE;
		var StrictMode = REACT_STRICT_MODE_TYPE;
		var Suspense = REACT_SUSPENSE_TYPE;
		var hasWarnedAboutDeprecatedIsAsyncMode = false;
		function isAsyncMode(object) {
			if (!hasWarnedAboutDeprecatedIsAsyncMode) {
				hasWarnedAboutDeprecatedIsAsyncMode = true;
				console["warn"]("The ReactIs.isAsyncMode() alias has been deprecated, and will be removed in React 17+. Update your code to use ReactIs.isConcurrentMode() instead. It has the exact same API.");
			}
			return isConcurrentMode(object) || typeOf(object) === REACT_ASYNC_MODE_TYPE;
		}
		function isConcurrentMode(object) {
			return typeOf(object) === REACT_CONCURRENT_MODE_TYPE;
		}
		function isContextConsumer(object) {
			return typeOf(object) === REACT_CONTEXT_TYPE;
		}
		function isContextProvider(object) {
			return typeOf(object) === REACT_PROVIDER_TYPE;
		}
		function isElement(object) {
			return typeof object === "object" && object !== null && object.$$typeof === REACT_ELEMENT_TYPE;
		}
		function isForwardRef(object) {
			return typeOf(object) === REACT_FORWARD_REF_TYPE;
		}
		function isFragment(object) {
			return typeOf(object) === REACT_FRAGMENT_TYPE;
		}
		function isLazy(object) {
			return typeOf(object) === REACT_LAZY_TYPE;
		}
		function isMemo(object) {
			return typeOf(object) === REACT_MEMO_TYPE;
		}
		function isPortal(object) {
			return typeOf(object) === REACT_PORTAL_TYPE;
		}
		function isProfiler(object) {
			return typeOf(object) === REACT_PROFILER_TYPE;
		}
		function isStrictMode(object) {
			return typeOf(object) === REACT_STRICT_MODE_TYPE;
		}
		function isSuspense(object) {
			return typeOf(object) === REACT_SUSPENSE_TYPE;
		}
		exports.AsyncMode = AsyncMode;
		exports.ConcurrentMode = ConcurrentMode;
		exports.ContextConsumer = ContextConsumer;
		exports.ContextProvider = ContextProvider;
		exports.Element = Element;
		exports.ForwardRef = ForwardRef;
		exports.Fragment = Fragment;
		exports.Lazy = Lazy;
		exports.Memo = Memo;
		exports.Portal = Portal;
		exports.Profiler = Profiler;
		exports.StrictMode = StrictMode;
		exports.Suspense = Suspense;
		exports.isAsyncMode = isAsyncMode;
		exports.isConcurrentMode = isConcurrentMode;
		exports.isContextConsumer = isContextConsumer;
		exports.isContextProvider = isContextProvider;
		exports.isElement = isElement;
		exports.isForwardRef = isForwardRef;
		exports.isFragment = isFragment;
		exports.isLazy = isLazy;
		exports.isMemo = isMemo;
		exports.isPortal = isPortal;
		exports.isProfiler = isProfiler;
		exports.isStrictMode = isStrictMode;
		exports.isSuspense = isSuspense;
		exports.isValidElementType = isValidElementType;
		exports.typeOf = typeOf;
	})();
}));
//#endregion
//#region node_modules/.pnpm/react-is@16.13.1/node_modules/react-is/index.js
var require_react_is = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_react_is_development();
}));
//#endregion
//#region node_modules/.pnpm/object-assign@4.1.1/node_modules/object-assign/index.js
/*
object-assign
(c) Sindre Sorhus
@license MIT
*/
var require_object_assign = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var getOwnPropertySymbols = Object.getOwnPropertySymbols;
	var hasOwnProperty = Object.prototype.hasOwnProperty;
	var propIsEnumerable = Object.prototype.propertyIsEnumerable;
	function toObject(val) {
		if (val === null || val === void 0) throw new TypeError("Object.assign cannot be called with null or undefined");
		return Object(val);
	}
	function shouldUseNative() {
		try {
			if (!Object.assign) return false;
			var test1 = /* @__PURE__ */ new String("abc");
			test1[5] = "de";
			if (Object.getOwnPropertyNames(test1)[0] === "5") return false;
			var test2 = {};
			for (var i = 0; i < 10; i++) test2["_" + String.fromCharCode(i)] = i;
			if (Object.getOwnPropertyNames(test2).map(function(n) {
				return test2[n];
			}).join("") !== "0123456789") return false;
			var test3 = {};
			"abcdefghijklmnopqrst".split("").forEach(function(letter) {
				test3[letter] = letter;
			});
			if (Object.keys(Object.assign({}, test3)).join("") !== "abcdefghijklmnopqrst") return false;
			return true;
		} catch (err) {
			return false;
		}
	}
	module.exports = shouldUseNative() ? Object.assign : function(target, source) {
		var from;
		var to = toObject(target);
		var symbols;
		for (var s = 1; s < arguments.length; s++) {
			from = Object(arguments[s]);
			for (var key in from) if (hasOwnProperty.call(from, key)) to[key] = from[key];
			if (getOwnPropertySymbols) {
				symbols = getOwnPropertySymbols(from);
				for (var i = 0; i < symbols.length; i++) if (propIsEnumerable.call(from, symbols[i])) to[symbols[i]] = from[symbols[i]];
			}
		}
		return to;
	};
}));
//#endregion
//#region node_modules/.pnpm/prop-types@15.8.1/node_modules/prop-types/lib/ReactPropTypesSecret.js
/**
* Copyright (c) 2013-present, Facebook, Inc.
*
* This source code is licensed under the MIT license found in the
* LICENSE file in the root directory of this source tree.
*/
var require_ReactPropTypesSecret = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = "SECRET_DO_NOT_PASS_THIS_OR_YOU_WILL_BE_FIRED";
}));
//#endregion
//#region node_modules/.pnpm/prop-types@15.8.1/node_modules/prop-types/lib/has.js
var require_has = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = Function.call.bind(Object.prototype.hasOwnProperty);
}));
//#endregion
//#region node_modules/.pnpm/prop-types@15.8.1/node_modules/prop-types/checkPropTypes.js
/**
* Copyright (c) 2013-present, Facebook, Inc.
*
* This source code is licensed under the MIT license found in the
* LICENSE file in the root directory of this source tree.
*/
var require_checkPropTypes = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var printWarning = function() {};
	var ReactPropTypesSecret = require_ReactPropTypesSecret();
	var loggedTypeFailures = {};
	var has = require_has();
	printWarning = function(text) {
		var message = "Warning: " + text;
		if (typeof console !== "undefined") console.error(message);
		try {
			throw new Error(message);
		} catch (x) {}
	};
	/**
	* Assert that the values match with the type specs.
	* Error messages are memorized and will only be shown once.
	*
	* @param {object} typeSpecs Map of name to a ReactPropType
	* @param {object} values Runtime values that need to be type-checked
	* @param {string} location e.g. "prop", "context", "child context"
	* @param {string} componentName Name of the component for error messages.
	* @param {?Function} getStack Returns the component stack.
	* @private
	*/
	function checkPropTypes(typeSpecs, values, location, componentName, getStack) {
		for (var typeSpecName in typeSpecs) if (has(typeSpecs, typeSpecName)) {
			var error;
			try {
				if (typeof typeSpecs[typeSpecName] !== "function") {
					var err = Error((componentName || "React class") + ": " + location + " type `" + typeSpecName + "` is invalid; it must be a function, usually from the `prop-types` package, but received `" + typeof typeSpecs[typeSpecName] + "`.This often happens because of typos such as `PropTypes.function` instead of `PropTypes.func`.");
					err.name = "Invariant Violation";
					throw err;
				}
				error = typeSpecs[typeSpecName](values, typeSpecName, componentName, location, null, ReactPropTypesSecret);
			} catch (ex) {
				error = ex;
			}
			if (error && !(error instanceof Error)) printWarning((componentName || "React class") + ": type specification of " + location + " `" + typeSpecName + "` is invalid; the type checker function must return `null` or an `Error` but returned a " + typeof error + ". You may have forgotten to pass an argument to the type checker creator (arrayOf, instanceOf, objectOf, oneOf, oneOfType, and shape all require an argument).");
			if (error instanceof Error && !(error.message in loggedTypeFailures)) {
				loggedTypeFailures[error.message] = true;
				var stack = getStack ? getStack() : "";
				printWarning("Failed " + location + " type: " + error.message + (stack != null ? stack : ""));
			}
		}
	}
	/**
	* Resets warning cache when testing.
	*
	* @private
	*/
	checkPropTypes.resetWarningCache = function() {
		loggedTypeFailures = {};
	};
	module.exports = checkPropTypes;
}));
//#endregion
//#region node_modules/.pnpm/prop-types@15.8.1/node_modules/prop-types/factoryWithTypeCheckers.js
/**
* Copyright (c) 2013-present, Facebook, Inc.
*
* This source code is licensed under the MIT license found in the
* LICENSE file in the root directory of this source tree.
*/
var require_factoryWithTypeCheckers = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var ReactIs = require_react_is();
	var assign = require_object_assign();
	var ReactPropTypesSecret = require_ReactPropTypesSecret();
	var has = require_has();
	var checkPropTypes = require_checkPropTypes();
	var printWarning = function() {};
	printWarning = function(text) {
		var message = "Warning: " + text;
		if (typeof console !== "undefined") console.error(message);
		try {
			throw new Error(message);
		} catch (x) {}
	};
	function emptyFunctionThatReturnsNull() {
		return null;
	}
	module.exports = function(isValidElement, throwOnDirectAccess) {
		var ITERATOR_SYMBOL = typeof Symbol === "function" && Symbol.iterator;
		var FAUX_ITERATOR_SYMBOL = "@@iterator";
		/**
		* Returns the iterator method function contained on the iterable object.
		*
		* Be sure to invoke the function with the iterable as context:
		*
		*     var iteratorFn = getIteratorFn(myIterable);
		*     if (iteratorFn) {
		*       var iterator = iteratorFn.call(myIterable);
		*       ...
		*     }
		*
		* @param {?object} maybeIterable
		* @return {?function}
		*/
		function getIteratorFn(maybeIterable) {
			var iteratorFn = maybeIterable && (ITERATOR_SYMBOL && maybeIterable[ITERATOR_SYMBOL] || maybeIterable[FAUX_ITERATOR_SYMBOL]);
			if (typeof iteratorFn === "function") return iteratorFn;
		}
		/**
		* Collection of methods that allow declaration and validation of props that are
		* supplied to React components. Example usage:
		*
		*   var Props = require('ReactPropTypes');
		*   var MyArticle = React.createClass({
		*     propTypes: {
		*       // An optional string prop named "description".
		*       description: Props.string,
		*
		*       // A required enum prop named "category".
		*       category: Props.oneOf(['News','Photos']).isRequired,
		*
		*       // A prop named "dialog" that requires an instance of Dialog.
		*       dialog: Props.instanceOf(Dialog).isRequired
		*     },
		*     render: function() { ... }
		*   });
		*
		* A more formal specification of how these methods are used:
		*
		*   type := array|bool|func|object|number|string|oneOf([...])|instanceOf(...)
		*   decl := ReactPropTypes.{type}(.isRequired)?
		*
		* Each and every declaration produces a function with the same signature. This
		* allows the creation of custom validation functions. For example:
		*
		*  var MyLink = React.createClass({
		*    propTypes: {
		*      // An optional string or URI prop named "href".
		*      href: function(props, propName, componentName) {
		*        var propValue = props[propName];
		*        if (propValue != null && typeof propValue !== 'string' &&
		*            !(propValue instanceof URI)) {
		*          return new Error(
		*            'Expected a string or an URI for ' + propName + ' in ' +
		*            componentName
		*          );
		*        }
		*      }
		*    },
		*    render: function() {...}
		*  });
		*
		* @internal
		*/
		var ANONYMOUS = "<<anonymous>>";
		var ReactPropTypes = {
			array: createPrimitiveTypeChecker("array"),
			bigint: createPrimitiveTypeChecker("bigint"),
			bool: createPrimitiveTypeChecker("boolean"),
			func: createPrimitiveTypeChecker("function"),
			number: createPrimitiveTypeChecker("number"),
			object: createPrimitiveTypeChecker("object"),
			string: createPrimitiveTypeChecker("string"),
			symbol: createPrimitiveTypeChecker("symbol"),
			any: createAnyTypeChecker(),
			arrayOf: createArrayOfTypeChecker,
			element: createElementTypeChecker(),
			elementType: createElementTypeTypeChecker(),
			instanceOf: createInstanceTypeChecker,
			node: createNodeChecker(),
			objectOf: createObjectOfTypeChecker,
			oneOf: createEnumTypeChecker,
			oneOfType: createUnionTypeChecker,
			shape: createShapeTypeChecker,
			exact: createStrictShapeTypeChecker
		};
		/**
		* inlined Object.is polyfill to avoid requiring consumers ship their own
		* https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Object/is
		*/
		function is(x, y) {
			if (x === y) return x !== 0 || 1 / x === 1 / y;
			else return x !== x && y !== y;
		}
		/**
		* We use an Error-like object for backward compatibility as people may call
		* PropTypes directly and inspect their output. However, we don't use real
		* Errors anymore. We don't inspect their stack anyway, and creating them
		* is prohibitively expensive if they are created too often, such as what
		* happens in oneOfType() for any type before the one that matched.
		*/
		function PropTypeError(message, data) {
			this.message = message;
			this.data = data && typeof data === "object" ? data : {};
			this.stack = "";
		}
		PropTypeError.prototype = Error.prototype;
		function createChainableTypeChecker(validate) {
			var manualPropTypeCallCache = {};
			var manualPropTypeWarningCount = 0;
			function checkType(isRequired, props, propName, componentName, location, propFullName, secret) {
				componentName = componentName || ANONYMOUS;
				propFullName = propFullName || propName;
				if (secret !== ReactPropTypesSecret) {
					if (throwOnDirectAccess) {
						var err = /* @__PURE__ */ new Error("Calling PropTypes validators directly is not supported by the `prop-types` package. Use `PropTypes.checkPropTypes()` to call them. Read more at http://fb.me/use-check-prop-types");
						err.name = "Invariant Violation";
						throw err;
					} else if (typeof console !== "undefined") {
						var cacheKey = componentName + ":" + propName;
						if (!manualPropTypeCallCache[cacheKey] && manualPropTypeWarningCount < 3) {
							printWarning("You are manually calling a React.PropTypes validation function for the `" + propFullName + "` prop on `" + componentName + "`. This is deprecated and will throw in the standalone `prop-types` package. You may be seeing this warning due to a third-party PropTypes library. See https://fb.me/react-warning-dont-call-proptypes for details.");
							manualPropTypeCallCache[cacheKey] = true;
							manualPropTypeWarningCount++;
						}
					}
				}
				if (props[propName] == null) {
					if (isRequired) {
						if (props[propName] === null) return new PropTypeError("The " + location + " `" + propFullName + "` is marked as required " + ("in `" + componentName + "`, but its value is `null`."));
						return new PropTypeError("The " + location + " `" + propFullName + "` is marked as required in " + ("`" + componentName + "`, but its value is `undefined`."));
					}
					return null;
				} else return validate(props, propName, componentName, location, propFullName);
			}
			var chainedCheckType = checkType.bind(null, false);
			chainedCheckType.isRequired = checkType.bind(null, true);
			return chainedCheckType;
		}
		function createPrimitiveTypeChecker(expectedType) {
			function validate(props, propName, componentName, location, propFullName, secret) {
				var propValue = props[propName];
				if (getPropType(propValue) !== expectedType) {
					var preciseType = getPreciseType(propValue);
					return new PropTypeError("Invalid " + location + " `" + propFullName + "` of type " + ("`" + preciseType + "` supplied to `" + componentName + "`, expected ") + ("`" + expectedType + "`."), { expectedType });
				}
				return null;
			}
			return createChainableTypeChecker(validate);
		}
		function createAnyTypeChecker() {
			return createChainableTypeChecker(emptyFunctionThatReturnsNull);
		}
		function createArrayOfTypeChecker(typeChecker) {
			function validate(props, propName, componentName, location, propFullName) {
				if (typeof typeChecker !== "function") return new PropTypeError("Property `" + propFullName + "` of component `" + componentName + "` has invalid PropType notation inside arrayOf.");
				var propValue = props[propName];
				if (!Array.isArray(propValue)) {
					var propType = getPropType(propValue);
					return new PropTypeError("Invalid " + location + " `" + propFullName + "` of type " + ("`" + propType + "` supplied to `" + componentName + "`, expected an array."));
				}
				for (var i = 0; i < propValue.length; i++) {
					var error = typeChecker(propValue, i, componentName, location, propFullName + "[" + i + "]", ReactPropTypesSecret);
					if (error instanceof Error) return error;
				}
				return null;
			}
			return createChainableTypeChecker(validate);
		}
		function createElementTypeChecker() {
			function validate(props, propName, componentName, location, propFullName) {
				var propValue = props[propName];
				if (!isValidElement(propValue)) {
					var propType = getPropType(propValue);
					return new PropTypeError("Invalid " + location + " `" + propFullName + "` of type " + ("`" + propType + "` supplied to `" + componentName + "`, expected a single ReactElement."));
				}
				return null;
			}
			return createChainableTypeChecker(validate);
		}
		function createElementTypeTypeChecker() {
			function validate(props, propName, componentName, location, propFullName) {
				var propValue = props[propName];
				if (!ReactIs.isValidElementType(propValue)) {
					var propType = getPropType(propValue);
					return new PropTypeError("Invalid " + location + " `" + propFullName + "` of type " + ("`" + propType + "` supplied to `" + componentName + "`, expected a single ReactElement type."));
				}
				return null;
			}
			return createChainableTypeChecker(validate);
		}
		function createInstanceTypeChecker(expectedClass) {
			function validate(props, propName, componentName, location, propFullName) {
				if (!(props[propName] instanceof expectedClass)) {
					var expectedClassName = expectedClass.name || ANONYMOUS;
					var actualClassName = getClassName(props[propName]);
					return new PropTypeError("Invalid " + location + " `" + propFullName + "` of type " + ("`" + actualClassName + "` supplied to `" + componentName + "`, expected ") + ("instance of `" + expectedClassName + "`."));
				}
				return null;
			}
			return createChainableTypeChecker(validate);
		}
		function createEnumTypeChecker(expectedValues) {
			if (!Array.isArray(expectedValues)) {
				if (arguments.length > 1) printWarning("Invalid arguments supplied to oneOf, expected an array, got " + arguments.length + " arguments. A common mistake is to write oneOf(x, y, z) instead of oneOf([x, y, z]).");
				else printWarning("Invalid argument supplied to oneOf, expected an array.");
				return emptyFunctionThatReturnsNull;
			}
			function validate(props, propName, componentName, location, propFullName) {
				var propValue = props[propName];
				for (var i = 0; i < expectedValues.length; i++) if (is(propValue, expectedValues[i])) return null;
				var valuesString = JSON.stringify(expectedValues, function replacer(key, value) {
					if (getPreciseType(value) === "symbol") return String(value);
					return value;
				});
				return new PropTypeError("Invalid " + location + " `" + propFullName + "` of value `" + String(propValue) + "` " + ("supplied to `" + componentName + "`, expected one of " + valuesString + "."));
			}
			return createChainableTypeChecker(validate);
		}
		function createObjectOfTypeChecker(typeChecker) {
			function validate(props, propName, componentName, location, propFullName) {
				if (typeof typeChecker !== "function") return new PropTypeError("Property `" + propFullName + "` of component `" + componentName + "` has invalid PropType notation inside objectOf.");
				var propValue = props[propName];
				var propType = getPropType(propValue);
				if (propType !== "object") return new PropTypeError("Invalid " + location + " `" + propFullName + "` of type " + ("`" + propType + "` supplied to `" + componentName + "`, expected an object."));
				for (var key in propValue) if (has(propValue, key)) {
					var error = typeChecker(propValue, key, componentName, location, propFullName + "." + key, ReactPropTypesSecret);
					if (error instanceof Error) return error;
				}
				return null;
			}
			return createChainableTypeChecker(validate);
		}
		function createUnionTypeChecker(arrayOfTypeCheckers) {
			if (!Array.isArray(arrayOfTypeCheckers)) {
				printWarning("Invalid argument supplied to oneOfType, expected an instance of array.");
				return emptyFunctionThatReturnsNull;
			}
			for (var i = 0; i < arrayOfTypeCheckers.length; i++) {
				var checker = arrayOfTypeCheckers[i];
				if (typeof checker !== "function") {
					printWarning("Invalid argument supplied to oneOfType. Expected an array of check functions, but received " + getPostfixForTypeWarning(checker) + " at index " + i + ".");
					return emptyFunctionThatReturnsNull;
				}
			}
			function validate(props, propName, componentName, location, propFullName) {
				var expectedTypes = [];
				for (var i = 0; i < arrayOfTypeCheckers.length; i++) {
					var checker = arrayOfTypeCheckers[i];
					var checkerResult = checker(props, propName, componentName, location, propFullName, ReactPropTypesSecret);
					if (checkerResult == null) return null;
					if (checkerResult.data && has(checkerResult.data, "expectedType")) expectedTypes.push(checkerResult.data.expectedType);
				}
				var expectedTypesMessage = expectedTypes.length > 0 ? ", expected one of type [" + expectedTypes.join(", ") + "]" : "";
				return new PropTypeError("Invalid " + location + " `" + propFullName + "` supplied to " + ("`" + componentName + "`" + expectedTypesMessage + "."));
			}
			return createChainableTypeChecker(validate);
		}
		function createNodeChecker() {
			function validate(props, propName, componentName, location, propFullName) {
				if (!isNode(props[propName])) return new PropTypeError("Invalid " + location + " `" + propFullName + "` supplied to " + ("`" + componentName + "`, expected a ReactNode."));
				return null;
			}
			return createChainableTypeChecker(validate);
		}
		function invalidValidatorError(componentName, location, propFullName, key, type) {
			return new PropTypeError((componentName || "React class") + ": " + location + " type `" + propFullName + "." + key + "` is invalid; it must be a function, usually from the `prop-types` package, but received `" + type + "`.");
		}
		function createShapeTypeChecker(shapeTypes) {
			function validate(props, propName, componentName, location, propFullName) {
				var propValue = props[propName];
				var propType = getPropType(propValue);
				if (propType !== "object") return new PropTypeError("Invalid " + location + " `" + propFullName + "` of type `" + propType + "` " + ("supplied to `" + componentName + "`, expected `object`."));
				for (var key in shapeTypes) {
					var checker = shapeTypes[key];
					if (typeof checker !== "function") return invalidValidatorError(componentName, location, propFullName, key, getPreciseType(checker));
					var error = checker(propValue, key, componentName, location, propFullName + "." + key, ReactPropTypesSecret);
					if (error) return error;
				}
				return null;
			}
			return createChainableTypeChecker(validate);
		}
		function createStrictShapeTypeChecker(shapeTypes) {
			function validate(props, propName, componentName, location, propFullName) {
				var propValue = props[propName];
				var propType = getPropType(propValue);
				if (propType !== "object") return new PropTypeError("Invalid " + location + " `" + propFullName + "` of type `" + propType + "` " + ("supplied to `" + componentName + "`, expected `object`."));
				for (var key in assign({}, props[propName], shapeTypes)) {
					var checker = shapeTypes[key];
					if (has(shapeTypes, key) && typeof checker !== "function") return invalidValidatorError(componentName, location, propFullName, key, getPreciseType(checker));
					if (!checker) return new PropTypeError("Invalid " + location + " `" + propFullName + "` key `" + key + "` supplied to `" + componentName + "`.\nBad object: " + JSON.stringify(props[propName], null, "  ") + "\nValid keys: " + JSON.stringify(Object.keys(shapeTypes), null, "  "));
					var error = checker(propValue, key, componentName, location, propFullName + "." + key, ReactPropTypesSecret);
					if (error) return error;
				}
				return null;
			}
			return createChainableTypeChecker(validate);
		}
		function isNode(propValue) {
			switch (typeof propValue) {
				case "number":
				case "string":
				case "undefined": return true;
				case "boolean": return !propValue;
				case "object":
					if (Array.isArray(propValue)) return propValue.every(isNode);
					if (propValue === null || isValidElement(propValue)) return true;
					var iteratorFn = getIteratorFn(propValue);
					if (iteratorFn) {
						var iterator = iteratorFn.call(propValue);
						var step;
						if (iteratorFn !== propValue.entries) {
							while (!(step = iterator.next()).done) if (!isNode(step.value)) return false;
						} else while (!(step = iterator.next()).done) {
							var entry = step.value;
							if (entry) {
								if (!isNode(entry[1])) return false;
							}
						}
					} else return false;
					return true;
				default: return false;
			}
		}
		function isSymbol(propType, propValue) {
			if (propType === "symbol") return true;
			if (!propValue) return false;
			if (propValue["@@toStringTag"] === "Symbol") return true;
			if (typeof Symbol === "function" && propValue instanceof Symbol) return true;
			return false;
		}
		function getPropType(propValue) {
			var propType = typeof propValue;
			if (Array.isArray(propValue)) return "array";
			if (propValue instanceof RegExp) return "object";
			if (isSymbol(propType, propValue)) return "symbol";
			return propType;
		}
		function getPreciseType(propValue) {
			if (typeof propValue === "undefined" || propValue === null) return "" + propValue;
			var propType = getPropType(propValue);
			if (propType === "object") {
				if (propValue instanceof Date) return "date";
				else if (propValue instanceof RegExp) return "regexp";
			}
			return propType;
		}
		function getPostfixForTypeWarning(value) {
			var type = getPreciseType(value);
			switch (type) {
				case "array":
				case "object": return "an " + type;
				case "boolean":
				case "date":
				case "regexp": return "a " + type;
				default: return type;
			}
		}
		function getClassName(propValue) {
			if (!propValue.constructor || !propValue.constructor.name) return ANONYMOUS;
			return propValue.constructor.name;
		}
		ReactPropTypes.checkPropTypes = checkPropTypes;
		ReactPropTypes.resetWarningCache = checkPropTypes.resetWarningCache;
		ReactPropTypes.PropTypes = ReactPropTypes;
		return ReactPropTypes;
	};
}));
//#endregion
//#region node_modules/.pnpm/prop-types@15.8.1/node_modules/prop-types/index.js
var require_prop_types = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var ReactIs = require_react_is();
	module.exports = require_factoryWithTypeCheckers()(ReactIs.isElement, true);
}));
//#endregion
//#region node_modules/.pnpm/focus-lock@1.3.6/node_modules/focus-lock/dist/es2015/constants.js
/**
* defines a focus group
*/
var FOCUS_GROUP = "data-focus-lock";
/**
* disables element discovery inside a group marked by key
*/
var FOCUS_DISABLED = "data-focus-lock-disabled";
/**
* allows uncontrolled focus within the marked area, effectively disabling focus lock for it's content
*/
var FOCUS_ALLOW = "data-no-focus-lock";
/**
* instructs autofocus engine to pick default autofocus inside a given node
* can be set on the element or container
*/
var FOCUS_AUTO = "data-autofocus-inside";
/**
* instructs autofocus to ignore elements within a given node
* can be set on the element or container
*/
var FOCUS_NO_AUTOFOCUS = "data-no-autofocus";
//#endregion
//#region node_modules/.pnpm/use-callback-ref@1.3.3_@types+react@19.2.18_react@19.2.8/node_modules/use-callback-ref/dist/es2015/assignRef.js
/**
* Assigns a value for a given ref, no matter of the ref format
* @param {RefObject} ref - a callback function or ref object
* @param value - a new value
*
* @see https://github.com/theKashey/use-callback-ref#assignref
* @example
* const refObject = useRef();
* const refFn = (ref) => {....}
*
* assignRef(refObject, "refValue");
* assignRef(refFn, "refValue");
*/
function assignRef(ref, value) {
	if (typeof ref === "function") ref(value);
	else if (ref) ref.current = value;
	return ref;
}
//#endregion
//#region node_modules/.pnpm/use-callback-ref@1.3.3_@types+react@19.2.18_react@19.2.8/node_modules/use-callback-ref/dist/es2015/useRef.js
/**
* creates a MutableRef with ref change callback
* @param initialValue - initial ref value
* @param {Function} callback - a callback to run when value changes
*
* @example
* const ref = useCallbackRef(0, (newValue, oldValue) => console.log(oldValue, '->', newValue);
* ref.current = 1;
* // prints 0 -> 1
*
* @see https://reactjs.org/docs/hooks-reference.html#useref
* @see https://github.com/theKashey/use-callback-ref#usecallbackref---to-replace-reactuseref
* @returns {MutableRefObject}
*/
function useCallbackRef(initialValue, callback) {
	var ref = (0, import_react.useState)(function() {
		return {
			value: initialValue,
			callback,
			facade: {
				get current() {
					return ref.value;
				},
				set current(value) {
					var last = ref.value;
					if (last !== value) {
						ref.value = value;
						ref.callback(value, last);
					}
				}
			}
		};
	})[0];
	ref.callback = callback;
	return ref.facade;
}
//#endregion
//#region node_modules/.pnpm/use-callback-ref@1.3.3_@types+react@19.2.18_react@19.2.8/node_modules/use-callback-ref/dist/es2015/useMergeRef.js
var useIsomorphicLayoutEffect = typeof window !== "undefined" ? import_react.useLayoutEffect : import_react.useEffect;
var currentValues = /* @__PURE__ */ new WeakMap();
/**
* Merges two or more refs together providing a single interface to set their value
* @param {RefObject|Ref} refs
* @returns {MutableRefObject} - a new ref, which translates all changes to {refs}
*
* @see {@link mergeRefs} a version without buit-in memoization
* @see https://github.com/theKashey/use-callback-ref#usemergerefs
* @example
* const Component = React.forwardRef((props, ref) => {
*   const ownRef = useRef();
*   const domRef = useMergeRefs([ref, ownRef]); // 👈 merge together
*   return <div ref={domRef}>...</div>
* }
*/
function useMergeRefs(refs, defaultValue) {
	var callbackRef = useCallbackRef(defaultValue || null, function(newValue) {
		return refs.forEach(function(ref) {
			return assignRef(ref, newValue);
		});
	});
	useIsomorphicLayoutEffect(function() {
		var oldValue = currentValues.get(callbackRef);
		if (oldValue) {
			var prevRefs_1 = new Set(oldValue);
			var nextRefs_1 = new Set(refs);
			var current_1 = callbackRef.current;
			prevRefs_1.forEach(function(ref) {
				if (!nextRefs_1.has(ref)) assignRef(ref, null);
			});
			nextRefs_1.forEach(function(ref) {
				if (!prevRefs_1.has(ref)) assignRef(ref, current_1);
			});
		}
		currentValues.set(callbackRef, refs);
	}, [refs]);
	return callbackRef;
}
//#endregion
//#region node_modules/.pnpm/react-focus-lock@2.13.7_@types+react@19.2.18_react@19.2.8/node_modules/react-focus-lock/dist/es2015/FocusGuard.js
var import_prop_types = /* @__PURE__ */ __toESM(require_prop_types());
var hiddenGuard = {
	width: "1px",
	height: "0px",
	padding: 0,
	overflow: "hidden",
	position: "fixed",
	top: "1px",
	left: "1px"
};
var InFocusGuard = function InFocusGuard(_ref) {
	var _ref$children = _ref.children, children = _ref$children === void 0 ? null : _ref$children;
	return /*#__PURE__*/ import_react.createElement(import_react.Fragment, null, /*#__PURE__*/ import_react.createElement("div", {
		key: "guard-first",
		"data-focus-guard": true,
		"data-focus-auto-guard": true,
		style: hiddenGuard
	}), children, children && /*#__PURE__*/ import_react.createElement("div", {
		key: "guard-last",
		"data-focus-guard": true,
		"data-focus-auto-guard": true,
		style: hiddenGuard
	}));
};
InFocusGuard.propTypes = { children: import_prop_types.default.node };
//#endregion
//#region node_modules/.pnpm/use-sidecar@1.1.3_@types+react@19.2.18_react@19.2.8/node_modules/use-sidecar/dist/es2015/medium.js
function ItoI(a) {
	return a;
}
function innerCreateMedium(defaults, middleware) {
	if (middleware === void 0) middleware = ItoI;
	var buffer = [];
	var assigned = false;
	return {
		read: function() {
			if (assigned) throw new Error("Sidecar: could not `read` from an `assigned` medium. `read` could be used only with `useMedium`.");
			if (buffer.length) return buffer[buffer.length - 1];
			return defaults;
		},
		useMedium: function(data) {
			var item = middleware(data, assigned);
			buffer.push(item);
			return function() {
				buffer = buffer.filter(function(x) {
					return x !== item;
				});
			};
		},
		assignSyncMedium: function(cb) {
			assigned = true;
			while (buffer.length) {
				var cbs = buffer;
				buffer = [];
				cbs.forEach(cb);
			}
			buffer = {
				push: function(x) {
					return cb(x);
				},
				filter: function() {
					return buffer;
				}
			};
		},
		assignMedium: function(cb) {
			assigned = true;
			var pendingQueue = [];
			if (buffer.length) {
				var cbs = buffer;
				buffer = [];
				cbs.forEach(cb);
				pendingQueue = buffer;
			}
			var executeQueue = function() {
				var cbs = pendingQueue;
				pendingQueue = [];
				cbs.forEach(cb);
			};
			var cycle = function() {
				return Promise.resolve().then(executeQueue);
			};
			cycle();
			buffer = {
				push: function(x) {
					pendingQueue.push(x);
					cycle();
				},
				filter: function(filter) {
					pendingQueue = pendingQueue.filter(filter);
					return buffer;
				}
			};
		}
	};
}
function createMedium(defaults, middleware) {
	if (middleware === void 0) middleware = ItoI;
	return innerCreateMedium(defaults, middleware);
}
function createSidecarMedium(options) {
	if (options === void 0) options = {};
	var medium = innerCreateMedium(null);
	medium.options = __assign({
		async: true,
		ssr: false
	}, options);
	return medium;
}
//#endregion
//#region node_modules/.pnpm/react-focus-lock@2.13.7_@types+react@19.2.18_react@19.2.8/node_modules/react-focus-lock/dist/es2015/medium.js
var mediumFocus = createMedium({}, function(_ref) {
	return {
		target: _ref.target,
		currentTarget: _ref.currentTarget
	};
});
var mediumBlur = createMedium();
var mediumEffect = createMedium();
var mediumSidecar = createSidecarMedium({
	async: true,
	ssr: typeof document !== "undefined"
});
//#endregion
//#region node_modules/.pnpm/react-focus-lock@2.13.7_@types+react@19.2.18_react@19.2.8/node_modules/react-focus-lock/dist/es2015/scope.js
var focusScope = /*#__PURE__*/ (0, import_react.createContext)(void 0);
//#endregion
//#region node_modules/.pnpm/react-focus-lock@2.13.7_@types+react@19.2.18_react@19.2.8/node_modules/react-focus-lock/dist/es2015/Lock.js
var emptyArray = [];
var FocusLock = /*#__PURE__*/ (0, import_react.forwardRef)(function FocusLockUI(props, parentRef) {
	var _extends2;
	var _useState = (0, import_react.useState)(), realObserved = _useState[0], setObserved = _useState[1];
	var observed = (0, import_react.useRef)();
	var isActive = (0, import_react.useRef)(false);
	var originalFocusedElement = (0, import_react.useRef)(null);
	var update = (0, import_react.useState)({})[1];
	var children = props.children, _props$disabled = props.disabled, disabled = _props$disabled === void 0 ? false : _props$disabled, _props$noFocusGuards = props.noFocusGuards, noFocusGuards = _props$noFocusGuards === void 0 ? false : _props$noFocusGuards, _props$persistentFocu = props.persistentFocus, persistentFocus = _props$persistentFocu === void 0 ? false : _props$persistentFocu, _props$crossFrame = props.crossFrame, crossFrame = _props$crossFrame === void 0 ? true : _props$crossFrame, _props$autoFocus = props.autoFocus, autoFocus = _props$autoFocus === void 0 ? true : _props$autoFocus, allowTextSelection = props.allowTextSelection, group = props.group, className = props.className, whiteList = props.whiteList, hasPositiveIndices = props.hasPositiveIndices, _props$shards = props.shards, shards = _props$shards === void 0 ? emptyArray : _props$shards, _props$as = props.as, Container = _props$as === void 0 ? "div" : _props$as, _props$lockProps = props.lockProps, containerProps = _props$lockProps === void 0 ? {} : _props$lockProps, SideCar = props.sideCar, _props$returnFocus = props.returnFocus, shouldReturnFocus = _props$returnFocus === void 0 ? false : _props$returnFocus, focusOptions = props.focusOptions, onActivationCallback = props.onActivation, onDeactivationCallback = props.onDeactivation;
	var id = (0, import_react.useState)({})[0];
	var onActivation = (0, import_react.useCallback)(function(_ref) {
		var captureFocusRestore = _ref.captureFocusRestore;
		if (!originalFocusedElement.current) {
			var _document;
			var activeElement = (_document = document) == null ? void 0 : _document.activeElement;
			originalFocusedElement.current = activeElement;
			if (activeElement !== document.body) originalFocusedElement.current = captureFocusRestore(activeElement);
		}
		if (observed.current && onActivationCallback) onActivationCallback(observed.current);
		isActive.current = true;
		update();
	}, [onActivationCallback]);
	var onDeactivation = (0, import_react.useCallback)(function() {
		isActive.current = false;
		if (onDeactivationCallback) onDeactivationCallback(observed.current);
		update();
	}, [onDeactivationCallback]);
	var returnFocus = (0, import_react.useCallback)(function(allowDefer) {
		var focusRestore = originalFocusedElement.current;
		if (focusRestore) {
			var returnFocusTo = (typeof focusRestore === "function" ? focusRestore() : focusRestore) || document.body;
			var howToReturnFocus = typeof shouldReturnFocus === "function" ? shouldReturnFocus(returnFocusTo) : shouldReturnFocus;
			if (howToReturnFocus) {
				var returnFocusOptions = typeof howToReturnFocus === "object" ? howToReturnFocus : void 0;
				originalFocusedElement.current = null;
				if (allowDefer) Promise.resolve().then(function() {
					return returnFocusTo.focus(returnFocusOptions);
				});
				else returnFocusTo.focus(returnFocusOptions);
			}
		}
	}, [shouldReturnFocus]);
	var onFocus = (0, import_react.useCallback)(function(event) {
		if (isActive.current) mediumFocus.useMedium(event);
	}, []);
	var onBlur = mediumBlur.useMedium;
	var setObserveNode = (0, import_react.useCallback)(function(newObserved) {
		if (observed.current !== newObserved) {
			observed.current = newObserved;
			setObserved(newObserved);
		}
	}, []);
	if (typeof allowTextSelection !== "undefined") console.warn("React-Focus-Lock: allowTextSelection is deprecated and enabled by default");
	(0, import_react.useEffect)(function() {
		if (!observed.current && typeof Container !== "string") console.error("FocusLock: could not obtain ref to internal node");
	}, []);
	var lockProps = _extends((_extends2 = {}, _extends2[FOCUS_DISABLED] = disabled && "disabled", _extends2[FOCUS_GROUP] = group, _extends2), containerProps);
	var hasLeadingGuards = noFocusGuards !== true;
	var hasTailingGuards = hasLeadingGuards && noFocusGuards !== "tail";
	var mergedRef = useMergeRefs([parentRef, setObserveNode]);
	var focusScopeValue = (0, import_react.useMemo)(function() {
		return {
			observed,
			shards,
			enabled: !disabled,
			get active() {
				return isActive.current;
			}
		};
	}, [
		disabled,
		isActive,
		shards,
		observed
	]);
	return /*#__PURE__*/ import_react.createElement(import_react.Fragment, null, hasLeadingGuards && [/*#__PURE__*/ import_react.createElement("div", {
		key: "guard-first",
		"data-focus-guard": true,
		tabIndex: disabled ? -1 : 0,
		style: hiddenGuard
	}), hasPositiveIndices ? /*#__PURE__*/ import_react.createElement("div", {
		key: "guard-nearest",
		"data-focus-guard": true,
		tabIndex: disabled ? -1 : 1,
		style: hiddenGuard
	}) : null], !disabled && /*#__PURE__*/ import_react.createElement(SideCar, {
		id,
		sideCar: mediumSidecar,
		observed: realObserved,
		disabled,
		persistentFocus,
		crossFrame,
		autoFocus,
		whiteList,
		shards,
		onActivation,
		onDeactivation,
		returnFocus,
		focusOptions,
		noFocusGuards
	}), /*#__PURE__*/ import_react.createElement(Container, _extends({ ref: mergedRef }, lockProps, {
		className,
		onBlur,
		onFocus
	}), /*#__PURE__*/ import_react.createElement(focusScope.Provider, { value: focusScopeValue }, children)), hasTailingGuards && /*#__PURE__*/ import_react.createElement("div", {
		"data-focus-guard": true,
		tabIndex: disabled ? -1 : 0,
		style: hiddenGuard
	}));
});
FocusLock.propTypes = {
	children: import_prop_types.node,
	disabled: import_prop_types.bool,
	returnFocus: (0, import_prop_types.oneOfType)([
		import_prop_types.bool,
		import_prop_types.object,
		import_prop_types.func
	]),
	focusOptions: import_prop_types.object,
	noFocusGuards: import_prop_types.bool,
	hasPositiveIndices: import_prop_types.bool,
	allowTextSelection: import_prop_types.bool,
	autoFocus: import_prop_types.bool,
	persistentFocus: import_prop_types.bool,
	crossFrame: import_prop_types.bool,
	group: import_prop_types.string,
	className: import_prop_types.string,
	whiteList: import_prop_types.func,
	shards: (0, import_prop_types.arrayOf)(import_prop_types.any),
	as: (0, import_prop_types.oneOfType)([
		import_prop_types.string,
		import_prop_types.func,
		import_prop_types.object
	]),
	lockProps: import_prop_types.object,
	onActivation: import_prop_types.func,
	onDeactivation: import_prop_types.func,
	sideCar: import_prop_types.any.isRequired
};
//#endregion
//#region node_modules/.pnpm/@babel+runtime@7.29.7/node_modules/@babel/runtime/helpers/esm/setPrototypeOf.js
function _setPrototypeOf(t, e) {
	return _setPrototypeOf = Object.setPrototypeOf ? Object.setPrototypeOf.bind() : function(t, e) {
		return t.__proto__ = e, t;
	}, _setPrototypeOf(t, e);
}
//#endregion
//#region node_modules/.pnpm/@babel+runtime@7.29.7/node_modules/@babel/runtime/helpers/esm/inheritsLoose.js
function _inheritsLoose(t, o) {
	t.prototype = Object.create(o.prototype), t.prototype.constructor = t, _setPrototypeOf(t, o);
}
//#endregion
//#region node_modules/.pnpm/@babel+runtime@7.29.7/node_modules/@babel/runtime/helpers/esm/typeof.js
function _typeof(o) {
	"@babel/helpers - typeof";
	return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function(o) {
		return typeof o;
	} : function(o) {
		return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o;
	}, _typeof(o);
}
//#endregion
//#region node_modules/.pnpm/@babel+runtime@7.29.7/node_modules/@babel/runtime/helpers/esm/toPrimitive.js
function toPrimitive(t, r) {
	if ("object" != _typeof(t) || !t) return t;
	var e = t[Symbol.toPrimitive];
	if (void 0 !== e) {
		var i = e.call(t, r || "default");
		if ("object" != _typeof(i)) return i;
		throw new TypeError("@@toPrimitive must return a primitive value.");
	}
	return ("string" === r ? String : Number)(t);
}
//#endregion
//#region node_modules/.pnpm/@babel+runtime@7.29.7/node_modules/@babel/runtime/helpers/esm/toPropertyKey.js
function toPropertyKey(t) {
	var i = toPrimitive(t, "string");
	return "symbol" == _typeof(i) ? i : i + "";
}
//#endregion
//#region node_modules/.pnpm/@babel+runtime@7.29.7/node_modules/@babel/runtime/helpers/esm/defineProperty.js
function _defineProperty(e, r, t) {
	return (r = toPropertyKey(r)) in e ? Object.defineProperty(e, r, {
		value: t,
		enumerable: !0,
		configurable: !0,
		writable: !0
	}) : e[r] = t, e;
}
//#endregion
//#region node_modules/.pnpm/react-clientside-effect@1.2.8_react@19.2.8/node_modules/react-clientside-effect/lib/index.es.js
function withSideEffect(reducePropsToState, handleStateChangeOnClient) {
	if (typeof reducePropsToState !== "function") throw new Error("Expected reducePropsToState to be a function.");
	if (typeof handleStateChangeOnClient !== "function") throw new Error("Expected handleStateChangeOnClient to be a function.");
	function getDisplayName(WrappedComponent) {
		return WrappedComponent.displayName || WrappedComponent.name || "Component";
	}
	return function wrap(WrappedComponent) {
		if (typeof WrappedComponent !== "function") throw new Error("Expected WrappedComponent to be a React component.");
		var mountedInstances = [];
		var state;
		function emitChange() {
			state = reducePropsToState(mountedInstances.map(function(instance) {
				return instance.props;
			}));
			handleStateChangeOnClient(state);
		}
		var SideEffect = /*#__PURE__*/ function(_PureComponent) {
			_inheritsLoose(SideEffect, _PureComponent);
			function SideEffect() {
				return _PureComponent.apply(this, arguments) || this;
			}
			SideEffect.peek = function peek() {
				return state;
			};
			var _proto = SideEffect.prototype;
			_proto.componentDidMount = function componentDidMount() {
				mountedInstances.push(this);
				emitChange();
			};
			_proto.componentDidUpdate = function componentDidUpdate() {
				emitChange();
			};
			_proto.componentWillUnmount = function componentWillUnmount() {
				var index = mountedInstances.indexOf(this);
				mountedInstances.splice(index, 1);
				emitChange();
			};
			_proto.render = function render() {
				return /*#__PURE__*/ import_react.createElement(WrappedComponent, this.props);
			};
			return SideEffect;
		}(import_react.PureComponent);
		_defineProperty(SideEffect, "displayName", "SideEffect(" + getDisplayName(WrappedComponent) + ")");
		return SideEffect;
	};
}
//#endregion
//#region node_modules/.pnpm/focus-lock@1.3.6/node_modules/focus-lock/dist/es2015/utils/array.js
var toArray = function(a) {
	var ret = Array(a.length);
	for (var i = 0; i < a.length; ++i) ret[i] = a[i];
	return ret;
};
var asArray = function(a) {
	return Array.isArray(a) ? a : [a];
};
var getFirst = function(a) {
	return Array.isArray(a) ? a[0] : a;
};
//#endregion
//#region node_modules/.pnpm/focus-lock@1.3.6/node_modules/focus-lock/dist/es2015/utils/is.js
var isElementHidden = function(node) {
	if (node.nodeType !== Node.ELEMENT_NODE) return false;
	var computedStyle = window.getComputedStyle(node, null);
	if (!computedStyle || !computedStyle.getPropertyValue) return false;
	return computedStyle.getPropertyValue("display") === "none" || computedStyle.getPropertyValue("visibility") === "hidden";
};
var getParentNode = function(node) {
	return node.parentNode && node.parentNode.nodeType === Node.DOCUMENT_FRAGMENT_NODE ? node.parentNode.host : node.parentNode;
};
var isTopNode = function(node) {
	return node === document || node && node.nodeType === Node.DOCUMENT_NODE;
};
var isInert = function(node) {
	return node.hasAttribute("inert");
};
/**
* @see https://github.com/testing-library/jest-dom/blob/main/src/to-be-visible.js
*/
var isVisibleUncached = function(node, checkParent) {
	return !node || isTopNode(node) || !isElementHidden(node) && !isInert(node) && checkParent(getParentNode(node));
};
var isVisibleCached = function(visibilityCache, node) {
	var cached = visibilityCache.get(node);
	if (cached !== void 0) return cached;
	var result = isVisibleUncached(node, isVisibleCached.bind(void 0, visibilityCache));
	visibilityCache.set(node, result);
	return result;
};
var isAutoFocusAllowedUncached = function(node, checkParent) {
	return node && !isTopNode(node) ? isAutoFocusAllowed(node) ? checkParent(getParentNode(node)) : false : true;
};
var isAutoFocusAllowedCached = function(cache, node) {
	var cached = cache.get(node);
	if (cached !== void 0) return cached;
	var result = isAutoFocusAllowedUncached(node, isAutoFocusAllowedCached.bind(void 0, cache));
	cache.set(node, result);
	return result;
};
var getDataset = function(node) {
	return node.dataset;
};
var isHTMLButtonElement = function(node) {
	return node.tagName === "BUTTON";
};
var isHTMLInputElement = function(node) {
	return node.tagName === "INPUT";
};
var isRadioElement = function(node) {
	return isHTMLInputElement(node) && node.type === "radio";
};
var notHiddenInput = function(node) {
	return !((isHTMLInputElement(node) || isHTMLButtonElement(node)) && (node.type === "hidden" || node.disabled));
};
var isAutoFocusAllowed = function(node) {
	var attribute = node.getAttribute(FOCUS_NO_AUTOFOCUS);
	return ![
		true,
		"true",
		""
	].includes(attribute);
};
var isGuard = function(node) {
	var _a;
	return Boolean(node && ((_a = getDataset(node)) === null || _a === void 0 ? void 0 : _a.focusGuard));
};
var isNotAGuard = function(node) {
	return !isGuard(node);
};
var isDefined = function(x) {
	return Boolean(x);
};
//#endregion
//#region node_modules/.pnpm/focus-lock@1.3.6/node_modules/focus-lock/dist/es2015/utils/tabOrder.js
var tabSort = function(a, b) {
	var aTab = Math.max(0, a.tabIndex);
	var bTab = Math.max(0, b.tabIndex);
	var tabDiff = aTab - bTab;
	var indexDiff = a.index - b.index;
	if (tabDiff) {
		if (!aTab) return 1;
		if (!bTab) return -1;
	}
	return tabDiff || indexDiff;
};
var getTabIndex = function(node) {
	if (node.tabIndex < 0) {
		if (!node.hasAttribute("tabindex")) return 0;
	}
	return node.tabIndex;
};
var orderByTabIndex = function(nodes, filterNegative, keepGuards) {
	return toArray(nodes).map(function(node, index) {
		var tabIndex = getTabIndex(node);
		return {
			node,
			index,
			tabIndex: keepGuards && tabIndex === -1 ? (node.dataset || {}).focusGuard ? 0 : -1 : tabIndex
		};
	}).filter(function(data) {
		return !filterNegative || data.tabIndex >= 0;
	}).sort(tabSort);
};
//#endregion
//#region node_modules/.pnpm/focus-lock@1.3.6/node_modules/focus-lock/dist/es2015/utils/tabUtils.js
var queryTabbables = [
	"button:enabled",
	"select:enabled",
	"textarea:enabled",
	"input:enabled",
	"a[href]",
	"area[href]",
	"summary",
	"iframe",
	"object",
	"embed",
	"audio[controls]",
	"video[controls]",
	"[tabindex]",
	"[contenteditable]",
	"[autofocus]"
].join(",");
var queryGuardTabbables = "".concat(queryTabbables, ", [data-focus-guard]");
var getFocusablesWithShadowDom = function(parent, withGuards) {
	return toArray((parent.shadowRoot || parent).children).reduce(function(acc, child) {
		return acc.concat(child.matches(withGuards ? queryGuardTabbables : queryTabbables) ? [child] : [], getFocusablesWithShadowDom(child));
	}, []);
};
var getFocusablesWithIFrame = function(parent, withGuards) {
	var _a;
	if (parent instanceof HTMLIFrameElement && ((_a = parent.contentDocument) === null || _a === void 0 ? void 0 : _a.body)) return getFocusables([parent.contentDocument.body], withGuards);
	return [parent];
};
var getFocusables = function(parents, withGuards) {
	return parents.reduce(function(acc, parent) {
		var _a;
		var focusableWithShadowDom = getFocusablesWithShadowDom(parent, withGuards);
		var focusableWithIframes = (_a = []).concat.apply(_a, focusableWithShadowDom.map(function(node) {
			return getFocusablesWithIFrame(node, withGuards);
		}));
		return acc.concat(focusableWithIframes, parent.parentNode ? toArray(parent.parentNode.querySelectorAll(queryTabbables)).filter(function(node) {
			return node === parent;
		}) : []);
	}, []);
};
/**
* return a list of focusable nodes within an area marked as "auto-focusable"
* @param parent
*/
var getParentAutofocusables = function(parent) {
	return toArray(parent.querySelectorAll("[".concat(FOCUS_AUTO, "]"))).map(function(node) {
		return getFocusables([node]);
	}).reduce(function(acc, nodes) {
		return acc.concat(nodes);
	}, []);
};
//#endregion
//#region node_modules/.pnpm/focus-lock@1.3.6/node_modules/focus-lock/dist/es2015/utils/DOMutils.js
/**
* given list of focusable elements keeps the ones user can interact with
* @param nodes
* @param visibilityCache
*/
var filterFocusable = function(nodes, visibilityCache) {
	return toArray(nodes).filter(function(node) {
		return isVisibleCached(visibilityCache, node);
	}).filter(function(node) {
		return notHiddenInput(node);
	});
};
var filterAutoFocusable = function(nodes, cache) {
	if (cache === void 0) cache = /* @__PURE__ */ new Map();
	return toArray(nodes).filter(function(node) {
		return isAutoFocusAllowedCached(cache, node);
	});
};
/**
* !__WARNING__! Low level API.
* @returns all tabbable nodes
*
* @see {@link getFocusableNodes} to get any focusable element
*
* @param topNodes - array of top level HTMLElements to search inside
* @param visibilityCache - an cache to store intermediate measurements. Expected to be a fresh `new Map` on every call
*/
var getTabbableNodes = function(topNodes, visibilityCache, withGuards) {
	return orderByTabIndex(filterFocusable(getFocusables(topNodes, withGuards), visibilityCache), true, withGuards);
};
/**
* !__WARNING__! Low level API.
*
* @returns anything "focusable", not only tabbable. The difference is in `tabIndex=-1`
* (without guards, as long as they are not expected to be ever focused)
*
* @see {@link getTabbableNodes} to get only tabble nodes element
*
* @param topNodes - array of top level HTMLElements to search inside
* @param visibilityCache - an cache to store intermediate measurements. Expected to be a fresh `new Map` on every call
*/
var getFocusableNodes = function(topNodes, visibilityCache) {
	return orderByTabIndex(filterFocusable(getFocusables(topNodes), visibilityCache), false);
};
/**
* return list of nodes which are expected to be auto-focused
* @param topNode
* @param visibilityCache
*/
var parentAutofocusables = function(topNode, visibilityCache) {
	return filterFocusable(getParentAutofocusables(topNode), visibilityCache);
};
var contains = function(scope, element) {
	if (scope.shadowRoot) return contains(scope.shadowRoot, element);
	else {
		if (Object.getPrototypeOf(scope).contains !== void 0 && Object.getPrototypeOf(scope).contains.call(scope, element)) return true;
		return toArray(scope.children).some(function(child) {
			var _a;
			if (child instanceof HTMLIFrameElement) {
				var iframeBody = (_a = child.contentDocument) === null || _a === void 0 ? void 0 : _a.body;
				if (iframeBody) return contains(iframeBody, element);
				return false;
			}
			return contains(child, element);
		});
	}
};
//#endregion
//#region node_modules/.pnpm/focus-lock@1.3.6/node_modules/focus-lock/dist/es2015/utils/all-affected.js
/**
* in case of multiple nodes nested inside each other
* keeps only top ones
* this is O(nlogn)
* @param nodes
* @returns {*}
*/
var filterNested = function(nodes) {
	var contained = /* @__PURE__ */ new Set();
	var l = nodes.length;
	for (var i = 0; i < l; i += 1) for (var j = i + 1; j < l; j += 1) {
		var position = nodes[i].compareDocumentPosition(nodes[j]);
		if ((position & Node.DOCUMENT_POSITION_CONTAINED_BY) > 0) contained.add(j);
		if ((position & Node.DOCUMENT_POSITION_CONTAINS) > 0) contained.add(i);
	}
	return nodes.filter(function(_, index) {
		return !contained.has(index);
	});
};
/**
* finds top most parent for a node
* @param node
* @returns {*}
*/
var getTopParent = function(node) {
	return node.parentNode ? getTopParent(node.parentNode) : node;
};
/**
* returns all "focus containers" inside a given node
* @param node - node or nodes to look inside
* @returns Element[]
*/
var getAllAffectedNodes = function(node) {
	return asArray(node).filter(Boolean).reduce(function(acc, currentNode) {
		var group = currentNode.getAttribute(FOCUS_GROUP);
		acc.push.apply(acc, group ? filterNested(toArray(getTopParent(currentNode).querySelectorAll("[".concat(FOCUS_GROUP, "=\"").concat(group, "\"]:not([").concat(FOCUS_DISABLED, "=\"disabled\"])")))) : [currentNode]);
		return acc;
	}, []);
};
//#endregion
//#region node_modules/.pnpm/focus-lock@1.3.6/node_modules/focus-lock/dist/es2015/utils/safe.js
var safeProbe = function(cb) {
	try {
		return cb();
	} catch (e) {
		return;
	}
};
//#endregion
//#region node_modules/.pnpm/focus-lock@1.3.6/node_modules/focus-lock/dist/es2015/utils/getActiveElement.js
/**
* returns active element from document or from nested shadowdoms
*/
/**
* returns current active element. If the active element is a "container" itself(shadowRoot or iframe) returns active element inside it
* @param [inDocument]
*/
var getActiveElement = function(inDocument) {
	if (inDocument === void 0) inDocument = document;
	if (!inDocument || !inDocument.activeElement) return;
	var activeElement = inDocument.activeElement;
	return activeElement.shadowRoot ? getActiveElement(activeElement.shadowRoot) : activeElement instanceof HTMLIFrameElement && safeProbe(function() {
		return activeElement.contentWindow.document;
	}) ? getActiveElement(activeElement.contentWindow.document) : activeElement;
};
//#endregion
//#region node_modules/.pnpm/focus-lock@1.3.6/node_modules/focus-lock/dist/es2015/focusInside.js
var focusInFrame = function(frame, activeElement) {
	return frame === activeElement;
};
var focusInsideIframe = function(topNode, activeElement) {
	return Boolean(toArray(topNode.querySelectorAll("iframe")).some(function(node) {
		return focusInFrame(node, activeElement);
	}));
};
/**
* @returns {Boolean} true, if the current focus is inside given node or nodes.
* Supports nodes hidden inside shadowDom
*/
var focusInside = function(topNode, activeElement) {
	if (activeElement === void 0) activeElement = getActiveElement(getFirst(topNode).ownerDocument);
	if (!activeElement || activeElement.dataset && activeElement.dataset.focusGuard) return false;
	return getAllAffectedNodes(topNode).some(function(node) {
		return contains(node, activeElement) || focusInsideIframe(node, activeElement);
	});
};
//#endregion
//#region node_modules/.pnpm/focus-lock@1.3.6/node_modules/focus-lock/dist/es2015/focusIsHidden.js
/**
* checks if focus is hidden FROM the focus-lock
* ie contained inside a node focus-lock shall ignore
*
* This is a utility function coupled with {@link FOCUS_ALLOW} constant
*
* @returns {boolean} focus is currently is in "allow" area
*/
var focusIsHidden = function(inDocument) {
	if (inDocument === void 0) inDocument = document;
	var activeElement = getActiveElement(inDocument);
	if (!activeElement) return false;
	return toArray(inDocument.querySelectorAll("[".concat(FOCUS_ALLOW, "]"))).some(function(node) {
		return contains(node, activeElement);
	});
};
//#endregion
//#region node_modules/.pnpm/focus-lock@1.3.6/node_modules/focus-lock/dist/es2015/utils/correctFocus.js
var findSelectedRadio = function(node, nodes) {
	return nodes.filter(isRadioElement).filter(function(el) {
		return el.name === node.name;
	}).filter(function(el) {
		return el.checked;
	})[0] || node;
};
var correctNode = function(node, nodes) {
	if (isRadioElement(node) && node.name) return findSelectedRadio(node, nodes);
	return node;
};
/**
* giving a set of radio inputs keeps only selected (tabbable) ones
* @param nodes
*/
var correctNodes = function(nodes) {
	var resultSet = /* @__PURE__ */ new Set();
	nodes.forEach(function(node) {
		return resultSet.add(correctNode(node, nodes));
	});
	return nodes.filter(function(node) {
		return resultSet.has(node);
	});
};
//#endregion
//#region node_modules/.pnpm/focus-lock@1.3.6/node_modules/focus-lock/dist/es2015/utils/firstFocus.js
var pickFirstFocus = function(nodes) {
	if (nodes[0] && nodes.length > 1) return correctNode(nodes[0], nodes);
	return nodes[0];
};
var pickFocusable = function(nodes, node) {
	return nodes.indexOf(correctNode(node, nodes));
};
//#endregion
//#region node_modules/.pnpm/focus-lock@1.3.6/node_modules/focus-lock/dist/es2015/solver.js
var NEW_FOCUS = "NEW_FOCUS";
/**
* Main solver for the "find next focus" question
* @param innerNodes - used to control "return focus"
* @param innerTabbables - used to control "autofocus"
* @param outerNodes
* @param activeElement
* @param lastNode
* @returns {number|string|undefined|*}
*/
var newFocus = function(innerNodes, innerTabbables, outerNodes, activeElement, lastNode) {
	var cnt = innerNodes.length;
	var firstFocus = innerNodes[0];
	var lastFocus = innerNodes[cnt - 1];
	var isOnGuard = isGuard(activeElement);
	if (activeElement && innerNodes.indexOf(activeElement) >= 0) return;
	var activeIndex = activeElement !== void 0 ? outerNodes.indexOf(activeElement) : -1;
	var lastIndex = lastNode ? outerNodes.indexOf(lastNode) : activeIndex;
	var lastNodeInside = lastNode ? innerNodes.indexOf(lastNode) : -1;
	if (activeIndex === -1) {
		if (lastNodeInside !== -1) return lastNodeInside;
		return NEW_FOCUS;
	}
	if (lastNodeInside === -1) return NEW_FOCUS;
	var indexDiff = activeIndex - lastIndex;
	var firstNodeIndex = outerNodes.indexOf(firstFocus);
	var lastNodeIndex = outerNodes.indexOf(lastFocus);
	var correctedNodes = correctNodes(outerNodes);
	var currentFocusableIndex = activeElement !== void 0 ? correctedNodes.indexOf(activeElement) : -1;
	var previousFocusableIndex = lastNode ? correctedNodes.indexOf(lastNode) : currentFocusableIndex;
	var tabbableNodes = correctedNodes.filter(function(node) {
		return node.tabIndex >= 0;
	});
	var currentTabbableIndex = activeElement !== void 0 ? tabbableNodes.indexOf(activeElement) : -1;
	var previousTabbableIndex = lastNode ? tabbableNodes.indexOf(lastNode) : currentTabbableIndex;
	var focusIndexDiff = currentTabbableIndex >= 0 && previousTabbableIndex >= 0 ? previousTabbableIndex - currentTabbableIndex : previousFocusableIndex - currentFocusableIndex;
	if (!indexDiff && lastNodeInside >= 0) return lastNodeInside;
	if (innerTabbables.length === 0) return lastNodeInside;
	var returnFirstNode = pickFocusable(innerNodes, innerTabbables[0]);
	var returnLastNode = pickFocusable(innerNodes, innerTabbables[innerTabbables.length - 1]);
	if (activeIndex <= firstNodeIndex && isOnGuard && Math.abs(indexDiff) > 1) return returnLastNode;
	if (activeIndex >= lastNodeIndex && isOnGuard && Math.abs(indexDiff) > 1) return returnFirstNode;
	if (indexDiff && Math.abs(focusIndexDiff) > 1) return lastNodeInside;
	if (activeIndex <= firstNodeIndex) return returnLastNode;
	if (activeIndex > lastNodeIndex) return returnFirstNode;
	if (indexDiff) {
		if (Math.abs(indexDiff) > 1) return lastNodeInside;
		return (cnt + lastNodeInside + indexDiff) % cnt;
	}
};
//#endregion
//#region node_modules/.pnpm/focus-lock@1.3.6/node_modules/focus-lock/dist/es2015/utils/auto-focus.js
var findAutoFocused = function(autoFocusables) {
	return function(node) {
		var _a;
		var autofocus = (_a = getDataset(node)) === null || _a === void 0 ? void 0 : _a.autofocus;
		return node.autofocus || autofocus !== void 0 && autofocus !== "false" || autoFocusables.indexOf(node) >= 0;
	};
};
var pickAutofocus = function(nodesIndexes, orderedNodes, groups) {
	var autoFocusable = filterAutoFocusable(nodesIndexes.map(function(_a) {
		return _a.node;
	}).filter(findAutoFocused(groups)));
	if (autoFocusable && autoFocusable.length) return pickFirstFocus(autoFocusable);
	return pickFirstFocus(filterAutoFocusable(orderedNodes));
};
//#endregion
//#region node_modules/.pnpm/focus-lock@1.3.6/node_modules/focus-lock/dist/es2015/utils/parenting.js
var getParents = function(node, parents) {
	if (parents === void 0) parents = [];
	parents.push(node);
	if (node.parentNode) getParents(node.parentNode.host || node.parentNode, parents);
	return parents;
};
/**
* finds a parent for both nodeA and nodeB
* @param nodeA
* @param nodeB
* @returns {boolean|*}
*/
var getCommonParent = function(nodeA, nodeB) {
	var parentsA = getParents(nodeA);
	var parentsB = getParents(nodeB);
	for (var i = 0; i < parentsA.length; i += 1) {
		var currentParent = parentsA[i];
		if (parentsB.indexOf(currentParent) >= 0) return currentParent;
	}
	return false;
};
var getTopCommonParent = function(baseActiveElement, leftEntry, rightEntries) {
	var activeElements = asArray(baseActiveElement);
	var leftEntries = asArray(leftEntry);
	var activeElement = activeElements[0];
	var topCommon = false;
	leftEntries.filter(Boolean).forEach(function(entry) {
		topCommon = getCommonParent(topCommon || entry, entry) || topCommon;
		rightEntries.filter(Boolean).forEach(function(subEntry) {
			var common = getCommonParent(activeElement, subEntry);
			if (common) {
				if (!topCommon || contains(common, topCommon)) topCommon = common;
				else topCommon = getCommonParent(common, topCommon);
			}
		});
	});
	return topCommon;
};
/**
* return list of nodes which are expected to be autofocused inside a given top nodes
* @param entries
* @param visibilityCache
*/
var allParentAutofocusables = function(entries, visibilityCache) {
	return entries.reduce(function(acc, node) {
		return acc.concat(parentAutofocusables(node, visibilityCache));
	}, []);
};
//#endregion
//#region node_modules/.pnpm/focus-lock@1.3.6/node_modules/focus-lock/dist/es2015/focusSolver.js
var reorderNodes = function(srcNodes, dstNodes) {
	var remap = /* @__PURE__ */ new Map();
	dstNodes.forEach(function(entity) {
		return remap.set(entity.node, entity);
	});
	return srcNodes.map(function(node) {
		return remap.get(node);
	}).filter(isDefined);
};
/**
* contains the main logic of the `focus-lock` package.
*
* ! you probably dont need this function !
*
* given top node(s) and the last active element returns the element to be focused next
* @returns element which should be focused to move focus inside
* @param topNode
* @param lastNode
*/
var focusSolver = function(topNode, lastNode) {
	var activeElement = getActiveElement(asArray(topNode).length > 0 ? document : getFirst(topNode).ownerDocument);
	var entries = getAllAffectedNodes(topNode).filter(isNotAGuard);
	var commonParent = getTopCommonParent(activeElement || topNode, topNode, entries);
	var visibilityCache = /* @__PURE__ */ new Map();
	var anyFocusable = getFocusableNodes(entries, visibilityCache);
	var innerElements = anyFocusable.filter(function(_a) {
		var node = _a.node;
		return isNotAGuard(node);
	});
	if (!innerElements[0]) return;
	var outerNodes = getFocusableNodes([commonParent], visibilityCache).map(function(_a) {
		return _a.node;
	});
	var orderedInnerElements = reorderNodes(outerNodes, innerElements);
	var innerFocusables = orderedInnerElements.map(function(_a) {
		return _a.node;
	});
	var innerTabbable = orderedInnerElements.filter(function(_a) {
		return _a.tabIndex >= 0;
	}).map(function(_a) {
		return _a.node;
	});
	var newId = newFocus(innerFocusables, innerTabbable, outerNodes, activeElement, lastNode);
	if (newId === "NEW_FOCUS") {
		var focusNode = pickAutofocus(anyFocusable, innerTabbable, allParentAutofocusables(entries, visibilityCache)) || pickAutofocus(anyFocusable, innerFocusables, allParentAutofocusables(entries, visibilityCache));
		if (focusNode) return { node: focusNode };
		else {
			console.warn("focus-lock: cannot find any node to move focus into");
			return;
		}
	}
	if (newId === void 0) return newId;
	return orderedInnerElements[newId];
};
//#endregion
//#region node_modules/.pnpm/focus-lock@1.3.6/node_modules/focus-lock/dist/es2015/focusables.js
/**
* traverses all related nodes (including groups) returning a list of all nodes(outer and internal) with meta information
* This is low-level API!
* @returns list of focusable elements inside a given top(!) node.
* @see {@link getFocusableNodes} providing a simpler API
*/
var expandFocusableNodes = function(topNode) {
	var entries = getAllAffectedNodes(topNode).filter(isNotAGuard);
	var outerNodes = orderByTabIndex(getFocusables([getTopCommonParent(topNode, topNode, entries)], true), true, true);
	var innerElements = getFocusables(entries, false);
	return outerNodes.map(function(_a) {
		var node = _a.node;
		return {
			node,
			index: _a.index,
			lockItem: innerElements.indexOf(node) >= 0,
			guard: isGuard(node)
		};
	});
};
//#endregion
//#region node_modules/.pnpm/focus-lock@1.3.6/node_modules/focus-lock/dist/es2015/commands.js
var focusOn = function(target, focusOptions) {
	if (!target) return;
	if ("focus" in target) target.focus(focusOptions);
	if ("contentWindow" in target && target.contentWindow) target.contentWindow.focus();
};
//#endregion
//#region node_modules/.pnpm/focus-lock@1.3.6/node_modules/focus-lock/dist/es2015/moveFocusInside.js
var guardCount = 0;
var lockDisabled = false;
/**
* The main functionality of the focus-lock package
*
* Contains focus at a given node.
* The last focused element will help to determine which element(first or last) should be focused.
* The found element will be focused.
*
* This is one time action (move), not a persistent focus-lock
*
* HTML markers (see {@link import('./constants').FOCUS_AUTO} constants) can control autofocus
* @see {@link focusSolver} for the same functionality without autofocus
*/
var moveFocusInside = function(topNode, lastNode, options) {
	if (options === void 0) options = {};
	var focusable = focusSolver(topNode, lastNode);
	if (lockDisabled) return;
	if (focusable) {
		/** +FOCUS-FIGHTING prevention **/
		if (guardCount > 2) {
			console.error("FocusLock: focus-fighting detected. Only one focus management system could be active. See https://github.com/theKashey/focus-lock/#focus-fighting");
			lockDisabled = true;
			setTimeout(function() {
				lockDisabled = false;
			}, 1);
			return;
		}
		guardCount++;
		focusOn(focusable.node, options.focusOptions);
		guardCount--;
	}
};
//#endregion
//#region node_modules/.pnpm/focus-lock@1.3.6/node_modules/focus-lock/dist/es2015/return-focus.js
function weakRef(value) {
	if (!value) return null;
	if (typeof WeakRef === "undefined") return function() {
		return value || null;
	};
	var w = value ? new WeakRef(value) : null;
	return function() {
		return (w === null || w === void 0 ? void 0 : w.deref()) || null;
	};
}
var recordElementLocation = function(element) {
	if (!element) return null;
	var stack = [];
	var currentElement = element;
	while (currentElement && currentElement !== document.body) {
		stack.push({
			current: weakRef(currentElement),
			parent: weakRef(currentElement.parentElement),
			left: weakRef(currentElement.previousElementSibling),
			right: weakRef(currentElement.nextElementSibling)
		});
		currentElement = currentElement.parentElement;
	}
	return {
		element: weakRef(element),
		stack,
		ownerDocument: element.ownerDocument
	};
};
var restoreFocusTo = function(location) {
	var _a, _b, _c, _d, _e;
	if (!location) return;
	var stack = location.stack, ownerDocument = location.ownerDocument;
	var visibilityCache = /* @__PURE__ */ new Map();
	for (var _i = 0, stack_1 = stack; _i < stack_1.length; _i++) {
		var line = stack_1[_i];
		var parent_1 = (_a = line.parent) === null || _a === void 0 ? void 0 : _a.call(line);
		if (parent_1 && ownerDocument.contains(parent_1)) {
			var left = (_b = line.left) === null || _b === void 0 ? void 0 : _b.call(line);
			var savedCurrent = line.current();
			var current = parent_1.contains(savedCurrent) ? savedCurrent : void 0;
			var right = (_c = line.right) === null || _c === void 0 ? void 0 : _c.call(line);
			var focusables = getTabbableNodes([parent_1], visibilityCache);
			var aim = (_e = (_d = current !== null && current !== void 0 ? current : left === null || left === void 0 ? void 0 : left.nextElementSibling) !== null && _d !== void 0 ? _d : right) !== null && _e !== void 0 ? _e : left;
			while (aim) {
				for (var _f = 0, focusables_1 = focusables; _f < focusables_1.length; _f++) {
					var focusable = focusables_1[_f];
					if (aim === null || aim === void 0 ? void 0 : aim.contains(focusable.node)) return focusable.node;
				}
				aim = aim.nextElementSibling;
			}
			if (focusables.length) return focusables[0].node;
		}
	}
};
/**
* Captures the current focused element to restore focus as close as possible in the future
* Handles situations where the focused element is removed from the DOM or no longer focusable
* moving focus to the closest focusable element
* @param targetElement - element where focus should be restored
* @returns a function returning a new element to focus
*/
var captureFocusRestore = function(targetElement) {
	var location = recordElementLocation(targetElement);
	return function() {
		return restoreFocusTo(location);
	};
};
//#endregion
//#region node_modules/.pnpm/focus-lock@1.3.6/node_modules/focus-lock/dist/es2015/sibling.js
/**
* for a given `element` in a given `scope` returns focusable siblings
* @param element - base element
* @param scope - common parent. Can be document, but better to narrow it down for performance reasons
* @returns {prev,next} - references to a focusable element before and after
* @returns undefined - if operation is not applicable
*/
var getRelativeFocusable = function(element, scope, useTabbables) {
	if (!element || !scope) {
		console.error("no element or scope given");
		return {};
	}
	var shards = asArray(scope);
	if (shards.every(function(shard) {
		return !contains(shard, element);
	})) {
		console.error("Active element is not contained in the scope");
		return {};
	}
	var focusables = useTabbables ? getTabbableNodes(shards, /* @__PURE__ */ new Map()) : getFocusableNodes(shards, /* @__PURE__ */ new Map());
	var current = focusables.findIndex(function(_a) {
		return _a.node === element;
	});
	if (current === -1) return;
	return {
		prev: focusables[current - 1],
		next: focusables[current + 1],
		first: focusables[0],
		last: focusables[focusables.length - 1]
	};
};
var getBoundary = function(shards, useTabbables) {
	var set = useTabbables ? getTabbableNodes(asArray(shards), /* @__PURE__ */ new Map()) : getFocusableNodes(asArray(shards), /* @__PURE__ */ new Map());
	return {
		first: set[0],
		last: set[set.length - 1]
	};
};
var defaultOptions = function(options) {
	return Object.assign({
		scope: document.body,
		cycle: true,
		onlyTabbable: true
	}, options);
};
var moveFocus = function(fromElement, options, cb) {
	if (options === void 0) options = {};
	var newOptions = defaultOptions(options);
	var solution = getRelativeFocusable(fromElement, newOptions.scope, newOptions.onlyTabbable);
	if (!solution) return;
	var target = cb(solution, newOptions.cycle);
	if (target) focusOn(target.node, newOptions.focusOptions);
};
/**
* focuses next element in the tab-order
* @param fromElement - common parent to scope active element search or tab cycle order
* @param {FocusNextOptions} [options] - focus options
*/
var focusNextElement = function(fromElement, options) {
	if (options === void 0) options = {};
	moveFocus(fromElement, options, function(_a, cycle) {
		var next = _a.next, first = _a.first;
		return next || cycle && first;
	});
};
/**
* focuses prev element in the tab order
* @param fromElement - common parent to scope active element search or tab cycle order
* @param {FocusNextOptions} [options] - focus options
*/
var focusPrevElement = function(fromElement, options) {
	if (options === void 0) options = {};
	moveFocus(fromElement, options, function(_a, cycle) {
		var prev = _a.prev, last = _a.last;
		return prev || cycle && last;
	});
};
var pickBoundary = function(scope, options, what) {
	var _a;
	var node = getBoundary(scope, (_a = options.onlyTabbable) !== null && _a !== void 0 ? _a : true)[what];
	if (node) focusOn(node.node, options.focusOptions);
};
/**
* focuses first element in the tab-order
* @param {FocusNextOptions} options - focus options
*/
var focusFirstElement = function(scope, options) {
	if (options === void 0) options = {};
	pickBoundary(scope, options, "first");
};
/**
* focuses last element in the tab order
* @param {FocusNextOptions} options - focus options
*/
var focusLastElement = function(scope, options) {
	if (options === void 0) options = {};
	pickBoundary(scope, options, "last");
};
//#endregion
//#region node_modules/.pnpm/react-focus-lock@2.13.7_@types+react@19.2.18_react@19.2.8/node_modules/react-focus-lock/dist/es2015/util.js
function deferAction(action) {
	setTimeout(action, 1);
}
var extractRef = function extractRef(ref) {
	return ref && "current" in ref ? ref.current : ref;
};
//#endregion
//#region node_modules/.pnpm/react-focus-lock@2.13.7_@types+react@19.2.18_react@19.2.8/node_modules/react-focus-lock/dist/es2015/Trap.js
var focusOnBody = function focusOnBody() {
	return document && document.activeElement === document.body;
};
var isFreeFocus = function isFreeFocus() {
	return focusOnBody() || focusIsHidden();
};
var lastActiveTrap = null;
var lastActiveFocus = null;
var tryRestoreFocus = function tryRestoreFocus() {
	return null;
};
var lastPortaledElement = null;
var focusWasOutsideWindow = false;
var windowFocused = false;
var defaultWhitelist = function defaultWhitelist() {
	return true;
};
var focusWhitelisted = function focusWhitelisted(activeElement) {
	return (lastActiveTrap.whiteList || defaultWhitelist)(activeElement);
};
var recordPortal = function recordPortal(observerNode, portaledElement) {
	lastPortaledElement = {
		observerNode,
		portaledElement
	};
};
var focusIsPortaledPair = function focusIsPortaledPair(element) {
	return lastPortaledElement && lastPortaledElement.portaledElement === element;
};
function autoGuard(startIndex, end, step, allNodes) {
	var lastGuard = null;
	var i = startIndex;
	do {
		var item = allNodes[i];
		if (item.guard) {
			if (item.node.dataset.focusAutoGuard) lastGuard = item;
		} else if (item.lockItem) {
			if (i !== startIndex) return;
			lastGuard = null;
		} else break;
	} while ((i += step) !== end);
	if (lastGuard) lastGuard.node.tabIndex = 0;
}
var focusWasOutside = function focusWasOutside(crossFrameOption) {
	if (crossFrameOption) return Boolean(focusWasOutsideWindow);
	return focusWasOutsideWindow === "meanwhile";
};
var checkInHost = function checkInHost(check, el, boundary) {
	return el && (el.host === check && (!el.activeElement || boundary.contains(el.activeElement)) || el.parentNode && checkInHost(check, el.parentNode, boundary));
};
var withinHost = function withinHost(activeElement, workingArea) {
	return workingArea.some(function(area) {
		return checkInHost(activeElement, area, area);
	});
};
var getNodeFocusables = function getNodeFocusables(nodes) {
	return getFocusableNodes(nodes, /* @__PURE__ */ new Map());
};
var isNotFocusable = function isNotFocusable(node) {
	return !getNodeFocusables([node.parentNode]).some(function(el) {
		return el.node === node;
	});
};
var activateTrap = function activateTrap() {
	var result = false;
	if (lastActiveTrap) {
		var _lastActiveTrap = lastActiveTrap, observed = _lastActiveTrap.observed, persistentFocus = _lastActiveTrap.persistentFocus, autoFocus = _lastActiveTrap.autoFocus, shards = _lastActiveTrap.shards, crossFrame = _lastActiveTrap.crossFrame, focusOptions = _lastActiveTrap.focusOptions, noFocusGuards = _lastActiveTrap.noFocusGuards;
		var workingNode = observed || lastPortaledElement && lastPortaledElement.portaledElement;
		if (focusOnBody() && lastActiveFocus && lastActiveFocus !== document.body) {
			if (!document.body.contains(lastActiveFocus) || isNotFocusable(lastActiveFocus)) {
				var newTarget = tryRestoreFocus();
				if (newTarget) newTarget.focus();
			}
		}
		var activeElement = document && document.activeElement;
		if (workingNode) {
			var workingArea = [workingNode].concat(shards.map(extractRef).filter(Boolean));
			var shouldForceRestoreFocus = function shouldForceRestoreFocus() {
				if (!focusWasOutside(crossFrame) || !noFocusGuards || !lastActiveFocus || windowFocused) return false;
				var nodes = getNodeFocusables(workingArea);
				var lastIndex = nodes.findIndex(function(_ref) {
					return _ref.node === lastActiveFocus;
				});
				return lastIndex === 0 || lastIndex === nodes.length - 1;
			};
			if (!activeElement || focusWhitelisted(activeElement)) {
				if (persistentFocus || shouldForceRestoreFocus() || !isFreeFocus() || !lastActiveFocus && autoFocus) {
					if (workingNode && !(focusInside(workingArea) || activeElement && withinHost(activeElement, workingArea) || focusIsPortaledPair(activeElement, workingNode))) {
						if (document && !lastActiveFocus && activeElement && !autoFocus) {
							if (activeElement.blur) activeElement.blur();
							document.body.focus();
						} else {
							result = moveFocusInside(workingArea, lastActiveFocus, { focusOptions });
							lastPortaledElement = {};
						}
					}
					lastActiveFocus = document && document.activeElement;
					if (lastActiveFocus !== document.body) tryRestoreFocus = captureFocusRestore(lastActiveFocus);
					focusWasOutsideWindow = false;
				}
			}
			if (document && activeElement !== document.activeElement && document.querySelector("[data-focus-auto-guard]")) {
				var newActiveElement = document && document.activeElement;
				var allNodes = expandFocusableNodes(workingArea);
				var focusedIndex = allNodes.map(function(_ref2) {
					return _ref2.node;
				}).indexOf(newActiveElement);
				if (focusedIndex > -1) {
					allNodes.filter(function(_ref3) {
						var guard = _ref3.guard, node = _ref3.node;
						return guard && node.dataset.focusAutoGuard;
					}).forEach(function(_ref4) {
						return _ref4.node.removeAttribute("tabIndex");
					});
					autoGuard(focusedIndex, allNodes.length, 1, allNodes);
					autoGuard(focusedIndex, -1, -1, allNodes);
				}
			}
		}
	}
	return result;
};
var onTrap = function onTrap(event) {
	if (activateTrap() && event) {
		event.stopPropagation();
		event.preventDefault();
	}
};
var onBlur = function onBlur() {
	return deferAction(activateTrap);
};
var onFocus = function onFocus(event) {
	var source = event.target;
	var currentNode = event.currentTarget;
	if (!currentNode.contains(source)) recordPortal(currentNode, source);
};
var FocusWatcher = function FocusWatcher() {
	return null;
};
var FocusTrap = function FocusTrap(_ref5) {
	var children = _ref5.children;
	return /*#__PURE__*/ import_react.createElement("div", {
		onBlur,
		onFocus
	}, children);
};
FocusTrap.propTypes = { children: import_prop_types.default.node.isRequired };
var onWindowFocus = function onWindowFocus() {
	windowFocused = true;
};
var onWindowBlur = function onWindowBlur() {
	windowFocused = false;
	focusWasOutsideWindow = "just";
	deferAction(function() {
		focusWasOutsideWindow = "meanwhile";
	});
};
var attachHandler = function attachHandler() {
	document.addEventListener("focusin", onTrap);
	document.addEventListener("focusout", onBlur);
	window.addEventListener("focus", onWindowFocus);
	window.addEventListener("blur", onWindowBlur);
};
var detachHandler = function detachHandler() {
	document.removeEventListener("focusin", onTrap);
	document.removeEventListener("focusout", onBlur);
	window.removeEventListener("focus", onWindowFocus);
	window.removeEventListener("blur", onWindowBlur);
};
function reducePropsToState(propsList) {
	return propsList.filter(function(_ref6) {
		return !_ref6.disabled;
	});
}
var focusLockAPI = {
	moveFocusInside,
	focusInside,
	focusNextElement,
	focusPrevElement,
	focusFirstElement,
	focusLastElement,
	captureFocusRestore
};
function handleStateChangeOnClient(traps) {
	var trap = traps.slice(-1)[0];
	if (trap && !lastActiveTrap) attachHandler();
	var lastTrap = lastActiveTrap;
	var sameTrap = lastTrap && trap && trap.id === lastTrap.id;
	lastActiveTrap = trap;
	if (lastTrap && !sameTrap) {
		lastTrap.onDeactivation();
		if (!traps.filter(function(_ref7) {
			return _ref7.id === lastTrap.id;
		}).length) lastTrap.returnFocus(!trap);
	}
	if (trap) {
		lastActiveFocus = null;
		if (!sameTrap || lastTrap.observed !== trap.observed) trap.onActivation(focusLockAPI);
		activateTrap(true);
		deferAction(activateTrap);
	} else {
		detachHandler();
		lastActiveFocus = null;
	}
}
mediumFocus.assignSyncMedium(onFocus);
mediumBlur.assignMedium(onBlur);
mediumEffect.assignMedium(function(cb) {
	return cb(focusLockAPI);
});
var Trap_default = withSideEffect(reducePropsToState, handleStateChangeOnClient)(FocusWatcher);
//#endregion
//#region node_modules/.pnpm/react-focus-lock@2.13.7_@types+react@19.2.18_react@19.2.8/node_modules/react-focus-lock/dist/es2015/Combination.js
var FocusLockCombination = /*#__PURE__*/ (0, import_react.forwardRef)(function FocusLockUICombination(props, ref) {
	return /*#__PURE__*/ import_react.createElement(FocusLock, _extends({
		sideCar: Trap_default,
		ref
	}, props));
});
var _ref = FocusLock.propTypes || {};
_ref.sideCar;
FocusLockCombination.propTypes = _objectWithoutPropertiesLoose(_ref, ["sideCar"]);
//#endregion
//#region node_modules/.pnpm/react-focus-lock@2.13.7_@types+react@19.2.18_react@19.2.8/node_modules/react-focus-lock/dist/es2015/index.js
var es2015_default = FocusLockCombination;
//#endregion
//#region node_modules/.pnpm/date-fns@4.4.0/node_modules/date-fns/startOfMinute.js
/**
* The {@link startOfMinute} function options.
*/
/**
* @name startOfMinute
* @category Minute Helpers
* @summary Return the start of a minute for the given date.
*
* @description
* Return the start of a minute for the given date.
* The result will be in the local timezone.
*
* @typeParam DateType - The `Date` type, the function operates on. Gets inferred from passed arguments. Allows to use extensions like [`UTCDate`](https://github.com/date-fns/utc).
* @typeParam ResultDate - The result `Date` type, it is the type returned from the context function if it is passed, or inferred from the arguments.
*
* @param date - The original date
* @param options - An object with options
*
* @returns The start of a minute
*
* @example
* // The start of a minute for 1 December 2014 22:15:45.400:
* const result = startOfMinute(new Date(2014, 11, 1, 22, 15, 45, 400))
* //=> Mon Dec 01 2014 22:15:00
*/
function startOfMinute(date, options) {
	const date_ = toDate(date, options?.in);
	date_.setSeconds(0, 0);
	return date_;
}
//#endregion
//#region node_modules/.pnpm/date-fns@4.4.0/node_modules/date-fns/addMonths.js
/**
* The {@link addMonths} function options.
*/
/**
* @name addMonths
* @category Month Helpers
* @summary Add the specified number of months to the given date.
*
* @description
* Add the specified number of months to the given date.
*
* @typeParam DateType - The `Date` type, the function operates on. Gets inferred from passed arguments. Allows to use extensions like [`UTCDate`](https://github.com/date-fns/utc).
* @typeParam ResultDate - The result `Date` type, it is the type returned from the context function if it is passed, or inferred from the arguments.
*
* @param date - The date to be changed
* @param amount - The amount of months to be added.
* @param options - The options object
*
* @returns The new date with the months added
*
* @example
* // Add 5 months to 1 September 2014:
* const result = addMonths(new Date(2014, 8, 1), 5)
* //=> Sun Feb 01 2015 00:00:00
*
* // Add one month to 30 January 2023:
* const result = addMonths(new Date(2023, 0, 30), 1)
* //=> Tue Feb 28 2023 00:00:00
*/
function addMonths(date, amount, options) {
	const _date = toDate(date, options?.in);
	if (isNaN(amount)) return constructFrom(options?.in || date, NaN);
	if (!amount) return _date;
	const dayOfMonth = _date.getDate();
	const endOfDesiredMonth = constructFrom(options?.in || date, _date.getTime());
	endOfDesiredMonth.setMonth(_date.getMonth() + amount + 1, 0);
	if (dayOfMonth >= endOfDesiredMonth.getDate()) return endOfDesiredMonth;
	else {
		_date.setFullYear(endOfDesiredMonth.getFullYear(), endOfDesiredMonth.getMonth(), dayOfMonth);
		return _date;
	}
}
//#endregion
//#region node_modules/.pnpm/date-fns@4.4.0/node_modules/date-fns/isSameDay.js
/**
* The {@link isSameDay} function options.
*/
/**
* @name isSameDay
* @category Day Helpers
* @summary Are the given dates in the same day (and year and month)?
*
* @description
* Are the given dates in the same day (and year and month)?
*
* @param laterDate - The first date to check
* @param earlierDate - The second date to check
* @param options - An object with options
*
* @returns The dates are in the same day (and year and month)
*
* @example
* // Are 4 September 06:00:00 and 4 September 18:00:00 in the same day?
* const result = isSameDay(new Date(2014, 8, 4, 6, 0), new Date(2014, 8, 4, 18, 0))
* //=> true
*
* @example
* // Are 4 September and 4 October in the same day?
* const result = isSameDay(new Date(2014, 8, 4), new Date(2014, 9, 4))
* //=> false
*
* @example
* // Are 4 September, 2014 and 4 September, 2015 in the same day?
* const result = isSameDay(new Date(2014, 8, 4), new Date(2015, 8, 4))
* //=> false
*/
function isSameDay(laterDate, earlierDate, options) {
	const [dateLeft_, dateRight_] = normalizeDates(options?.in, laterDate, earlierDate);
	return +startOfDay(dateLeft_) === +startOfDay(dateRight_);
}
//#endregion
//#region node_modules/.pnpm/date-fns@4.4.0/node_modules/date-fns/isSameMonth.js
/**
* The {@link isSameMonth} function options.
*/
/**
* @name isSameMonth
* @category Month Helpers
* @summary Are the given dates in the same month (and year)?
*
* @description
* Are the given dates in the same month (and year)?
*
* @param laterDate - The first date to check
* @param earlierDate - The second date to check
* @param options - An object with options
*
* @returns The dates are in the same month (and year)
*
* @example
* // Are 2 September 2014 and 25 September 2014 in the same month?
* const result = isSameMonth(new Date(2014, 8, 2), new Date(2014, 8, 25))
* //=> true
*
* @example
* // Are 2 September 2014 and 25 September 2015 in the same month?
* const result = isSameMonth(new Date(2014, 8, 2), new Date(2015, 8, 25))
* //=> false
*/
function isSameMonth(laterDate, earlierDate, options) {
	const [laterDate_, earlierDate_] = normalizeDates(options?.in, laterDate, earlierDate);
	return laterDate_.getFullYear() === earlierDate_.getFullYear() && laterDate_.getMonth() === earlierDate_.getMonth();
}
//#endregion
//#region node_modules/.pnpm/date-fns@4.4.0/node_modules/date-fns/_lib/normalizeInterval.js
function normalizeInterval(context, interval) {
	const [start, end] = normalizeDates(context, interval.start, interval.end);
	return {
		start,
		end
	};
}
//#endregion
//#region node_modules/.pnpm/date-fns@4.4.0/node_modules/date-fns/addWeeks.js
/**
* The {@link addWeeks} function options.
*/
/**
* @name addWeeks
* @category Week Helpers
* @summary Add the specified number of weeks to the given date.
*
* @description
* Add the specified number of weeks to the given date.
*
* @typeParam DateType - The `Date` type, the function operates on. Gets inferred from passed arguments. Allows to use extensions like [`UTCDate`](https://github.com/date-fns/utc).
* @typeParam ResultDate - The result `Date` type, it is the type returned from the context function if it is passed, or inferred from the arguments.
*
* @param date - The date to be changed
* @param amount - The amount of weeks to be added.
* @param options - An object with options
*
* @returns The new date with the weeks added
*
* @example
* // Add 4 weeks to 1 September 2014:
* const result = addWeeks(new Date(2014, 8, 1), 4)
* //=> Mon Sep 29 2014 00:00:00
*/
function addWeeks(date, amount, options) {
	return addDays(date, amount * 7, options);
}
//#endregion
//#region node_modules/.pnpm/date-fns@4.4.0/node_modules/date-fns/eachWeekOfInterval.js
/**
* The {@link eachWeekOfInterval} function options.
*/
/**
* The {@link eachWeekOfInterval} function result type. It resolves the proper data type.
* It uses the first argument date object type, starting from the interval start date,
* then the end interval date. If a context function is passed, it uses the context function return type.
*/
/**
* @name eachWeekOfInterval
* @category Interval Helpers
* @summary Return the array of weeks within the specified time interval.
*
* @description
* Return the array of weeks within the specified time interval.
*
* @param interval - The interval.
* @param options - An object with options.
*
* @returns The array with starts of weeks from the week of the interval start to the week of the interval end
*
* @example
* // Each week within interval 6 October 2014 - 23 November 2014:
* const result = eachWeekOfInterval({
*   start: new Date(2014, 9, 6),
*   end: new Date(2014, 10, 23)
* })
* //=> [
* //   Sun Oct 05 2014 00:00:00,
* //   Sun Oct 12 2014 00:00:00,
* //   Sun Oct 19 2014 00:00:00,
* //   Sun Oct 26 2014 00:00:00,
* //   Sun Nov 02 2014 00:00:00,
* //   Sun Nov 09 2014 00:00:00,
* //   Sun Nov 16 2014 00:00:00,
* //   Sun Nov 23 2014 00:00:00
* // ]
*/
function eachWeekOfInterval(interval, options) {
	const { start, end } = normalizeInterval(options?.in, interval);
	let reversed = +start > +end;
	const startDateWeek = reversed ? startOfWeek(end, options) : startOfWeek(start, options);
	const endDateWeek = reversed ? startOfWeek(start, options) : startOfWeek(end, options);
	startDateWeek.setHours(15);
	endDateWeek.setHours(15);
	const endTime = +endDateWeek.getTime();
	let currentDate = startDateWeek;
	let step = options?.step ?? 1;
	if (!step) return [];
	if (step < 0) {
		step = -step;
		reversed = !reversed;
	}
	const dates = [];
	while (+currentDate <= endTime) {
		currentDate.setHours(0);
		dates.push(constructFrom(start, currentDate));
		currentDate = addWeeks(currentDate, step);
		currentDate.setHours(15);
	}
	return reversed ? dates.reverse() : dates;
}
//#endregion
//#region node_modules/.pnpm/date-fns@4.4.0/node_modules/date-fns/lastDayOfMonth.js
/**
* The {@link lastDayOfMonth} function options.
*/
/**
* @name lastDayOfMonth
* @category Month Helpers
* @summary Return the last day of a month for the given date.
*
* @description
* Return the last day of a month for the given date.
* The result will be in the local timezone.
*
* @typeParam DateType - The `Date` type, the function operates on. Gets inferred from passed arguments. Allows to use extensions like [`UTCDate`](https://github.com/date-fns/utc).
* @typeParam ResultDate - The result `Date` type, it is the type returned from the context function if it is passed, or inferred from the arguments.
*
* @param date - The original date
* @param options - An object with options
*
* @returns The last day of a month
*
* @example
* // The last day of a month for 2 September 2014 11:55:00:
* const result = lastDayOfMonth(new Date(2014, 8, 2, 11, 55, 0))
* //=> Tue Sep 30 2014 00:00:00
*/
function lastDayOfMonth(date, options) {
	const _date = toDate(date, options?.in);
	const month = _date.getMonth();
	_date.setFullYear(_date.getFullYear(), month + 1, 0);
	_date.setHours(0, 0, 0, 0);
	return toDate(_date, options?.in);
}
//#endregion
//#region node_modules/.pnpm/date-fns@4.4.0/node_modules/date-fns/startOfMonth.js
/**
* The {@link startOfMonth} function options.
*/
/**
* @name startOfMonth
* @category Month Helpers
* @summary Return the start of a month for the given date.
*
* @description
* Return the start of a month for the given date. The result will be in the local timezone.
*
* @typeParam DateType - The `Date` type, the function operates on. Gets inferred from passed arguments.
* Allows to use extensions like [`UTCDate`](https://github.com/date-fns/utc).
* @typeParam ResultDate - The result `Date` type, it is the type returned from the context function if it is passed,
* or inferred from the arguments.
*
* @param date - The original date
* @param options - An object with options
*
* @returns The start of a month
*
* @example
* // The start of a month for 2 September 2014 11:55:00:
* const result = startOfMonth(new Date(2014, 8, 2, 11, 55, 0))
* //=> Mon Sep 01 2014 00:00:00
*/
function startOfMonth(date, options) {
	const _date = toDate(date, options?.in);
	_date.setDate(1);
	_date.setHours(0, 0, 0, 0);
	return _date;
}
//#endregion
//#region node_modules/.pnpm/date-fns@4.4.0/node_modules/date-fns/setMinutes.js
/**
* The {@link setMinutes} function options.
*/
/**
* @name setMinutes
* @category Minute Helpers
* @summary Set the minutes to the given date.
*
* @description
* Set the minutes to the given date.
*
* @typeParam DateType - The `Date` type, the function operates on. Gets inferred from passed arguments. Allows using extensions like [`UTCDate`](https://github.com/date-fns/utc).
* @typeParam ResultDate - The result `Date` type, returned from the context function, or inferred from the arguments.
*
* @param date - The date to be changed
* @param minutes - The minutes of the new date
* @param options - An object with options
*
* @returns The new date with the minutes set
*
* @example
* // Set 45 minutes to 1 September 2014 11:30:40:
* const result = setMinutes(new Date(2014, 8, 1, 11, 30, 40), 45)
* //=> Mon Sep 01 2014 11:45:40
*/
function setMinutes(date, minutes, options) {
	const date_ = toDate(date, options?.in);
	date_.setMinutes(minutes);
	return date_;
}
//#endregion
//#region node_modules/.pnpm/date-fns@4.4.0/node_modules/date-fns/setDate.js
/**
* The {@link setDate} function options.
*/
/**
* @name setDate
* @category Day Helpers
* @summary Set the day of the month to the given date.
*
* @description
* Set the day of the month to the given date.
*
* @typeParam DateType - The `Date` type, the function operates on. Gets inferred from passed arguments. Allows using extensions like [`UTCDate`](https://github.com/date-fns/utc).
* @typeParam ResultDate - The result `Date` type, it is the type returned from the context function if it is passed, or inferred from the arguments.
*
* @param date - The date to be changed
* @param dayOfMonth - The day of the month of the new date
* @param options - The options
*
* @returns The new date with the day of the month set
*
* @example
* // Set the 30th day of the month to 1 September 2014:
* const result = setDate(new Date(2014, 8, 1), 30)
* //=> Tue Sep 30 2014 00:00:00
*/
function setDate(date, dayOfMonth, options) {
	const _date = toDate(date, options?.in);
	_date.setDate(dayOfMonth);
	return _date;
}
//#endregion
//#region node_modules/.pnpm/date-fns@4.4.0/node_modules/date-fns/setHours.js
/**
* The {@link setHours} function options.
*/
/**
* @name setHours
* @category Hour Helpers
* @summary Set the hours to the given date.
*
* @description
* Set the hours to the given date.
*
* @typeParam DateType - The `Date` type, the function operates on. Gets inferred from passed arguments. Allows to use extensions like [`UTCDate`](https://github.com/date-fns/utc).
* @typeParam ResultDate - The result `Date` type, it is the type returned from the context function if it is passed, or inferred from the arguments.
*
* @param date - The date to be changed
* @param hours - The hours of the new date
* @param options - An object with options
*
* @returns The new date with the hours set
*
* @example
* // Set 4 hours to 1 September 2014 11:30:00:
* const result = setHours(new Date(2014, 8, 1, 11, 30), 4)
* //=> Mon Sep 01 2014 04:30:00
*/
function setHours(date, hours, options) {
	const _date = toDate(date, options?.in);
	_date.setHours(hours);
	return _date;
}
//#endregion
//#region node_modules/.pnpm/date-fns@4.4.0/node_modules/date-fns/getDaysInMonth.js
/**
* The {@link getDaysInMonth} function options.
*/
/**
* @name getDaysInMonth
* @category Month Helpers
* @summary Get the number of days in a month of the given date.
*
* @description
* Get the number of days in a month of the given date, considering the context if provided.
*
* @param date - The given date
* @param options - An object with options
*
* @returns The number of days in a month
*
* @example
* // How many days are in February 2000?
* const result = getDaysInMonth(new Date(2000, 1))
* //=> 29
*/
function getDaysInMonth(date, options) {
	const _date = toDate(date, options?.in);
	const year = _date.getFullYear();
	const monthIndex = _date.getMonth();
	const lastDayOfMonth = constructFrom(_date, 0);
	lastDayOfMonth.setFullYear(year, monthIndex + 1, 0);
	lastDayOfMonth.setHours(0, 0, 0, 0);
	return lastDayOfMonth.getDate();
}
//#endregion
//#region node_modules/.pnpm/date-fns@4.4.0/node_modules/date-fns/setMonth.js
/**
* The {@link setMonth} function options.
*/
/**
* @name setMonth
* @category Month Helpers
* @summary Set the month to the given date.
*
* @description
* Set the month to the given date.
*
* @typeParam DateType - The `Date` type, the function operates on. Gets inferred from passed arguments. Allows to use extensions like [`UTCDate`](https://github.com/date-fns/utc).
* @typeParam ResultDate - The result `Date` type, it is the type returned from the context function if it is passed, or inferred from the arguments.
*
* @param date - The date to be changed
* @param month - The month index to set (0-11)
* @param options - The options
*
* @returns The new date with the month set
*
* @example
* // Set February to 1 September 2014:
* const result = setMonth(new Date(2014, 8, 1), 1)
* //=> Sat Feb 01 2014 00:00:00
*/
function setMonth(date, month, options) {
	const _date = toDate(date, options?.in);
	const year = _date.getFullYear();
	const day = _date.getDate();
	const midMonth = constructFrom(options?.in || date, 0);
	midMonth.setFullYear(year, month, 15);
	midMonth.setHours(0, 0, 0, 0);
	const daysInMonth = getDaysInMonth(midMonth);
	_date.setMonth(month, Math.min(day, daysInMonth));
	return _date;
}
//#endregion
//#region node_modules/.pnpm/date-fns@4.4.0/node_modules/date-fns/setYear.js
/**
* The {@link setYear} function options.
*/
/**
* @name setYear
* @category Year Helpers
* @summary Set the year to the given date.
*
* @description
* Set the year to the given date.
*
* @typeParam DateType - The `Date` type, the function operates on. Gets inferred from passed arguments. Allows to use extensions like [`UTCDate`](https://github.com/date-fns/utc).
* @typeParam ResultDate - The result `Date` type, it is the type returned from the context function if it is passed, or inferred from the arguments.
*
* @param date - The date to be changed
* @param year - The year of the new date
* @param options - An object with options.
*
* @returns The new date with the year set
*
* @example
* // Set year 2013 to 1 September 2014:
* const result = setYear(new Date(2014, 8, 1), 2013)
* //=> Sun Sep 01 2013 00:00:00
*/
function setYear(date, year, options) {
	const date_ = toDate(date, options?.in);
	if (isNaN(+date_)) return constructFrom(options?.in || date, NaN);
	date_.setFullYear(year);
	return date_;
}
//#endregion
//#region node_modules/.pnpm/date-fns@4.4.0/node_modules/date-fns/isPast.js
/**
* @name isPast
* @category Common Helpers
* @summary Is the given date in the past?
* @pure false
*
* @description
* Is the given date in the past?
*
* @param date - The date to check
*
* @returns The date is in the past
*
* @example
* // If today is 6 October 2014, is 2 July 2014 in the past?
* const result = isPast(new Date(2014, 6, 2))
* //=> true
*/
function isPast(date) {
	return +toDate(date) < Date.now();
}
//#endregion
//#region node_modules/.pnpm/sanity@6.9.2_@babel+core@7.29.7_@babel+runtime@7.29.7_@noble+hashes@2.3.0_@rolldown+plu_bc69515520d3729ef0d8dbe997d16e6c/node_modules/sanity/lib/useScheduledDraftMenuActions-DqsFlU0p.js
/**
* Uses the `_system` field to check if a version is a published version.
*
* By default, any published variant will match. Use the constraint options to
* limit matching to the base variant, or to a particular variant id.
*
* @beta
*/
function isPublishedVersion(version, options = { constraint: { anyVariant: !0 } }) {
	let { constraint } = options;
	return !isReleaseVersion(version) && version._system.bundleId === void 0 ? "variant" in constraint ? isVariantVersion(version) && getVariantId(version._system.variant?._ref ?? "") === constraint.variant : constraint.baseVariant ? !isVariantVersion(version) && version._system.group._ref === version._id : !!constraint.anyVariant : !1;
}
/**
* Uses the `_system` field to check if a version is a draft version.
*
* By default, any draft variant will match. Use the constraint options to
* limit matching to the base variant, or to a particular variant id.
*
* @beta
*/
function isDraftVersion(version, options = { constraint: { anyVariant: !0 } }) {
	let { constraint } = options;
	return version._system?.bundleId === "drafts" ? "variant" in constraint ? isVariantVersion(version) && getVariantId(version._system.variant?._ref ?? "") === constraint.variant : constraint.baseVariant ? !isVariantVersion(version) && version._id === getDraftId(version._system.group._ref) : !!constraint.anyVariant : !1;
}
/**
* Uses the _system field to check if a version is a variant version
* @beta
*/
function isVariantVersion(version) {
	return !!version._system?.variant;
}
/**
* Uses the _system field to check if a version is a release version
* @beta
*/
function isReleaseVersion(version) {
	return !!version._system?.release;
}
/**
* Uses the _system field to determine the version type of a document.
* @beta
*/
function readVersionType(version) {
	if (version !== void 0) {
		if (isAgentBundleName(getVersionFromId(version._id))) return "agent";
		if (isPublishedVersion(version)) return "published";
		if (isReleaseVersion(version)) return "release";
		if (isDraftVersion(version)) return "draft";
	}
}
function exists(value) {
	return !!value?._rev;
}
var EMPTY_RESULT = {
	draft: void 0,
	published: void 0,
	versions: {}
};
/**
* Derives the legacy `useDocumentVersionInfo` shape from `useDocumentVersions.versions`.
*
* @internal
*/
function getDocumentVersionInfoFromVersions(versions) {
	let groupId = versions[0]?._system.group._ref;
	if (!groupId) return EMPTY_RESULT;
	if (!versions.every((version) => version._system.group._ref === groupId)) throw Error("Some versions are not in the same group, this is not supported");
	let nonVariantVersions = versions.filter((version) => !isVariantVersion(version)), published = nonVariantVersions.find((version) => isPublishedVersion(version, { constraint: { baseVariant: !0 } })), draft = nonVariantVersions.find((version) => isDraftVersion(version, { constraint: { baseVariant: !0 } })), releaseVersions = {};
	for (let version of nonVariantVersions) {
		let releaseId = version._system.release?._ref;
		releaseId && (releaseVersions[getReleaseIdFromReleaseDocumentId(releaseId)] = exists(version) ? version : void 0);
	}
	return {
		draft: exists(draft) ? draft : void 0,
		published: exists(published) ? published : void 0,
		versions: releaseVersions
	};
}
var TimeZoneCitySpan = /* @__PURE__ */ gt.span.withConfig({
	displayName: "TimeZoneCitySpan",
	componentId: "sc-wf17ev-0"
})(({ theme }) => ft`color:${theme.sanity.color.base.fg};font-weight:500;margin-left:1em;`);
var TimeZoneOffsetSpan = /* @__PURE__ */ gt.span.withConfig({
	displayName: "TimeZoneOffsetSpan",
	componentId: "sc-wf17ev-1"
})(({ theme }) => ft`color:${theme.sanity.color.muted.default.enabled.fg};font-weight:500;`);
var TimeZoneAlternativeNameSpan = /* @__PURE__ */ gt.span.withConfig({
	displayName: "TimeZoneAlternativeNameSpan",
	componentId: "sc-wf17ev-2"
})(({ theme }) => ft`color:${theme.sanity.color.input.default.readOnly.fg};float:right;`);
var DialogTimeZone = (props) => {
	let $ = (0, import_compiler_runtime.c)(66), { onClose, timeZoneScope } = props, { setTimeZone, allTimeZones, timeZone, getLocalTimeZone, getTimeZone } = useTimeZone(timeZoneScope), [selectedTz, setSelectedTz] = (0, import_react.useState)(timeZone), [showAllOptions, setShowAllOptions] = (0, import_react.useState)(!1), { t } = useTranslation$1("studio"), t0;
	$[0] === t ? t0 = $[1] : (t0 = t("time-zone.dialog-info.scheduled-publishing"), $[0] = t, $[1] = t0);
	let t1;
	$[2] === t ? t1 = $[3] : (t1 = t("time-zone.dialog-info.content-releases"), $[2] = t, $[3] = t1);
	let t2;
	$[4] === t ? t2 = $[5] : (t2 = t("time-zone.dialog-info.input"), $[4] = t, $[5] = t2);
	let t3;
	$[6] !== t0 || $[7] !== t1 || $[8] !== t2 ? (t3 = {
		scheduledPublishing: t0,
		contentReleases: t1,
		input: t2
	}, $[6] = t0, $[7] = t1, $[8] = t2, $[9] = t3) : t3 = $[9];
	let timeZoneScopeTypeToLabel = t3, t4;
	$[10] === getTimeZone ? t4 = $[11] : (t4 = (value) => {
		if (!value) {
			setSelectedTz(void 0);
			return;
		}
		setShowAllOptions(!1), setSelectedTz(getTimeZone(value));
	}, $[10] = getTimeZone, $[11] = t4);
	let handleTimeZoneChange = t4, t5;
	$[12] === Symbol.for("react.memo_cache_sentinel") ? (t5 = (newQuery) => {
		newQuery && setShowAllOptions(!1);
	}, $[12] = t5) : t5 = $[12];
	let handleQueryChange = t5, t6;
	$[13] === Symbol.for("react.memo_cache_sentinel") ? (t6 = () => {
		setShowAllOptions(!0);
	}, $[13] = t6) : t6 = $[13];
	let handleAutocompleteOpenButtonClick = t6, t7;
	$[14] === getLocalTimeZone ? t7 = $[15] : (t7 = () => setSelectedTz(getLocalTimeZone()), $[14] = getLocalTimeZone, $[15] = t7);
	let handleTimeZoneSelectLocal = t7, t8;
	$[16] !== onClose || $[17] !== selectedTz || $[18] !== setTimeZone ? (t8 = () => {
		selectedTz && setTimeZone(selectedTz), onClose?.();
	}, $[16] = onClose, $[17] = selectedTz, $[18] = setTimeZone, $[19] = t8) : t8 = $[19];
	let handleTimeZoneUpdate = t8, isDirty = selectedTz?.name !== timeZone.name;
	selectedTz?.name;
	let t9;
	$[20] === getLocalTimeZone ? t9 = $[21] : (t9 = getLocalTimeZone(), $[20] = getLocalTimeZone, $[21] = t9);
	let isLocalTzSelected = selectedTz?.name === t9.name, renderOption = _temp$3, renderValue = _temp2, t10;
	$[22] === showAllOptions ? t10 = $[23] : (t10 = (filterQuery, option_1) => showAllOptions || filterQuery === "" ? !0 : `${option_1.city} (GMT${option_1.offset}) ${option_1.alternativeName}`.toLowerCase().includes(filterQuery.toLowerCase()), $[22] = showAllOptions, $[23] = t10);
	let filterOption = t10, t11 = !isDirty || !selectedTz, t12;
	$[24] !== handleTimeZoneUpdate || $[25] !== t11 ? (t12 = { confirmButton: {
		text: "Update time zone",
		disabled: t11,
		onClick: handleTimeZoneUpdate,
		tone: "primary"
	} }, $[24] = handleTimeZoneUpdate, $[25] = t11, $[26] = t12) : t12 = $[26];
	let t13 = timeZoneScopeTypeToLabel[timeZoneScope.type], t14;
	$[27] === t13 ? t14 = $[28] : (t14 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
		size: 1,
		children: t13
	}), $[27] = t13, $[28] = t14);
	let t15;
	$[29] === t ? t15 = $[30] : (t15 = t("time-zone.time-zone"), $[29] = t, $[30] = t15);
	let t16;
	$[31] === t15 ? t16 = $[32] : (t16 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
		size: 1,
		weight: "semibold",
		children: t15
	}), $[31] = t15, $[32] = t16);
	let t17;
	$[33] !== isLocalTzSelected || $[34] !== t ? (t17 = isLocalTzSelected && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
		muted: !0,
		size: 1,
		children: t("time-zone.local-time")
	}), $[33] = isLocalTzSelected, $[34] = t, $[35] = t17) : t17 = $[35];
	let t18;
	$[36] !== t16 || $[37] !== t17 ? (t18 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Inline, {
		gap: 2,
		children: [t16, t17]
	}), $[36] = t16, $[37] = t17, $[38] = t18) : t18 = $[38];
	let t19;
	$[39] !== handleTimeZoneSelectLocal || $[40] !== isLocalTzSelected || $[41] !== t ? (t19 = !isLocalTzSelected && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
		size: 1,
		weight: "medium",
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("a", {
			onClick: handleTimeZoneSelectLocal,
			style: { cursor: "pointer" },
			children: t("time-zone.action.select-local-time-zone")
		})
	}), $[39] = handleTimeZoneSelectLocal, $[40] = isLocalTzSelected, $[41] = t, $[42] = t19) : t19 = $[42];
	let t20;
	$[43] !== t18 || $[44] !== t19 ? (t20 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Flex, {
		align: "center",
		justify: "space-between",
		children: [t18, t19]
	}), $[43] = t18, $[44] = t19, $[45] = t20) : t20 = $[45];
	let t21;
	$[46] === Symbol.for("react.memo_cache_sentinel") ? (t21 = { onClick: handleAutocompleteOpenButtonClick }, $[46] = t21) : t21 = $[46];
	let t22;
	$[47] === t ? t22 = $[48] : (t22 = t("time-zone.action.search-for-timezone-placeholder"), $[47] = t, $[48] = t22);
	let t23;
	$[49] === Symbol.for("react.memo_cache_sentinel") ? (t23 = {
		floatingBoundary: document.body,
		referenceBoundary: document.body,
		constrainSize: !0,
		placement: "bottom-start"
	}, $[49] = t23) : t23 = $[49];
	let t24 = selectedTz?.value, t25;
	$[50] !== allTimeZones || $[51] !== filterOption || $[52] !== handleTimeZoneChange || $[53] !== t22 || $[54] !== t24 ? (t25 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Autocomplete, {
		fontSize: 2,
		icon: SearchIcon,
		id: "timezone",
		onChange: handleTimeZoneChange,
		onQueryChange: handleQueryChange,
		openButton: t21,
		options: allTimeZones,
		padding: 4,
		filterOption,
		placeholder: t22,
		popover: t23,
		renderOption,
		renderValue,
		tabIndex: -1,
		value: t24
	}), $[50] = allTimeZones, $[51] = filterOption, $[52] = handleTimeZoneChange, $[53] = t22, $[54] = t24, $[55] = t25) : t25 = $[55];
	let t26;
	$[56] !== t20 || $[57] !== t25 ? (t26 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Stack, {
		gap: 3,
		children: [t20, t25]
	}), $[56] = t20, $[57] = t25, $[58] = t26) : t26 = $[58];
	let t27;
	$[59] !== t14 || $[60] !== t26 ? (t27 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Stack, {
		padding: 4,
		gap: 5,
		children: [t14, t26]
	}), $[59] = t14, $[60] = t26, $[61] = t27) : t27 = $[61];
	let t28;
	return $[62] !== onClose || $[63] !== t12 || $[64] !== t27 ? (t28 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Dialog$1, {
		footer: t12,
		header: "Select time zone",
		id: "time-zone",
		onClose,
		width: 1,
		children: t27
	}), $[62] = onClose, $[63] = t12, $[64] = t27, $[65] = t28) : t28 = $[65], t28;
};
function _temp$3(option) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Card, {
		as: "button",
		padding: 3,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Text, {
			size: 1,
			textOverflow: "ellipsis",
			children: [
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)(TimeZoneCitySpan, { children: option.city }),
				/* @__PURE__ */ (0, import_jsx_runtime.jsxs)(TimeZoneOffsetSpan, { children: [
					" ",
					"(",
					"GMT",
					option.offset,
					")"
				] }),
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)(TimeZoneAlternativeNameSpan, { children: option.alternativeName })
			]
		})
	});
}
function _temp2(_value, option_0) {
	return option_0 ? `${option_0.alternativeName} (${option_0.namePretty})` : "";
}
function useDialogTimeZone(timeZoneScope) {
	let $ = (0, import_compiler_runtime.c)(9), [visible, setVisible] = (0, import_react.useState)(!1), t0;
	$[0] === Symbol.for("react.memo_cache_sentinel") ? (t0 = () => {
		setVisible(!1);
	}, $[0] = t0) : t0 = $[0];
	let hide = t0, t1;
	$[1] === Symbol.for("react.memo_cache_sentinel") ? (t1 = () => {
		setVisible(!0);
	}, $[1] = t1) : t1 = $[1];
	let show = t1, t2;
	$[2] !== timeZoneScope || $[3] !== visible ? (t2 = {
		onClose: hide,
		visible,
		timeZoneScope
	}, $[2] = timeZoneScope, $[3] = visible, $[4] = t2) : t2 = $[4];
	let dialogProps = t2, t3 = visible ? DialogTimeZone : null, t4;
	return $[5] !== dialogProps || $[6] !== t3 || $[7] !== timeZoneScope ? (t4 = {
		DialogTimeZone: t3,
		dialogProps,
		timeZoneScope,
		dialogTimeZoneShow: show,
		hide
	}, $[5] = dialogProps, $[6] = t3, $[7] = timeZoneScope, $[8] = t4) : t4 = $[8], t4;
}
/**
* A TextInput that only emit onChange when it has to
* By default it will only emit onChange when: 1) user hits enter or 2) user leaves the
* field (e.g. onBlur) and the input value at this time is different from the given `value` prop
*/
function LazyTextInput(t0) {
	let $ = (0, import_compiler_runtime.c)(23), forwardedRef, onBlur, onChange, onKeyPress, rest, value;
	$[0] === t0 ? (forwardedRef = $[1], onBlur = $[2], onChange = $[3], onKeyPress = $[4], rest = $[5], value = $[6]) : ({ref: forwardedRef, onChange, onBlur, onKeyPress, value, ...rest} = t0, $[0] = t0, $[1] = forwardedRef, $[2] = onBlur, $[3] = onChange, $[4] = onKeyPress, $[5] = rest, $[6] = value);
	let [inputValue, setInputValue] = (0, import_react.useState)(), t1;
	$[7] === Symbol.for("react.memo_cache_sentinel") ? (t1 = (event) => {
		setInputValue(event.currentTarget.value);
	}, $[7] = t1) : t1 = $[7];
	let handleChange = t1, t2;
	$[8] !== onChange || $[9] !== value ? (t2 = (event_0) => {
		event_0.currentTarget.value !== `${value}` && onChange && onChange(event_0), setInputValue(void 0);
	}, $[8] = onChange, $[9] = value, $[10] = t2) : t2 = $[10];
	let checkEvent = t2, t3;
	$[11] !== checkEvent || $[12] !== onBlur ? (t3 = (e) => {
		checkEvent(e), onBlur && onBlur(e);
	}, $[11] = checkEvent, $[12] = onBlur, $[13] = t3) : t3 = $[13];
	let handleBlur = t3, t4;
	$[14] !== checkEvent || $[15] !== onKeyPress ? (t4 = (e_0) => {
		e_0.key === "Enter" && checkEvent(e_0), onKeyPress && onKeyPress(e_0);
	}, $[14] = checkEvent, $[15] = onKeyPress, $[16] = t4) : t4 = $[16];
	let handleKeyPress = t4, t5 = inputValue === void 0 ? value : inputValue, t6;
	return $[17] !== forwardedRef || $[18] !== handleBlur || $[19] !== handleKeyPress || $[20] !== rest || $[21] !== t5 ? (t6 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(TextInput, {
		...rest,
		ref: forwardedRef,
		value: t5,
		onChange: handleChange,
		onBlur: handleBlur,
		onKeyPress: handleKeyPress
	}), $[17] = forwardedRef, $[18] = handleBlur, $[19] = handleKeyPress, $[20] = rest, $[21] = t5, $[22] = t6) : t6 = $[22], t6;
}
/**
* The locale namespace for the releases tool
*
* @public
*/
var singleDocReleaseNamespace = "singleDocRelease";
var singleDocReleaseUsEnglishLocaleBundle = {
	locale: "en-US",
	namespace: singleDocReleaseNamespace,
	resources: () => import("./resources-DETfuBug-BlXlF2c8.js")
};
/**
* Safely extracts an error message from an unknown value that was thrown.
* JavaScript allows throwing any value, not just Error instances.
*
* @param error - The unknown value that was thrown
* @returns A string representation of the error
* @internal
*/
function getErrorMessage(error) {
	return error instanceof Error ? error.message : String(error);
}
var publishedArchivedReleaseDocumentsCache = Object.create(null);
var buildPublishedArchivedReleaseDocumentsObservable = ({ getClient, release }) => {
	let client = getClient(RELEASES_STUDIO_CLIENT_OPTIONS), observableClient = client.observable, dataset = client.config().dataset;
	if (!release.finalDocumentStates?.length) return of({
		loading: !1,
		results: [],
		error: null
	});
	function batchRequestDocumentFromHistory(startIndex) {
		let finalIndex = startIndex + 10;
		return observableClient.request({ url: `/data/history/${dataset}/documents/${release.finalDocumentStates?.slice(startIndex, finalIndex).map((d) => d.id).join(",")}?lastRevision=true` }).pipe(map(({ documents }) => ({
			documents,
			finalIndex
		})));
	}
	return batchRequestDocumentFromHistory(0).pipe(expand((response) => release.finalDocumentStates && response.finalIndex < release.finalDocumentStates.length ? of(null).pipe(delay(100), switchMap(() => batchRequestDocumentFromHistory(response.finalIndex))) : EMPTY), reduce((documents, batch) => documents.concat(batch.documents), [])).pipe(map((documents) => ({
		loading: !1,
		results: documents.map((document) => ({
			document,
			memoKey: v4(),
			validation: {
				validation: [],
				hasError: !1,
				isValidating: !1
			}
		})),
		error: null
	})), catchError((error) => of({
		loading: !1,
		results: [],
		error
	})));
};
var getPublishedArchivedReleaseDocumentsObservable = ({ getClient, release }) => {
	let cacheKey = `${getReleaseIdFromReleaseDocumentId(release._id)}-archived`;
	return publishedArchivedReleaseDocumentsCache[cacheKey] || (publishedArchivedReleaseDocumentsCache[cacheKey] = buildPublishedArchivedReleaseDocumentsObservable({
		getClient,
		release
	}).pipe(finalize(() => {
		delete publishedArchivedReleaseDocumentsCache[cacheKey];
	}), shareReplay(1))), publishedArchivedReleaseDocumentsCache[cacheKey];
};
var getReleaseDocumentsObservable = ({ schema, documentPreviewStore, getClient, releaseId, i18n, releasesState$, currentUser }) => releasesState$.pipe(map((releasesState) => releasesState.releases.get(getReleaseDocumentIdFromReleaseId(releaseId))), filter(Boolean), distinctUntilChanged((prev, next) => prev.state === next.state && prev.finalDocumentStates === next.finalDocumentStates), switchMap((release) => {
	if (release.state === "published" || release.state === "archived") return getPublishedArchivedReleaseDocumentsObservable({
		getClient,
		release
	});
	let cacheKey = [
		releaseId,
		release.state,
		release.finalDocumentStates?.flatMap((doc) => doc.id),
		release._rev
	].join("-");
	return getBundleDocumentsObservable({
		schema,
		documentPreviewStore,
		i18n,
		getClient,
		currentUser,
		groqFilter: "sanity::partOfRelease($releaseId)",
		params: { releaseId },
		skipValidation: isGoingToUnpublish,
		cacheKey
	});
}), startWith({
	loading: !0,
	results: [],
	error: null
}), shareReplay(1));
/**
* Hook to fetch the documents that belong to a release.
*
* Decides, based on the release state, whether to use the generic active-release pipeline
* ({@link getBundleDocumentsObservable}) or the published/archived history pipeline
* ({@link getPublishedArchivedReleaseDocumentsObservable}).
*
* @internal
*/
function useReleaseDocuments(releaseId) {
	let $ = (0, import_compiler_runtime.c)(9), documentPreviewStore = useDocumentPreviewStore(), { getClient, i18n, currentUser } = useSource(), schema = useSchema(), { state$: releasesState$ } = useReleasesStore(), t0;
	$[0] !== currentUser || $[1] !== documentPreviewStore || $[2] !== getClient || $[3] !== i18n || $[4] !== releaseId || $[5] !== releasesState$ || $[6] !== schema ? (t0 = getReleaseDocumentsObservable({
		schema,
		documentPreviewStore,
		getClient,
		releaseId,
		i18n,
		releasesState$,
		currentUser
	}), $[0] = currentUser, $[1] = documentPreviewStore, $[2] = getClient, $[3] = i18n, $[4] = releaseId, $[5] = releasesState$, $[6] = schema, $[7] = t0) : t0 = $[7];
	let releaseDocumentsObservable = t0, t1;
	return $[8] === Symbol.for("react.memo_cache_sentinel") ? (t1 = {
		loading: !0,
		results: [],
		error: null
	}, $[8] = t1) : t1 = $[8], useObservable(releaseDocumentsObservable, t1);
}
/**
* Hook to get the first document from a scheduled draft release bundle.
*
* @internal
*/
function useScheduledDraftDocument(releaseDocumentId, t0) {
	let $ = (0, import_compiler_runtime.c)(19), t1;
	$[0] === t0 ? t1 = $[1] : (t1 = t0 === void 0 ? {} : t0, $[0] = t0, $[1] = t1);
	let { includePreview: t2 } = t1, includePreview = t2 !== void 0 && t2, t3;
	$[2] === releaseDocumentId ? t3 = $[3] : (t3 = releaseDocumentId ? getReleaseIdFromReleaseDocumentId(releaseDocumentId) : "", $[2] = releaseDocumentId, $[3] = t3);
	let { results: documents, loading, error } = useReleaseDocuments(t3), schema = useSchema(), firstDocument = documents?.[0]?.document, firstDocumentValidation = documents?.[0]?.validation.validation, documentsCount = documents?.length || 0, t4;
	$[4] !== firstDocument || $[5] !== schema ? (t4 = firstDocument ? schema.get(firstDocument._type) : null, $[4] = firstDocument, $[5] = schema, $[6] = t4) : t4 = $[6];
	let schemaType = t4, t5 = includePreview && !!firstDocument && !!schemaType, t6 = schemaType || void 0, t7;
	$[7] !== firstDocument || $[8] !== t5 || $[9] !== t6 ? (t7 = {
		enabled: t5,
		schemaType: t6,
		value: firstDocument
	}, $[7] = firstDocument, $[8] = t5, $[9] = t6, $[10] = t7) : t7 = $[10];
	let { value: previewValue, isLoading: previewLoading } = unstable_useValuePreview(t7), t8 = includePreview ? previewValue : void 0, t9 = includePreview ? previewLoading : !1, t10;
	return $[11] !== documentsCount || $[12] !== error || $[13] !== firstDocument || $[14] !== firstDocumentValidation || $[15] !== loading || $[16] !== t8 || $[17] !== t9 ? (t10 = {
		firstDocument,
		firstDocumentPreview: t8,
		firstDocumentValidation,
		documentsCount,
		loading,
		error,
		previewLoading: t9
	}, $[11] = documentsCount, $[12] = error, $[13] = firstDocument, $[14] = firstDocumentValidation, $[15] = loading, $[16] = t8, $[17] = t9, $[18] = t10) : t10 = $[18], t10;
}
/** When a scheduled draft is successfully created */
var ScheduledDraftCreated = defineEvent({
	name: "Scheduled Draft Created",
	version: 1,
	description: "User scheduled a draft for publishing"
});
var ScheduledDraftRescheduled = defineEvent({
	name: "Scheduled Draft Rescheduled",
	version: 1,
	description: "User changed the publish time of a scheduled draft"
});
var ScheduledDraftCancelled = defineEvent({
	name: "Scheduled Draft Cancelled",
	version: 1,
	description: "User cancelled a scheduled draft"
});
/**
* @internal
*/
function useScheduleDraftOperations() {
	let $ = (0, import_compiler_runtime.c)(25), { t } = useTranslation$1(), client = useClient(DEFAULT_STUDIO_CLIENT_OPTIONS), releaseOperations = useReleaseOperations(), { data: allReleases } = useAllReleases(), telemetry = useTelemetry(), t0;
	$[0] === releaseOperations ? t0 = $[1] : (t0 = async (title, scheduleAt, opts) => {
		let releaseDocumentId = createReleaseId();
		return await releaseOperations.createRelease({
			_id: releaseDocumentId,
			metadata: {
				title,
				description: "",
				releaseType: "scheduled",
				cardinality: "one",
				intendedPublishAt: scheduleAt.toISOString()
			}
		}, opts), releaseDocumentId;
	}, $[0] = releaseOperations, $[1] = t0);
	let createScheduledDraftRelease = t0, t1;
	$[2] !== createScheduledDraftRelease || $[3] !== releaseOperations || $[4] !== t || $[5] !== telemetry ? (t1 = async (documentId, publishAt, opts_0) => {
		let releaseTitle = t("scheduled-drafts.release.title"), releaseDocumentId_0 = await createScheduledDraftRelease(releaseTitle, publishAt, opts_0), draftId = getDraftId(documentId), releaseId = getReleaseIdFromReleaseDocumentId(releaseDocumentId_0);
		return await releaseOperations.createVersion(releaseId, draftId, opts_0), await releaseOperations.schedule(releaseDocumentId_0, publishAt, opts_0), telemetry.log(ScheduledDraftCreated), releaseDocumentId_0;
	}, $[2] = createScheduledDraftRelease, $[3] = releaseOperations, $[4] = t, $[5] = telemetry, $[6] = t1) : t1 = $[6];
	let handleCreateScheduledDraft = t1, t2;
	$[7] === releaseOperations ? t2 = $[8] : (t2 = async (release, opts_1) => {
		isReleaseScheduledOrScheduling(release) && await releaseOperations.unschedule(release._id, opts_1), await releaseOperations.publishRelease(release._id, opts_1);
	}, $[7] = releaseOperations, $[8] = t2);
	let handlePublishScheduledDraft = t2, t3;
	$[9] !== allReleases || $[10] !== client || $[11] !== releaseOperations || $[12] !== telemetry ? (t3 = async (releaseDocumentId_1, shouldCopyToDraft, publishedId, opts_2) => {
		let release_0 = allReleases.find((r) => r._id === releaseDocumentId_1);
		if (!release_0) throw Error(`Release with ID ${releaseDocumentId_1} not found`);
		if (shouldCopyToDraft && publishedId) {
			let releaseId_0 = getReleaseIdFromReleaseDocumentId(releaseDocumentId_1), scheduledDraftId = getVersionId(publishedId, releaseId_0), scheduledDraftDoc = await client.getDocument(scheduledDraftId);
			if (scheduledDraftDoc) {
				let draftId_0 = getDraftId(publishedId), { _rev, ...contentWithoutRev } = scheduledDraftDoc;
				await client.createOrReplace({
					...contentWithoutRev,
					_id: draftId_0
				});
			}
		}
		(release_0.state === "scheduled" || release_0.state === "scheduling") && await releaseOperations.unschedule(releaseDocumentId_1, opts_2), release_0.state !== "archived" && release_0.state !== "published" && await releaseOperations.archive(releaseDocumentId_1, opts_2), await releaseOperations.deleteRelease(releaseDocumentId_1, opts_2), telemetry.log(ScheduledDraftCancelled, { keptAsDraft: shouldCopyToDraft });
	}, $[9] = allReleases, $[10] = client, $[11] = releaseOperations, $[12] = telemetry, $[13] = t3) : t3 = $[13];
	let handleDeleteScheduledDraft = t3, t4;
	$[14] !== releaseOperations || $[15] !== telemetry ? (t4 = async (release_1, newPublishAt, opts_3) => {
		let fromPaused = isPausedCardinalityOneRelease(release_1);
		await releaseOperations.updateRelease({
			_id: release_1._id,
			metadata: {
				...release_1.metadata,
				intendedPublishAt: newPublishAt.toISOString()
			}
		}, opts_3), await releaseOperations.schedule(release_1._id, newPublishAt, opts_3), telemetry.log(ScheduledDraftRescheduled, { fromPaused });
	}, $[14] = releaseOperations, $[15] = telemetry, $[16] = t4) : t4 = $[16];
	let handleRescheduleScheduledDraft = t4, t5;
	$[17] === releaseOperations ? t5 = $[18] : (t5 = async (release_2, opts_4) => {
		await releaseOperations.unschedule(release_2._id, opts_4);
	}, $[17] = releaseOperations, $[18] = t5);
	let handlePauseScheduledDraft = t5, t6;
	return $[19] !== handleCreateScheduledDraft || $[20] !== handleDeleteScheduledDraft || $[21] !== handlePauseScheduledDraft || $[22] !== handlePublishScheduledDraft || $[23] !== handleRescheduleScheduledDraft ? (t6 = {
		createScheduledDraft: handleCreateScheduledDraft,
		publishScheduledDraft: handlePublishScheduledDraft,
		deleteScheduledDraft: handleDeleteScheduledDraft,
		rescheduleScheduledDraft: handleRescheduleScheduledDraft,
		pauseScheduledDraft: handlePauseScheduledDraft
	}, $[19] = handleCreateScheduledDraft, $[20] = handleDeleteScheduledDraft, $[21] = handlePauseScheduledDraft, $[22] = handlePublishScheduledDraft, $[23] = handleRescheduleScheduledDraft, $[24] = t6) : t6 = $[24], t6;
}
function getDialogDescription(draftVersionInfo, scheduledDraftBaseRev) {
	return draftVersionInfo ? scheduledDraftBaseRev === draftVersionInfo._rev ? {
		bodyKey: "release.dialog.delete-schedule-draft.body-already-current",
		copy: {
			visible: !1,
			default: !1
		}
	} : {
		bodyKey: "release.dialog.delete-schedule-draft.body-with-choice",
		copy: {
			visible: !0,
			default: !0
		}
	} : {
		bodyKey: "release.dialog.delete-schedule-draft.body-will-save-to-draft",
		copy: {
			visible: !1,
			default: !0
		}
	};
}
function useDeleteScheduledDraft(firstDocumentPreview, onClose, deleteOperation, onDeleteComplete) {
	let { t } = useTranslation$1(), toast = useToast(), [isDeleting, setIsDeleting] = (0, import_react.useState)(!1);
	return {
		isDeleting,
		handleDeleteSchedule: (0, import_react.useCallback)(async () => {
			setIsDeleting(!0);
			let didDelete = !1, run = async () => {
				await deleteOperation(), didDelete = !0, toast.push({
					closable: !0,
					status: "success",
					description: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Translate, {
						t,
						i18nKey: "release.toast.delete-schedule-draft.success",
						values: { title: firstDocumentPreview?.title || t("preview.default.title-fallback") }
					})
				});
			};
			await run().catch((error) => {
				console.error("Failed to delete scheduled draft:", error), toast.push({
					closable: !0,
					status: "error",
					description: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Translate, {
						t,
						i18nKey: "release.toast.delete-schedule-draft.error",
						values: {
							title: firstDocumentPreview?.title || t("preview.default.title-fallback"),
							error: getErrorMessage(error)
						}
					})
				});
			}).finally(() => {
				if (setIsDeleting(!1), onClose(), didDelete) try {
					onDeleteComplete?.();
				} catch (error_0) {
					console.error("onDeleteComplete callback failed:", error_0);
				}
			});
		}, [
			toast,
			t,
			firstDocumentPreview?.title,
			onClose,
			deleteOperation,
			onDeleteComplete
		])
	};
}
/**
* Shared Dialog component that renders the delete confirmation UI shell.
*/
function DeleteScheduledDraftDialogContent(t0) {
	let $ = (0, import_compiler_runtime.c)(21), { onClose, handleDeleteSchedule, isDeleting, children } = t0, { t } = useTranslation$1(), t1;
	$[0] === t ? t1 = $[1] : (t1 = t("release.dialog.delete-schedule-draft.header"), $[0] = t, $[1] = t1);
	let t2;
	$[2] !== isDeleting || $[3] !== onClose ? (t2 = {
		disabled: isDeleting,
		onClick: onClose
	}, $[2] = isDeleting, $[3] = onClose, $[4] = t2) : t2 = $[4];
	let t3;
	$[5] === t ? t3 = $[6] : (t3 = t("release.dialog.delete-schedule-draft.confirm"), $[5] = t, $[6] = t3);
	let t4;
	$[7] !== handleDeleteSchedule || $[8] !== isDeleting || $[9] !== t3 ? (t4 = {
		text: t3,
		tone: "critical",
		onClick: handleDeleteSchedule,
		disabled: isDeleting,
		loading: isDeleting
	}, $[7] = handleDeleteSchedule, $[8] = isDeleting, $[9] = t3, $[10] = t4) : t4 = $[10];
	let t5;
	$[11] !== t2 || $[12] !== t4 ? (t5 = {
		cancelButton: t2,
		confirmButton: t4
	}, $[11] = t2, $[12] = t4, $[13] = t5) : t5 = $[13];
	let t6;
	$[14] === children ? t6 = $[15] : (t6 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Stack, {
		gap: 3,
		paddingX: 3,
		marginBottom: 2,
		children
	}), $[14] = children, $[15] = t6);
	let t7;
	return $[16] !== onClose || $[17] !== t1 || $[18] !== t5 || $[19] !== t6 ? (t7 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Dialog$1, {
		id: "delete-schedule-dialog",
		header: t1,
		onClose,
		width: 0,
		padding: !1,
		footer: t5,
		children: t6
	}), $[16] = onClose, $[17] = t1, $[18] = t5, $[19] = t6, $[20] = t7) : t7 = $[20], t7;
}
function DeleteScheduledDraftDialogWithCopyToDraft(t0) {
	let $ = (0, import_compiler_runtime.c)(39), { documentId, documentType, onClose, onDeleteComplete, release } = t0, { t } = useTranslation$1(), schema = useSchema(), operations = useScheduleDraftOperations(), t1;
	$[0] === Symbol.for("react.memo_cache_sentinel") ? (t1 = { includePreview: !0 }, $[0] = t1) : t1 = $[0];
	let { firstDocument, firstDocumentPreview } = useScheduledDraftDocument(release._id, t1), t2;
	$[1] === documentId ? t2 = $[2] : (t2 = getPublishedId(documentId), $[1] = documentId, $[2] = t2);
	let publishedId = t2, t3;
	$[3] === publishedId ? t3 = $[4] : (t3 = { documentId: publishedId }, $[3] = publishedId, $[4] = t3);
	let { versions } = useDocumentVersions(t3), t4;
	$[5] === versions ? t4 = $[6] : (t4 = getDocumentVersionInfoFromVersions(versions), $[5] = versions, $[6] = t4);
	let versionsInfo = t4, scheduledDraftBaseRev = firstDocument?._system?.base?.rev, t5;
	$[7] !== scheduledDraftBaseRev || $[8] !== versionsInfo.draft ? (t5 = getDialogDescription(versionsInfo.draft, scheduledDraftBaseRev), $[7] = scheduledDraftBaseRev, $[8] = versionsInfo.draft, $[9] = t5) : t5 = $[9];
	let dialogDescription = t5, [shouldCopyToDraft, setShouldCopyToDraft] = (0, import_react.useState)(dialogDescription.copy.default), t6;
	$[10] !== dialogDescription.copy.default || $[11] !== dialogDescription.copy.visible || $[12] !== operations || $[13] !== publishedId || $[14] !== release._id || $[15] !== shouldCopyToDraft ? (t6 = async () => {
		let shouldCopy = dialogDescription.copy.visible ? shouldCopyToDraft : dialogDescription.copy.default;
		await operations.deleteScheduledDraft(release._id, shouldCopy, publishedId);
	}, $[10] = dialogDescription.copy.default, $[11] = dialogDescription.copy.visible, $[12] = operations, $[13] = publishedId, $[14] = release._id, $[15] = shouldCopyToDraft, $[16] = t6) : t6 = $[16];
	let { isDeleting, handleDeleteSchedule } = useDeleteScheduledDraft(firstDocumentPreview, onClose, t6, onDeleteComplete), t7;
	$[17] !== documentType || $[18] !== schema ? (t7 = schema.get(documentType), $[17] = documentType, $[18] = schema, $[19] = t7) : t7 = $[19];
	let schemaType = t7, t8;
	$[20] !== firstDocument || $[21] !== schemaType ? (t8 = schemaType && firstDocument ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Preview, {
		value: firstDocument,
		schemaType
	}) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)(LoadingBlock, {}), $[20] = firstDocument, $[21] = schemaType, $[22] = t8) : t8 = $[22];
	let t9;
	$[23] !== dialogDescription.bodyKey || $[24] !== t ? (t9 = t(dialogDescription.bodyKey), $[23] = dialogDescription.bodyKey, $[24] = t, $[25] = t9) : t9 = $[25];
	let t10;
	$[26] === t9 ? t10 = $[27] : (t10 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Box, {
		paddingX: 2,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
			size: 1,
			muted: !0,
			children: t9
		})
	}), $[26] = t9, $[27] = t10);
	let t11;
	$[28] !== dialogDescription.copy.visible || $[29] !== shouldCopyToDraft || $[30] !== t ? (t11 = dialogDescription.copy.visible && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Box, {
		paddingX: 2,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
			size: 1,
			muted: !0,
			children: t("release.dialog.delete-schedule-draft.different-changes-explanation")
		})
	}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Box, {
		paddingX: 2,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Flex, {
			align: "center",
			gap: 3,
			as: "label",
			children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Checkbox, {
				checked: shouldCopyToDraft,
				onChange: (event) => setShouldCopyToDraft(event.currentTarget.checked)
			}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
				size: 1,
				muted: !0,
				children: t("release.dialog.delete-schedule-draft.copy-checkbox")
			})]
		})
	})] }), $[28] = dialogDescription.copy.visible, $[29] = shouldCopyToDraft, $[30] = t, $[31] = t11) : t11 = $[31];
	let t12;
	return $[32] !== handleDeleteSchedule || $[33] !== isDeleting || $[34] !== onClose || $[35] !== t10 || $[36] !== t11 || $[37] !== t8 ? (t12 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(DeleteScheduledDraftDialogContent, {
		onClose,
		handleDeleteSchedule,
		isDeleting,
		children: [
			t8,
			t10,
			t11
		]
	}), $[32] = handleDeleteSchedule, $[33] = isDeleting, $[34] = onClose, $[35] = t10, $[36] = t11, $[37] = t8, $[38] = t12) : t12 = $[38], t12;
}
/**
* Used when there's no document in the release, avoiding unnecessary calls to useDocumentVersions.
*/
function DeleteScheduledDraftDialogWithEmptyRelease(t0) {
	let $ = (0, import_compiler_runtime.c)(13), { onClose, onDeleteComplete, release } = t0, { t } = useTranslation$1(), operations = useScheduleDraftOperations(), t1;
	$[0] === Symbol.for("react.memo_cache_sentinel") ? (t1 = { includePreview: !0 }, $[0] = t1) : t1 = $[0];
	let { firstDocumentPreview } = useScheduledDraftDocument(release._id, t1), t2;
	$[1] !== operations || $[2] !== release._id ? (t2 = async () => {
		await operations.deleteScheduledDraft(release._id, !1, void 0);
	}, $[1] = operations, $[2] = release._id, $[3] = t2) : t2 = $[3];
	let { isDeleting, handleDeleteSchedule } = useDeleteScheduledDraft(firstDocumentPreview, onClose, t2, onDeleteComplete), t3;
	$[4] === t ? t3 = $[5] : (t3 = t("release.dialog.delete-schedule-draft.body-already-current"), $[4] = t, $[5] = t3);
	let t4;
	$[6] === t3 ? t4 = $[7] : (t4 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Box, {
		paddingX: 2,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
			size: 1,
			muted: !0,
			children: t3
		})
	}), $[6] = t3, $[7] = t4);
	let t5;
	return $[8] !== handleDeleteSchedule || $[9] !== isDeleting || $[10] !== onClose || $[11] !== t4 ? (t5 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(DeleteScheduledDraftDialogContent, {
		onClose,
		handleDeleteSchedule,
		isDeleting,
		children: t4
	}), $[8] = handleDeleteSchedule, $[9] = isDeleting, $[10] = onClose, $[11] = t4, $[12] = t5) : t5 = $[12], t5;
}
function DeleteScheduledDraftDialog(t0) {
	let $ = (0, import_compiler_runtime.c)(10), { documentId, documentType, onClose, onDeleteComplete, release } = t0;
	if (!documentId || !documentType) {
		let t1;
		return $[0] !== onClose || $[1] !== onDeleteComplete || $[2] !== release ? (t1 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(DeleteScheduledDraftDialogWithEmptyRelease, {
			onClose,
			onDeleteComplete,
			release
		}), $[0] = onClose, $[1] = onDeleteComplete, $[2] = release, $[3] = t1) : t1 = $[3], t1;
	}
	let t1;
	return $[4] !== documentId || $[5] !== documentType || $[6] !== onClose || $[7] !== onDeleteComplete || $[8] !== release ? (t1 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(DeleteScheduledDraftDialogWithCopyToDraft, {
		documentId,
		documentType,
		onClose,
		onDeleteComplete,
		release
	}), $[4] = documentId, $[5] = documentType, $[6] = onClose, $[7] = onDeleteComplete, $[8] = release, $[9] = t1) : t1 = $[9], t1;
}
/**
* @internal
*/
function PublishScheduledDraftDialog(props) {
	let $ = (0, import_compiler_runtime.c)(40), { onClose, release, documentType } = props, { t } = useTranslation$1(), toast = useToast(), schema = useSchema(), operations = useScheduleDraftOperations(), [isPublishing, setIsPublishing] = (0, import_react.useState)(!1), t0;
	$[0] === Symbol.for("react.memo_cache_sentinel") ? (t0 = { includePreview: !0 }, $[0] = t0) : t0 = $[0];
	let { firstDocument, firstDocumentPreview } = useScheduledDraftDocument(release._id, t0), t1;
	$[1] !== documentType || $[2] !== schema ? (t1 = documentType ? schema.get(documentType) : null, $[1] = documentType, $[2] = schema, $[3] = t1) : t1 = $[3];
	let schemaType = t1, t2;
	$[4] !== firstDocumentPreview?.title || $[5] !== onClose || $[6] !== operations || $[7] !== release || $[8] !== t || $[9] !== toast ? (t2 = async () => {
		setIsPublishing(!0);
		let run = async () => {
			await operations.publishScheduledDraft(release), toast.push({
				closable: !0,
				status: "success",
				description: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Translate, {
					t,
					i18nKey: "release.toast.publish-scheduled-draft.success",
					values: { title: firstDocumentPreview?.title || t("preview.default.title-fallback") }
				})
			});
		};
		try {
			await run();
		} catch (t3) {
			let error = t3;
			console.error("Failed to run scheduled draft:", error), toast.push({
				closable: !0,
				status: "error",
				description: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Translate, {
					t,
					i18nKey: "release.toast.publish-scheduled-draft.error",
					values: {
						title: firstDocumentPreview?.title || t("preview.default.title-fallback"),
						error: getErrorMessage(error)
					}
				})
			});
		}
		setIsPublishing(!1), onClose();
	}, $[4] = firstDocumentPreview?.title, $[5] = onClose, $[6] = operations, $[7] = release, $[8] = t, $[9] = toast, $[10] = t2) : t2 = $[10], firstDocumentPreview?.title;
	let handlePublishScheduledDraft = t2, t3;
	$[11] === t ? t3 = $[12] : (t3 = t("release.dialog.publish-scheduled-draft.header"), $[11] = t, $[12] = t3);
	let t4;
	$[13] !== isPublishing || $[14] !== onClose ? (t4 = {
		disabled: isPublishing,
		onClick: onClose
	}, $[13] = isPublishing, $[14] = onClose, $[15] = t4) : t4 = $[15];
	let t5;
	$[16] === t ? t5 = $[17] : (t5 = t("release.dialog.publish-scheduled-draft.confirm"), $[16] = t, $[17] = t5);
	let t6;
	$[18] !== handlePublishScheduledDraft || $[19] !== isPublishing || $[20] !== t5 ? (t6 = {
		text: t5,
		tone: "primary",
		onClick: handlePublishScheduledDraft,
		disabled: isPublishing,
		loading: isPublishing
	}, $[18] = handlePublishScheduledDraft, $[19] = isPublishing, $[20] = t5, $[21] = t6) : t6 = $[21];
	let t7;
	$[22] !== t4 || $[23] !== t6 ? (t7 = {
		cancelButton: t4,
		confirmButton: t6
	}, $[22] = t4, $[23] = t6, $[24] = t7) : t7 = $[24];
	let t8;
	$[25] !== firstDocument || $[26] !== schemaType ? (t8 = schemaType && firstDocument ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Preview, {
		value: firstDocument,
		schemaType
	}) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)(LoadingBlock, {}), $[25] = firstDocument, $[26] = schemaType, $[27] = t8) : t8 = $[27];
	let t9;
	$[28] === t ? t9 = $[29] : (t9 = t("release.dialog.publish-scheduled-draft.body"), $[28] = t, $[29] = t9);
	let t10;
	$[30] === t9 ? t10 = $[31] : (t10 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Box, {
		paddingX: 2,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
			size: 1,
			muted: !0,
			children: t9
		})
	}), $[30] = t9, $[31] = t10);
	let t11;
	$[32] !== t10 || $[33] !== t8 ? (t11 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Stack, {
		gap: 3,
		paddingX: 3,
		marginBottom: 2,
		children: [t8, t10]
	}), $[32] = t10, $[33] = t8, $[34] = t11) : t11 = $[34];
	let t12;
	return $[35] !== onClose || $[36] !== t11 || $[37] !== t3 || $[38] !== t7 ? (t12 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Dialog$1, {
		id: "publish-scheduled-draft-dialog",
		"data-testid": "publish-scheduled-draft-dialog",
		header: t3,
		onClose,
		width: 0,
		padding: !1,
		footer: t7,
		children: t11
	}), $[35] = onClose, $[36] = t11, $[37] = t3, $[38] = t7, $[39] = t12) : t12 = $[39], t12;
}
var TimeInput = gt(TextInput).attrs(() => ({ type: "time" })).withConfig({
	displayName: "TimeInput",
	componentId: "sc-2glwbn-0"
})`line-height:1;`;
function CalendarDay(props) {
	let $ = (0, import_compiler_runtime.c)(30), { date, focused, isCurrentMonth, isToday, onSelect, selected, isPastDisabled } = props, t0;
	$[0] !== date || $[1] !== onSelect ? (t0 = () => {
		onSelect(date);
	}, $[0] = date, $[1] = onSelect, $[2] = t0) : t0 = $[2];
	let handleClick = t0, t1;
	$[3] === date ? t1 = $[4] : (t1 = date.toDateString(), $[3] = date, $[4] = t1);
	let t2 = focused ? "true" : "", t3;
	$[5] !== date || $[6] !== isPastDisabled || $[7] !== isToday ? (t3 = isPastDisabled && !isToday && isPast(date), $[5] = date, $[6] = isPastDisabled, $[7] = isToday, $[8] = t3) : t3 = $[8];
	let t4 = isToday || selected ? "primary" : "default", t5;
	$[9] === date ? t5 = $[10] : (t5 = date.toDateString().replaceAll(" ", "-"), $[9] = date, $[10] = t5);
	let t6 = `calendar-day-${t5}`, t7 = !selected && !isCurrentMonth, t8;
	$[11] === Symbol.for("react.memo_cache_sentinel") ? (t8 = { textAlign: "center" }, $[11] = t8) : t8 = $[11];
	let t9 = isCurrentMonth ? "medium" : "regular", t10;
	$[12] === date ? t10 = $[13] : (t10 = date.getDate(), $[12] = date, $[13] = t10);
	let t11;
	$[14] !== t10 || $[15] !== t7 || $[16] !== t9 ? (t11 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
		muted: t7,
		size: 1,
		style: t8,
		weight: t9,
		children: t10
	}), $[14] = t10, $[15] = t7, $[16] = t9, $[17] = t11) : t11 = $[17];
	let t12;
	$[18] !== handleClick || $[19] !== selected || $[20] !== t1 || $[21] !== t11 || $[22] !== t2 || $[23] !== t3 || $[24] !== t4 || $[25] !== t6 ? (t12 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Card, {
		"aria-label": t1,
		"aria-pressed": selected,
		as: "button",
		__unstable_focusRing: !0,
		"data-weekday": !0,
		"data-focused": t2,
		role: "button",
		tabIndex: -1,
		disabled: t3,
		onClick: handleClick,
		padding: 2,
		radius: 2,
		selected,
		tone: t4,
		"data-testid": t6,
		children: t11
	}), $[18] = handleClick, $[19] = selected, $[20] = t1, $[21] = t11, $[22] = t2, $[23] = t3, $[24] = t4, $[25] = t6, $[26] = t12) : t12 = $[26];
	let t13;
	return $[27] !== selected || $[28] !== t12 ? (t13 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
		"aria-selected": selected,
		"data-ui": "CalendarDay",
		children: t12
	}), $[27] = selected, $[28] = t12, $[29] = t13) : t13 = $[29], t13;
}
var DEFAULT_MONTH_NAMES = [
	"January",
	"February",
	"March",
	"April",
	"May",
	"June",
	"July",
	"August",
	"September",
	"October",
	"November",
	"December"
];
var DEFAULT_WEEK_DAY_NAMES = [
	"Sun",
	"Mon",
	"Tue",
	"Wed",
	"Thu",
	"Fri",
	"Sat"
];
var ARROW_KEYS = [
	"ArrowUp",
	"ArrowDown",
	"ArrowLeft",
	"ArrowRight"
];
var DEFAULT_TIME_PRESETS = [
	[0, 0],
	[6, 0],
	[12, 0],
	[18, 0],
	[23, 59]
];
var TAIL_WEEKDAYS = [
	1,
	2,
	3,
	4,
	5,
	6
];
var getWeekStartsOfMonth = (date, weekStartsOn) => {
	let firstDay = startOfMonth(date);
	return eachWeekOfInterval({
		start: firstDay,
		end: lastDayOfMonth(firstDay)
	}, { weekStartsOn: weekStartsOn === 7 ? 0 : weekStartsOn });
};
var getWeekDaysFromWeekStarts = (weekStarts) => weekStarts.map((weekStart) => [weekStart, ...TAIL_WEEKDAYS.map((d) => addDays(weekStart, d))]);
var useWeeksOfMonth = (date) => {
	let $ = (0, import_compiler_runtime.c)(3), { weekInfo } = useCurrentLocale(), t0;
	return $[0] !== date || $[1] !== weekInfo.firstDay ? (t0 = getWeekDaysFromWeekStarts(getWeekStartsOfMonth(date, weekInfo.firstDay)).map(_temp$2), $[0] = date, $[1] = weekInfo.firstDay, $[2] = t0) : t0 = $[2], t0;
};
var formatTime = (hours, minutes) => `${`${hours}`.padStart(2, "0")}:${`${minutes}`.padStart(2, "0")}`;
function _temp$2(days) {
	return {
		number: getWeek(days[0]),
		days
	};
}
function CalendarMonth(props) {
	let $ = (0, import_compiler_runtime.c)(24), { weekInfo: t0 } = useCurrentLocale(), { firstDay: weekStartDay } = t0, weekDayNames = props.weekDayNames;
	if (weekStartDay === 7) {
		let t1;
		$[0] === props.weekDayNames ? t1 = $[1] : (t1 = [props.weekDayNames[6], ...props.weekDayNames.slice(0, 6)], $[0] = props.weekDayNames, $[1] = t1), weekDayNames = t1;
	} else if (weekStartDay === 6) {
		let t1;
		$[2] === props.weekDayNames ? t1 = $[3] : (t1 = [...props.weekDayNames.slice(5), ...props.weekDayNames.slice(0, 5)], $[2] = props.weekDayNames, $[3] = t1), weekDayNames = t1;
	}
	let t1 = props.hidden || !1, t2;
	$[4] === Symbol.for("react.memo_cache_sentinel") ? (t2 = { gridTemplateColumns: "repeat(7, minmax(44px, 46px))" }, $[4] = t2) : t2 = $[4];
	let t3;
	$[5] === weekDayNames ? t3 = $[6] : (t3 = weekDayNames.map(_temp$1), $[5] = weekDayNames, $[6] = t3);
	let t4 = useWeeksOfMonth(props.date), t5;
	if ($[7] !== props.focused || $[8] !== props.isPastDisabled || $[9] !== props.onSelect || $[10] !== props.selected || $[11] !== t4) {
		let t6;
		$[13] !== props.focused || $[14] !== props.isPastDisabled || $[15] !== props.onSelect || $[16] !== props.selected ? (t6 = (week, weekIdx) => week.days.map((date, dayIdx) => {
			let focused = props.focused && isSameDay(date, props.focused), selected = props.selected && isSameDay(date, props.selected), isToday = isSameDay(date, /* @__PURE__ */ new Date()), isCurrentMonth = props.focused && isSameMonth(date, props.focused);
			return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(CalendarDay, {
				date,
				focused,
				isCurrentMonth,
				isToday,
				onSelect: props.onSelect,
				selected,
				isPastDisabled: props.isPastDisabled
			}, `${weekIdx}-${dayIdx}`);
		}), $[13] = props.focused, $[14] = props.isPastDisabled, $[15] = props.onSelect, $[16] = props.selected, $[17] = t6) : t6 = $[17], t5 = t4.map(t6), $[7] = props.focused, $[8] = props.isPastDisabled, $[9] = props.onSelect, $[10] = props.selected, $[11] = t4, $[12] = t5;
	} else t5 = $[12];
	let t6;
	$[18] !== t3 || $[19] !== t5 ? (t6 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Grid, {
		gap: 1,
		style: t2,
		children: [t3, t5]
	}), $[18] = t3, $[19] = t5, $[20] = t6) : t6 = $[20];
	let t7;
	return $[21] !== t1 || $[22] !== t6 ? (t7 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Box, {
		"aria-hidden": t1,
		"data-ui": "CalendarMonth",
		children: t6
	}), $[21] = t1, $[22] = t6, $[23] = t7) : t7 = $[23], t7;
}
function _temp$1(weekday) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Box, {
		paddingY: 2,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
			size: 1,
			weight: "medium",
			style: { textAlign: "center" },
			children: weekday
		})
	}, weekday);
}
var features = {
	dayPresets: !1,
	timePresets: !1
};
var YearInput = (props) => {
	let $ = (0, import_compiler_runtime.c)(8), onChange, restProps;
	$[0] === props ? (onChange = $[1], restProps = $[2]) : ({onChange, ...restProps} = props, $[0] = props, $[1] = onChange, $[2] = restProps);
	let t0;
	$[3] === onChange ? t0 = $[4] : (t0 = (event) => {
		let numericValue = parseInt(event.currentTarget.value, 10);
		isNaN(numericValue) || onChange(numericValue);
	}, $[3] = onChange, $[4] = t0);
	let handleChange = t0, t1;
	return $[5] !== handleChange || $[6] !== restProps ? (t1 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(LazyTextInput, {
		"data-testid": "date-input",
		...restProps,
		fontSize: 1,
		onChange: handleChange,
		inputMode: "numeric",
		padding: 2,
		radius: 2
	}), $[5] = handleChange, $[6] = restProps, $[7] = t1) : t1 = $[7], t1;
};
var MONTH_PICKER_VARIANT = {
	select: "select",
	carousel: "carousel"
};
var PRESERVE_FOCUS_ELEMENT = /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
	"data-preserve-focus": !0,
	style: {
		overflow: "hidden",
		position: "absolute",
		outline: "none"
	},
	tabIndex: -1
});
var CALENDAR_ICON_BUTTON_PROPS = { padding: 2 };
function Calendar(props) {
	let $ = (0, import_compiler_runtime.c)(148), forwardedRef, isPastDisabled, labels, onSelect, onTimeZoneOpen, restProps, selectTime, t0, t1, t2, t3, timeZoneScope, value;
	$[0] === props ? (forwardedRef = $[1], isPastDisabled = $[2], labels = $[3], onSelect = $[4], onTimeZoneOpen = $[5], restProps = $[6], selectTime = $[7], t0 = $[8], t1 = $[9], t2 = $[10], t3 = $[11], timeZoneScope = $[12], value = $[13]) : ({ref: forwardedRef, selectTime, value, timeStep: t0, onSelect, labels, isPastDisabled, monthPickerVariant: t1, padding: t2, showTimeZone: t3, timeZoneScope, onTimeZoneOpen, ...restProps} = props, $[0] = props, $[1] = forwardedRef, $[2] = isPastDisabled, $[3] = labels, $[4] = onSelect, $[5] = onTimeZoneOpen, $[6] = restProps, $[7] = selectTime, $[8] = t0, $[9] = t1, $[10] = t2, $[11] = t3, $[12] = timeZoneScope, $[13] = value);
	let timeStep = t0 === void 0 ? 1 : t0, monthPickerVariant = t1 === void 0 ? "select" : t1, padding = t2 === void 0 ? 2 : t2, showTimeZone = t3 !== void 0 && t3, { timeZone, zoneDateToUtc, utcToCurrentZoneDate } = useTimeZone(timeZoneScope), t4;
	$[14] !== utcToCurrentZoneDate || $[15] !== value ? (t4 = utcToCurrentZoneDate(value), $[14] = utcToCurrentZoneDate, $[15] = value, $[16] = t4) : t4 = $[16];
	let currentTzDate = t4, [focusedDate, setFocusedDate] = (0, import_react.useState)(value), t5;
	$[17] !== focusedDate || $[18] !== timeZone?.name ? (t5 = () => [Number(format(focusedDate, "MM", { timeZone: timeZone?.name })) - 1, Number(format(focusedDate, "YYYY", { timeZone: timeZone?.name }))], $[17] = focusedDate, $[18] = timeZone?.name, $[19] = t5) : t5 = $[19], timeZone?.name;
	let t6;
	$[20] === t5 ? t6 = $[21] : (t6 = t5(), $[20] = t5, $[21] = t6);
	let [displayMonth, displayYear] = t6, { DialogTimeZone, dialogProps, dialogTimeZoneShow } = useDialogTimeZone(timeZoneScope), handleTimeZoneOpen = onTimeZoneOpen ?? dialogTimeZoneShow, t7;
	$[22] === focusedDate ? t7 = $[23] : (t7 = (month) => setFocusedDate(setDate(setMonth(focusedDate, month), 1)), $[22] = focusedDate, $[23] = t7);
	let setFocusedDateMonth = t7, t8;
	$[24] === setFocusedDateMonth ? t8 = $[25] : (t8 = (e) => setFocusedDateMonth(Number(e.currentTarget.value)), $[24] = setFocusedDateMonth, $[25] = t8);
	let handleFocusedMonthChange = t8, t9;
	$[26] === focusedDate ? t9 = $[27] : (t9 = (by) => setFocusedDate(addMonths(focusedDate, by)), $[26] = focusedDate, $[27] = t9);
	let moveFocusedDate = t9, t10;
	$[28] === focusedDate ? t10 = $[29] : (t10 = (year) => setFocusedDate(setYear(focusedDate, year)), $[28] = focusedDate, $[29] = t10);
	let setFocusedDateYear = t10, t11;
	$[30] !== currentTzDate || $[31] !== onSelect || $[32] !== timeZone || $[33] !== zoneDateToUtc ? (t11 = (date) => {
		let newDate = setMinutes(setHours(date, currentTzDate.getHours()), currentTzDate.getMinutes());
		if (!timeZone) {
			onSelect(newDate);
			return;
		}
		let utcDate = zoneDateToUtc(newDate);
		onSelect(utcDate);
	}, $[30] = currentTzDate, $[31] = onSelect, $[32] = timeZone, $[33] = zoneDateToUtc, $[34] = t11) : t11 = $[34];
	let handleDateChange = t11, t12;
	$[35] !== currentTzDate || $[36] !== onSelect || $[37] !== timeZone || $[38] !== zoneDateToUtc ? (t12 = (hours, mins) => {
		if (!timeZone) {
			onSelect(setHours(setMinutes(currentTzDate, mins), hours));
			return;
		}
		let newZonedDate = setHours(setMinutes(new TZDate(currentTzDate, timeZone.name), mins), hours), utcDate_0 = zoneDateToUtc(newZonedDate);
		onSelect(utcDate_0);
	}, $[35] = currentTzDate, $[36] = onSelect, $[37] = timeZone, $[38] = zoneDateToUtc, $[39] = t12) : t12 = $[39];
	let handleTimeChange = t12;
	timeZone?.name;
	let t13 = timeZone?.name, t14;
	$[40] !== currentTzDate || $[41] !== t13 ? (t14 = format(currentTzDate, "HH:mm", { timeZone: t13 }), $[40] = currentTzDate, $[41] = t13, $[42] = t14) : t14 = $[42];
	let timeFromDate = t14, [timeValue, setTimeValue] = (0, import_react.useState)(timeFromDate), t15, t16;
	$[43] === timeFromDate ? (t15 = $[44], t16 = $[45]) : (t15 = () => {
		setTimeValue(timeFromDate);
	}, t16 = [timeFromDate], $[43] = timeFromDate, $[44] = t15, $[45] = t16), (0, import_react.useEffect)(t15, t16);
	let t17;
	$[46] === handleTimeChange ? t17 = $[47] : (t17 = (event) => {
		let nextValue = event.currentTarget.value;
		if (nextValue) {
			setTimeValue(nextValue);
			let date_0 = parse(nextValue, "HH:mm", /* @__PURE__ */ new Date());
			if (!isValid(date_0)) return;
			handleTimeChange(date_0.getHours(), date_0.getMinutes());
		} else setTimeValue(void 0);
	}, $[46] = handleTimeChange, $[47] = t17);
	let handleTimeChangeInputChange = t17, ref = (0, import_react.useRef)(null), t18;
	$[48] === Symbol.for("react.memo_cache_sentinel") ? (t18 = () => ref.current, $[48] = t18) : t18 = $[48], (0, import_react.useImperativeHandle)(forwardedRef, t18);
	let t19;
	$[49] === Symbol.for("react.memo_cache_sentinel") ? (t19 = () => {
		ref.current?.querySelector("[data-focused=\"true\"]")?.focus();
	}, $[49] = t19) : t19 = $[49];
	let focusCurrentWeekDay = t19, t20;
	$[50] === focusedDate ? t20 = $[51] : (t20 = (event_0) => {
		if (!ARROW_KEYS.includes(event_0.key)) return;
		let target = event_0.currentTarget;
		if (event_0.preventDefault(), target.hasAttribute("data-calendar-grid")) {
			focusCurrentWeekDay();
			return;
		}
		event_0.key === "ArrowUp" && setFocusedDate(addDays(focusedDate, -7)), event_0.key === "ArrowDown" && setFocusedDate(addDays(focusedDate, 7)), event_0.key === "ArrowLeft" && setFocusedDate(addDays(focusedDate, -1)), event_0.key === "ArrowRight" && setFocusedDate(addDays(focusedDate, 1)), ref.current?.querySelector("[data-preserve-focus]")?.focus();
	}, $[50] = focusedDate, $[51] = t20);
	let handleKeyDown = t20, t21, t22;
	$[52] === Symbol.for("react.memo_cache_sentinel") ? (t21 = () => {
		focusCurrentWeekDay();
	}, t22 = [focusCurrentWeekDay], $[52] = t21, $[53] = t22) : (t21 = $[52], t22 = $[53]), (0, import_react.useEffect)(t21, t22);
	let t23;
	$[54] === Symbol.for("react.memo_cache_sentinel") ? (t23 = () => {
		document.activeElement?.matches("[data-calendar-grid], [data-calendar-grid] [data-preserve-focus]") && focusCurrentWeekDay();
	}, $[54] = t23) : t23 = $[54];
	let t24;
	$[55] === focusedDate ? t24 = $[56] : (t24 = [
		ref,
		focusCurrentWeekDay,
		focusedDate
	], $[55] = focusedDate, $[56] = t24), (0, import_react.useEffect)(t23, t24);
	let t25;
	$[57] === handleDateChange ? t25 = $[58] : (t25 = () => handleDateChange(addDays(/* @__PURE__ */ new Date(), -1)), $[57] = handleDateChange, $[58] = t25);
	let handleYesterdayClick = t25, t26;
	$[59] === handleDateChange ? t26 = $[60] : (t26 = () => handleDateChange(/* @__PURE__ */ new Date()), $[59] = handleDateChange, $[60] = t26);
	let handleTodayClick = t26, t27;
	$[61] === handleDateChange ? t27 = $[62] : (t27 = () => handleDateChange(addDays(/* @__PURE__ */ new Date(), 1)), $[61] = handleDateChange, $[62] = t27);
	let handleTomorrowClick = t27, t28;
	bb0: {
		if (monthPickerVariant === "carousel") {
			let t29;
			$[63] === Symbol.for("react.memo_cache_sentinel") ? (t29 = {
				borderBottom: "1px solid var(--card-border-color)",
				minHeight: "55px",
				position: "sticky",
				top: 0
			}, $[63] = t29) : t29 = $[63];
			let t30 = labels.monthNames[(focusedDate || /* @__PURE__ */ new Date())?.getMonth()], t31;
			$[64] === focusedDate ? t31 = $[65] : (t31 = (focusedDate || /* @__PURE__ */ new Date())?.getFullYear(), $[64] = focusedDate, $[65] = t31);
			let t32;
			$[66] !== t30 || $[67] !== t31 ? (t32 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Flex, {
				align: "center",
				flex: 1,
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Text, {
					weight: "medium",
					size: 1,
					children: [
						t30,
						" ",
						t31
					]
				})
			}), $[66] = t30, $[67] = t31, $[68] = t32) : t32 = $[68];
			let t33;
			$[69] === moveFocusedDate ? t33 = $[70] : (t33 = () => moveFocusedDate(-1), $[69] = moveFocusedDate, $[70] = t33);
			let t34;
			$[71] === Symbol.for("react.memo_cache_sentinel") ? (t34 = { content: "Previous month" }, $[71] = t34) : t34 = $[71];
			let t35;
			$[72] === t33 ? t35 = $[73] : (t35 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button$1, {
				icon: ChevronLeftIcon,
				mode: "bleed",
				onClick: t33,
				"data-testid": "calendar-prev-month",
				tooltipProps: t34
			}), $[72] = t33, $[73] = t35);
			let t36;
			$[74] === moveFocusedDate ? t36 = $[75] : (t36 = () => moveFocusedDate(1), $[74] = moveFocusedDate, $[75] = t36);
			let t37;
			$[76] === Symbol.for("react.memo_cache_sentinel") ? (t37 = { content: "Next month" }, $[76] = t37) : t37 = $[76];
			let t38;
			$[77] === t36 ? t38 = $[78] : (t38 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button$1, {
				icon: ChevronRightIcon,
				mode: "bleed",
				onClick: t36,
				"data-testid": "calendar-next-month",
				tooltipProps: t37
			}), $[77] = t36, $[78] = t38);
			let t39;
			$[79] !== t35 || $[80] !== t38 ? (t39 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Flex, {
				paddingRight: 3,
				gap: 2,
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(TooltipDelayGroupProvider$1, { children: [t35, t38] })
			}), $[79] = t35, $[80] = t38, $[81] = t39) : t39 = $[81];
			let t40;
			$[82] !== t32 || $[83] !== t39 ? (t40 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Flex, {
				align: "center",
				paddingLeft: 4,
				style: t29,
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Flex, {
					align: "center",
					flex: 1,
					justify: "space-between",
					children: [t32, t39]
				})
			}), $[82] = t32, $[83] = t39, $[84] = t40) : t40 = $[84], t28 = t40;
			break bb0;
		}
		let t29;
		$[85] !== displayMonth || $[86] !== handleFocusedMonthChange || $[87] !== labels.monthNames ? (t29 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Box, {
			flex: 1,
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(CalendarMonthSelect, {
				onChange: handleFocusedMonthChange,
				monthNames: labels.monthNames,
				value: displayMonth
			})
		}), $[85] = displayMonth, $[86] = handleFocusedMonthChange, $[87] = labels.monthNames, $[88] = t29) : t29 = $[88];
		let t30;
		$[89] !== labels.goToNextYear || $[90] !== labels.goToPreviousYear ? (t30 = {
			goToNextYear: labels.goToNextYear,
			goToPreviousYear: labels.goToPreviousYear
		}, $[89] = labels.goToNextYear, $[90] = labels.goToPreviousYear, $[91] = t30) : t30 = $[91];
		let t31;
		$[92] !== displayYear || $[93] !== moveFocusedDate || $[94] !== setFocusedDateYear || $[95] !== t30 ? (t31 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Box, {
			marginLeft: 2,
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(CalendarYearSelect, {
				moveFocusedDate,
				labels: t30,
				onChange: setFocusedDateYear,
				value: displayYear
			})
		}), $[92] = displayYear, $[93] = moveFocusedDate, $[94] = setFocusedDateYear, $[95] = t30, $[96] = t31) : t31 = $[96];
		let t32;
		$[97] !== t29 || $[98] !== t31 ? (t32 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Flex, { children: [t29, t31] }), $[97] = t29, $[98] = t31, $[99] = t32) : t32 = $[99], t28 = t32;
	}
	let monthPicker = t28, t29;
	$[100] === onSelect ? t29 = $[101] : (t29 = () => {
		let now = /* @__PURE__ */ new Date();
		onSelect(now), setFocusedDate(now);
	}, $[100] = onSelect, $[101] = t29);
	let handleNowClick = t29, t30;
	$[102] !== handleTodayClick || $[103] !== handleTomorrowClick || $[104] !== handleYesterdayClick || $[105] !== labels.goToToday || $[106] !== labels.goToTomorrow || $[107] !== labels.goToYesterday ? (t30 = features.dayPresets && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Grid, {
		gridTemplateColumns: 3,
		"data-ui": "CalendaryDayPresets",
		gap: 1,
		children: [
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button$1, {
				text: labels.goToYesterday,
				mode: "bleed",
				onClick: handleYesterdayClick
			}),
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button$1, {
				text: labels.goToToday,
				mode: "bleed",
				onClick: handleTodayClick
			}),
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button$1, {
				text: labels.goToTomorrow,
				mode: "bleed",
				onClick: handleTomorrowClick
			})
		]
	}), $[102] = handleTodayClick, $[103] = handleTomorrowClick, $[104] = handleYesterdayClick, $[105] = labels.goToToday, $[106] = labels.goToTomorrow, $[107] = labels.goToYesterday, $[108] = t30) : t30 = $[108];
	let t31;
	$[109] !== currentTzDate || $[110] !== focusedDate || $[111] !== handleDateChange || $[112] !== isPastDisabled || $[113] !== labels.weekDayNamesShort ? (t31 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(CalendarMonth, {
		weekDayNames: labels.weekDayNamesShort,
		date: focusedDate,
		focused: focusedDate,
		onSelect: handleDateChange,
		selected: currentTzDate,
		isPastDisabled
	}), $[109] = currentTzDate, $[110] = focusedDate, $[111] = handleDateChange, $[112] = isPastDisabled, $[113] = labels.weekDayNamesShort, $[114] = t31) : t31 = $[114];
	let t32;
	$[115] !== handleKeyDown || $[116] !== t31 ? (t32 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Box, {
		"data-calendar-grid": !0,
		onKeyDown: handleKeyDown,
		marginY: 2,
		overflow: "hidden",
		tabIndex: 0,
		children: [t31, PRESERVE_FOCUS_ELEMENT]
	}), $[115] = handleKeyDown, $[116] = t31, $[117] = t32) : t32 = $[117];
	let t33;
	$[118] !== monthPicker || $[119] !== padding || $[120] !== t30 || $[121] !== t32 ? (t33 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Box, {
		padding,
		children: [
			t30,
			monthPicker,
			t32
		]
	}), $[118] = monthPicker, $[119] = padding, $[120] = t30, $[121] = t32, $[122] = t33) : t33 = $[122];
	let t34;
	$[123] === Symbol.for("react.memo_cache_sentinel") ? (t34 = { borderTop: "1px solid var(--card-border-color)" }, $[123] = t34) : t34 = $[123];
	let t35;
	$[124] !== currentTzDate || $[125] !== handleNowClick || $[126] !== handleTimeChange || $[127] !== handleTimeChangeInputChange || $[128] !== handleTimeZoneOpen || $[129] !== labels || $[130] !== selectTime || $[131] !== showTimeZone || $[132] !== timeStep || $[133] !== timeValue || $[134] !== timeZone?.abbreviation ? (t35 = selectTime && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
		/* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Flex, {
			align: "center",
			children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(TimeInput, {
				"aria-label": labels.selectTime,
				value: timeValue,
				onChange: handleTimeChangeInputChange,
				step: timeStep * 60
			}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Box, {
				marginLeft: 2,
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button$1, {
					text: labels.setToCurrentTime,
					mode: "bleed",
					onClick: handleNowClick
				})
			})]
		}),
		showTimeZone && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button$1, {
			icon: EarthGlobeIcon,
			mode: "bleed",
			size: "default",
			text: `${timeZone?.abbreviation}`,
			onClick: handleTimeZoneOpen
		}),
		features.timePresets && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Flex, {
			direction: "row",
			justify: "center",
			align: "center",
			style: { marginTop: 5 },
			children: DEFAULT_TIME_PRESETS.map((t36) => {
				let [hours_0, minutes] = t36, text = formatTime(hours_0, minutes);
				return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(CalendarTimePresetButton, {
					hours: hours_0,
					minutes,
					onTimeChange: handleTimeChange,
					text,
					"aria-label": labels.setToTimePreset(text, currentTzDate)
				}, `${hours_0}-${minutes}`);
			})
		})
	] }), $[124] = currentTzDate, $[125] = handleNowClick, $[126] = handleTimeChange, $[127] = handleTimeChangeInputChange, $[128] = handleTimeZoneOpen, $[129] = labels, $[130] = selectTime, $[131] = showTimeZone, $[132] = timeStep, $[133] = timeValue, $[134] = timeZone?.abbreviation, $[135] = t35) : t35 = $[135];
	let t36;
	$[136] !== DialogTimeZone || $[137] !== dialogProps || $[138] !== onTimeZoneOpen || $[139] !== showTimeZone ? (t36 = showTimeZone && !onTimeZoneOpen && DialogTimeZone && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(DialogTimeZone, { ...dialogProps }), $[136] = DialogTimeZone, $[137] = dialogProps, $[138] = onTimeZoneOpen, $[139] = showTimeZone, $[140] = t36) : t36 = $[140];
	let t37;
	$[141] !== t35 || $[142] !== t36 ? (t37 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Box, {
		padding: 2,
		style: t34,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Flex, {
			align: "center",
			justify: "space-between",
			children: [t35, t36]
		})
	}), $[141] = t35, $[142] = t36, $[143] = t37) : t37 = $[143];
	let t38;
	return $[144] !== restProps || $[145] !== t33 || $[146] !== t37 ? (t38 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Box, {
		"data-testid": "calendar",
		"data-ui": "Calendar",
		...restProps,
		ref,
		children: [t33, t37]
	}), $[144] = restProps, $[145] = t33, $[146] = t37, $[147] = t38) : t38 = $[147], t38;
}
function CalendarTimePresetButton(props) {
	let $ = (0, import_compiler_runtime.c)(8), { hours, minutes, text, onTimeChange } = props, t0;
	$[0] !== hours || $[1] !== minutes || $[2] !== onTimeChange ? (t0 = () => {
		onTimeChange(hours, minutes);
	}, $[0] = hours, $[1] = minutes, $[2] = onTimeChange, $[3] = t0) : t0 = $[3];
	let handleClick = t0, t1 = props["aria-label"], t2;
	return $[4] !== handleClick || $[5] !== t1 || $[6] !== text ? (t2 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button$1, {
		text,
		"aria-label": t1,
		mode: "bleed",
		onClick: handleClick
	}), $[4] = handleClick, $[5] = t1, $[6] = text, $[7] = t2) : t2 = $[7], t2;
}
function CalendarMonthSelect(props) {
	let $ = (0, import_compiler_runtime.c)(6), { onChange, value, monthNames } = props, t0;
	$[0] === monthNames ? t0 = $[1] : (t0 = monthNames.map(_temp), $[0] = monthNames, $[1] = t0);
	let t1;
	return $[2] !== onChange || $[3] !== t0 || $[4] !== value ? (t1 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Flex, {
		flex: 1,
		gap: 1,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Box, {
			flex: 1,
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Select, {
				fontSize: 1,
				radius: 2,
				value,
				onChange,
				padding: 2,
				children: t0
			})
		})
	}), $[2] = onChange, $[3] = t0, $[4] = value, $[5] = t1) : t1 = $[5], t1;
}
function _temp(monthName, i) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("option", {
		value: i,
		children: monthName
	}, i);
}
function CalendarYearSelect(props) {
	let $ = (0, import_compiler_runtime.c)(20), { moveFocusedDate, onChange, value, labels } = props, t0;
	$[0] === moveFocusedDate ? t0 = $[1] : (t0 = () => moveFocusedDate(-12), $[0] = moveFocusedDate, $[1] = t0);
	let handlePrevYearClick = t0, t1;
	$[2] === moveFocusedDate ? t1 = $[3] : (t1 = () => moveFocusedDate(12), $[2] = moveFocusedDate, $[3] = t1);
	let handleNextYearClick = t1, t2;
	$[4] === Symbol.for("react.memo_cache_sentinel") ? (t2 = { content: "Previous year" }, $[4] = t2) : t2 = $[4];
	let t3;
	$[5] !== handlePrevYearClick || $[6] !== labels.goToPreviousYear ? (t3 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button$1, {
		"aria-label": labels.goToPreviousYear,
		onClick: handlePrevYearClick,
		mode: "bleed",
		icon: ChevronLeftIcon,
		tooltipProps: t2,
		...CALENDAR_ICON_BUTTON_PROPS
	}), $[5] = handlePrevYearClick, $[6] = labels.goToPreviousYear, $[7] = t3) : t3 = $[7];
	let t4;
	$[8] === Symbol.for("react.memo_cache_sentinel") ? (t4 = { width: 48 }, $[8] = t4) : t4 = $[8];
	let t5;
	$[9] !== onChange || $[10] !== value ? (t5 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(YearInput, {
		value,
		onChange,
		radius: 0,
		style: t4
	}), $[9] = onChange, $[10] = value, $[11] = t5) : t5 = $[11];
	let t6;
	$[12] === Symbol.for("react.memo_cache_sentinel") ? (t6 = { content: "Next year" }, $[12] = t6) : t6 = $[12];
	let t7;
	$[13] !== handleNextYearClick || $[14] !== labels.goToNextYear ? (t7 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button$1, {
		"aria-label": labels.goToNextYear,
		onClick: handleNextYearClick,
		mode: "bleed",
		icon: ChevronRightIcon,
		tooltipProps: t6,
		...CALENDAR_ICON_BUTTON_PROPS
	}), $[13] = handleNextYearClick, $[14] = labels.goToNextYear, $[15] = t7) : t7 = $[15];
	let t8;
	return $[16] !== t3 || $[17] !== t5 || $[18] !== t7 ? (t8 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Flex, {
		gap: 1,
		children: [
			t3,
			t5,
			t7
		]
	}), $[16] = t3, $[17] = t5, $[18] = t7, $[19] = t8) : t8 = $[19], t8;
}
function DatePicker(props) {
	let $ = (0, import_compiler_runtime.c)(23), _value, calendarLabels, onChange, onTimeZoneOpen, ref, rest, t0, t1, timeZoneScope;
	$[0] === props ? (_value = $[1], calendarLabels = $[2], onChange = $[3], onTimeZoneOpen = $[4], ref = $[5], rest = $[6], t0 = $[7], t1 = $[8], timeZoneScope = $[9]) : ({ref, value: _value, onChange, calendarLabels, padding: t0, showTimeZone: t1, timeZoneScope, onTimeZoneOpen, ...rest} = props, $[0] = props, $[1] = _value, $[2] = calendarLabels, $[3] = onChange, $[4] = onTimeZoneOpen, $[5] = ref, $[6] = rest, $[7] = t0, $[8] = t1, $[9] = timeZoneScope);
	let padding = t0 === void 0 ? 2 : t0, showTimeZone = t1 !== void 0 && t1, t2;
	bb0: {
		if (_value) {
			t2 = _value;
			break bb0;
		}
		let now;
		$[10] === Symbol.for("react.memo_cache_sentinel") ? (now = /* @__PURE__ */ new Date(), now.setSeconds(0, 0), $[10] = now) : now = $[10], t2 = now;
	}
	let value = t2, t3;
	$[11] === onChange ? t3 = $[12] : (t3 = (nextDate) => {
		onChange(nextDate);
	}, $[11] = onChange, $[12] = t3);
	let handleSelect = t3, t4;
	return $[13] !== calendarLabels || $[14] !== handleSelect || $[15] !== onTimeZoneOpen || $[16] !== padding || $[17] !== ref || $[18] !== rest || $[19] !== showTimeZone || $[20] !== timeZoneScope || $[21] !== value ? (t4 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Calendar, {
		...rest,
		labels: calendarLabels,
		ref,
		value,
		onSelect: handleSelect,
		padding,
		showTimeZone,
		timeZoneScope,
		onTimeZoneOpen
	}), $[13] = calendarLabels, $[14] = handleSelect, $[15] = onTimeZoneOpen, $[16] = padding, $[17] = ref, $[18] = rest, $[19] = showTimeZone, $[20] = timeZoneScope, $[21] = value, $[22] = t4) : t4 = $[22], t4;
}
function DatePickerPopoverContent(t0) {
	let $ = (0, import_compiler_runtime.c)(4), { popoverRef, onClose, children } = t0, { isTopLayer } = useLayer(), t1;
	$[0] === popoverRef ? t1 = $[1] : (t1 = () => [popoverRef.current], $[0] = popoverRef, $[1] = t1), useClickOutsideEvent(isTopLayer && onClose, t1);
	let t2;
	return $[2] === children ? t2 = $[3] : (t2 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(import_jsx_runtime.Fragment, { children }), $[2] = children, $[3] = t2), t2;
}
function DateTimeInput(props) {
	let $ = (0, import_compiler_runtime.c)(70), calendarLabels, customValidity, disableInput, forwardedRef, inputValue, isPastDisabled, monthPickerVariant, onChange, onInputChange, padding, readOnly, rest, selectTime, t0, t1, timeStep, timeZoneScope, value;
	$[0] === props ? (calendarLabels = $[1], customValidity = $[2], disableInput = $[3], forwardedRef = $[4], inputValue = $[5], isPastDisabled = $[6], monthPickerVariant = $[7], onChange = $[8], onInputChange = $[9], padding = $[10], readOnly = $[11], rest = $[12], selectTime = $[13], t0 = $[14], t1 = $[15], timeStep = $[16], timeZoneScope = $[17], value = $[18]) : ({ref: forwardedRef, value, inputValue, onInputChange, onChange, selectTime, timeStep, calendarLabels, readOnly, constrainSize: t0, monthPickerVariant, customValidity, padding, disableInput, isPastDisabled, showTimeZone: t1, timeZoneScope, ...rest} = props, $[0] = props, $[1] = calendarLabels, $[2] = customValidity, $[3] = disableInput, $[4] = forwardedRef, $[5] = inputValue, $[6] = isPastDisabled, $[7] = monthPickerVariant, $[8] = onChange, $[9] = onInputChange, $[10] = padding, $[11] = readOnly, $[12] = rest, $[13] = selectTime, $[14] = t0, $[15] = t1, $[16] = timeStep, $[17] = timeZoneScope, $[18] = value);
	let constrainSize = t0 === void 0 || t0, showTimeZone = t1 !== void 0 && t1, { t } = useTranslation$1(), popoverRef = (0, import_react.useRef)(null), ref = (0, import_react.useRef)(null), buttonRef = (0, import_react.useRef)(null), { zoneDateToUtc } = useTimeZone(timeZoneScope), { DialogTimeZone, dialogProps, dialogTimeZoneShow, hide: dialogTimeZoneHide } = useDialogTimeZone(timeZoneScope), [referenceElement, setReferenceElement] = (0, import_react.useState)(null), t2;
	$[19] === Symbol.for("react.memo_cache_sentinel") ? (t2 = () => ref.current, $[19] = t2) : t2 = $[19], (0, import_react.useImperativeHandle)(forwardedRef, t2);
	let t3, t4;
	$[20] === Symbol.for("react.memo_cache_sentinel") ? (t3 = () => setReferenceElement(ref.current), t4 = [], $[20] = t3, $[21] = t4) : (t3 = $[20], t4 = $[21]), (0, import_react.useEffect)(t3, t4);
	let [isPickerOpen, setPickerOpen] = (0, import_react.useState)(!1), t5;
	$[22] === Symbol.for("react.memo_cache_sentinel") ? (t5 = () => setPickerOpen(!1), $[22] = t5) : t5 = $[22];
	let handleClose = t5, t6;
	$[23] === Symbol.for("react.memo_cache_sentinel") ? (t6 = () => {
		ref.current?.focus(), ref.current?.select();
	}, $[23] = t6) : t6 = $[23];
	let handleDeactivation = t6, t7;
	$[24] === Symbol.for("react.memo_cache_sentinel") ? (t7 = (e) => {
		e.key === "Escape" && setPickerOpen(!1);
	}, $[24] = t7) : t7 = $[24];
	let handleKeyUp = t7, t8;
	$[25] === Symbol.for("react.memo_cache_sentinel") ? (t8 = () => setPickerOpen(!0), $[25] = t8) : t8 = $[25];
	let handleClick = t8, t9;
	$[26] === dialogTimeZoneShow ? t9 = $[27] : (t9 = () => {
		setPickerOpen(!1), dialogTimeZoneShow();
	}, $[26] = dialogTimeZoneShow, $[27] = t9);
	let handleTimeZoneOpen = t9, t10;
	$[28] === dialogTimeZoneHide ? t10 = $[29] : (t10 = () => {
		dialogTimeZoneHide(), setPickerOpen(!0);
	}, $[28] = dialogTimeZoneHide, $[29] = t10);
	let handleTimeZoneClose = t10, t11;
	$[30] !== inputValue || $[31] !== isPastDisabled || $[32] !== zoneDateToUtc ? (t11 = inputValue && isPastDisabled && isPast(zoneDateToUtc(new Date(inputValue))), $[30] = inputValue, $[31] = isPastDisabled, $[32] = zoneDateToUtc, $[33] = t11) : t11 = $[33];
	let isDateInPastWarningShown = t11, t12;
	$[34] !== calendarLabels || $[35] !== readOnly ? (t12 = readOnly ? null : /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Flex, {
		style: { padding: "5px" },
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button$1, {
			disabled: readOnly,
			"aria-label": calendarLabels.ariaLabel,
			ref: buttonRef,
			icon: CalendarIcon,
			mode: "bleed",
			onClick: handleClick,
			style: { display: "block" },
			"data-testid": "select-date-button",
			tooltipProps: { content: calendarLabels.tooltipText }
		})
	}), $[34] = calendarLabels, $[35] = readOnly, $[36] = t12) : t12 = $[36];
	let suffix = t12, t13 = disableInput || readOnly, t14;
	$[37] !== calendarLabels || $[38] !== constrainSize || $[39] !== handleTimeZoneOpen || $[40] !== isDateInPastWarningShown || $[41] !== isPastDisabled || $[42] !== isPickerOpen || $[43] !== monthPickerVariant || $[44] !== onChange || $[45] !== padding || $[46] !== referenceElement || $[47] !== selectTime || $[48] !== showTimeZone || $[49] !== suffix || $[50] !== t || $[51] !== timeStep || $[52] !== timeZoneScope || $[53] !== value ? (t14 = isPickerOpen ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(LayerProvider, {
		zOffset: 1e3,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Popover$1, {
			constrainSize,
			"data-testid": "date-input-dialog",
			referenceElement,
			portal: !0,
			content: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(DatePickerPopoverContent, {
				popoverRef,
				onClose: handleClose,
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Box, {
					overflow: "auto",
					children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(es2015_default, {
						onDeactivation: handleDeactivation,
						children: [isDateInPastWarningShown && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Card, {
							margin: 1,
							padding: 2,
							radius: 2,
							shadow: 1,
							tone: "critical",
							children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
								size: 1,
								children: t("inputs.dateTime.past-date-warning")
							})
						}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(DatePicker, {
							monthPickerVariant,
							calendarLabels,
							selectTime,
							timeStep,
							onKeyUp: handleKeyUp,
							value,
							isPastDisabled,
							onChange,
							padding,
							showTimeZone,
							timeZoneScope,
							onTimeZoneOpen: handleTimeZoneOpen
						})]
					})
				})
			}),
			open: !0,
			placement: "bottom",
			ref: popoverRef,
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(import_jsx_runtime.Fragment, { children: suffix })
		})
	}) : suffix, $[37] = calendarLabels, $[38] = constrainSize, $[39] = handleTimeZoneOpen, $[40] = isDateInPastWarningShown, $[41] = isPastDisabled, $[42] = isPickerOpen, $[43] = monthPickerVariant, $[44] = onChange, $[45] = padding, $[46] = referenceElement, $[47] = selectTime, $[48] = showTimeZone, $[49] = suffix, $[50] = t, $[51] = timeStep, $[52] = timeZoneScope, $[53] = value, $[54] = t14) : t14 = $[54];
	let t15;
	$[55] !== customValidity || $[56] !== inputValue || $[57] !== onInputChange || $[58] !== rest || $[59] !== t13 || $[60] !== t14 ? (t15 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(LazyTextInput, {
		ref,
		"data-testid": "date-input",
		...rest,
		readOnly: t13,
		value: inputValue,
		onChange: onInputChange,
		customValidity,
		suffix: t14
	}), $[55] = customValidity, $[56] = inputValue, $[57] = onInputChange, $[58] = rest, $[59] = t13, $[60] = t14, $[61] = t15) : t15 = $[61];
	let t16;
	$[62] !== DialogTimeZone || $[63] !== dialogProps || $[64] !== handleTimeZoneClose || $[65] !== showTimeZone ? (t16 = showTimeZone && DialogTimeZone && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(DialogTimeZone, {
		...dialogProps,
		onClose: handleTimeZoneClose
	}), $[62] = DialogTimeZone, $[63] = dialogProps, $[64] = handleTimeZoneClose, $[65] = showTimeZone, $[66] = t16) : t16 = $[66];
	let t17;
	return $[67] !== t15 || $[68] !== t16 ? (t17 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [t15, t16] }), $[67] = t15, $[68] = t16, $[69] = t17) : t17 = $[69], t17;
}
function isValidDate(date) {
	return date instanceof Date && !isNaN(date.valueOf());
}
/**
* @internal
*/
function getCalendarLabels(t) {
	return {
		ariaLabel: t("calendar.button.aria-label"),
		goToTomorrow: t("calendar.action.go-to-tomorrow"),
		goToToday: t("calendar.action.go-to-today"),
		goToYesterday: t("calendar.action.go-to-yesterday"),
		goToNextMonth: t("calendar.action.go-to-next-month"),
		goToPreviousMonth: t("calendar.action.go-to-previous-month"),
		goToNextYear: t("calendar.action.go-to-next-year"),
		goToPreviousYear: t("calendar.action.go-to-previous-year"),
		setToCurrentTime: t("calendar.action.set-to-current-time"),
		selectTime: t("calendar.action.select-time"),
		tooltipText: t("calendar.button.tooltip-text"),
		monthNames: [
			t("calendar.month-names.january"),
			t("calendar.month-names.february"),
			t("calendar.month-names.march"),
			t("calendar.month-names.april"),
			t("calendar.month-names.may"),
			t("calendar.month-names.june"),
			t("calendar.month-names.july"),
			t("calendar.month-names.august"),
			t("calendar.month-names.september"),
			t("calendar.month-names.october"),
			t("calendar.month-names.november"),
			t("calendar.month-names.december")
		],
		weekDayNamesShort: [
			t("calendar.weekday-names.short.monday"),
			t("calendar.weekday-names.short.tuesday"),
			t("calendar.weekday-names.short.wednesday"),
			t("calendar.weekday-names.short.thursday"),
			t("calendar.weekday-names.short.friday"),
			t("calendar.weekday-names.short.saturday"),
			t("calendar.weekday-names.short.sunday")
		],
		setToTimePreset: (time, date) => t("calendar.action.set-to-time-preset", {
			time,
			date
		})
	};
}
var Z_OFFSET = { toast: [100, 11e3] };
var SCHEDULED_PUBLISHING_TIME_ZONE_SCOPE = { type: "scheduledPublishing" };
var CONTENT_RELEASES_TIME_ZONE_SCOPE = { type: "contentReleases" };
var SCHEDULE_DRAFT_DIALOG_CONFIG = {
	schedule: {
		headerI18nKey: "schedule-publish-dialog.header",
		descriptionI18nKey: "schedule-publish-dialog.description",
		confirmButtonTextI18nKey: "schedule-publish-dialog.confirm"
	},
	"edit-schedule": {
		headerI18nKey: "release.dialog.edit-schedule.header",
		descriptionI18nKey: "release.dialog.edit-schedule.body",
		confirmButtonTextI18nKey: "release.dialog.edit-schedule.confirm"
	}
};
function ScheduleDraftDialog(props) {
	let $ = (0, import_compiler_runtime.c)(110), { onClose, onSchedule, variant, loading: t0, initialDate } = props, loading = t0 !== void 0 && t0, { t } = useTranslation$1(), t1;
	$[0] === initialDate ? t1 = $[1] : (t1 = () => new Date(initialDate || /* @__PURE__ */ new Date()), $[0] = initialDate, $[1] = t1);
	let [publishAt, setPublishAt] = (0, import_react.useState)(t1), { utcToCurrentZoneDate, zoneDateToUtc } = useTimeZone(CONTENT_RELEASES_TIME_ZONE_SCOPE), T0, T1, T2, T3, _isScheduledDateInPast, t10, t11, t12, t13, t14, t15, t16, t17, t18, t19, t2, t20, t21, t3, t4, t5, t6, t7, t8, t9;
	if ($[2] !== loading || $[3] !== onClose || $[4] !== onSchedule || $[5] !== publishAt || $[6] !== t || $[7] !== utcToCurrentZoneDate || $[8] !== variant || $[9] !== zoneDateToUtc) {
		let timeZoneAdjustedPublishAt = publishAt ? utcToCurrentZoneDate(publishAt) : void 0, t22;
		$[35] === t ? t22 = $[36] : (t22 = getCalendarLabels(t), $[35] = t, $[36] = t22);
		let calendarLabels = t22, dialogConfig = SCHEDULE_DRAFT_DIALOG_CONFIG[variant], t23;
		$[37] !== publishAt || $[38] !== zoneDateToUtc ? (t23 = () => !publishAt || isBefore(zoneDateToUtc(publishAt), /* @__PURE__ */ new Date()), $[37] = publishAt, $[38] = zoneDateToUtc, $[39] = t23) : t23 = $[39];
		let isScheduledDateInPast = t23, t24;
		$[40] === zoneDateToUtc ? t24 = $[41] : (t24 = (date) => {
			date && setPublishAt(zoneDateToUtc(startOfMinute(date)));
		}, $[40] = zoneDateToUtc, $[41] = t24);
		let handleDateTimeChange = t24, t25;
		$[42] === zoneDateToUtc ? t25 = $[43] : (t25 = (event) => {
			let date_0 = event.currentTarget.value, parsedDate = zoneDateToUtc(parse(date_0, "PP HH:mm", /* @__PURE__ */ new Date()));
			isValid(parsedDate) && setPublishAt(parsedDate);
		}, $[42] = zoneDateToUtc, $[43] = t25);
		let handleInputChange = t25, t26;
		$[44] !== isScheduledDateInPast || $[45] !== onSchedule || $[46] !== publishAt ? (t26 = async () => {
			!publishAt || isScheduledDateInPast() || onSchedule(publishAt);
		}, $[44] = isScheduledDateInPast, $[45] = onSchedule, $[46] = publishAt, $[47] = t26) : t26 = $[47];
		let handleConfirmSchedule = t26, t27;
		$[48] === isScheduledDateInPast ? t27 = $[49] : (t27 = isScheduledDateInPast(), $[48] = isScheduledDateInPast, $[49] = t27), _isScheduledDateInPast = t27, T3 = Dialog$1, t16 = "schedule-draft-dialog", $[50] !== dialogConfig.headerI18nKey || $[51] !== t ? (t17 = t(dialogConfig.headerI18nKey), $[50] = dialogConfig.headerI18nKey, $[51] = t, $[52] = t17) : t17 = $[52], t18 = loading ? void 0 : onClose, t19 = 0, t20 = !1;
		let t28;
		$[53] !== dialogConfig.confirmButtonTextI18nKey || $[54] !== t ? (t28 = t(dialogConfig.confirmButtonTextI18nKey), $[53] = dialogConfig.confirmButtonTextI18nKey, $[54] = t, $[55] = t28) : t28 = $[55];
		let t29 = _isScheduledDateInPast || loading || !publishAt, t30;
		$[56] !== handleConfirmSchedule || $[57] !== loading || $[58] !== t28 || $[59] !== t29 ? (t30 = {
			text: t28,
			tone: "primary",
			onClick: handleConfirmSchedule,
			loading,
			disabled: t29
		}, $[56] = handleConfirmSchedule, $[57] = loading, $[58] = t28, $[59] = t29, $[60] = t30) : t30 = $[60];
		let t31;
		$[61] === loading ? t31 = $[62] : (t31 = { disabled: loading }, $[61] = loading, $[62] = t31), $[63] !== t30 || $[64] !== t31 ? (t21 = {
			confirmButton: t30,
			cancelButton: t31
		}, $[63] = t30, $[64] = t31, $[65] = t21) : t21 = $[65], T2 = Stack, t11 = 4, t12 = 4, t13 = 4, t14 = 1;
		let t32;
		$[66] !== dialogConfig.descriptionI18nKey || $[67] !== t ? (t32 = t(dialogConfig.descriptionI18nKey), $[66] = dialogConfig.descriptionI18nKey, $[67] = t, $[68] = t32) : t32 = $[68], $[69] === t32 ? t15 = $[70] : (t15 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
			size: 1,
			muted: !0,
			children: t32
		}), $[69] = t32, $[70] = t15), T1 = Stack, t9 = 3;
		let t33;
		$[71] === t ? t33 = $[72] : (t33 = t("release.schedule-dialog.select-publish-date-label"), $[71] = t, $[72] = t33), $[73] === t33 ? t10 = $[74] : (t10 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
			size: 1,
			weight: "semibold",
			children: t33
		}), $[73] = t33, $[74] = t10), T0 = DateTimeInput, t2 = !0, t3 = MONTH_PICKER_VARIANT.carousel, t4 = handleDateTimeChange, t5 = handleInputChange, t6 = timeZoneAdjustedPublishAt, t7 = calendarLabels, t8 = timeZoneAdjustedPublishAt ? format$1(timeZoneAdjustedPublishAt, "PP HH:mm") : "", $[2] = loading, $[3] = onClose, $[4] = onSchedule, $[5] = publishAt, $[6] = t, $[7] = utcToCurrentZoneDate, $[8] = variant, $[9] = zoneDateToUtc, $[10] = T0, $[11] = T1, $[12] = T2, $[13] = T3, $[14] = _isScheduledDateInPast, $[15] = t10, $[16] = t11, $[17] = t12, $[18] = t13, $[19] = t14, $[20] = t15, $[21] = t16, $[22] = t17, $[23] = t18, $[24] = t19, $[25] = t2, $[26] = t20, $[27] = t21, $[28] = t3, $[29] = t4, $[30] = t5, $[31] = t6, $[32] = t7, $[33] = t8, $[34] = t9;
	} else T0 = $[10], T1 = $[11], T2 = $[12], T3 = $[13], _isScheduledDateInPast = $[14], t10 = $[15], t11 = $[16], t12 = $[17], t13 = $[18], t14 = $[19], t15 = $[20], t16 = $[21], t17 = $[22], t18 = $[23], t19 = $[24], t2 = $[25], t20 = $[26], t21 = $[27], t3 = $[28], t4 = $[29], t5 = $[30], t6 = $[31], t7 = $[32], t8 = $[33], t9 = $[34];
	let t22;
	$[75] !== T0 || $[76] !== t2 || $[77] !== t3 || $[78] !== t4 || $[79] !== t5 || $[80] !== t6 || $[81] !== t7 || $[82] !== t8 ? (t22 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(T0, {
		selectTime: t2,
		monthPickerVariant: t3,
		onChange: t4,
		onInputChange: t5,
		value: t6,
		calendarLabels: t7,
		inputValue: t8,
		constrainSize: !1,
		isPastDisabled: !0,
		showTimeZone: !0,
		timeZoneScope: CONTENT_RELEASES_TIME_ZONE_SCOPE
	}), $[75] = T0, $[76] = t2, $[77] = t3, $[78] = t4, $[79] = t5, $[80] = t6, $[81] = t7, $[82] = t8, $[83] = t22) : t22 = $[83];
	let t23;
	$[84] !== T1 || $[85] !== t10 || $[86] !== t22 || $[87] !== t9 ? (t23 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)("label", { children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(T1, {
		gap: t9,
		children: [t10, t22]
	}) }), $[84] = T1, $[85] = t10, $[86] = t22, $[87] = t9, $[88] = t23) : t23 = $[88];
	let t24;
	$[89] !== _isScheduledDateInPast || $[90] !== t ? (t24 = _isScheduledDateInPast && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Card, {
		marginBottom: 1,
		padding: 2,
		radius: 2,
		shadow: 1,
		tone: "critical",
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
			size: 1,
			children: t("release.schedule-dialog.publish-date-in-past-warning")
		})
	}), $[89] = _isScheduledDateInPast, $[90] = t, $[91] = t24) : t24 = $[91];
	let t25;
	$[92] !== T2 || $[93] !== t11 || $[94] !== t12 || $[95] !== t13 || $[96] !== t14 || $[97] !== t15 || $[98] !== t23 || $[99] !== t24 ? (t25 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(T2, {
		gap: t11,
		paddingX: t12,
		paddingBottom: t13,
		paddingTop: t14,
		children: [
			t15,
			t23,
			t24
		]
	}), $[92] = T2, $[93] = t11, $[94] = t12, $[95] = t13, $[96] = t14, $[97] = t15, $[98] = t23, $[99] = t24, $[100] = t25) : t25 = $[100];
	let t26;
	return $[101] !== T3 || $[102] !== t16 || $[103] !== t17 || $[104] !== t18 || $[105] !== t19 || $[106] !== t20 || $[107] !== t21 || $[108] !== t25 ? (t26 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(T3, {
		id: t16,
		header: t17,
		onClose: t18,
		width: t19,
		padding: t20,
		footer: t21,
		children: t25
	}), $[101] = T3, $[102] = t16, $[103] = t17, $[104] = t18, $[105] = t19, $[106] = t20, $[107] = t21, $[108] = t25, $[109] = t26) : t26 = $[109], t26;
}
/**
* Hook that provides reusable scheduled draft menu action props that can be used to create MenuItem components.
*
* @internal
*/
function useScheduledDraftMenuActions(options) {
	let $ = (0, import_compiler_runtime.c)(67), { release, documentType, documentId, disabled: t0, onActionComplete, onDeleteComplete } = options, disabled = t0 !== void 0 && t0, { t } = useTranslation$1(), toast = useToast(), operations = useScheduleDraftOperations(), [selectedAction, setSelectedAction] = (0, import_react.useState)(null), [isPerformingOperation, setIsPerformingOperation] = (0, import_react.useState)(!1), [isScheduling, setIsScheduling] = (0, import_react.useState)(!1), t1;
	$[0] === Symbol.for("react.memo_cache_sentinel") ? (t1 = { includePreview: !0 }, $[0] = t1) : t1 = $[0];
	let { firstDocumentPreview, loading: documentLoading } = useScheduledDraftDocument(release?._id, t1), t2;
	$[1] !== firstDocumentPreview?.title || $[2] !== onActionComplete || $[3] !== operations || $[4] !== release || $[5] !== t || $[6] !== toast ? (t2 = async () => {
		if (!release) return;
		setIsPerformingOperation(!0);
		let run = async () => {
			await operations.pauseScheduledDraft(release), onActionComplete?.();
		};
		try {
			await run();
		} catch (t3) {
			let error = t3;
			console.error("Failed to pause scheduled draft:", error), toast.push({
				closable: !0,
				status: "error",
				description: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Translate, {
					t,
					i18nKey: "release.toast.pause-scheduled-draft.error",
					values: {
						title: firstDocumentPreview?.title || t("preview.default.title-fallback"),
						error: getErrorMessage(error)
					}
				})
			});
		}
		setIsPerformingOperation(!1), setSelectedAction(null);
	}, $[1] = firstDocumentPreview?.title, $[2] = onActionComplete, $[3] = operations, $[4] = release, $[5] = t, $[6] = toast, $[7] = t2) : t2 = $[7], firstDocumentPreview?.title;
	let handleEditSchedule = t2, t3;
	$[8] === Symbol.for("react.memo_cache_sentinel") ? (t3 = (action) => {
		setSelectedAction(action);
	}, $[8] = t3) : t3 = $[8];
	let handleMenuItemClick = t3, t4;
	$[9] === isPerformingOperation ? t4 = $[10] : (t4 = () => {
		isPerformingOperation || setSelectedAction(null);
	}, $[9] = isPerformingOperation, $[10] = t4);
	let handleDialogClose = t4, t5;
	$[11] !== handleDialogClose || $[12] !== operations || $[13] !== release || $[14] !== t || $[15] !== toast ? (t5 = async (publishAt) => {
		if (!release) return;
		setIsScheduling(!0);
		let run_0 = async () => {
			await operations.rescheduleScheduledDraft(release, publishAt), toast.push({
				closable: !0,
				status: "success",
				title: t("release.toast.schedule-publish.success")
			});
		};
		try {
			await run_0();
		} catch (t6) {
			let error_0 = t6;
			console.error("Failed to schedule draft:", error_0), toast.push({
				closable: !0,
				status: "error",
				title: t("release.toast.schedule-publish.error", { error: getErrorMessage(error_0) })
			});
		}
		setIsScheduling(!1), handleDialogClose();
	}, $[11] = handleDialogClose, $[12] = operations, $[13] = release, $[14] = t, $[15] = toast, $[16] = t5) : t5 = $[16];
	let handleSchedulePublish = t5, baseDisabled = disabled || isPerformingOperation || documentLoading, t6;
	$[17] === t ? t6 = $[18] : (t6 = t("release.action.publish-now"), $[17] = t, $[18] = t6);
	let t7;
	$[19] === Symbol.for("react.memo_cache_sentinel") ? (t7 = () => handleMenuItemClick("publish-now"), $[19] = t7) : t7 = $[19];
	let t8;
	$[20] !== baseDisabled || $[21] !== t6 ? (t8 = {
		icon: PublishIcon,
		text: t6,
		tone: "default",
		onClick: t7,
		disabled: baseDisabled,
		"data-testid": "publish-now-menu-item"
	}, $[20] = baseDisabled, $[21] = t6, $[22] = t8) : t8 = $[22];
	let t9;
	$[23] === t ? t9 = $[24] : (t9 = t("release.action.edit-schedule"), $[23] = t, $[24] = t9);
	let t10;
	$[25] !== baseDisabled || $[26] !== handleEditSchedule || $[27] !== t9 ? (t10 = {
		icon: EditIcon,
		text: t9,
		tone: "default",
		onClick: handleEditSchedule,
		disabled: baseDisabled,
		"data-testid": "edit-schedule-menu-item"
	}, $[25] = baseDisabled, $[26] = handleEditSchedule, $[27] = t9, $[28] = t10) : t10 = $[28];
	let t11;
	$[29] === t ? t11 = $[30] : (t11 = t("release.action.schedule-publish"), $[29] = t, $[30] = t11);
	let t12;
	$[31] === Symbol.for("react.memo_cache_sentinel") ? (t12 = () => handleMenuItemClick("schedule-publish"), $[31] = t12) : t12 = $[31];
	let t13;
	$[32] !== baseDisabled || $[33] !== t11 ? (t13 = {
		icon: CalendarIcon,
		text: t11,
		tone: "default",
		onClick: t12,
		disabled: baseDisabled,
		"data-testid": "schedule-publish-menu-item"
	}, $[32] = baseDisabled, $[33] = t11, $[34] = t13) : t13 = $[34];
	let t14;
	$[35] === t ? t14 = $[36] : (t14 = t("release.action.delete-schedule"), $[35] = t, $[36] = t14);
	let t15;
	$[37] === Symbol.for("react.memo_cache_sentinel") ? (t15 = () => handleMenuItemClick("delete-schedule"), $[37] = t15) : t15 = $[37];
	let t16;
	$[38] !== baseDisabled || $[39] !== t14 ? (t16 = {
		icon: TrashIcon,
		text: t14,
		tone: "critical",
		onClick: t15,
		disabled: baseDisabled,
		"data-testid": "delete-schedule-menu-item"
	}, $[38] = baseDisabled, $[39] = t14, $[40] = t16) : t16 = $[40];
	let t17;
	$[41] !== t10 || $[42] !== t13 || $[43] !== t16 || $[44] !== t8 ? (t17 = {
		publishNow: t8,
		editSchedule: t10,
		schedulePublish: t13,
		deleteSchedule: t16
	}, $[41] = t10, $[42] = t13, $[43] = t16, $[44] = t8, $[45] = t17) : t17 = $[45];
	let actions = t17, t18;
	bb0: {
		if (!selectedAction || !release) {
			t18 = null;
			break bb0;
		}
		switch (selectedAction) {
			case "publish-now": {
				let t19;
				$[46] !== documentType || $[47] !== handleDialogClose || $[48] !== release ? (t19 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(PublishScheduledDraftDialog, {
					release,
					documentType,
					onClose: handleDialogClose
				}), $[46] = documentType, $[47] = handleDialogClose, $[48] = release, $[49] = t19) : t19 = $[49], t18 = t19;
				break bb0;
			}
			case "delete-schedule": {
				let t19;
				$[50] !== documentId || $[51] !== documentType || $[52] !== handleDialogClose || $[53] !== onDeleteComplete || $[54] !== release ? (t19 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(DeleteScheduledDraftDialog, {
					release,
					documentType,
					documentId,
					onClose: handleDialogClose,
					onDeleteComplete
				}), $[50] = documentId, $[51] = documentType, $[52] = handleDialogClose, $[53] = onDeleteComplete, $[54] = release, $[55] = t19) : t19 = $[55], t18 = t19;
				break bb0;
			}
			case "schedule-publish": {
				let t19 = release?.metadata?.intendedPublishAt, t20;
				$[56] !== handleDialogClose || $[57] !== handleSchedulePublish || $[58] !== isScheduling || $[59] !== t19 ? (t20 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ScheduleDraftDialog, {
					onClose: handleDialogClose,
					onSchedule: handleSchedulePublish,
					variant: "schedule",
					loading: isScheduling,
					initialDate: t19
				}), $[56] = handleDialogClose, $[57] = handleSchedulePublish, $[58] = isScheduling, $[59] = t19, $[60] = t20) : t20 = $[60], t18 = t20;
				break bb0;
			}
			default: t18 = null;
		}
	}
	let dialogs = t18, t19;
	return $[61] !== actions || $[62] !== dialogs || $[63] !== handleDialogClose || $[64] !== isPerformingOperation || $[65] !== selectedAction ? (t19 = {
		actions,
		dialogs,
		isPerformingOperation,
		selectedAction,
		handleDialogClose
	}, $[61] = actions, $[62] = dialogs, $[63] = handleDialogClose, $[64] = isPerformingOperation, $[65] = selectedAction, $[66] = t19) : t19 = $[66], t19;
}
//#endregion
export { isBefore as $, setYear as A, isSameDay as B, useDialogTimeZone as C, useScheduledDraftMenuActions as D, useScheduledDraftDocument as E, startOfMonth as F, toPropertyKey as G, startOfMinute as H, lastDayOfMonth as I, _setPrototypeOf as J, _typeof as K, eachWeekOfInterval as L, setHours as M, setDate as N, useWeeksOfMonth as O, setMinutes as P, _objectWithoutPropertiesLoose as Q, addWeeks as R, singleDocReleaseUsEnglishLocaleBundle as S, useScheduleDraftOperations as T, es2015_default as U, addMonths as V, _defineProperty as W, require_react_is as X, require_prop_types as Y, _extends as Z, isReleaseVersion as _, DatePicker as a, readVersionType as b, MONTH_PICKER_VARIANT as c, Z_OFFSET as d, expand as et, getCalendarLabels as f, isPublishedVersion as g, isDraftVersion as h, DEFAULT_WEEK_DAY_NAMES as i, setMonth as j, isPast as k, SCHEDULED_PUBLISHING_TIME_ZONE_SCOPE as l, getErrorMessage as m, CONTENT_RELEASES_TIME_ZONE_SCOPE as n, DateTimeInput as o, getDocumentVersionInfoFromVersions as p, _inheritsLoose as q, DEFAULT_MONTH_NAMES as r, LazyTextInput as s, ARROW_KEYS as t, ScheduleDraftDialog as u, isValidDate as v, useReleaseDocuments as w, singleDocReleaseNamespace as x, isVariantVersion as y, isSameMonth as z };
