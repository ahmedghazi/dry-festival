//#region node_modules/.pnpm/@sanity+client@7.26.2/node_modules/@sanity/client/dist/_chunks-es/isRecord.js
function isRecord(value) {
	return typeof value == "object" && value !== null && !Array.isArray(value);
}
//#endregion
//#region node_modules/.pnpm/@sanity+client@7.26.2/node_modules/@sanity/client/dist/_chunks-es/resolveEditInfo.js
var rePropName = /[^.[\]]+|\[(?:(-?\d+(?:\.\d+)?)|(["'])((?:(?!\2)[^\\]|\\.)*?)\2)\]|(?=(?:\.|\[\])(?:\.|\[\]|$))/g;
var reKeySegment = /_key\s*==\s*['"](.*)['"]/;
var reIndexTuple = /^\d*:\d*$/;
function isIndexSegment(segment) {
	return typeof segment == "number" || typeof segment == "string" && /^\[\d+\]$/.test(segment);
}
function isKeySegment(segment) {
	return typeof segment == "string" ? reKeySegment.test(segment.trim()) : typeof segment == "object" && "_key" in segment;
}
function isIndexTuple(segment) {
	if (typeof segment == "string" && reIndexTuple.test(segment)) return !0;
	if (!Array.isArray(segment) || segment.length !== 2) return !1;
	const [from, to] = segment;
	return (typeof from == "number" || from === "") && (typeof to == "number" || to === "");
}
function get(obj, path, defaultVal) {
	const select = typeof path == "string" ? fromString(path) : path;
	if (!Array.isArray(select)) throw new Error("Path must be an array or a string");
	let acc = obj;
	for (let i = 0; i < select.length; i++) {
		const segment = select[i];
		if (isIndexSegment(segment)) {
			if (!Array.isArray(acc)) return defaultVal;
			acc = acc[segment];
		}
		if (isKeySegment(segment)) {
			if (!Array.isArray(acc)) return defaultVal;
			acc = acc.find((item) => item._key === segment._key);
		}
		if (typeof segment == "string" && (acc = typeof acc == "object" && acc !== null ? acc[segment] : void 0), typeof acc > "u") return defaultVal;
	}
	return acc;
}
function toString(path) {
	if (!Array.isArray(path)) throw new Error("Path is not an array");
	return path.reduce((target, segment, i) => {
		const segmentType = typeof segment;
		if (segmentType === "number") return `${target}[${segment}]`;
		if (segmentType === "string") return `${target}${i === 0 ? "" : "."}${segment}`;
		if (isKeySegment(segment) && segment._key) return `${target}[_key=="${segment._key}"]`;
		if (Array.isArray(segment)) {
			const [from, to] = segment;
			return `${target}[${from}:${to}]`;
		}
		throw new Error(`Unsupported path segment \`${JSON.stringify(segment)}\``);
	}, "");
}
function fromString(path) {
	if (typeof path != "string") throw new Error("Path is not a string");
	const segments = path.match(rePropName);
	if (!segments) throw new Error("Invalid path string");
	return segments.map(parsePathSegment);
}
function parsePathSegment(segment) {
	return isIndexSegment(segment) ? parseIndexSegment(segment) : isKeySegment(segment) ? parseKeySegment(segment) : isIndexTuple(segment) ? parseIndexTupleSegment(segment) : segment;
}
function parseIndexSegment(segment) {
	return Number(segment.replace(/[^\d]/g, ""));
}
function parseKeySegment(segment) {
	return { _key: segment.match(reKeySegment)[1] };
}
function parseIndexTupleSegment(segment) {
	const [from, to] = segment.split(":").map((seg) => seg === "" ? seg : Number(seg));
	return [from, to];
}
var studioPath = /* @__PURE__ */ Object.freeze({
	__proto__: null,
	fromString,
	get,
	isIndexSegment,
	isIndexTuple,
	isKeySegment,
	reKeySegment,
	toString
});
var DRAFTS_FOLDER = "drafts";
var VERSION_FOLDER = "versions";
var DRAFTS_PREFIX$1 = `${DRAFTS_FOLDER}.`;
var VERSION_PREFIX = `${VERSION_FOLDER}.`;
function isDraftId(id) {
	return id.startsWith(DRAFTS_PREFIX$1);
}
function isVersionId(id) {
	return id.startsWith(VERSION_PREFIX);
}
function isPublishedId(id) {
	return !isDraftId(id) && !isVersionId(id);
}
function getDraftId(id) {
	if (isVersionId(id)) return DRAFTS_PREFIX$1 + getPublishedId(id);
	return isDraftId(id) ? id : DRAFTS_PREFIX$1 + id;
}
function getVersionId(id, version) {
	if (version === "drafts" || version === "published") throw new Error("Version can not be \"published\" or \"drafts\"");
	return `${VERSION_PREFIX}${version}.${getPublishedId(id)}`;
}
function getVersionFromId(id) {
	if (!isVersionId(id)) return;
	const [_versionPrefix, versionId] = id.split(".");
	return versionId;
}
function getPublishedId(id) {
	return isVersionId(id) ? id.split(".").slice(2).join(".") : isDraftId(id) ? id.slice(DRAFTS_PREFIX$1.length) : id;
}
var ESCAPE = {
	"\f": "\\f",
	"\n": "\\n",
	"\r": "\\r",
	"	": "\\t",
	"'": "\\'",
	"\\": "\\\\"
};
var UNESCAPE = {
	"\\f": "\f",
	"\\n": `
`,
	"\\r": "\r",
	"\\t": "	",
	"\\'": "'",
	"\\\\": "\\"
};
function jsonPath(path) {
	return `$${path.map((segment) => typeof segment == "string" ? `['${segment.replace(/[\f\n\r\t'\\]/g, (match) => ESCAPE[match])}']` : typeof segment == "number" ? `[${segment}]` : segment._key !== "" ? `[?(@._key=='${segment._key.replace(/['\\]/g, (match) => ESCAPE[match])}')]` : `[${segment._index}]`).join("")}`;
}
function jsonPathArray(path) {
	return path.map((segment) => typeof segment == "string" ? `['${segment.replace(/[\f\n\r\t'\\]/g, (match) => ESCAPE[match])}']` : typeof segment == "number" ? `[${segment}]` : segment._key !== "" ? `[?(@._key=='${segment._key.replace(/['\\]/g, (match) => ESCAPE[match])}')]` : `[${segment._index}]`);
}
function parseJsonPath(path) {
	const parsed = [], parseRe = /\['(.*?)'\]|\[(\d+)\]|\[\?\(@\._key=='(.*?)'\)\]/g;
	let match;
	for (; (match = parseRe.exec(path)) !== null;) {
		if (match[1] !== void 0) {
			const key = match[1].replace(/\\(\\|f|n|r|t|')/g, (m) => UNESCAPE[m]);
			parsed.push(key);
			continue;
		}
		if (match[2] !== void 0) {
			parsed.push(parseInt(match[2], 10));
			continue;
		}
		if (match[3] !== void 0) {
			const _key = match[3].replace(/\\(\\')/g, (m) => UNESCAPE[m]);
			parsed.push({
				_key,
				_index: -1
			});
			continue;
		}
	}
	return parsed;
}
function jsonPathToMappingPath(path) {
	return path.map((segment) => {
		if (typeof segment == "string" || typeof segment == "number") return segment;
		if (segment._index !== -1) return segment._index;
		throw new Error(`invalid segment:${JSON.stringify(segment)}`);
	});
}
function resolveMapping(resultPath, csm) {
	if (!csm?.mappings) return;
	const resultMappingPath = jsonPath(jsonPathToMappingPath(resultPath));
	if (csm.mappings[resultMappingPath] !== void 0) return {
		mapping: csm.mappings[resultMappingPath],
		matchedPath: resultMappingPath,
		pathSuffix: ""
	};
	const resultMappingPathArray = jsonPathArray(jsonPathToMappingPath(resultPath));
	for (let i = resultMappingPathArray.length - 1; i >= 0; i--) {
		const key = `$${resultMappingPathArray.slice(0, i).join("")}`, mappingFound = csm.mappings[key];
		if (mappingFound) return {
			mapping: mappingFound,
			matchedPath: key,
			pathSuffix: resultMappingPath.substring(key.length)
		};
	}
}
function isArray(value) {
	return value !== null && Array.isArray(value);
}
function walkMap(value, mappingFn, path = []) {
	if (isArray(value)) return value.map((v, idx) => {
		if (isRecord(v)) {
			const _key = v._key;
			if (typeof _key == "string") return walkMap(v, mappingFn, path.concat({
				_key,
				_index: idx
			}));
		}
		return walkMap(v, mappingFn, path.concat(idx));
	});
	if (isRecord(value)) {
		if (value._type === "block" || value._type === "span") {
			const result = { ...value };
			return value._type === "block" ? result.children = walkMap(value.children, mappingFn, path.concat("children")) : value._type === "span" && (result.text = walkMap(value.text, mappingFn, path.concat("text"))), result;
		}
		return Object.fromEntries(Object.entries(value).map(([k, v]) => [k, walkMap(v, mappingFn, path.concat(k))]));
	}
	return mappingFn(value, path);
}
//#endregion
//#region node_modules/.pnpm/sanity@6.9.2_@babel+core@7.29.7_@babel+runtime@7.29.7_@noble+hashes@2.3.0_@rolldown+plu_bc69515520d3729ef0d8dbe997d16e6c/node_modules/sanity/lib/draftUtils-D3HJGQaW.js
/**
* @internal
*/
function isNonNullable(value) {
	return value != null;
}
var DRAFTS_PREFIX = `${DRAFTS_FOLDER}.`;
/**
*
* Checks if the document ID `documentId` has the same ID as `equalsDocumentId`,
* ignoring the draft prefix.
*
* @public
*
* @param documentId - The document ID to check
* @param equalsDocumentId - The document ID to check against
*
* @example
* Draft vs published document ID, but representing the same document:
* ```
* // Prints "true":
* console.log(documentIdEquals('drafts.agot', 'agot'));
* ```
* @example
* Different documents:
* ```
* // Prints "false":
* console.log(documentIdEquals('hp-tcos', 'hp-hbp'));
* ```
*
* @returns `true` if the document IDs are equal, `false` otherwise
*/
function documentIdEquals(documentId, equalsDocumentId) {
	return getPublishedId(documentId) === getPublishedId(equalsDocumentId);
}
/** @internal */
function isDraft(document) {
	return isDraftId(document._id);
}
/**
* TODO: Improve return type based on presence of `version` option.
*
* @internal
*/
function getIdPair(id, { version } = {}) {
	if (version === "drafts" || version === "published") throw Error("Version can not be \"published\" or \"drafts\"");
	return {
		publishedId: getPublishedId(id),
		draftId: getDraftId(id),
		...version ? { versionId: getVersionId(id, version) } : {}
	};
}
/**
* System bundles are sets of documents owned by the system.
*
* - Draft documents contain data that has not yet been published. These documents all exist in the "drafts" path.
* - Published documents contain data that has been published. These documents all exist in the root path.
*
* These differ to user bundles, which are created when a user establishes a custom set of documents
* (e.g. by creating a release).
*
* @public
*/
var systemBundles = ["drafts", "published"];
/** @internal */
function isSystemBundle(maybeSystemBundle) {
	return systemBundles.includes(maybeSystemBundle);
}
/** @internal */
var systemBundleNames = ["draft", "published"];
/**
* `isSystemBundle` should be preferred, but some parts of the codebase currently use the singular
* "draft" name instead of the plural "drafts".
*
* @internal
*/
function isSystemBundleName(maybeSystemBundleName) {
	return systemBundleNames.includes(maybeSystemBundleName);
}
/**
*  @internal
*  Given a perspective stack and a document id, returns true if the document id matches any of the provided perspectives
*  e.g. `idMatchesPerspective('['summer'], 'versions.summer.foo') === true`
*  e.g. `idMatchesPerspective('['drafts', 'summer'], 'versions.summer.foo') === true`
*  e.g. `idMatchesPerspective('['drafts'], 'versions.summer.foo') === false`
*  e.g. `idMatchesPerspective('['drafts', 'summer'], 'versions.winter.foo') === false`
*
* Note: a published id will match any perspective
*   e.g. `idMatchesPerspective('['drafts', 'summer'], 'foo') === true`
*/
function idMatchesPerspective(perspectiveStack, documentId) {
	return isPublishedId(documentId) ? !0 : perspectiveStack.some((perspective) => perspective === "drafts" ? isDraftId(documentId) : getVersionFromId(documentId) === perspective);
}
/** @internal */
function createDraftFrom(document) {
	return {
		...document,
		_id: getDraftId(document._id)
	};
}
/** @internal */
function newDraftFrom(document) {
	return {
		...document,
		_id: DRAFTS_PREFIX
	};
}
/** @internal */
function createPublishedFrom(document) {
	return {
		...document,
		_id: getPublishedId(document._id)
	};
}
/** @internal */
function collate(documents) {
	let byId = documents.reduce((res, doc) => {
		let publishedId = getPublishedId(doc._id), entry = res.get(publishedId);
		return entry || (entry = {
			id: publishedId,
			type: doc._type,
			published: void 0,
			draft: void 0,
			versions: []
		}, res.set(publishedId, entry)), isPublishedId(doc._id) && (entry.published = doc), isDraftId(doc._id) && (entry.draft = doc), isVersionId(doc._id) && entry.versions.push(doc), res;
	}, /* @__PURE__ */ new Map());
	return Array.from(byId.values());
}
/** @internal */
function removeDupes(documents) {
	return collate(documents).map((entry) => entry.draft || entry.published || entry.versions[0]).filter(isNonNullable);
}
/**
* Checks whether `id` is a document group id, i.e. an id carrying neither the `drafts.` nor the
* `versions.` prefix. The group document is the published document, so this is the same check as
* {@link isPublishedId}, named for call sites that care about the group rather than the published
* version.
*
* @internal
*
* @param id - The document ID to check
*
* @returns `true` if the document ID is a group ID, `false` otherwise
*/
function isDocumentGroupId(id) {
	return isPublishedId(id);
}
//#endregion
export { isPublishedId as C, studioPath as D, resolveMapping as E, toString as O, isDraftId as S, parseJsonPath as T, get as _, getIdPair as a, getVersionFromId as b, isDraft as c, isSystemBundleName as d, newDraftFrom as f, VERSION_FOLDER as g, DRAFTS_FOLDER as h, documentIdEquals as i, walkMap as k, isNonNullable as l, systemBundles as m, createDraftFrom as n, idMatchesPerspective as o, removeDupes as p, createPublishedFrom as r, isDocumentGroupId as s, collate as t, isSystemBundle as u, getDraftId as v, isVersionId as w, getVersionId as x, getPublishedId as y };
