import { Di as nanoid } from "./datastores-D4iZNSXV-CqhbcdwE.js";
//#region node_modules/.pnpm/sanity@6.9.2_@babel+core@7.29.7_@babel+runtime@7.29.7_@noble+hashes@2.3.0_@rolldown+plu_bc69515520d3729ef0d8dbe997d16e6c/node_modules/sanity/lib/assignId-C5X-uvpT.js
var randomIdCache = /* @__PURE__ */ new WeakMap();
/**
* given an object, this function randomly generates an ID and returns it. this
* result is then saved in a WeakMap so subsequent requests for the same object
* will receive the same ID
*/
function assignId(obj) {
	let cachedValue = randomIdCache.get(obj);
	if (cachedValue) return cachedValue;
	let id = nanoid();
	return randomIdCache.set(obj, id), id;
}
//#endregion
export { assignId as t };
