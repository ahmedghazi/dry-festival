import { i as __toESM } from "./rolldown-runtime-B-lAHAz2.js";
import { t as require_react } from "./react.js";
import { Ct as ReleasesUpsellContext, it as PerspectiveContext, t as ActiveWorkspaceMatcherContext } from "./_singletons-BJ3onRY-.js";
import { t as require_compiler_runtime } from "./compiler-runtime-gazgokOh.js";
import { t as require_jsx_runtime } from "./react_jsx-runtime.js";
import { An as rem, At as TextInput, Cn as EMPTY_ARRAY$1, Dn as _getArrayProp, Ft as focusFirstDescendant, Ht as Stack, Kt as Box, Mt as _raf, S as Skeleton, Ut as Text, Vt as Flex, _t as hues, cn as require_react_is, dn as ft, fn as gt, hn as _isEnterToClickElement, jt as _hasFocus, k as TextSkeleton, qt as Card, wn as EMPTY_RECORD, zt as Button } from "./dist-MhpRzy4S.js";
import { t as SpinnerIcon } from "./Spinner-LMgmp7Qq.js";
import { t as ChevronDownIcon } from "./ChevronDown-Gg6Kxi4p.js";
import { $t as useRenderingContextStore, An as normalizeDates, Bt as snapshotPair, Ct as memoize$1, Dn as isValid, Ei as customAlphabet, En as format, Et as normalizeDocumentPairTarget, F as createSWR, Gn as tz, Hi as switchMap, Jt as useGrantsStore, Kt as useDocumentPreviewStore, Mn as parse, Oi as QuickLRU, On as differenceInCalendarDays, Q as getReleaseIdFromReleaseDocumentId, Rn as getDefaultOptions, Vi as takeUntil, Wi as scan, Wn as enUS, Wt as useComlinkStore, Xi as take, Yi as distinctUntilChanged, a as RELEASE_DOCUMENTS_PATH, bt as listenQuery, ca as fromEvent, en as useResourceCache, h as VARIANT_DOCUMENT_TYPE, i as RELEASES_STUDIO_CLIENT_OPTIONS, la as defer, m as VARIANT_DOCUMENTS_PATH, mn as require_dataloader, na as bufferTime, o as RELEASE_DOCUMENT_TYPE, on as validateDocumentWithReferences, p as VARIANTS_STUDIO_CLIENT_OPTIONS, pt as isRecord$4, qt as useDocumentStore, r as DOCUMENT_SYSTEM_FIELD, sa as merge, ta as reduce, tn as useSchema, zi as withLatestFrom } from "./datastores-D4iZNSXV-CqhbcdwE.js";
import { r as useTranslation$1 } from "./useTranslation-Cqs6Itwv-DwNRrRK4.js";
import { S as __read, i as pipe, l as noop, n as Subject, r as Observable, t as BehaviorSubject, w as __spreadArray } from "./BehaviorSubject-Bt_vJCli.js";
import { i as createOperatorSubscriber, o as operate, t as map } from "./map-pLAxL0_r.js";
import { C as mergeMap, D as from, E as of, I as popScheduler, L as asyncScheduler, M as innerFrom, V as ReplaySubject, _ as finalize, b as concat$1, d as useWorkspace, g as share, h as shareReplay, i as useSyncObservable, l as useClient, m as startWith, o as DEFAULT_STUDIO_CLIENT_OPTIONS, p as tap, t as useObservable, u as useSource, v as catchError, x as concatAll, y as timer } from "./dist-DPi9e2VS.js";
import { T as EMPTY, h as useToast, y as combineLatest } from "./dist-B2gx505F.js";
import { t as filter } from "./filter-CWG4UsI5.js";
import { i as Tooltip$1 } from "./Button-CGZ3ETwD-CQ5Daaas.js";
import { n as createHookFromObservableFactory, o as Popover, r as useCurrentUser } from "./LoadingBlock-Cy2Pckab-CaBiwwPj.js";
import { S as isDraftId, a as getIdPair, b as getVersionFromId, v as getDraftId, w as isVersionId, x as getVersionId, y as getPublishedId } from "./draftUtils-D3HJGQaW-xgf4xoyc.js";
import { t as WarningOutlineIcon } from "./WarningOutline-CDXJ-d73.js";
import { $ as isValidationErrorMarker } from "./lib-VzCx2ZCB.js";
import { F as baseGetTag, w as isArray, y as isObjectLike } from "./_baseIsEqual-CfttHLnp.js";
import { n as v4 } from "./partition-pdpBl5Pv.js";
import { t as useProjectId } from "./useProjectId-bGwoCAuz-Bz0j80Q9.js";
import { t as DocumentIcon } from "./Document-BqTorX02.js";
import { t as useSWR } from "./use-swr-emll9s78-B_GUL9K5.js";
//#region node_modules/.pnpm/rxjs@7.8.2/node_modules/rxjs/dist/esm5/internal/observable/iif.js
function iif(condition, trueResult, falseResult) {
	return defer(function() {
		return condition() ? trueResult : falseResult;
	});
}
//#endregion
//#region node_modules/.pnpm/rxjs@7.8.2/node_modules/rxjs/dist/esm5/internal/operators/concat.js
function concat() {
	var args = [];
	for (var _i = 0; _i < arguments.length; _i++) args[_i] = arguments[_i];
	var scheduler = popScheduler(args);
	return operate(function(source, subscriber) {
		concatAll()(from(__spreadArray([source], __read(args)), scheduler)).subscribe(subscriber);
	});
}
//#endregion
//#region node_modules/.pnpm/rxjs@7.8.2/node_modules/rxjs/dist/esm5/internal/operators/concatWith.js
function concatWith() {
	var otherSources = [];
	for (var _i = 0; _i < arguments.length; _i++) otherSources[_i] = arguments[_i];
	return concat.apply(void 0, __spreadArray([], __read(otherSources)));
}
//#endregion
//#region node_modules/.pnpm/rxjs@7.8.2/node_modules/rxjs/dist/esm5/internal/operators/count.js
function count(predicate) {
	return reduce(function(total, value, i) {
		return !predicate || predicate(value, i) ? total + 1 : total;
	}, 0);
}
//#endregion
//#region node_modules/.pnpm/rxjs@7.8.2/node_modules/rxjs/dist/esm5/internal/operators/ignoreElements.js
function ignoreElements() {
	return operate(function(source, subscriber) {
		source.subscribe(createOperatorSubscriber(subscriber, noop));
	});
}
//#endregion
//#region node_modules/.pnpm/rxjs@7.8.2/node_modules/rxjs/dist/esm5/internal/operators/mapTo.js
function mapTo(value) {
	return map(function() {
		return value;
	});
}
//#endregion
//#region node_modules/.pnpm/rxjs@7.8.2/node_modules/rxjs/dist/esm5/internal/operators/delayWhen.js
function delayWhen(delayDurationSelector, subscriptionDelay) {
	if (subscriptionDelay) return function(source) {
		return concat$1(subscriptionDelay.pipe(take(1), ignoreElements()), source.pipe(delayWhen(delayDurationSelector)));
	};
	return mergeMap(function(value, index) {
		return innerFrom(delayDurationSelector(value, index)).pipe(take(1), mapTo(value));
	});
}
//#endregion
//#region node_modules/.pnpm/rxjs@7.8.2/node_modules/rxjs/dist/esm5/internal/operators/delay.js
function delay(due, scheduler) {
	if (scheduler === void 0) scheduler = asyncScheduler;
	var duration = timer(due, scheduler);
	return delayWhen(function() {
		return duration;
	});
}
//#endregion
//#region node_modules/.pnpm/sanity@6.9.2_@babel+core@7.29.7_@babel+runtime@7.29.7_@noble+hashes@2.3.0_@rolldown+plu_bc69515520d3729ef0d8dbe997d16e6c/node_modules/sanity/lib/createAgentBundlesStore-BJwwD0vJ.js
var INITIAL_STATE$4 = {
	bundles: [],
	loading: !0
};
var EMPTY_STATE = {
	bundles: [],
	loading: !1
};
/**
* Returns whether the given version name (e.g. from `getVersionFromId`) is an
* agent bundle name (starts with `agent-`).
*
* @internal
*/
function isAgentBundleName(versionName) {
	return typeof versionName == "string" && versionName.startsWith("agent-");
}
/**
* Creates an observable that listens to the agent's `bundles/mine/listen` SSE
* endpoint. Emits `AgentBundlesState` values whenever the set of active bundles
* changes.
*
* The returned observable is ref-counted — the SSE connection is opened on the
* first subscriber and closed when the last subscriber unsubscribes.
*/
function createAgentBundlesStore(context) {
	let { organizationId$, client } = context;
	return { state$: organizationId$.pipe(distinctUntilChanged(), switchMap((organizationId) => organizationId ? listenToBundles(client, organizationId) : of(EMPTY_STATE)), startWith(INITIAL_STATE$4), share({ connector: () => new ReplaySubject(1) })) };
}
var eventSourcePolyfill$ = defer(() => import("./browser-zyDgpbgs.js")).pipe(map(({ default: ES }) => ES), shareReplay(1));
function listenToBundles(client, organizationId) {
	let { token, withCredentials } = client.config(), url = client.getUrl(`/agent/${organizationId}/bundles/mine/listen`), esOptions = {};
	return (token || withCredentials) && (esOptions.withCredentials = !0), token && (esOptions.headers = { Authorization: `Bearer ${token}` }), (esOptions.headers ? eventSourcePolyfill$ : of(EventSource)).pipe(map((ES) => new ES(url, esOptions))).pipe(switchMap((es) => new Observable((subscriber) => (es.addEventListener("bundles", ((event) => {
		try {
			let data = JSON.parse(event.data);
			subscriber.next({
				bundles: data.bundles,
				loading: !1
			});
		} catch {}
	})), es.addEventListener("error", (() => {})), () => {
		es.close();
	}))), catchError(() => of(EMPTY_STATE)));
}
//#endregion
//#region node_modules/.pnpm/clsx@2.1.1/node_modules/clsx/dist/clsx.mjs
var import_react = /* @__PURE__ */ __toESM(require_react());
var import_compiler_runtime = require_compiler_runtime();
var import_jsx_runtime = require_jsx_runtime();
function r(e) {
	var t, f, n = "";
	if ("string" == typeof e || "number" == typeof e) n += e;
	else if ("object" == typeof e) if (Array.isArray(e)) {
		var o = e.length;
		for (t = 0; t < o; t++) e[t] && (f = r(e[t])) && (n && (n += " "), n += f);
	} else for (f in e) e[f] && (n && (n += " "), n += f);
	return n;
}
function clsx() {
	for (var e, t, f = 0, n = "", o = arguments.length; f < o; f++) (e = arguments[f]) && (t = r(e)) && (n && (n += " "), n += t);
	return n;
}
//#endregion
//#region node_modules/.pnpm/rxjs-mergemap-array@0.1.0_rxjs@7.8.2/node_modules/rxjs-mergemap-array/dist/index.js
var import_react_is = /* @__PURE__ */ __toESM(require_react_is(), 1);
var INITIAL_STATE$3 = {
	current: [],
	added: [],
	removed: []
};
var EMPTY_ARRAY = [];
function mergeMapArray(project, isEqual = (a, b) => a === b) {
	return (input) => {
		const sharedInput = input.pipe(share()), state$ = sharedInput.pipe(scan((state, next) => {
			const added = next.filter((item) => !state.current.find((current) => isEqual(current, item))), removed = state.current.filter((item) => !next.find((current) => isEqual(current, item)));
			return {
				current: next,
				added: uniqueBy(added, isEqual),
				removed: uniqueBy(removed, isEqual)
			};
		}, INITIAL_STATE$3)).pipe(share(), filter((state) => state.added.length > 0 || state.removed.length > 0)), removed$ = state$.pipe(mergeMap((state) => state.removed)), added$ = state$.pipe(mergeMap((state) => state.added)), empty = sharedInput.pipe(filter((arr) => arr.length === 0), map(() => EMPTY_ARRAY)), mapped = added$.pipe(mergeMap((element) => {
			const removed = removed$.pipe(filter((k) => isEqual(k, element))).pipe(share());
			return merge(removed.pipe(map(() => ({
				type: "remove",
				element
			}))), project(element).pipe(takeUntil(removed), map((projected) => ({
				type: "emit",
				element,
				projected
			}))));
		}), withLatestFrom(sharedInput), scan((acc, [event, inputArray]) => inputArray.flatMap((item) => isEqual(item, event.element) ? event.type === "remove" ? [] : {
			item,
			emitted: !0,
			value: event.projected
		} : acc.find((v) => v && isEqual(v.item, item))), []), filter((v) => v.every((item) => item == null ? void 0 : item.emitted)), map((v) => v.map((item) => item.value)));
		return merge(empty, mapped);
	};
}
function uniqueBy(array, predicate) {
	const deduped = [];
	let hasDuplicates = !1;
	for (const item of array) deduped.find((previous) => predicate(previous, item)) ? hasDuplicates = !0 : deduped.push(item);
	return hasDuplicates ? deduped : array;
}
//#endregion
//#region node_modules/.pnpm/date-fns@4.4.0/node_modules/date-fns/formatRelative.js
var import_dataloader = /* @__PURE__ */ __toESM(require_dataloader(), 1);
/**
* The {@link formatRelative} function options.
*/
/**
* @name formatRelative
* @category Common Helpers
* @summary Represent the date in words relative to the given base date.
*
* @description
* Represent the date in words relative to the given base date.
*
* | Distance to the base date | Result                    |
* |---------------------------|---------------------------|
* | Previous 6 days           | last Sunday at 04:30 AM   |
* | Last day                  | yesterday at 04:30 AM     |
* | Same day                  | today at 04:30 AM         |
* | Next day                  | tomorrow at 04:30 AM      |
* | Next 6 days               | Sunday at 04:30 AM        |
* | Other                     | 12/31/2017                |
*
* @param date - The date to format
* @param baseDate - The date to compare with
* @param options - An object with options
*
* @returns The date in words
*
* @throws `date` must not be Invalid Date
* @throws `baseDate` must not be Invalid Date
* @throws `options.locale` must contain `localize` property
* @throws `options.locale` must contain `formatLong` property
* @throws `options.locale` must contain `formatRelative` property
*
* @example
* // Represent the date of 6 days ago in words relative to the given base date. In this example, today is Wednesday
* const result = formatRelative(subDays(new Date(), 6), new Date())
* //=> "last Thursday at 12:45 AM"
*/
function formatRelative(date, baseDate, options) {
	const [date_, baseDate_] = normalizeDates(options?.in, date, baseDate);
	const defaultOptions = getDefaultOptions();
	const locale = options?.locale ?? defaultOptions.locale ?? enUS;
	const weekStartsOn = options?.weekStartsOn ?? options?.locale?.options?.weekStartsOn ?? defaultOptions.weekStartsOn ?? defaultOptions.locale?.options?.weekStartsOn ?? 0;
	const diff = differenceInCalendarDays(date_, baseDate_);
	if (isNaN(diff)) throw new RangeError("Invalid time value");
	let token;
	if (diff < -6) token = "other";
	else if (diff < -1) token = "lastWeek";
	else if (diff < 0) token = "yesterday";
	else if (diff < 1) token = "today";
	else if (diff < 2) token = "tomorrow";
	else if (diff < 7) token = "nextWeek";
	else token = "other";
	const formatStr = locale.formatRelative(token, date_, baseDate_, {
		locale,
		weekStartsOn
	});
	return format(date_, formatStr, {
		locale,
		weekStartsOn
	});
}
//#endregion
//#region node_modules/.pnpm/use-device-pixel-ratio@1.1.2_react@19.2.8/node_modules/use-device-pixel-ratio/dist/index.module.js
/**
* Get the device pixel ratio, potentially rounded and capped.
* Will emit new values if it changes.
*
* @param options
* @returns The current device pixel ratio, or the default if none can be resolved
*/
function useDevicePixelRatio(options) {
	const dpr = getDevicePixelRatio(options);
	const [currentDpr, setCurrentDpr] = (0, import_react.useState)(dpr);
	const { defaultDpr, maxDpr, round } = options || {};
	(0, import_react.useEffect)(() => {
		if (!(typeof window !== "undefined" && "matchMedia" in window)) return;
		const updateDpr = () => setCurrentDpr(getDevicePixelRatio({
			defaultDpr,
			maxDpr,
			round
		}));
		const mediaMatcher = window.matchMedia(`screen and (resolution: ${currentDpr}dppx)`);
		if (mediaMatcher.addEventListener) mediaMatcher.addEventListener("change", updateDpr);
		else mediaMatcher.addListener(updateDpr);
		return () => {
			if (mediaMatcher.removeEventListener) mediaMatcher.removeEventListener("change", updateDpr);
			else mediaMatcher.removeListener(updateDpr);
		};
	}, [
		currentDpr,
		defaultDpr,
		maxDpr,
		round
	]);
	return currentDpr;
}
/**
* Returns the current device pixel ratio (DPR) given the passed options
*
* @param options
* @returns current device pixel ratio
*/
function getDevicePixelRatio(options) {
	const { defaultDpr = 1, maxDpr = 3, round = true } = options || {};
	const dpr = typeof window !== "undefined" && typeof window.devicePixelRatio === "number" ? window.devicePixelRatio : defaultDpr;
	return Math.min(Math.max(1, round ? Math.floor(dpr) : dpr), maxDpr);
}
//#endregion
//#region node_modules/.pnpm/@sanity+asset-utils@2.3.0/node_modules/@sanity/asset-utils/dist/index.js
var cdnUrl = "https://cdn.sanity.io";
var cdnUrlPattern = /^https:\/\/cdn\.sanity\./;
var customCdnUrlPattern = /^https:\/\/cdn\.[^/]+\/(images|files)\/[^/]+\/.*?[a-zA-Z0-9_]{24,40}.*$/;
var fileAssetFilenamePattern = /^([a-zA-Z0-9_]{24,40}|[a-f0-9]{40})+\.[a-z0-9]+$/;
var fileAssetIdPattern = /^file-([a-zA-Z0-9_]{24,40}|[a-f0-9]{40})+-[a-z0-9]+$/;
var imageAssetFilenamePattern = /^([a-zA-Z0-9_]{24,40}|[a-f0-9]{40})-\d+x\d+\.[a-z0-9]+$/;
var imageAssetIdPattern = /^image-([a-zA-Z0-9_]{24,40}|[a-f0-9]{40})+-\d+x\d+-[a-z0-9]+$/;
var pathPattern = /^(images|files)\/([a-z0-9]+)\/([a-z0-9][-\w]*)\//;
var idPattern = /^(?:image-(?:[a-zA-Z0-9_]{24,40}|[a-f0-9]{40})+-\d+x\d+-[a-z0-9]+|file-(?:[a-zA-Z0-9_]{24,40}|[a-f0-9]{40})+-[a-z0-9]+)$/;
var inProgressAssetId = "upload-in-progress-placeholder";
var UnresolvableError = class extends Error {
	constructor(inputSource, message = "Failed to resolve asset ID from source") {
		super(message), this.unresolvable = !0, this.input = inputSource;
	}
};
function isUnresolvableError(err) {
	const error = err;
	return !!(error.unresolvable && "input" in error);
}
function getForgivingResolver(method) {
	return (...args) => {
		try {
			return method(...args);
		} catch (err) {
			if (isUnresolvableError(err)) return;
			throw err;
		}
	};
}
function isObject(obj) {
	return obj !== null && !Array.isArray(obj) && typeof obj == "object";
}
function isReference(ref) {
	return isObject(ref) && typeof ref._ref == "string";
}
function isAssetIdStub(stub) {
	return isObject(stub) && typeof stub._id == "string";
}
function isAssetPathStub(stub) {
	return isObject(stub) && typeof stub.path == "string";
}
function isAssetUrlStub(stub) {
	return isObject(stub) && typeof stub.url == "string";
}
function isImageAssetId(documentId) {
	return imageAssetIdPattern.test(documentId);
}
function isFileAssetId(documentId) {
	return fileAssetIdPattern.test(documentId);
}
function isAssetObjectStub(stub) {
	const item = stub;
	return isObject(item) && !!item.asset && typeof item.asset == "object";
}
function isInProgressUpload$1(stub) {
	const item = stub;
	return isObject(item) && !!item._upload && !("asset" in item);
}
function isCdnUrl(url) {
	return cdnUrlPattern.test(url) || customCdnUrlPattern.test(url);
}
var DEFAULT_CROP = Object.freeze({
	left: 0,
	top: 0,
	bottom: 0,
	right: 0
});
var DEFAULT_HOTSPOT = Object.freeze({
	x: .5,
	y: .5,
	height: 1,
	width: 1
});
var isDefaultCrop = (crop) => {
	const { top, bottom, left, right } = crop, { top: defaultTop, bottom: defaultBottom, left: defaultLeft, right: defaultRight } = DEFAULT_CROP;
	return top === defaultTop && bottom === defaultBottom && left === defaultLeft && right === defaultRight;
};
var isDefaultHotspot = (hotspot) => {
	const { x, y, width, height } = hotspot, { x: defaultX, y: defaultY, width: defaultWidth, height: defaultHeight } = DEFAULT_HOTSPOT;
	return x === defaultX && y === defaultY && width === defaultWidth && height === defaultHeight;
};
function getUrlPath(url) {
	if (pathPattern.test(url)) return url;
	if (!isCdnUrl(url)) throw new UnresolvableError(`Failed to resolve path from URL "${url}"`);
	return new URL(url).pathname.replace(/^\/+/, "");
}
var tryGetUrlPath = getForgivingResolver(getUrlPath);
function getUrlFilename(url) {
	const filename = (tryGetUrlPath(url) || url).replace(/^(images|files)\/[a-z0-9]+\/[a-z0-9][-\w]\/*/, "");
	if (!isValidFilename(filename)) throw new UnresolvableError(`Failed to resolve filename from URL "${url}"`);
	return filename;
}
var tryGetUrlFilename = getForgivingResolver(getUrlFilename);
function isValidFilename(filename) {
	return fileAssetFilenamePattern.test(filename) || imageAssetFilenamePattern.test(filename);
}
var exampleFileId = "file-027401f31c3ac1e6d78c5d539ccd1beff72b9b11-pdf";
var exampleImageId = "image-027401f31c3ac1e6d78c5d539ccd1beff72b9b11-2000x3000-jpg";
function parseAssetId$1(documentId) {
	if (imageAssetIdPattern.test(documentId)) return parseImageAssetId(documentId);
	if (fileAssetIdPattern.test(documentId)) return parseFileAssetId(documentId);
	throw new Error(`Invalid image/file asset ID: ${documentId}`);
}
function parseFileAssetId(documentId) {
	if (!fileAssetIdPattern.test(documentId)) throw new Error(`Malformed file asset ID '${documentId}'. Expected an id like "${exampleFileId}"`);
	const [, assetId, extension] = documentId.split("-");
	return {
		type: "file",
		assetId,
		extension
	};
}
function parseImageAssetId(documentId) {
	const [, assetId, dimensionString, extension] = documentId.split("-"), [width, height] = (dimensionString || "").split("x").map(Number);
	if (!assetId || !dimensionString || !extension || !(width > 0) || !(height > 0)) throw new Error(`Malformed asset ID '${documentId}'. Expected an id like "${exampleImageId}".`);
	return {
		type: "image",
		assetId,
		width,
		height,
		extension
	};
}
function parseAssetFilename(filename) {
	const file = tryGetUrlFilename(filename) || "";
	if (!isValidFilename(file)) throw new Error(`Invalid image/file asset filename: ${filename}`);
	try {
		return parseAssetId$1(`${imageAssetFilenamePattern.test(file) ? "image" : "file"}-${file.replace(/\.([a-z0-9+]+)$/i, "-$1")}`);
	} catch {
		throw new Error(`Invalid image/file asset filename: ${filename}`);
	}
}
function parseAssetUrl(url) {
	if (!isCdnUrl(url)) throw new Error(`URL is not a valid Sanity asset URL: ${url}`);
	const path = new URL(url).pathname.replace(/^\/+/, ""), [projectPath, , projectId, dataset] = path.match(pathPattern) || [];
	if (!projectPath || !projectId || !dataset) throw new Error(`URL is not a valid Sanity asset URL: ${url}`);
	const [filename, vanityFilename] = path.slice(projectPath.length).split("/");
	return {
		...parseAssetFilename(filename),
		projectId,
		dataset,
		vanityFilename
	};
}
function parseImageAssetUrl(url) {
	const parsed = parseAssetUrl(url);
	if (parsed.type !== "image") throw new Error(`URL is not a valid Sanity image asset URL: ${url}`);
	return parsed;
}
function getAssetUrlType(url) {
	try {
		return parseAssetUrl(url).type;
	} catch {
		return !1;
	}
}
function getImageDimensions(src) {
	if (isInProgressUpload$1(src)) return {
		width: 0,
		height: 0,
		aspectRatio: 0
	};
	const { width, height } = parseImageAssetId(getAssetDocumentId(src));
	return {
		width,
		height,
		aspectRatio: width / height
	};
}
var tryGetImageDimensions = getForgivingResolver(getImageDimensions);
function getAssetDocumentId(src) {
	if (isInProgressUpload$1(src)) return inProgressAssetId;
	const source = isAssetObjectStub(src) ? src.asset : src;
	let id = "";
	if (typeof source == "string" ? id = getIdFromString(source) : isReference(source) ? id = source._ref : isAssetIdStub(source) ? id = source._id : isAssetPathStub(source) ? id = idFromUrl(`${cdnUrl}/${source.path}`) : isAssetUrlStub(source) && (id = idFromUrl(source.url)), !(id && idPattern.test(id))) throw new UnresolvableError(src);
	return id;
}
var tryGetAssetDocumentId = getForgivingResolver(getAssetDocumentId);
function getIdFromString(str) {
	if (idPattern.test(str)) return str;
	const path = isCdnUrl(str) ? new URL(str).pathname : str;
	if (path.indexOf("/images") === 0 || path.indexOf("/files") === 0) return idFromUrl(str);
	if (pathPattern.test(str)) return idFromUrl(`${cdnUrl}/${str}`);
	if (isFileAssetFilename(str)) return idFromUrl(`${cdnUrl}/files/a/b/${str}`);
	if (isImageAssetFilename(str)) return idFromUrl(`${cdnUrl}/images/a/b/${str}`);
	throw new UnresolvableError(str);
}
function idFromUrl(url) {
	const [type, , , fileName] = getUrlPath(url).split("/");
	return `${type.replace(/s$/, "")}-${fileName.replace(/\./g, "-")}`;
}
function isImageAssetFilename(filename) {
	return imageAssetFilenamePattern.test(filename);
}
function isFileAssetFilename(filename) {
	return fileAssetFilenamePattern.test(filename);
}
function isFileSource(src) {
	const assetId = tryGetAssetDocumentId(src);
	return assetId ? assetId.startsWith("file-") : !1;
}
function isImageSource(src) {
	const assetId = tryGetAssetDocumentId(src);
	return assetId ? assetId.startsWith("image-") : !1;
}
function isSanityImageUrl(url) {
	return getAssetUrlType(url) === "image";
}
//#endregion
//#region node_modules/.pnpm/lodash-es@4.18.1/node_modules/lodash-es/isString.js
/** `Object#toString` result references. */
var stringTag = "[object String]";
/**
* Checks if `value` is classified as a `String` primitive or object.
*
* @static
* @since 0.1.0
* @memberOf _
* @category Lang
* @param {*} value The value to check.
* @returns {boolean} Returns `true` if `value` is a string, else `false`.
* @example
*
* _.isString('abc');
* // => true
*
* _.isString(1);
* // => false
*/
function isString(value) {
	return typeof value == "string" || !isArray(value) && isObjectLike(value) && baseGetTag(value) == stringTag;
}
//#endregion
//#region node_modules/.pnpm/@sanity+image-url@2.1.1/node_modules/@sanity/image-url/lib/_chunks-es/compat.js
var example = "image-Tb9Ew8CXIwaY6R1kjMvI0uRR-2000x3000-jpg";
function parseAssetId(ref) {
	const [, id, dimensionString, format] = ref.split("-");
	if (!id || !dimensionString || !format) throw new Error(`Malformed asset _ref '${ref}'. Expected an id like "${example}".`);
	const [imgWidthStr, imgHeightStr] = dimensionString.split("x"), width = +imgWidthStr, height = +imgHeightStr;
	if (!(isFinite(width) && isFinite(height))) throw new Error(`Malformed asset _ref '${ref}'. Expected an id like "${example}".`);
	return {
		id,
		width,
		height,
		format
	};
}
var isRef = (src) => {
	const source = src;
	return source ? typeof source._ref == "string" : !1;
};
var isAsset = (src) => {
	const source = src;
	return source ? typeof source._id == "string" : !1;
};
var isAssetStub = (src) => {
	const source = src;
	return source && source.asset ? typeof source.asset.url == "string" : !1;
};
var isInProgressUpload = (src) => {
	if (typeof src == "object" && src !== null) {
		const obj = src;
		return obj._upload && (!obj.asset || !obj.asset._ref);
	}
	return !1;
};
function parseSource(source) {
	if (!source) return null;
	let image;
	if (typeof source == "string" && isUrl(source)) image = { asset: { _ref: urlToId(source) } };
	else if (typeof source == "string") image = { asset: { _ref: source } };
	else if (isRef(source)) image = { asset: source };
	else if (isAsset(source)) image = { asset: { _ref: source._id || "" } };
	else if (isAssetStub(source)) image = { asset: { _ref: urlToId(source.asset.url) } };
	else if (typeof source.asset == "object") image = { ...source };
	else return null;
	const img = source;
	return img.crop && (image.crop = img.crop), img.hotspot && (image.hotspot = img.hotspot), applyDefaults(image);
}
function isUrl(url) {
	return /^https?:\/\//.test(`${url}`);
}
function urlToId(url) {
	return `image-${url.split("/").slice(-1)[0]}`.replace(/\.([a-z]+)$/, "-$1");
}
function applyDefaults(image) {
	if (image.crop && image.hotspot) return image;
	const result = { ...image };
	return result.crop || (result.crop = {
		left: 0,
		top: 0,
		bottom: 0,
		right: 0
	}), result.hotspot || (result.hotspot = {
		x: .5,
		y: .5,
		height: 1,
		width: 1
	}), result;
}
var SPEC_NAME_TO_URL_NAME_MAPPINGS = [
	["width", "w"],
	["height", "h"],
	["format", "fm"],
	["download", "dl"],
	["blur", "blur"],
	["sharpen", "sharp"],
	["invert", "invert"],
	["orientation", "or"],
	["minHeight", "min-h"],
	["maxHeight", "max-h"],
	["minWidth", "min-w"],
	["maxWidth", "max-w"],
	["quality", "q"],
	["fit", "fit"],
	["crop", "crop"],
	["saturation", "sat"],
	["auto", "auto"],
	["dpr", "dpr"],
	["pad", "pad"],
	["frame", "frame"]
];
function urlForImage(options) {
	let spec = { ...options || {} };
	const source = spec.source;
	delete spec.source;
	const image = parseSource(source);
	if (!image) {
		if (source && isInProgressUpload(source)) return "data:image/png;base64,iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAQAAAC1HAwCAAAAC0lEQVR42mP8HwQACfsD/QNViZkAAAAASUVORK5CYII=";
		throw new Error(`Unable to resolve image URL from source (${JSON.stringify(source)})`);
	}
	const asset = parseAssetId(image.asset._ref || image.asset._id || ""), cropLeft = Math.round(image.crop.left * asset.width), cropTop = Math.round(image.crop.top * asset.height), crop = {
		left: cropLeft,
		top: cropTop,
		width: Math.round(asset.width - image.crop.right * asset.width - cropLeft),
		height: Math.round(asset.height - image.crop.bottom * asset.height - cropTop)
	}, hotSpotVerticalRadius = image.hotspot.height * asset.height / 2, hotSpotHorizontalRadius = image.hotspot.width * asset.width / 2, hotSpotCenterX = image.hotspot.x * asset.width, hotSpotCenterY = image.hotspot.y * asset.height, hotspot = {
		left: hotSpotCenterX - hotSpotHorizontalRadius,
		top: hotSpotCenterY - hotSpotVerticalRadius,
		right: hotSpotCenterX + hotSpotHorizontalRadius,
		bottom: hotSpotCenterY + hotSpotVerticalRadius
	};
	return spec.rect || spec.focalPoint || spec.ignoreImageParams || spec.crop || (spec = {
		...spec,
		...fit({
			crop,
			hotspot
		}, spec)
	}), specToImageUrl({
		...spec,
		asset
	});
}
function specToImageUrl(spec) {
	const cdnUrl = (spec.baseUrl || "https://cdn.sanity.io").replace(/\/+$/, ""), vanityStub = spec.vanityName ? `/${spec.vanityName}` : "", filename = `${spec.asset.id}-${spec.asset.width}x${spec.asset.height}.${spec.asset.format}${vanityStub}`;
	let baseUrl;
	spec.mediaLibraryId ? baseUrl = `${cdnUrl}/media-libraries/${spec.mediaLibraryId}/images/${filename}` : spec.canvasId ? baseUrl = `${cdnUrl}/images/canvases/${spec.canvasId}/${filename}` : baseUrl = `${cdnUrl}/images/${spec.projectId}/${spec.dataset}/${filename}`;
	const params = [];
	if (spec.rect) {
		const { left, top, width, height } = spec.rect;
		(left !== 0 || top !== 0 || height !== spec.asset.height || width !== spec.asset.width) && params.push(`rect=${left},${top},${width},${height}`);
	}
	spec.bg && params.push(`bg=${spec.bg}`), spec.focalPoint && (params.push(`fp-x=${spec.focalPoint.x}`), params.push(`fp-y=${spec.focalPoint.y}`));
	const flip = [spec.flipHorizontal && "h", spec.flipVertical && "v"].filter(Boolean).join("");
	return flip && params.push(`flip=${flip}`), SPEC_NAME_TO_URL_NAME_MAPPINGS.forEach((mapping) => {
		const [specName, param] = mapping;
		typeof spec[specName] < "u" ? params.push(`${param}=${encodeURIComponent(spec[specName])}`) : typeof spec[param] < "u" && params.push(`${param}=${encodeURIComponent(spec[param])}`);
	}), params.length === 0 ? baseUrl : `${baseUrl}?${params.join("&")}`;
}
function fit(source, spec) {
	let cropRect;
	const imgWidth = spec.width, imgHeight = spec.height;
	if (!(imgWidth && imgHeight)) return {
		width: imgWidth,
		height: imgHeight,
		rect: source.crop
	};
	const crop = source.crop, hotspot = source.hotspot, desiredAspectRatio = imgWidth / imgHeight;
	if (crop.width / crop.height > desiredAspectRatio) {
		const height = Math.round(crop.height), width = Math.round(height * desiredAspectRatio), top = Math.max(0, Math.round(crop.top)), hotspotXCenter = Math.round((hotspot.right - hotspot.left) / 2 + hotspot.left);
		let left = Math.max(0, Math.round(hotspotXCenter - width / 2));
		left < crop.left ? left = crop.left : left + width > crop.left + crop.width && (left = crop.left + crop.width - width), cropRect = {
			left,
			top,
			width,
			height
		};
	} else {
		const width = crop.width, height = Math.round(width / desiredAspectRatio), left = Math.max(0, Math.round(crop.left)), hotspotYCenter = Math.round((hotspot.bottom - hotspot.top) / 2 + hotspot.top);
		let top = Math.max(0, Math.round(hotspotYCenter - height / 2));
		top < crop.top ? top = crop.top : top + height > crop.top + crop.height && (top = crop.top + crop.height - height), cropRect = {
			left,
			top,
			width,
			height
		};
	}
	return {
		width: imgWidth,
		height: imgHeight,
		rect: cropRect
	};
}
var validFits = [
	"clip",
	"crop",
	"fill",
	"fillmax",
	"max",
	"scale",
	"min"
];
var validCrops = [
	"top",
	"bottom",
	"left",
	"right",
	"center",
	"focalpoint",
	"entropy"
];
var validAutoModes = ["format"];
function isSanityModernClientLike(client) {
	return client && "config" in client ? typeof client.config == "function" : !1;
}
function isSanityClientLike(client) {
	return client && "clientConfig" in client ? typeof client.clientConfig == "object" : !1;
}
function clientConfigToOptions(config) {
	const { apiHost: apiUrl, projectId, dataset } = config, baseOptions = { baseUrl: (apiUrl || "https://api.sanity.io").replace(/^https:\/\/api\./, "https://cdn.") }, resource = config.resource ?? config["~experimental_resource"];
	if (resource?.type === "media-library") {
		if (typeof resource.id != "string" || resource.id.length === 0) throw new Error("Media library clients must include an id in \"resource\"");
		return {
			...baseOptions,
			mediaLibraryId: resource.id
		};
	}
	if (resource?.type === "canvas") {
		if (typeof resource.id != "string" || resource.id.length === 0) throw new Error("Canvas clients must include an id in \"resource\"");
		return {
			...baseOptions,
			canvasId: resource.id
		};
	}
	if (resource?.type === "dataset") {
		if (typeof resource.id != "string" || resource.id.length === 0) throw new Error("Dataset clients must include an id in \"resource\"");
		const [resourceProjectId, resourceDataset] = resource.id.split(".");
		if (!resourceProjectId || !resourceDataset) throw new Error("Dataset resource id must be in the format \"projectId.dataset\", got: " + resource.id);
		return {
			...baseOptions,
			projectId: resourceProjectId,
			dataset: resourceDataset
		};
	}
	return {
		...baseOptions,
		projectId,
		dataset
	};
}
function rewriteSpecName(key) {
	const specs = SPEC_NAME_TO_URL_NAME_MAPPINGS;
	for (const entry of specs) {
		const [specName, param] = entry;
		if (key === specName || key === param) return specName;
	}
	return key;
}
function getOptions(_options) {
	let options = {};
	return isSanityModernClientLike(_options) ? options = clientConfigToOptions(_options.config()) : isSanityClientLike(_options) ? options = clientConfigToOptions(_options.clientConfig) : options = _options || {}, options;
}
function createBuilder(Builder, _options) {
	return new Builder(null, getOptions(_options));
}
function createImageUrlBuilder(options) {
	return createBuilder(ImageUrlBuilderImpl, options);
}
function constructNewOptions(currentOptions, options) {
	const baseUrl = options.baseUrl || currentOptions.baseUrl, newOptions = { baseUrl };
	for (const key in options) if (options.hasOwnProperty(key)) {
		const specKey = rewriteSpecName(key);
		newOptions[specKey] = options[key];
	}
	return {
		baseUrl,
		...newOptions
	};
}
var ImageUrlBuilderImpl = class ImageUrlBuilderImpl {
	options;
	constructor(parent, options) {
		this.options = parent ? {
			...parent.options || {},
			...options || {}
		} : { ...options || {} };
	}
	withOptions(options) {
		const newOptions = constructNewOptions(this.options, options);
		return new ImageUrlBuilderImpl(this, newOptions);
	}
	image(source) {
		return this.withOptions({ source });
	}
	dataset(dataset) {
		return this.withOptions({ dataset });
	}
	projectId(projectId) {
		return this.withOptions({ projectId });
	}
	withClient(client) {
		const newOptions = getOptions(client), preservedOptions = { ...this.options };
		return delete preservedOptions.baseUrl, delete preservedOptions.projectId, delete preservedOptions.dataset, delete preservedOptions.mediaLibraryId, delete preservedOptions.canvasId, new ImageUrlBuilderImpl(null, {
			...newOptions,
			...preservedOptions
		});
	}
	bg(bg) {
		return this.withOptions({ bg });
	}
	dpr(dpr) {
		return this.withOptions(dpr && dpr !== 1 ? { dpr } : {});
	}
	width(width) {
		return this.withOptions({ width });
	}
	height(height) {
		return this.withOptions({ height });
	}
	focalPoint(x, y) {
		return this.withOptions({ focalPoint: {
			x,
			y
		} });
	}
	maxWidth(maxWidth) {
		return this.withOptions({ maxWidth });
	}
	minWidth(minWidth) {
		return this.withOptions({ minWidth });
	}
	maxHeight(maxHeight) {
		return this.withOptions({ maxHeight });
	}
	minHeight(minHeight) {
		return this.withOptions({ minHeight });
	}
	size(width, height) {
		return this.withOptions({
			width,
			height
		});
	}
	blur(blur) {
		return this.withOptions({ blur });
	}
	sharpen(sharpen) {
		return this.withOptions({ sharpen });
	}
	rect(left, top, width, height) {
		return this.withOptions({ rect: {
			left,
			top,
			width,
			height
		} });
	}
	format(format) {
		return this.withOptions({ format });
	}
	invert(invert) {
		return this.withOptions({ invert });
	}
	orientation(orientation) {
		return this.withOptions({ orientation });
	}
	quality(quality) {
		return this.withOptions({ quality });
	}
	forceDownload(download) {
		return this.withOptions({ download });
	}
	flipHorizontal() {
		return this.withOptions({ flipHorizontal: !0 });
	}
	flipVertical() {
		return this.withOptions({ flipVertical: !0 });
	}
	ignoreImageParams() {
		return this.withOptions({ ignoreImageParams: !0 });
	}
	fit(value) {
		if (validFits.indexOf(value) === -1) throw new Error(`Invalid fit mode "${value}"`);
		return this.withOptions({ fit: value });
	}
	crop(value) {
		if (validCrops.indexOf(value) === -1) throw new Error(`Invalid crop mode "${value}"`);
		return this.withOptions({ crop: value });
	}
	saturation(saturation) {
		return this.withOptions({ saturation });
	}
	auto(value) {
		if (validAutoModes.indexOf(value) === -1) throw new Error(`Invalid auto mode "${value}"`);
		return this.withOptions({ auto: value });
	}
	pad(pad) {
		return this.withOptions({ pad });
	}
	vanityName(value) {
		return this.withOptions({ vanityName: value });
	}
	frame(frame) {
		if (frame !== 1) throw new Error(`Invalid frame value "${frame}"`);
		return this.withOptions({ frame });
	}
	url() {
		return urlForImage(this.options);
	}
	toString() {
		return this.url();
	}
};
function once(fn) {
	let didCall = !1, returnValue;
	return (...args) => (didCall || (returnValue = fn(...args), didCall = !0), returnValue);
}
var createWarningPrinter = (message) => once((...args) => {
	console.warn(message.join(" "), ...args);
});
createWarningPrinter(["The default export of @sanity/image-url has been deprecated. Use the named export `createImageUrlBuilder` instead."]);
//#endregion
//#region node_modules/.pnpm/sanity@6.9.2_@babel+core@7.29.7_@babel+runtime@7.29.7_@noble+hashes@2.3.0_@rolldown+plu_bc69515520d3729ef0d8dbe997d16e6c/node_modules/sanity/lib/useBundleDocuments-CiRQnzQh.js
/**
* @beta
*/
var releaseTypes = [
	"asap",
	"scheduled",
	"undecided"
];
/**
* @beta
*/
function isReleaseType(maybeReleaseType) {
	return typeof maybeReleaseType == "string" && releaseTypes.includes(maybeReleaseType);
}
/**
* @internal
*/
function isReleaseDocument(doc) {
	return typeof doc == "object" && !!doc && "_type" in doc && doc._type === "system.release";
}
/**
* Check if the release is a cardinality one release
*
* @internal
*/
function isCardinalityOneRelease(release) {
	return release.metadata?.cardinality === "one";
}
/**
* Check if perspective represents a cardinality one release
*
* @internal
*/
function isCardinalityOnePerspective(perspective) {
	return isReleaseDocument(perspective) && isCardinalityOneRelease(perspective);
}
/**
* Checks if a release is a paused cardinality one release (scheduled draft).
*
* @internal
*/
function isPausedCardinalityOneRelease(release) {
	return release ? release.state === "active" && release.metadata.releaseType === "scheduled" && isCardinalityOneRelease(release) && !!release.metadata.intendedPublishAt : !1;
}
/**
* @internal
*/
var LATEST = "drafts";
var PUBLISHED$1 = "published";
var DEFAULT_RELEASE_TYPE = "asap";
var ARCHIVED_RELEASE_STATES = ["archived", "published"];
var RELEASE_TYPES_TONES = {
	asap: { tone: "caution" },
	scheduled: { tone: "suggest" },
	undecided: { tone: "neutral" }
};
var RUNTIME_TIME_ZONE = Intl.DateTimeFormat().resolvedOptions().timeZone;
var formatRelativeLocale = (date, baseDate, timeZone = RUNTIME_TIME_ZONE) => {
	let relative = formatRelative(date, baseDate, { in: tz(timeZone) });
	return isValid(parse(relative, "MM/dd/yyyy", /* @__PURE__ */ new Date())) ? new Date(date).toLocaleDateString(void 0, { timeZone }) : relative;
};
var createBundleId = customAlphabet("abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789", 8);
/**
* Create a unique release id. This is used as the release id for documents included in the release.
* @internal
*/
function createReleaseId() {
	return `${RELEASE_DOCUMENTS_PATH}.r${createBundleId()}`;
}
/**
* @beta
* @param documentId - The document id, e.g. `my-document-id` or `drafts.my-document-id` or `summer.my-document-id`
* @param perspective - The current perspective, e.g. `release.summer` or undefined, it can be obtained from `useRouter().stickyParams.perspective`
* @returns boolean - `true` if the document is in the current perspective.
* e.g:
* - document: `summer.my-document-id`, perspective: `release.summer` : **true**
* - document: `my-document-id`, perspective: `release.summer` : **false**
* - document: `summer.my-document-id`perspective: `release.winter` : **false**
* - document: `summer.my-document-id`, perspective: `undefined` : **false**
* - document: `my-document-id`, perspective: `undefined` : **true**
* - document: `drafts.my-document-id`, perspective: `undefined` : **true**
*/
function getDocumentIsInPerspective(documentId, perspective) {
	let releaseId = getVersionFromId(documentId);
	return perspective ? perspective === void 0 || releaseId === "Published" ? !1 : releaseId === perspective : !isVersionId(documentId);
}
/** @internal */
function getPublishDateFromRelease(release) {
	let dateString = release.publishedAt || release.publishAt || release.metadata.intendedPublishAt;
	return dateString ? new Date(dateString) : null;
}
/**
* Formats a release's publish date as a locale-aware relative string. `timeZone`
* defaults to the host runtime's TZ; inside React components, prefer
* `useFormatRelativeLocalePublishDate` to bind the studio's selected timezone.
*
* @internal
*/
function formatRelativeLocalePublishDate(release, timeZone) {
	let publishDate = getPublishDateFromRelease(release);
	return publishDate ? formatRelativeLocale(publishDate, /* @__PURE__ */ new Date(), timeZone) : "";
}
/** @internal */
function isPublishedPerspective(perspective) {
	return perspective === "published";
}
/** @internal */
function isDraftPerspective(perspective) {
	return perspective === LATEST;
}
/** @internal */
function isReleaseScheduledOrScheduling(release) {
	return release.metadata.releaseType === "scheduled" && (release.state === "scheduled" || release.state === "scheduling");
}
/** @internal */
var getReleaseDefaults = () => ({
	_id: createReleaseId(),
	metadata: {
		title: "",
		description: "",
		releaseType: DEFAULT_RELEASE_TYPE,
		cardinality: "many"
	}
});
/**
* Check if the release is archived
*
* @internal */
function isNotArchivedRelease(release) {
	return release.state !== "archived";
}
/**
* @internal
*/
function shouldShowReleaseInView(cardinalityView) {
	return (release) => {
		let isCardinalityOne = isCardinalityOneRelease(release);
		return cardinalityView === "drafts" ? isCardinalityOne : !isCardinalityOne;
	};
}
/** @internal */
function filterReleasesForOverview(options) {
	let { releases, archivedReleases, cardinalityView, releaseGroupMode, dateFilter } = options, sourceReleases = releaseGroupMode === "archived" ? archivedReleases : releases, applyDateFilter = (items) => {
		if (!dateFilter) return items;
		let [startOfDayForTimeZone, endOfDayForTimeZone] = dateFilter.getTimezoneAdjustedDateTimeRange(dateFilter.filterDate);
		return items.filter((release) => {
			let publishAt = release.publishAt || release.metadata.intendedPublishAt;
			if (!publishAt || release.metadata.releaseType !== "scheduled") return !1;
			let publishDateUTC = new Date(publishAt);
			return publishDateUTC >= startOfDayForTimeZone && publishDateUTC <= endOfDayForTimeZone;
		});
	}, dateFiltered = applyDateFilter(sourceReleases);
	return cardinalityView === "drafts" ? releaseGroupMode === "active" ? dateFiltered.filter((release) => release.state === "scheduled" || release.state === "scheduling") : releaseGroupMode === "paused" ? dateFiltered.filter((release) => isPausedCardinalityOneRelease(release)) : dateFiltered : dateFiltered;
}
/** @internal */
function getReleaseTone(release) {
	if (isPublishedPerspective(release)) return "positive";
	if (isDraftPerspective(release)) return "default";
	if (isReleaseDocument(release)) {
		if (release.state === "archived") return "default";
		if (release?.metadata?.releaseType === "asap") return RELEASE_TYPES_TONES.asap.tone;
		if (release?.metadata?.releaseType === "undecided") return RELEASE_TYPES_TONES.undecided.tone;
		if (isPausedCardinalityOneRelease(release)) return "caution";
		if (release?.metadata?.releaseType === "scheduled") return RELEASE_TYPES_TONES.scheduled.tone;
	}
	return isAgentBundleName(release) ? "suggest" : "default";
}
/**
* Sorts releases by their release type and created date.
* @internal
*/
function sortReleases(releases = []) {
	return [...releases].sort((a, b) => {
		if (a.metadata.releaseType === "undecided" && b.metadata.releaseType !== "undecided") return -1;
		if (a.metadata.releaseType !== "undecided" && b.metadata.releaseType === "undecided") return 1;
		if (a.metadata.releaseType === "undecided" && b.metadata.releaseType === "undecided") return new Date(b._createdAt).getTime() - new Date(a._createdAt).getTime();
		if (a.metadata.releaseType === "scheduled" && b.metadata.releaseType === "scheduled") {
			let aPublishAt = a.publishAt || a.metadata.intendedPublishAt;
			if (!aPublishAt) return 1;
			let bPublishAt = b.publishAt || b.metadata.intendedPublishAt;
			return bPublishAt ? new Date(bPublishAt).getTime() - new Date(aPublishAt).getTime() : -1;
		}
		return a.metadata.releaseType === "asap" && b.metadata.releaseType !== "asap" ? 1 : a.metadata.releaseType !== "asap" && b.metadata.releaseType === "asap" ? -1 : a.metadata.releaseType === "asap" && b.metadata.releaseType === "asap" ? new Date(b._createdAt).getTime() - new Date(a._createdAt).getTime() : 0;
	});
}
var DRAFTS = ["drafts"];
var PUBLISHED = ["published"];
function getReleasesPerspectiveStack({ selectedPerspectiveName, releases, excludedPerspectives, isDraftModelEnabled }) {
	let defaultPerspective = isDraftModelEnabled ? DRAFTS : PUBLISHED;
	if (!selectedPerspectiveName) return defaultPerspective;
	if (selectedPerspectiveName === "published") return PUBLISHED;
	let sorted = sortReleases(releases).map((release) => getReleaseIdFromReleaseDocumentId(release._id)), selectedIndex = sorted.indexOf(selectedPerspectiveName);
	return selectedIndex === -1 ? [selectedPerspectiveName, ...defaultPerspective] : sorted.slice(selectedIndex).concat(defaultPerspective).filter((name) => !excludedPerspectives.includes(name));
}
var getFetchQuery = (releaseIds) => {
	let getSafeKey = (id) => `release_${id.replaceAll("-", "_")}`;
	return releaseIds.reduce(({ subquery: accSubquery, projection: accProjection }, releaseId) => {
		let bundleId = getReleaseIdFromReleaseDocumentId(releaseId), safeId = getSafeKey(bundleId);
		return {
			subquery: `${accSubquery}"${safeId}": *[sanity::partOfRelease("${bundleId}")]{_updatedAt, "docId": string::split(_id, ".")[2] } | order(_updatedAt desc),`,
			projection: `${accProjection}"${releaseId}": {
              "updatedAt": ${safeId}[0]._updatedAt,
              "documentIds": ${safeId}[].docId,
            },`
		};
	}, {
		subquery: "",
		projection: ""
	});
};
var createReleaseMetadataAggregator = (client) => {
	let aggregatorFetch$ = (releaseIds, isInitialLoad = !1) => {
		if (!releaseIds?.length || !client) return of({
			data: null,
			error: null,
			loading: !1
		});
		let { subquery: queryAllDocumentsInReleases, projection: projectionToBundleMetadata } = getFetchQuery(releaseIds), fetchData$ = client.observable.fetch(`{${queryAllDocumentsInReleases}}{${projectionToBundleMetadata}}`, {}, { tag: "release-docs.fetch" }).pipe(switchMap((releaseDocumentIdResponse) => of({
			data: Object.entries(releaseDocumentIdResponse).reduce((existing, el) => {
				let [releaseId, metadata] = el;
				return {
					...existing,
					[releaseId]: {
						...metadata,
						documentCount: metadata.documentIds?.length || 0
					}
				};
			}, {}),
			error: null,
			loading: !1
		})), catchError((error) => (console.error("Failed to fetch release metadata", error), of({
			data: null,
			error,
			loading: !1
		}))));
		return iif(() => isInitialLoad, fetchData$.pipe(startWith({
			loading: !0,
			data: null,
			error: null
		})), fetchData$);
	}, aggregatorListener$ = (releaseIds) => !releaseIds?.length || !client ? EMPTY : client.observable.listen(`*[(${releaseIds.reduce((accQuery, releaseId, index) => `${accQuery}${index === 0 ? "" : " ||"} sanity::partOfRelease("${releaseId}")`, "")})]`, {}, {
		includeResult: !0,
		includeAllVersions: !0,
		visibility: "query",
		events: ["mutation"],
		tag: "release-docs.listen"
	}).pipe(catchError((error) => (console.error("Failed to listen for release metadata", error), EMPTY)), bufferTime(1e3), filter((entriesArray) => entriesArray.length > 0), switchMap((entriesArray) => {
		let mutatedReleaseIds = entriesArray.reduce((accReleaseIds, event) => {
			if ("type" in event && event.type === "mutation") {
				let releaseId = event.documentId.split(".")[1];
				return accReleaseIds.includes(releaseId) ? accReleaseIds : [...accReleaseIds, releaseId];
			}
			return accReleaseIds;
		}, []);
		return mutatedReleaseIds.length ? aggregatorFetch$(mutatedReleaseIds) : EMPTY;
	}));
	return (releaseIds) => merge(aggregatorFetch$(releaseIds, !0), aggregatorListener$(releaseIds));
};
function createReleasesSet(releases) {
	return (releases ?? []).reduce((acc, bundle) => (acc.set(bundle._id, bundle), acc), /* @__PURE__ */ new Map());
}
function releasesReducer(state, action) {
	switch (action.type) {
		case "LOADING_STATE_CHANGED": return {
			...state,
			state: action.payload.loading ? "loading" : "loaded",
			error: action.payload.error
		};
		case "RELEASES_SET": {
			let releasesById = createReleasesSet(action.payload);
			return {
				...state,
				releases: releasesById
			};
		}
		default: return state;
	}
}
var QUERY$1 = `*[${`_type=="${RELEASE_DOCUMENT_TYPE}" && _id in path("${RELEASE_DOCUMENTS_PATH}.*")`}] {
  _id,
  _type,
  _rev,
  _createdAt,
  _updatedAt,
  state,
  finalDocumentStates,
  publishAt,
  publishedAt,
  "metadata": coalesce(metadata, {
    "title": "",
    "releaseType": "undecided",
  }),
  // Content Lake initially encoded non-error states as {error: {message: ""}}. This projection
  // ensures the error field only appears if the document has a non-empty error message.
  ...select(
    length(error.message) > 0 => { error },
    {}
  ),
} | order(_createdAt desc)`;
var INITIAL_STATE$2 = {
	releases: /* @__PURE__ */ new Map(),
	state: "initialising"
};
/**
* The releases store is initialised lazily when first subscribed to. Upon subscription, it will
* fetch a list of releases and create a listener to keep the locally held state fresh.
*
* The store is not disposed of when all subscriptions are closed. After it has been initialised,
* it will keep listening for the duration of the app's lifecycle. Subsequent subscriptions will be
* given the latest state upon subscription.
*/
function createReleaseStore(context) {
	let { client } = context, dispatch$ = new Subject(), fetchPending$ = new BehaviorSubject(!1);
	function dispatch(action) {
		dispatch$.next(action);
	}
	let listFetch$ = of({ action: {
		type: "LOADING_STATE_CHANGED",
		payload: {
			loading: !0,
			error: void 0
		}
	} }).pipe(filter(() => !fetchPending$.value), tap(() => fetchPending$.next(!0)), concatWith(listenQuery(client, QUERY$1, {}, { tag: "releases.listen" }).pipe(tap(() => fetchPending$.next(!1)), map((releases) => ({ response: releases })))), catchError((error) => of({ action: {
		type: "LOADING_STATE_CHANGED",
		payload: {
			loading: !1,
			error
		}
	} })), switchMap((entry) => "action" in entry ? of(entry.action) : of({
		type: "RELEASES_SET",
		payload: entry.response
	}, {
		type: "LOADING_STATE_CHANGED",
		payload: {
			loading: !1,
			error: void 0
		}
	}))), state$ = concat$1(merge(listFetch$, dispatch$)).pipe(filter((action) => action !== void 0), scan((state, action) => releasesReducer(state, action), INITIAL_STATE$2), startWith(INITIAL_STATE$2), shareReplay(1));
	return {
		state$,
		errorCount$: state$.pipe(releaseStoreErrorCount(), shareReplay(1)),
		getMetadataStateForSlugs$: createReleaseMetadataAggregator(client),
		dispatch
	};
}
/**
* @internal
*/
function releaseStoreErrorCount() {
	return pipe(switchMap(({ releases }) => from(releases.values()).pipe(filter((release) => release.state === "active"), filter((release) => release.error !== void 0), count())), distinctUntilChanged());
}
/** @internal */
function useReleasesStore() {
	let $ = (0, import_compiler_runtime.c)(5), resourceCache = useResourceCache(), workspace = useWorkspace(), previewStore = useDocumentPreviewStore(), studioClient = useClient(RELEASES_STUDIO_CLIENT_OPTIONS), releaseStore;
	return $[0] !== previewStore || $[1] !== resourceCache || $[2] !== studioClient || $[3] !== workspace ? (releaseStore = resourceCache.get({
		dependencies: [workspace, previewStore],
		namespace: "ReleasesStore"
	}) || createReleaseStore({
		client: studioClient,
		previewStore
	}), resourceCache.set({
		dependencies: [workspace, previewStore],
		namespace: "ReleasesStore",
		value: releaseStore
	}), $[0] = previewStore, $[1] = resourceCache, $[2] = studioClient, $[3] = workspace, $[4] = releaseStore) : releaseStore = $[4], releaseStore;
}
/**
* Hook to get the (non archived, non published) active releases
* @internal
*/
function useActiveReleases() {
	let $ = (0, import_compiler_runtime.c)(8), { state$, dispatch } = useReleasesStore(), state = useSyncObservable(state$), t0;
	$[0] === state.releases ? t0 = $[1] : (t0 = sortReleases(Array.from(state.releases.values()).filter(_temp$4)).reverse(), $[0] = state.releases, $[1] = t0);
	let releasesAsArray = t0, t1 = state.error, t2;
	$[2] === Symbol.for("react.memo_cache_sentinel") ? (t2 = ["loading", "initialising"], $[2] = t2) : t2 = $[2];
	let t3 = t2.includes(state.state), t4;
	return $[3] !== dispatch || $[4] !== releasesAsArray || $[5] !== state.error || $[6] !== t3 ? (t4 = {
		data: releasesAsArray,
		dispatch,
		error: t1,
		loading: t3
	}, $[3] = dispatch, $[4] = releasesAsArray, $[5] = state.error, $[6] = t3, $[7] = t4) : t4 = $[7], t4;
}
function _temp$4(release) {
	return !ARCHIVED_RELEASE_STATES.includes(release.state);
}
var PREVIEW_SIZES = {
	block: {
		icon: 25,
		media: {
			width: 33,
			height: 33
		}
	},
	blockImage: {
		icon: 25,
		media: {
			width: 600,
			height: 400
		}
	},
	compact: {
		icon: 21,
		media: {
			width: 25,
			height: 25
		}
	},
	default: {
		icon: 21,
		media: {
			width: 33,
			height: 33
		}
	},
	detail: {
		icon: 25,
		media: {
			width: 73,
			height: 73
		}
	},
	inline: {
		icon: 15,
		media: {
			width: 15,
			height: 15
		}
	},
	media: {
		icon: 25,
		media: {
			width: 160,
			height: 160
		}
	}
};
var MediaWrapper = /* @__PURE__ */ gt.span.withConfig({
	displayName: "MediaWrapper",
	componentId: "sc-r6m3px-0"
})((props) => {
	let { $dimensions, $layout, $radius, $responsive } = props, width = $dimensions.width || 0, height = $dimensions.width || 0, iconSize = PREVIEW_SIZES[$layout].icon;
	return ft`position:relative;width:${$responsive ? "100%" : rem(width)};height:${$responsive ? "100%" : rem(height)};min-width:${$responsive ? void 0 : rem(width)};border-radius:${({ theme }) => rem(theme.sanity.radius[$radius])};display:flex;overflow:hidden;overflow:clip;align-items:center;justify-content:center;& img{position:absolute;left:0;top:0;width:100%;height:100%;object-fit:contain;border-radius:inherit;}& svg{color:var(--card-icon-color);display:block;flex:1;&:not([data-sanity-icon]){height:1em;width:1em;max-width:1em;max-height:1em;}&[data-sanity-icon]{display:block;font-size:calc(${iconSize} / 16 * 1em);}}& > span[data-border]{display:block;position:absolute;left:0;top:0;right:0;bottom:0;box-shadow:inset 0 0 0 1px var(--card-fg-color);opacity:0.1;border-radius:inherit;pointer-events:none;}`;
});
MediaWrapper.displayName = "Styled(MediaWrapper)";
function Media(props) {
	let $ = (0, import_compiler_runtime.c)(14), { border: t0, dimensions, layout, media, radius: t1, responsive: t2, styles } = props, border = t0 === void 0 || t0, radius = t1 === void 0 ? 1 : t1, responsive = t2 !== void 0 && t2, t3 = styles?.media, t4;
	$[0] !== dimensions || $[1] !== layout || $[2] !== media ? (t4 = renderMedia({
		dimensions,
		layout,
		media
	}), $[0] = dimensions, $[1] = layout, $[2] = media, $[3] = t4) : t4 = $[3];
	let t5;
	$[4] === border ? t5 = $[5] : (t5 = border && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { "data-border": !0 }), $[4] = border, $[5] = t5);
	let t6;
	return $[6] !== dimensions || $[7] !== layout || $[8] !== radius || $[9] !== responsive || $[10] !== t3 || $[11] !== t4 || $[12] !== t5 ? (t6 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(MediaWrapper, {
		$dimensions: dimensions,
		$layout: layout,
		$radius: radius,
		$responsive: responsive,
		className: t3,
		"data-testid": "Media",
		children: [t4, t5]
	}), $[6] = dimensions, $[7] = layout, $[8] = radius, $[9] = responsive, $[10] = t3, $[11] = t4, $[12] = t5, $[13] = t6) : t6 = $[13], t6;
}
function renderMedia(props) {
	let { dimensions, layout, media, styles } = props;
	return typeof media == "string" ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
		as: "span",
		className: styles?.mediaString,
		size: 1,
		children: media
	}) : (0, import_react_is.isValidElementType)(media) ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(media, {
		dimensions,
		layout
	}) : (0, import_react.isValidElement)(media) ? media : null;
}
function renderPreviewMedia(value, layout, dimensions) {
	return (0, import_react_is.isValidElementType)(value) ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(value, {
		layout,
		dimensions
	}) : typeof value == "string" ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { children: value }) : (0, import_react.isValidElement)(value) || value === null || typeof value == "number" || typeof value == "boolean" ? value : null;
}
function renderPreviewNode(value, layout, fallbackNode) {
	return typeof value == "string" ? value : (0, import_react_is.isValidElementType)(value) ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(value, { layout }) : value || fallbackNode;
}
function resolveBlockImageDimensions(source) {
	let sourceDimensions = tryGetImageDimensions(source);
	if (sourceDimensions === void 0) return;
	let max = PREVIEW_SIZES.blockImage.media, scale = Math.min(max.width / sourceDimensions.width, 1);
	return {
		width: Math.round(sourceDimensions.width * scale),
		height: Math.round(sourceDimensions.height * scale),
		fit: "max",
		dpr: getDevicePixelRatio()
	};
}
var DEFAULT_MEDIA_DIMENSIONS$6 = {
	...PREVIEW_SIZES.compact.media,
	aspect: 1,
	fit: "crop",
	dpr: getDevicePixelRatio()
};
var Root$3 = gt(Flex).withConfig({
	displayName: "Root",
	componentId: "sc-68vx4r-0"
})`height:${rem(PREVIEW_SIZES.compact.media.height)};box-sizing:content-box;`;
var TitleSkeleton$2 = gt(TextSkeleton).attrs({
	animated: !0,
	radius: 1,
	size: 1
}).withConfig({
	displayName: "TitleSkeleton",
	componentId: "sc-68vx4r-1"
})`max-width:${rem(160)};width:80%;`;
/**
* @hidden
* @beta */
function CompactPreview(props) {
	let $ = (0, import_compiler_runtime.c)(32), { children, isPlaceholder, media, status, title } = props, { t } = useTranslation$1(), t0;
	$[0] === status ? t0 = $[1] : (t0 = status && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Box, {
		"data-testid": "compact-preview__status",
		paddingLeft: 4,
		paddingRight: 1,
		children: renderPreviewNode(status, "compact")
	}), $[0] = status, $[1] = t0);
	let statusNode = t0;
	if (isPlaceholder) {
		let t1 = media ? 1 : 2, t2;
		$[2] === media ? t2 = $[3] : (t2 = media && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Skeleton, {
			animated: !0,
			radius: 2,
			style: PREVIEW_SIZES.compact.media
		}), $[2] = media, $[3] = t2);
		let t3;
		$[4] === Symbol.for("react.memo_cache_sentinel") ? (t3 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Stack, {
			"data-testid": "compact-preview__heading",
			flex: 1,
			gap: 2,
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(TitleSkeleton$2, {})
		}), $[4] = t3) : t3 = $[4];
		let t4;
		$[5] !== statusNode || $[6] !== t2 ? (t4 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Flex, {
			align: "center",
			flex: 1,
			gap: 2,
			children: [
				t2,
				t3,
				statusNode
			]
		}), $[5] = statusNode, $[6] = t2, $[7] = t4) : t4 = $[7];
		let t5;
		return $[8] !== t1 || $[9] !== t4 ? (t5 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Root$3, {
			align: "center",
			"data-testid": "default-preview",
			paddingLeft: t1,
			paddingRight: 2,
			paddingY: 1,
			children: t4
		}), $[8] = t1, $[9] = t4, $[10] = t5) : t5 = $[10], t5;
	}
	let t1 = media ? 1 : 2, t2;
	$[11] === media ? t2 = $[12] : (t2 = media && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Media, {
		border: !1,
		dimensions: DEFAULT_MEDIA_DIMENSIONS$6,
		layout: "compact",
		media
	}), $[11] = media, $[12] = t2);
	let t3;
	$[13] === Symbol.for("react.memo_cache_sentinel") ? (t3 = { color: "inherit" }, $[13] = t3) : t3 = $[13];
	let t4;
	$[14] === title ? t4 = $[15] : (t4 = title && renderPreviewNode(title, "compact"), $[14] = title, $[15] = t4);
	let t5;
	$[16] !== t || $[17] !== title ? (t5 = !title && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
		style: { color: "var(--card-muted-fg-color)" },
		children: t("preview.default.title-fallback")
	}), $[16] = t, $[17] = title, $[18] = t5) : t5 = $[18];
	let t6;
	$[19] !== t4 || $[20] !== t5 ? (t6 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Stack, {
		"data-testid": "compact-preview__header",
		flex: 1,
		gap: 2,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Text, {
			size: 1,
			style: t3,
			textOverflow: "ellipsis",
			weight: "medium",
			children: [t4, t5]
		})
	}), $[19] = t4, $[20] = t5, $[21] = t6) : t6 = $[21];
	let t7;
	$[22] !== statusNode || $[23] !== t2 || $[24] !== t6 ? (t7 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Flex, {
		align: "center",
		flex: 1,
		gap: 2,
		children: [
			t2,
			t6,
			statusNode
		]
	}), $[22] = statusNode, $[23] = t2, $[24] = t6, $[25] = t7) : t7 = $[25];
	let t8;
	$[26] === children ? t8 = $[27] : (t8 = children && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Box, {
		marginLeft: 1,
		children
	}), $[26] = children, $[27] = t8);
	let t9;
	return $[28] !== t1 || $[29] !== t7 || $[30] !== t8 ? (t9 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Root$3, {
		align: "center",
		"data-testid": "compact-preview",
		paddingLeft: t1,
		paddingRight: 2,
		paddingY: 1,
		children: [t7, t8]
	}), $[28] = t1, $[29] = t7, $[30] = t8, $[31] = t9) : t9 = $[31], t9;
}
var Root$2 = gt(Card).withConfig({
	displayName: "Root",
	componentId: "sc-8ay9u9-0"
})`overflow:hidden;overflow:clip;`;
var Bar = /* @__PURE__ */ gt(Card).withConfig({
	displayName: "Bar",
	componentId: "sc-8ay9u9-1"
})(({ theme }) => {
	let { color } = theme.sanity;
	return ft`height:${.5}rem;background:${hues.blue[color.dark ? 400 : 500].hex};transition:transform 75ms;`;
});
/**
* @hidden
* @beta */
function LinearProgress(props) {
	let $ = (0, import_compiler_runtime.c)(2), { value } = props, t0 = `translate3d(${value - 100}%, 0, 0)`, t1;
	return $[0] === t0 ? t1 = $[1] : (t1 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Root$2, {
		radius: 5,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Bar, {
			radius: 5,
			style: { transform: t0 }
		})
	}), $[0] = t0, $[1] = t1), t1;
}
var DEFAULT_MEDIA_DIMENSIONS$5 = {
	...PREVIEW_SIZES.default.media,
	aspect: 1,
	fit: "crop",
	dpr: getDevicePixelRatio()
};
var Root$1 = gt(Flex).withConfig({
	displayName: "Root",
	componentId: "sc-9cbebo-0"
})`height:${rem(PREVIEW_SIZES.default.media.height)};box-sizing:content-box;`;
var TitleSkeleton$1 = gt(TextSkeleton).attrs({
	animated: !0,
	radius: 1,
	size: 1
}).withConfig({
	displayName: "TitleSkeleton",
	componentId: "sc-9cbebo-1"
})`max-width:${rem(160)};width:80%;`;
var SubtitleSkeleton$1 = gt(TextSkeleton).attrs({
	animated: !0,
	radius: 1,
	size: 1
}).withConfig({
	displayName: "SubtitleSkeleton",
	componentId: "sc-9cbebo-2"
})`max-width:${rem(120)};width:60%;`;
/**
* @hidden
* @beta */
function DefaultPreview$1(props) {
	let $ = (0, import_compiler_runtime.c)(49), { title, subtitle, media, status, isPlaceholder, children, styles, progress } = props, { t } = useTranslation$1(), t0 = styles?.root, t1 = !!subtitle && styles?.hasSubtitle, t2;
	$[0] !== t0 || $[1] !== t1 ? (t2 = clsx(t0, t1), $[0] = t0, $[1] = t1, $[2] = t2) : t2 = $[2];
	let rootClassName = t2, isUploading = typeof progress == "number" && progress > -1, t3;
	$[3] !== status || $[4] !== styles?.status ? (t3 = status && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Box, {
		className: styles?.status,
		"data-testid": "default-preview__status",
		children: renderPreviewNode(status, "default")
	}), $[3] = status, $[4] = styles?.status, $[5] = t3) : t3 = $[5];
	let statusNode = t3;
	if (isPlaceholder) {
		let t4 = styles?.placeholder, t5 = media ? 2 : 3, t6;
		$[6] === media ? t6 = $[7] : (t6 = media && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Box, {
			flex: "none",
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Skeleton, {
				animated: !0,
				delay: 300,
				radius: 1,
				style: PREVIEW_SIZES.default.media
			})
		}), $[6] = media, $[7] = t6);
		let t7;
		$[8] === Symbol.for("react.memo_cache_sentinel") ? (t7 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Stack, {
			"data-testid": "default-preview__heading",
			flex: 1,
			gap: 2,
			children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(TitleSkeleton$1, { delay: 300 }), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(SubtitleSkeleton$1, { delay: 300 })]
		}), $[8] = t7) : t7 = $[8];
		let t8;
		$[9] === statusNode ? t8 = $[10] : (t8 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Box, {
			flex: "none",
			padding: 1,
			children: statusNode
		}), $[9] = statusNode, $[10] = t8);
		let t9;
		$[11] !== t6 || $[12] !== t8 ? (t9 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Flex, {
			align: "center",
			flex: 1,
			gap: 2,
			children: [
				t6,
				t7,
				t8
			]
		}), $[11] = t6, $[12] = t8, $[13] = t9) : t9 = $[13];
		let t10;
		return $[14] !== t4 || $[15] !== t5 || $[16] !== t9 ? (t10 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Root$1, {
			align: "center",
			className: t4,
			"data-testid": "default-preview",
			padding: 2,
			paddingLeft: t5,
			children: t9
		}), $[14] = t4, $[15] = t5, $[16] = t9, $[17] = t10) : t10 = $[17], t10;
	}
	let t4 = media ? 2 : 3, t5;
	$[18] !== media || $[19] !== styles ? (t5 = media && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Box, {
		flex: "none",
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Media, {
			dimensions: DEFAULT_MEDIA_DIMENSIONS$5,
			layout: "default",
			media,
			styles
		})
	}), $[18] = media, $[19] = styles, $[20] = t5) : t5 = $[20];
	let t6 = styles?.heading, t7;
	$[21] !== isUploading || $[22] !== progress ? (t7 = isUploading && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(LinearProgress, { value: progress }), $[21] = isUploading, $[22] = progress, $[23] = t7) : t7 = $[23];
	let t8;
	$[24] !== isUploading || $[25] !== styles?.subtitle || $[26] !== styles?.title || $[27] !== subtitle || $[28] !== t || $[29] !== title ? (t8 = !isUploading && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [/* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Text, {
		className: styles?.title,
		size: 1,
		style: { color: "inherit" },
		textOverflow: "ellipsis",
		weight: "medium",
		children: [title && renderPreviewNode(title, "default"), !title && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
			style: { color: "var(--card-muted-fg-color)" },
			children: t("preview.default.title-fallback")
		})]
	}), subtitle && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
		muted: !0,
		size: 1,
		textOverflow: "ellipsis",
		className: styles?.subtitle,
		children: renderPreviewNode(subtitle, "default")
	})] }), $[24] = isUploading, $[25] = styles?.subtitle, $[26] = styles?.title, $[27] = subtitle, $[28] = t, $[29] = title, $[30] = t8) : t8 = $[30];
	let t9;
	$[31] !== t6 || $[32] !== t7 || $[33] !== t8 ? (t9 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Stack, {
		className: t6,
		"data-testid": "default-preview__header",
		flex: 1,
		gap: 2,
		children: [t7, t8]
	}), $[31] = t6, $[32] = t7, $[33] = t8, $[34] = t9) : t9 = $[34];
	let t10;
	$[35] === statusNode ? t10 = $[36] : (t10 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Box, {
		flex: "none",
		padding: 1,
		children: statusNode
	}), $[35] = statusNode, $[36] = t10);
	let t11;
	$[37] !== children || $[38] !== styles?.children ? (t11 = children && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
		className: styles?.children,
		children
	}), $[37] = children, $[38] = styles?.children, $[39] = t11) : t11 = $[39];
	let t12;
	$[40] !== t10 || $[41] !== t11 || $[42] !== t5 || $[43] !== t9 ? (t12 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Flex, {
		align: "center",
		flex: 1,
		gap: 2,
		children: [
			t5,
			t9,
			t10,
			t11
		]
	}), $[40] = t10, $[41] = t11, $[42] = t5, $[43] = t9, $[44] = t12) : t12 = $[44];
	let t13;
	return $[45] !== rootClassName || $[46] !== t12 || $[47] !== t4 ? (t13 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Root$1, {
		align: "center",
		className: rootClassName,
		"data-testid": "default-preview",
		padding: 2,
		paddingLeft: t4,
		children: t12
	}), $[45] = rootClassName, $[46] = t12, $[47] = t4, $[48] = t13) : t13 = $[48], t13;
}
var RootFlex = gt(Flex).attrs({ align: "center" }).withConfig({
	displayName: "RootFlex",
	componentId: "sc-k47es-0"
})`height:${rem(PREVIEW_SIZES.detail.media.height)};`;
var StatusBox = gt(Box).withConfig({
	displayName: "StatusBox",
	componentId: "sc-k47es-1"
})`white-space:nowrap;`;
var MediaSkeleton$1 = gt(Skeleton).attrs({
	animated: !0,
	radius: 2
}).withConfig({
	displayName: "MediaSkeleton",
	componentId: "sc-k47es-2"
})`width:${rem(PREVIEW_SIZES.detail.media.width)};height:${rem(PREVIEW_SIZES.detail.media.height)};`;
var TitleSkeleton = gt(TextSkeleton).attrs({
	animated: !0,
	radius: 1,
	size: 1
}).withConfig({
	displayName: "TitleSkeleton",
	componentId: "sc-k47es-3"
})`max-width:${rem(160)};width:80%;`;
var SubtitleSkeleton = gt(TextSkeleton).attrs({
	animated: !0,
	radius: 1,
	size: 1
}).withConfig({
	displayName: "SubtitleSkeleton",
	componentId: "sc-k47es-4"
})`max-width:${rem(120)};width:60%;`;
var DescriptionSkeleton = gt(TextSkeleton).attrs({
	animated: !0,
	radius: 1,
	size: 1
}).withConfig({
	displayName: "DescriptionSkeleton",
	componentId: "sc-k47es-5"
})`max-width:${rem(180)};width:90%;`;
var DescriptionText = /* @__PURE__ */ gt(Text).withConfig({
	displayName: "DescriptionText",
	componentId: "sc-k47es-6"
})(({ theme }) => {
	let { fonts } = theme.sanity, maxHeight = fonts.text.sizes[1].lineHeight * 2;
	return ft`& > span{max-height:${rem(maxHeight)};display:-webkit-box;overflow:hidden;overflow:clip;text-overflow:ellipsis;-webkit-line-clamp:${2};-webkit-box-orient:vertical;}`;
});
var DEFAULT_MEDIA_DIMENSIONS$4 = {
	...PREVIEW_SIZES.detail.media,
	fit: "crop",
	aspect: 1,
	dpr: getDevicePixelRatio()
};
/**
* @hidden
* @beta */
function DetailPreview(props) {
	let $ = (0, import_compiler_runtime.c)(47), { title, subtitle, description, mediaDimensions: t0, media, status, children, isPlaceholder } = props, mediaDimensions = t0 === void 0 ? DEFAULT_MEDIA_DIMENSIONS$4 : t0, { t } = useTranslation$1(), t1;
	$[0] === status ? t1 = $[1] : (t1 = status && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(StatusBox, {
		marginLeft: 3,
		paddingRight: 1,
		children: renderPreviewNode(status, "detail")
	}), $[0] = status, $[1] = t1);
	let statusNode = t1;
	if (isPlaceholder) {
		let t2 = media ? 2 : 3, t3;
		$[2] === media ? t3 = $[3] : (t3 = media && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(MediaSkeleton$1, { "data-testid": "detail-preview__media" }), $[2] = media, $[3] = t3);
		let t4, t5;
		$[4] === Symbol.for("react.memo_cache_sentinel") ? (t4 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(TitleSkeleton, {}), t5 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(SubtitleSkeleton, {}), $[4] = t4, $[5] = t5) : (t4 = $[4], t5 = $[5]);
		let t6;
		$[6] === description ? t6 = $[7] : (t6 = description && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Box, {
			marginTop: 1,
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(DescriptionSkeleton, {})
		}), $[6] = description, $[7] = t6);
		let t7;
		$[8] === t6 ? t7 = $[9] : (t7 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Flex, {
			align: "center",
			"data-testid": "detail-preview__header",
			flex: 1,
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Stack, {
				flex: 1,
				gap: 2,
				children: [
					t4,
					t5,
					t6
				]
			})
		}), $[8] = t6, $[9] = t7);
		let t8;
		$[10] !== statusNode || $[11] !== t3 || $[12] !== t7 ? (t8 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Flex, {
			align: "center",
			flex: 1,
			gap: 3,
			children: [
				t3,
				t7,
				statusNode
			]
		}), $[10] = statusNode, $[11] = t3, $[12] = t7, $[13] = t8) : t8 = $[13];
		let t9;
		return $[14] !== t2 || $[15] !== t8 ? (t9 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(RootFlex, {
			"data-testid": "detail-preview",
			paddingLeft: t2,
			paddingRight: 2,
			paddingY: 2,
			children: t8
		}), $[14] = t2, $[15] = t8, $[16] = t9) : t9 = $[16], t9;
	}
	let t2 = media ? 2 : 3, t3;
	$[17] !== media || $[18] !== mediaDimensions ? (t3 = media && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Media, {
		dimensions: mediaDimensions,
		layout: "detail",
		media
	}), $[17] = media, $[18] = mediaDimensions, $[19] = t3) : t3 = $[19];
	let t4;
	$[20] === Symbol.for("react.memo_cache_sentinel") ? (t4 = { color: "inherit" }, $[20] = t4) : t4 = $[20];
	let t5;
	$[21] === title ? t5 = $[22] : (t5 = title && renderPreviewNode(title, "detail"), $[21] = title, $[22] = t5);
	let t6;
	$[23] !== t || $[24] !== title ? (t6 = !title && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(import_jsx_runtime.Fragment, { children: t("preview.default.title-fallback") }), $[23] = t, $[24] = title, $[25] = t6) : t6 = $[25];
	let t7;
	$[26] !== t5 || $[27] !== t6 ? (t7 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Text, {
		textOverflow: "ellipsis",
		size: 1,
		style: t4,
		weight: "medium",
		children: [t5, t6]
	}), $[26] = t5, $[27] = t6, $[28] = t7) : t7 = $[28];
	let t8;
	$[29] === subtitle ? t8 = $[30] : (t8 = subtitle && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
		muted: !0,
		size: 1,
		textOverflow: "ellipsis",
		children: renderPreviewNode(subtitle, "detail")
	}), $[29] = subtitle, $[30] = t8);
	let t9;
	$[31] === description ? t9 = $[32] : (t9 = description && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Box, {
		marginTop: 1,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(DescriptionText, {
			muted: !0,
			size: 1,
			children: renderPreviewNode(description, "detail")
		})
	}), $[31] = description, $[32] = t9);
	let t10;
	$[33] !== t7 || $[34] !== t8 || $[35] !== t9 ? (t10 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Stack, {
		flex: 1,
		gap: 2,
		children: [
			t7,
			t8,
			t9
		]
	}), $[33] = t7, $[34] = t8, $[35] = t9, $[36] = t10) : t10 = $[36];
	let t11;
	$[37] !== statusNode || $[38] !== t10 ? (t11 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Flex, {
		align: "center",
		"data-testid": "detail-preview__header",
		flex: 1,
		children: [t10, statusNode]
	}), $[37] = statusNode, $[38] = t10, $[39] = t11) : t11 = $[39];
	let t12;
	$[40] !== children || $[41] !== t11 || $[42] !== t3 ? (t12 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Flex, {
		align: "center",
		flex: 1,
		gap: 3,
		children: [
			t3,
			t11,
			children
		]
	}), $[40] = children, $[41] = t11, $[42] = t3, $[43] = t12) : t12 = $[43];
	let t13;
	return $[44] !== t12 || $[45] !== t2 ? (t13 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(RootFlex, {
		"data-testid": "detail-preview",
		paddingLeft: t2,
		paddingRight: 2,
		paddingY: 2,
		children: t12
	}), $[44] = t12, $[45] = t2, $[46] = t13) : t13 = $[46], t13;
}
var Root = gt.svg.withConfig({
	displayName: "Root",
	componentId: "sc-wwiywq-0"
})`width:${43}px;height:${43}px;transform:rotate(-90deg);`;
var BgCircle = /* @__PURE__ */ gt.circle.withConfig({
	displayName: "BgCircle",
	componentId: "sc-wwiywq-1"
})(({ theme }) => {
	let { color } = theme.sanity;
	return ft`fill:none;stroke:${hues.gray[color.dark ? 900 : 100].hex};stroke-width:${3}px;`;
});
var ProgressCircle = /* @__PURE__ */ gt.circle.withConfig({
	displayName: "ProgressCircle",
	componentId: "sc-wwiywq-2"
})(({ theme }) => {
	let { color } = theme.sanity;
	return ft`fill:none;stroke:${hues.blue[color.dark ? 400 : 500].hex};stroke-width:${3}px;transition:stroke-dashoffset 75ms;`;
});
/**
* @hidden
* @beta */
function CircularProgress(props) {
	let $ = (0, import_compiler_runtime.c)(3), { value: valueProp } = props, value = Math.min(Math.max(valueProp, 0), 100), circ = 2 * Math.PI * 20, offset = (100 - value) / 100 * circ, viewBox = `${43 / 2} ${43 / 2} 43 43`, t0;
	$[0] === Symbol.for("react.memo_cache_sentinel") ? (t0 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(BgCircle, {
		cx: 43,
		cy: 43,
		r: 20
	}), $[0] = t0) : t0 = $[0];
	let t1 = `${offset}px`, t2;
	return $[1] === t1 ? t2 = $[2] : (t2 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Root, {
		viewBox,
		children: [t0, /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProgressCircle, {
			cx: 43,
			cy: 43,
			r: 20,
			style: {
				strokeDasharray: circ,
				strokeDashoffset: t1
			}
		})]
	}), $[1] = t1, $[2] = t2), t2;
}
var RootBox$1 = gt(Box).withConfig({
	displayName: "RootBox",
	componentId: "sc-ycvidm-0"
})`position:relative;`;
var MediaFlex = gt(Flex).attrs({
	align: "center",
	justify: "center"
}).withConfig({
	displayName: "MediaFlex",
	componentId: "sc-ycvidm-1"
})`position:absolute;left:0;top:0;right:0;bottom:0;`;
var MediaSkeleton = gt(Skeleton).attrs({
	animated: !0,
	radius: 2
}).withConfig({
	displayName: "MediaSkeleton",
	componentId: "sc-ycvidm-2"
})`width:100%;height:100%;`;
var ProgressFlex = gt(Flex).attrs({
	align: "center",
	justify: "center"
}).withConfig({
	displayName: "ProgressFlex",
	componentId: "sc-ycvidm-3"
})`position:absolute;left:0;top:0;right:0;bottom:0;&:before{background-color:var(--card-bg-color);opacity:0.75;content:'';display:block;position:absolute;left:0;top:0;right:0;bottom:0;}> svg{position:relative;z-index:2;}`;
var TooltipContentStack = gt(Stack).attrs({ gap: 2 }).withConfig({
	displayName: "TooltipContentStack",
	componentId: "sc-ycvidm-4"
})`max-width:${rem(200)};`;
var DEFAULT_MEDIA_DIMENSIONS$3 = {
	...PREVIEW_SIZES.media.media,
	aspect: 1,
	fit: "crop",
	dpr: getDevicePixelRatio()
};
/**
* @hidden
* @beta */
function MediaPreview(props) {
	let $ = (0, import_compiler_runtime.c)(30), { media, mediaDimensions: t0, children, isPlaceholder, progress: t1, subtitle, title, withBorder: t2, withRadius: t3 } = props, mediaDimensions = t0 === void 0 ? DEFAULT_MEDIA_DIMENSIONS$3 : t0, progress = t1 === void 0 ? -1 : t1, withBorder = t2 === void 0 || t2, withRadius = t3 === void 0 || t3, t4 = `${100 / (mediaDimensions.aspect || 1)}%`, t5;
	$[0] === t4 ? t5 = $[1] : (t5 = { paddingBottom: t4 }, $[0] = t4, $[1] = t5);
	let STYLES_PADDER = t5, t6;
	bb0: {
		if (!title || !subtitle) {
			t6 = null;
			break bb0;
		}
		let t7;
		$[2] === title ? t7 = $[3] : (t7 = title && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
			align: "center",
			size: 1,
			weight: "medium",
			children: renderPreviewNode(title, "media")
		}), $[2] = title, $[3] = t7);
		let t8;
		$[4] === subtitle ? t8 = $[5] : (t8 = subtitle && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
			align: "center",
			muted: !0,
			size: 1,
			children: renderPreviewNode(subtitle, "media")
		}), $[4] = subtitle, $[5] = t8);
		let t9;
		$[6] !== t7 || $[7] !== t8 ? (t9 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(TooltipContentStack, { children: [t7, t8] }), $[6] = t7, $[7] = t8, $[8] = t9) : t9 = $[8], t6 = t9;
	}
	let tooltipContent = t6, t7;
	$[9] === STYLES_PADDER ? t7 = $[10] : (t7 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: STYLES_PADDER }), $[9] = STYLES_PADDER, $[10] = t7);
	let t8 = !tooltipContent, t9;
	$[11] !== isPlaceholder || $[12] !== media || $[13] !== mediaDimensions || $[14] !== withBorder || $[15] !== withRadius ? (t9 = isPlaceholder ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(MediaSkeleton, {}) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Media, {
		border: withBorder,
		dimensions: mediaDimensions,
		layout: "media",
		media,
		radius: +!!withRadius,
		responsive: !0
	}), $[11] = isPlaceholder, $[12] = media, $[13] = mediaDimensions, $[14] = withBorder, $[15] = withRadius, $[16] = t9) : t9 = $[16];
	let t10;
	$[17] === progress ? t10 = $[18] : (t10 = typeof progress == "number" && progress > -1 && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProgressFlex, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(CircularProgress, { value: progress }) }), $[17] = progress, $[18] = t10);
	let t11;
	$[19] !== t10 || $[20] !== t9 ? (t11 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(MediaFlex, { children: [t9, t10] }), $[19] = t10, $[20] = t9, $[21] = t11) : t11 = $[21];
	let t12;
	$[22] !== t11 || $[23] !== t8 || $[24] !== tooltipContent ? (t12 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Tooltip$1, {
		content: tooltipContent,
		disabled: t8,
		placement: "top",
		portal: !0,
		children: t11
	}), $[22] = t11, $[23] = t8, $[24] = tooltipContent, $[25] = t12) : t12 = $[25];
	let t13;
	return $[26] !== children || $[27] !== t12 || $[28] !== t7 ? (t13 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Box, {
		padding: 2,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(RootBox$1, {
			"data-testid": "media-preview",
			flex: 1,
			overflow: "hidden",
			children: [
				t7,
				t12,
				children
			]
		})
	}), $[26] = children, $[27] = t12, $[28] = t7, $[29] = t13) : t13 = $[29], t13;
}
var HeaderFlex$1 = gt(Flex).attrs({ align: "center" }).withConfig({
	displayName: "HeaderFlex",
	componentId: "sc-xnkyfo-0"
})`height:${rem(PREVIEW_SIZES.block.media.height)};white-space:nowrap;position:relative;z-index:1;`;
var MediaCard = gt(Card).withConfig({
	displayName: "MediaCard",
	componentId: "sc-xnkyfo-1"
})`overflow:hidden;position:relative;padding-bottom:${({ $ratio }) => $ratio}%;& > span{position:absolute;top:0;left:0;right:0;bottom:0;}`;
var RootBox = gt(Box).attrs({ overflow: "hidden" }).withConfig({
	displayName: "RootBox",
	componentId: "sc-xnkyfo-2"
})`border-radius:${({ theme }) => theme.sanity.radius[1]}px;`;
var DEFAULT_MEDIA_DIMENSIONS$2 = {
	...PREVIEW_SIZES.blockImage.media,
	fit: "fillmax",
	dpr: getDevicePixelRatio()
};
var getRatio = (dimensions) => {
	let { height, width } = dimensions;
	return !height || !width ? 1 : height / width * 100;
};
/**
* @hidden
* @beta */
function BlockImagePreview(props) {
	let $ = (0, import_compiler_runtime.c)(35), { actions, title, subtitle, description, fallbackTitle: t0, mediaDimensions: t1, media, children, status } = props, fallbackTitle = t0 === void 0 ? "Untitled" : t0, mediaDimensions = t1 === void 0 ? DEFAULT_MEDIA_DIMENSIONS$2 : t1, t2;
	$[0] !== fallbackTitle || $[1] !== title ? (t2 = (title || fallbackTitle) && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
		size: 1,
		textOverflow: "ellipsis",
		weight: "medium",
		children: title ? renderPreviewNode(title, "block") : fallbackTitle
	}), $[0] = fallbackTitle, $[1] = title, $[2] = t2) : t2 = $[2];
	let t3;
	$[3] === subtitle ? t3 = $[4] : (t3 = subtitle && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
		muted: !0,
		size: 1,
		textOverflow: "ellipsis",
		children: renderPreviewNode(subtitle, "block")
	}), $[3] = subtitle, $[4] = t3);
	let t4;
	$[5] !== t2 || $[6] !== t3 ? (t4 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Stack, {
		flex: 1,
		gap: 2,
		children: [t2, t3]
	}), $[5] = t2, $[6] = t3, $[7] = t4) : t4 = $[7];
	let t5;
	$[8] === status ? t5 = $[9] : (t5 = status && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Box, {
		paddingX: 2,
		paddingY: 3,
		children: renderPreviewNode(status, "block")
	}), $[8] = status, $[9] = t5);
	let t6 = actions, t7;
	$[10] !== t5 || $[11] !== t6 ? (t7 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Flex, {
		gap: 1,
		paddingLeft: 1,
		children: [t5, t6]
	}), $[10] = t5, $[11] = t6, $[12] = t7) : t7 = $[12];
	let t8;
	$[13] !== t4 || $[14] !== t7 ? (t8 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(HeaderFlex$1, {
		paddingBottom: 3,
		paddingLeft: 2,
		paddingRight: 1,
		paddingTop: 1,
		children: [t4, t7]
	}), $[13] = t4, $[14] = t7, $[15] = t8) : t8 = $[15];
	let t9;
	$[16] === mediaDimensions ? t9 = $[17] : (t9 = getRatio(mediaDimensions), $[16] = mediaDimensions, $[17] = t9);
	let t10 = media, t11;
	$[18] !== mediaDimensions || $[19] !== t10 ? (t11 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Media, {
		border: !1,
		dimensions: mediaDimensions,
		layout: "blockImage",
		media: t10,
		radius: 0,
		responsive: !0
	}), $[18] = mediaDimensions, $[19] = t10, $[20] = t11) : t11 = $[20];
	let t12;
	$[21] !== t11 || $[22] !== t9 ? (t12 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(MediaCard, {
		$ratio: t9,
		__unstable_checkered: !0,
		display: "flex",
		sizing: "border",
		radius: 2,
		tone: "inherit",
		children: t11
	}), $[21] = t11, $[22] = t9, $[23] = t12) : t12 = $[23];
	let t13;
	$[24] !== t12 || $[25] !== t8 ? (t13 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Stack, { children: [t8, t12] }), $[24] = t12, $[25] = t8, $[26] = t13) : t13 = $[26];
	let t14;
	$[27] === description ? t14 = $[28] : (t14 = description && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Box, {
		paddingX: 2,
		paddingY: 3,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
			muted: !0,
			size: 1,
			children: renderPreviewNode(description, "block")
		})
	}), $[27] = description, $[28] = t14);
	let t15;
	$[29] === children ? t15 = $[30] : (t15 = children && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { children }), $[29] = children, $[30] = t15);
	let t16;
	return $[31] !== t13 || $[32] !== t14 || $[33] !== t15 ? (t16 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(RootBox, { children: [
		t13,
		t14,
		t15
	] }), $[31] = t13, $[32] = t14, $[33] = t15, $[34] = t16) : t16 = $[34], t16;
}
var DEFAULT_MEDIA_DIMENSIONS$1 = {
	...PREVIEW_SIZES.block.media,
	aspect: 1,
	fit: "crop",
	dpr: getDevicePixelRatio()
};
var HeaderFlex = gt(Flex).attrs({ align: "center" }).withConfig({
	displayName: "HeaderFlex",
	componentId: "sc-hzs580-0"
})`min-height:${rem(PREVIEW_SIZES.block.media.height)};`;
/**
* @hidden
* @beta */
function BlockPreview(props) {
	let $ = (0, import_compiler_runtime.c)(34), { actions, title, subtitle, description, mediaDimensions: t0, media, progress, status, children } = props, mediaDimensions = t0 === void 0 ? DEFAULT_MEDIA_DIMENSIONS$1 : t0, isUploading = typeof progress == "number" && progress > -1, t1;
	$[0] !== media || $[1] !== mediaDimensions ? (t1 = media && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Media, {
		dimensions: mediaDimensions,
		layout: "block",
		media
	}), $[0] = media, $[1] = mediaDimensions, $[2] = t1) : t1 = $[2];
	let t2 = media ? 2 : 1, t3;
	$[3] === title ? t3 = $[4] : (t3 = title ? renderPreviewNode(title, "block") : "Untitled", $[3] = title, $[4] = t3);
	let t4;
	$[5] === t3 ? t4 = $[6] : (t4 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
		size: 1,
		textOverflow: "ellipsis",
		weight: "medium",
		children: t3
	}), $[5] = t3, $[6] = t4);
	let t5;
	$[7] === subtitle ? t5 = $[8] : (t5 = subtitle && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Box, {
		marginTop: 2,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
			muted: !0,
			size: 1,
			textOverflow: "ellipsis",
			children: renderPreviewNode(subtitle, "block")
		})
	}), $[7] = subtitle, $[8] = t5);
	let t6;
	$[9] === description ? t6 = $[10] : (t6 = description && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Box, {
		marginTop: 3,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
			muted: !0,
			size: 1,
			textOverflow: "ellipsis",
			children: renderPreviewNode(description, "block")
		})
	}), $[9] = description, $[10] = t6);
	let t7;
	$[11] !== isUploading || $[12] !== progress ? (t7 = isUploading && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Box, {
		marginTop: 3,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(LinearProgress, { value: progress })
	}), $[11] = isUploading, $[12] = progress, $[13] = t7) : t7 = $[13];
	let t8;
	$[14] !== t2 || $[15] !== t4 || $[16] !== t5 || $[17] !== t6 || $[18] !== t7 ? (t8 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Box, {
		flex: 1,
		paddingLeft: t2,
		children: [
			t4,
			t5,
			t6,
			t7
		]
	}), $[14] = t2, $[15] = t4, $[16] = t5, $[17] = t6, $[18] = t7, $[19] = t8) : t8 = $[19];
	let t9;
	$[20] === status ? t9 = $[21] : (t9 = status && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Box, {
		paddingX: 2,
		paddingY: 3,
		children: renderPreviewNode(status, "block")
	}), $[20] = status, $[21] = t9);
	let t10 = actions, t11;
	$[22] !== t10 || $[23] !== t9 ? (t11 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Flex, {
		gap: 1,
		paddingLeft: 1,
		children: [t9, t10]
	}), $[22] = t10, $[23] = t9, $[24] = t11) : t11 = $[24];
	let t12;
	$[25] !== t1 || $[26] !== t11 || $[27] !== t8 ? (t12 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(HeaderFlex, {
		"data-testid": "block-preview__header",
		children: [
			t1,
			t8,
			t11
		]
	}), $[25] = t1, $[26] = t11, $[27] = t8, $[28] = t12) : t12 = $[28];
	let t13;
	$[29] === children ? t13 = $[30] : (t13 = children && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
		"data-testid": "block-preview__children",
		children
	}), $[29] = children, $[30] = t13);
	let t14;
	return $[31] !== t12 || $[32] !== t13 ? (t14 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Stack, {
		"data-testid": "block-preview",
		gap: 1,
		children: [t12, t13]
	}), $[31] = t12, $[32] = t13, $[33] = t14) : t14 = $[33], t14;
}
var RootSpan = gt.span.withConfig({
	displayName: "RootSpan",
	componentId: "sc-n8xnri-0"
})`display:inline-flex;align-items:center;vertical-align:top;height:calc(1em - 1px);max-width:100%;`;
var MediaSpan = gt.span.withConfig({
	displayName: "MediaSpan",
	componentId: "sc-n8xnri-1"
})`position:relative;display:inline-block;width:calc(1em - 1px);height:calc(1em - 1px);min-width:calc(1em - 1px);& img{position:absolute;top:0;left:0;width:100%;height:100%;object-fit:cover;border-radius:${({ theme }) => rem(theme.sanity.radius[1])};}& img + span{position:absolute;left:0;top:0;right:0;bottom:0;box-shadow:inset 0 0 0 1px var(--card-fg-color);opacity:0.2;border-radius:${({ theme }) => rem(theme.sanity.radius[1])};}& svg{display:block;font-size:calc(14 / 16 * 1em);margin:1px 0;&[data-sanity-icon]{font-size:calc(18 / 16 * 1em);margin:calc(1px + (2 / 18 * -1em)) 0;}}`;
var TextSpan = /* @__PURE__ */ gt(Text).attrs({ forwardedAs: "span" }).withConfig({
	displayName: "TextSpan",
	componentId: "sc-n8xnri-2"
})(({ theme }) => {
	let textFont = theme.sanity.fonts.text, textSize = textFont.sizes[1];
	return ft`font-size:calc(${textSize.fontSize} / 16 * 1em);font-weight:${textFont.weights.medium};box-sizing:border-box;display:inline-block;vertical-align:top;line-height:${textSize.lineHeight / textSize.fontSize};padding-left:0.5em;padding-right:calc(0.5em - 2px);min-width:0;& > span{display:block;white-space:nowrap;text-overflow:ellipsis;overflow:hidden;overflow:clip;}`;
});
var DEFAULT_MEDIA_DIMENSIONS = {
	...PREVIEW_SIZES.inline.media,
	fit: "crop",
	aspect: 1,
	dpr: getDevicePixelRatio()
};
/**
* @hidden
* @beta */
function InlinePreview(props) {
	let $ = (0, import_compiler_runtime.c)(11), { title, fallbackTitle: t0, media, mediaDimensions: t1 } = props, fallbackTitle = t0 === void 0 ? "Untitled" : t0, mediaDimensions = t1 === void 0 ? DEFAULT_MEDIA_DIMENSIONS : t1, t2;
	$[0] !== media || $[1] !== mediaDimensions ? (t2 = media && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(MediaSpan, {
		"data-testid": "inline-preview-media",
		children: [renderPreviewMedia(media, "inline", mediaDimensions), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {})]
	}), $[0] = media, $[1] = mediaDimensions, $[2] = t2) : t2 = $[2];
	let t3;
	$[3] !== fallbackTitle || $[4] !== title ? (t3 = renderPreviewNode(title, "inline", fallbackTitle), $[3] = fallbackTitle, $[4] = title, $[5] = t3) : t3 = $[5];
	let t4;
	$[6] === t3 ? t4 = $[7] : (t4 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(TextSpan, {
		"data-testid": "inline-preview-title",
		size: 1,
		children: t3
	}), $[6] = t3, $[7] = t4);
	let t5;
	return $[8] !== t2 || $[9] !== t4 ? (t5 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(RootSpan, {
		"data-testid": "inline-preview",
		children: [t2, t4]
	}), $[8] = t2, $[9] = t4, $[10] = t5) : t5 = $[10], t5;
}
function validatePlugin(pluginResult) {
	let messages = ["projectId" in pluginResult && "`projectId` not allowed in plugin configuration", "dataset" in pluginResult && "`dataset` not allowed in plugin configuration"].filter(isString);
	if (messages.length) throw Error(`Invalid plugin configuration:\n${messages.map((message) => `\t${message}`).join("\n")}`);
}
/**
* @hidden
* @beta */
function definePlugin(arg) {
	if (typeof arg == "function") {
		let pluginFactory = arg;
		return (options) => {
			let result = pluginFactory(options);
			return validatePlugin(result), result;
		};
	}
	return validatePlugin(arg), () => arg;
}
/**
* @deprecated Use `definePlugin` instead
*
* @hidden
* @beta
*/
function createPlugin(arg) {
	return definePlugin(arg);
}
function SchedulePublishingStudioLayout(props) {
	let $ = (0, import_compiler_runtime.c)(5), toast = useToast(), t0, t1;
	$[0] === toast ? (t0 = $[1], t1 = $[2]) : (t0 = () => {
		console.error("Scheduled publishing plugin is added by default, please remove this plugin from your config. \n        \nIf you have a custom date config, you can use the scheduledPublishing API to customize the date input.\n        \nSee: https://www.sanity.io/docs/scheduled-publishing.\n        "), toast.push({
			id: "scheduled-publishing-deprecated",
			closable: !0,
			duration: 6e4,
			status: "error",
			title: "Scheduled publishing plugin is deprecated",
			description: "The scheduled publishing plugin is now deprecated, you should remove the plugin from your configuration. If you have a custom date config, you can use the scheduledPublishing API to customize the date input. See: https://www.sanity.io/docs/scheduled-publishing."
		});
	}, t1 = [toast], $[0] = toast, $[1] = t0, $[2] = t1), (0, import_react.useEffect)(t0, t1);
	let t2;
	return $[3] === props ? t2 = $[4] : (t2 = props.renderDefault(props), $[3] = props, $[4] = t2), t2;
}
/**
* Shows an error in console and a toast to prevent users from importing the deprecated plugin.
* Gives information about how to upgrade to the new version.
*/
var deprecatedScheduledPublishingPlugin = definePlugin({
	name: "sanity/deprecated/scheduled-publishing",
	studio: { components: { layout: SchedulePublishingStudioLayout } }
});
var DEPRECATED_PLUGINS = ["scheduled-publishing"];
var flattenConfig = ({ plugins = [], ...currentConfig }, path) => {
	let rootConfig = {
		config: currentConfig,
		path: [...path, currentConfig.name]
	}, allPlugins = plugins.flatMap((plugin) => flattenConfig(plugin, [...path, currentConfig.name])), deprecatedScheduledPublishing = allPlugins.find((p) => p.config.name === "scheduled-publishing");
	return deprecatedScheduledPublishing && allPlugins.push({
		path: deprecatedScheduledPublishing.path,
		config: deprecatedScheduledPublishingPlugin()
	}), [...allPlugins.filter((plugin) => !DEPRECATED_PLUGINS.includes(plugin.config.name)), rootConfig];
};
var emptyRender = () => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(import_react.Fragment, {});
function _createMiddlewareComponent(DefaultComponent, middlewareComponents) {
	function MiddlewareComponent(outerProps) {
		let $ = (0, import_compiler_runtime.c)(4), next = _temp;
		if ($[0] === Symbol.for("react.memo_cache_sentinel")) {
			for (let Middleware of middlewareComponents) {
				let renderDefault = next;
				next = (props_0) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Middleware, {
					...props_0,
					renderDefault
				});
			}
			$[0] = next;
		} else next = $[0];
		let t0;
		$[1] === Symbol.for("react.memo_cache_sentinel") ? (t0 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Skeleton, {
			padding: 3,
			radius: 1,
			animated: !0
		}), $[1] = t0) : t0 = $[1];
		let t1;
		return $[2] === outerProps ? t1 = $[3] : (t1 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(import_react.Suspense, {
			fallback: t0,
			children: next({
				...outerProps,
				renderDefault: emptyRender
			})
		}), $[2] = outerProps, $[3] = t1), t1;
	}
	function _temp(props) {
		return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(DefaultComponent, { ...props });
	}
	return MiddlewareComponent;
}
/**
* @internal
* This hook returns a component based on the Components API middleware chain.
*
* - The `pick` function is used to select a component from the provided plugin options in the configuration.
* - The `defaultComponent` is the default component that gets rendered with `renderDefault`.
*   The `renderDefault` function is added to the props of the middleware components so that they can render the default
*   component and continue the middleware chain.
*
* @example
* Example usage of:
*
* ```ts
*  const StudioLayout = useMiddlewareComponents({
*   pick: (plugin) => plugin.studio?.components?.layout,
*   defaultComponent: StudioLayout,
*  })
*
* return <StudioLayout />
*```
*/
function useMiddlewareComponents(props) {
	let $ = (0, import_compiler_runtime.c)(4), { options } = useSource().__internal, { defaultComponent, pick } = props, t0;
	if ($[0] !== defaultComponent || $[1] !== options || $[2] !== pick) {
		let flattened = [...flattenConfig(options, [])];
		flattened.reverse(), t0 = _createMiddlewareComponent(defaultComponent, flattened.map((t1) => {
			let { config } = t1;
			return pick(config);
		}).filter(Boolean)), $[0] = defaultComponent, $[1] = options, $[2] = pick, $[3] = t0;
	} else t0 = $[3];
	return t0;
}
/** @internal */
function isString$1(value) {
	return typeof value == "string";
}
/**
* Creates a Media Library-specific client with an explicitly configured API
* host that includes the project ID in the subdomain. This is to satisfy CORS
* requirements, as browser requests will fail against the global host because
* it does not include CORS headers. (Setting `resource` on a client makes it
* ignore `useProjectHostname` and target the global API host.)
*
* @internal
*/
function createMediaLibraryClient(client, libraryId) {
	let { apiHost, projectId } = client.config();
	if (!projectId) throw Error("Cannot create Media Library client: missing projectId in client config");
	let baseUrl = new URL(apiHost);
	return baseUrl.hostname = `${projectId}.${baseUrl.hostname}`, client.withConfig({
		apiHost: baseUrl.origin,
		resource: {
			id: libraryId,
			type: "media-library"
		}
	});
}
function hasOwnRecord(value, key) {
	return typeof value == "object" && !!value && Object.prototype.hasOwnProperty.call(value, key);
}
function makeMediaLibraryRef(libraryId, assetId) {
	return `media-library:${libraryId}:${assetId}`;
}
function containsMediaProperty(value) {
	return !hasOwnRecord(value, "media") || !hasOwnRecord(value.media, "_ref") ? !1 : typeof value.media._ref == "string";
}
function isMediaLibraryRef(ref) {
	if (typeof ref != "string") return !1;
	let [resourceType, resourceId, documentId] = ref.split(":", 3);
	return resourceType === "media-library" && !!resourceId && !!documentId;
}
/**
* Attempts to extract a Media Library ref from a source or raw string value.
*
* @internal
*/
function getMediaLibraryRef(value) {
	let maybeRef = containsMediaProperty(value) ? value?.media?._ref : value;
	return isMediaLibraryRef(maybeRef) ? maybeRef : void 0;
}
var loaders = /* @__PURE__ */ new Map();
var policyCache = new QuickLRU({
	maxAge: 3e5,
	maxSize: 2e3
});
/**
* Batch fetch asset access policies from the Media Library API and populate the
* cache.
*/
async function fetchAccessPoliciesBatch(params) {
	let { assetIds, client, libraryId } = params;
	if (assetIds.length === 0) return [];
	try {
		let results = await client.fetch("*[_id in $ids]{_id, cdnAccessPolicy}", { ids: assetIds }), policyById = Object.fromEntries(results.map((doc) => [doc._id, doc.cdnAccessPolicy]));
		return assetIds.map((assetId) => {
			let policy = policyById[assetId], assetRef = makeMediaLibraryRef(libraryId, assetId);
			return policyCache.set(assetRef, policy), policy;
		});
	} catch (error) {
		return console.error("Error fetching cdnAccessPolicy batch", {
			libraryId,
			assetIds,
			error
		}), assetIds.forEach((assetId) => {
			let assetRef = makeMediaLibraryRef(libraryId, assetId);
			policyCache.set(assetRef, void 0);
		}), assetIds.map(() => void 0);
	}
}
/**
* Resolve (or create) the DataLoader responsible for fetching asset access
* policies from a given Media Library.
*
* NOTE: The first client passed for a given library ID will be used to create the
* DataLoader instance, subsequent clients for the same library ID will be ignored.
*/
function resolveAssetPolicyLoader(client, libraryId) {
	let existingLoader = loaders.get(libraryId);
	if (existingLoader) return existingLoader;
	let mediaLibraryClient = createMediaLibraryClient(client, libraryId), loader = new import_dataloader.default((assetIds) => fetchAccessPoliciesBatch({
		assetIds,
		client: mediaLibraryClient,
		libraryId
	}), { cache: !1 });
	return loaders.set(libraryId, loader), loader;
}
/**
* Schedule an asset access policy fetch using the library-specific DataLoader.
* Returns the cached policy value if it's already been resolved.
*
* @internal
*/
function enqueueAssetAccessPolicyFetch(assetRef, client) {
	let [, libraryId, assetId] = assetRef.split(":", 3);
	if (!libraryId || !assetId || !client) return Promise.resolve(void 0);
	if (policyCache.has(assetRef)) {
		let policy = policyCache.get(assetRef) ?? void 0;
		return Promise.resolve(policy);
	}
	return resolveAssetPolicyLoader(client, libraryId).load(assetId);
}
/**
* Resolve the effective access policy for a given image source, including
* Media Library specific checks when possible.
*
* @internal
*/
function useAccessPolicy(params) {
	let $ = (0, import_compiler_runtime.c)(5), { client, source } = params, t0;
	$[0] === source ? t0 = $[1] : (t0 = getMediaLibraryRef(source), $[0] = source, $[1] = t0);
	let ref = t0, canCheck = !!(ref && client.config().token), requestKey = canCheck ? ref : null, t1;
	$[2] === client ? t1 = $[3] : (t1 = async (key) => enqueueAssetAccessPolicyFetch(key, client), $[2] = client, $[3] = t1);
	let fetcher = t1, t2;
	$[4] === Symbol.for("react.memo_cache_sentinel") ? (t2 = {
		dedupingInterval: 0,
		revalidateOnFocus: !1,
		revalidateOnReconnect: !1,
		shouldRetryOnError: !1
	}, $[4] = t2) : t2 = $[4];
	let { data: cdnAccessPolicy, isLoading } = useSWR(requestKey, fetcher, t2);
	return ref ? canCheck ? isLoading ? "checking" : cdnAccessPolicy ?? "unknown" : "unknown" : "public";
}
var CDN_HOST_STAGING = "https://sanity-cdn.work";
var API_HOST_STAGING = "https://api.sanity.work";
var CDN_HOST_PRODUCTION = "https://sanity-cdn.com";
var API_HOST_PRODUCTION = "https://api.sanity.io";
function useToken() {
	let $ = (0, import_compiler_runtime.c)(3), t0;
	$[0] === Symbol.for("react.memo_cache_sentinel") ? (t0 = { apiVersion: "vX" }, $[0] = t0) : t0 = $[0];
	let client = useClient(t0), t1;
	$[1] === client ? t1 = $[2] : (t1 = client.config(), $[1] = client, $[2] = t1);
	let { token } = t1;
	return token;
}
var requestCache = /* @__PURE__ */ new Map();
var IDLE_RESULT = {
	error: null,
	isLoading: !1,
	objectUrl: void 0
};
var LOADING_RESULT = {
	error: null,
	isLoading: !0,
	objectUrl: void 0
};
function createCacheKey(url, token) {
	if (!(!url || !token)) return `${token}:${url}`;
}
function notifyListeners(entry) {
	entry.listeners.forEach((listener) => listener());
}
function deriveResultFromEntry(entry) {
	switch (entry.status) {
		case "pending": return entry.objectUrl ? {
			objectUrl: entry.objectUrl,
			isLoading: !0,
			error: null
		} : LOADING_RESULT;
		case "success": return {
			objectUrl: entry.objectUrl,
			isLoading: !1,
			error: null
		};
		case "error": return {
			objectUrl: void 0,
			isLoading: !1,
			error: entry.error
		};
		default: return IDLE_RESULT;
	}
}
function createFetchCacheEntry(url, token) {
	let abortController = new AbortController(), entry = {
		abortController,
		blob: null,
		cleanupTimer: void 0,
		error: null,
		listeners: /* @__PURE__ */ new Set(),
		objectUrl: void 0,
		status: "pending"
	};
	return fetch(url, {
		cache: "force-cache",
		headers: { Authorization: `Bearer ${token}` },
		signal: abortController.signal
	}).then((res) => {
		if (res.ok) return res.blob();
		throw Error(`Failed to fetch image (HTTP ${res.status})`);
	}).then((blob) => {
		entry.blob = blob, entry.error = null, entry.status = "success", entry.objectUrl = URL.createObjectURL(blob), notifyListeners(entry);
	}).catch((error) => {
		entry.blob = null, entry.error = error, entry.status = "error", notifyListeners(entry);
	}), entry;
}
/**
* Fetches an authenticated image (using a token) and returns an object URL for
* use as an `<img src>`
*
* Caches using a url/token pair so multiple components can share a single
* fetch, and keeps cache entries alive briefly after the last consumer unmounts
* to avoid duplicate fetches when in strict mode.
*
* Automatically revokes object URLs and disposes cache entries when no longer
* referenced.
*
* @internal
*/
function useImageObjectUrl(url) {
	let $ = (0, import_compiler_runtime.c)(10), token = useToken(), t0;
	$[0] !== token || $[1] !== url ? (t0 = createCacheKey(url, token), $[0] = token, $[1] = url, $[2] = t0) : t0 = $[2];
	let cacheKey = t0, t1;
	$[3] === cacheKey ? t1 = $[4] : (t1 = () => {
		if (!cacheKey) return IDLE_RESULT;
		let entry = requestCache.get(cacheKey);
		return entry ? deriveResultFromEntry(entry) : LOADING_RESULT;
	}, $[3] = cacheKey, $[4] = t1);
	let [state, setState] = (0, import_react.useState)(t1), t2, t3;
	return $[5] !== cacheKey || $[6] !== token || $[7] !== url ? (t2 = () => {
		if (!cacheKey) {
			setState(_temp$3);
			return;
		}
		let entry_0 = requestCache.get(cacheKey);
		entry_0 || (entry_0 = createFetchCacheEntry(url, token), requestCache.set(cacheKey, entry_0)), entry_0.cleanupTimer && (clearTimeout(entry_0.cleanupTimer), entry_0.cleanupTimer = void 0);
		let updateState = () => setState(deriveResultFromEntry(entry_0));
		return updateState(), entry_0.listeners.add(updateState), () => {
			entry_0.listeners.delete(updateState), entry_0.listeners.size === 0 && (entry_0.cleanupTimer = setTimeout(() => {
				entry_0.abortController.abort(), entry_0.objectUrl && URL.revokeObjectURL(entry_0.objectUrl), requestCache.delete(cacheKey);
			}, 2e3));
		};
	}, t3 = [
		cacheKey,
		token,
		url
	], $[5] = cacheKey, $[6] = token, $[7] = url, $[8] = t2, $[9] = t3) : (t2 = $[8], t3 = $[9]), (0, import_react.useEffect)(t2, t3), state;
}
function _temp$3(prev) {
	return prev.objectUrl || prev.isLoading || prev.error ? IDLE_RESULT : prev;
}
/**
* Hook for resolving image URLs with Private asset support.
*
* Handles applying URL transformations and object URL generation when needed.
*
* @internal
*/
function useImageUrl(params) {
	let $ = (0, import_compiler_runtime.c)(10), { accessPolicy: t0, imageSource, imageUrlBuilder, transform } = params, accessPolicy = t0 === void 0 ? "public" : t0, isPolicyCheckInProgress = accessPolicy === "checking", isPrivate = accessPolicy === "private", t1;
	bb0: {
		if (!imageSource || isPolicyCheckInProgress) {
			t1 = void 0;
			break bb0;
		}
		if (transform) {
			let t2;
			$[0] !== imageSource || $[1] !== imageUrlBuilder || $[2] !== transform ? (t2 = transform(imageUrlBuilder, imageSource), $[0] = imageSource, $[1] = imageUrlBuilder, $[2] = transform, $[3] = t2) : t2 = $[3], t1 = t2;
			break bb0;
		}
		let t2;
		$[4] !== imageSource || $[5] !== imageUrlBuilder ? (t2 = imageUrlBuilder.image(imageSource).url(), $[4] = imageSource, $[5] = imageUrlBuilder, $[6] = t2) : t2 = $[6], t1 = t2;
	}
	let networkUrl = t1, { objectUrl, isLoading: objectUrlIsLoading } = useImageObjectUrl(isPrivate ? networkUrl : void 0), isLoading = isPolicyCheckInProgress || objectUrlIsLoading, url = isLoading ? void 0 : objectUrl ?? networkUrl, t2;
	return $[7] !== isLoading || $[8] !== url ? (t2 = {
		isLoading,
		url
	}, $[7] = isLoading, $[8] = url, $[9] = t2) : t2 = $[9], t2;
}
var _previewComponents = {
	block: BlockPreview,
	blockImage: BlockImagePreview,
	compact: CompactPreview,
	default: DefaultPreview$1,
	detail: DetailPreview,
	inline: InlinePreview,
	media: MediaPreview
};
function FallbackIcon() {
	let $ = (0, import_compiler_runtime.c)(1), t0;
	return $[0] === Symbol.for("react.memo_cache_sentinel") ? (t0 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(DocumentIcon, { className: "sanity-studio__preview-fallback-icon" }), $[0] = t0) : t0 = $[0], t0;
}
function SanityDefaultMedia(t0) {
	let $ = (0, import_compiler_runtime.c)(17), { client, dimensions, imageSource, imageUrlBuilder, title } = t0, t1;
	$[0] === dimensions ? t1 = $[1] : (t1 = (builder, val) => {
		let width = dimensions.width ?? 100, height = dimensions.height ?? 100, fit = dimensions.fit, dpr = dimensions.dpr ?? 1;
		return builder.image(val).withOptions({
			width,
			height,
			fit,
			dpr
		}).url() || "";
	}, $[0] = dimensions, $[1] = t1);
	let transform = t1, t2;
	$[2] !== client || $[3] !== imageSource ? (t2 = {
		client,
		source: imageSource
	}, $[2] = client, $[3] = imageSource, $[4] = t2) : t2 = $[4];
	let accessPolicy = useAccessPolicy(t2), t3;
	$[5] !== accessPolicy || $[6] !== imageSource || $[7] !== imageUrlBuilder || $[8] !== transform ? (t3 = {
		accessPolicy,
		imageSource,
		imageUrlBuilder,
		transform
	}, $[5] = accessPolicy, $[6] = imageSource, $[7] = imageUrlBuilder, $[8] = transform, $[9] = t3) : t3 = $[9];
	let { isLoading, url } = useImageUrl(t3), [hasImageError, setHasImageError] = (0, import_react.useState)(!1), t4;
	$[10] === Symbol.for("react.memo_cache_sentinel") ? (t4 = () => setHasImageError(!0), $[10] = t4) : t4 = $[10];
	let handleError = t4;
	if (isLoading) {
		let t5;
		return $[11] === Symbol.for("react.memo_cache_sentinel") ? (t5 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Skeleton, {
			animated: !0,
			style: {
				width: "100%",
				height: "100%"
			}
		}), $[11] = t5) : t5 = $[11], t5;
	}
	if (accessPolicy === "unknown" && hasImageError) {
		let t5;
		$[12] === Symbol.for("react.memo_cache_sentinel") ? (t5 = {
			width: "100%",
			height: "100%"
		}, $[12] = t5) : t5 = $[12];
		let t6;
		return $[13] === Symbol.for("react.memo_cache_sentinel") ? (t6 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Card, {
			tone: "critical",
			style: t5,
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Flex, {
				justify: "center",
				align: "center",
				style: {
					width: "100%",
					height: "100%"
				},
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(WarningOutlineIcon, {})
			})
		}), $[13] = t6) : t6 = $[13], t6;
	}
	let t5 = typeof title == "string" ? title : void 0, t6;
	return $[14] !== t5 || $[15] !== url ? (t6 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)("img", {
		alt: t5,
		onError: handleError,
		referrerPolicy: "strict-origin-when-cross-origin",
		src: url
	}), $[14] = t5, $[15] = url, $[16] = t6) : t6 = $[16], t6;
}
/**
* Used in cases where no custom preview component is provided
* @internal
* */
var SanityDefaultPreview = (0, import_react.memo)(function SanityDefaultPreview(props) {
	let $ = (0, import_compiler_runtime.c)(37), Icon, imageUrl, layout, mediaProp, restProps, title, tooltip;
	$[0] === props ? (Icon = $[1], imageUrl = $[2], layout = $[3], mediaProp = $[4], restProps = $[5], title = $[6], tooltip = $[7]) : ({icon: Icon, layout, media: mediaProp, imageUrl, title, tooltip, ...restProps} = props, $[0] = props, $[1] = Icon, $[2] = imageUrl, $[3] = layout, $[4] = mediaProp, $[5] = restProps, $[6] = title, $[7] = tooltip);
	let client = useClient(DEFAULT_STUDIO_CLIENT_OPTIONS), t0;
	$[8] === client ? t0 = $[9] : (t0 = createImageUrlBuilder(client), $[8] = client, $[9] = t0);
	let imageBuilder = t0, t1;
	$[10] !== client || $[11] !== imageBuilder || $[12] !== mediaProp || $[13] !== title ? (t1 = (options) => {
		let imageSource = mediaProp;
		if (isString$1(imageSource) && isSanityImageUrl(imageSource)) {
			let { assetId } = parseImageAssetUrl(imageSource);
			imageSource = assetId;
		}
		return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(SanityDefaultMedia, {
			client,
			dimensions: options.dimensions,
			imageSource,
			imageUrlBuilder: imageBuilder,
			title
		});
	}, $[10] = client, $[11] = imageBuilder, $[12] = mediaProp, $[13] = title, $[14] = t1) : t1 = $[14];
	let renderMedia = t1, t2;
	$[15] === Icon ? t2 = $[16] : (t2 = () => (0, import_jsx_runtime.jsx)(Icon || FallbackIcon, {}), $[15] = Icon, $[16] = t2);
	let renderIcon = t2, t3;
	bb0: {
		if (Icon === !1) {
			t3 = !1;
			break bb0;
		}
		if (isString$1(mediaProp) && isSanityImageUrl(mediaProp)) {
			t3 = renderMedia;
			break bb0;
		}
		if (isImageSource(mediaProp)) {
			t3 = renderMedia;
			break bb0;
		}
		if ((0, import_react_is.isValidElementType)(mediaProp)) {
			t3 = mediaProp;
			break bb0;
		}
		if ((0, import_react.isValidElement)(mediaProp)) {
			t3 = mediaProp;
			break bb0;
		}
		if (isString$1(imageUrl)) {
			let t4 = isString$1(title) ? title : void 0, t5;
			$[17] !== imageUrl || $[18] !== t4 ? (t5 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)("img", {
				src: imageUrl,
				alt: t4,
				referrerPolicy: "strict-origin-when-cross-origin"
			}), $[17] = imageUrl, $[18] = t4, $[19] = t5) : t5 = $[19], t3 = t5;
			break bb0;
		}
		t3 = renderIcon;
	}
	let media = t3, t4;
	bb1: {
		if (props.mediaDimensions) {
			t4 = props.mediaDimensions;
			break bb1;
		}
		if (layout !== "blockImage") {
			t4 = void 0;
			break bb1;
		}
		if (!isImageSource(mediaProp)) {
			t4 = void 0;
			break bb1;
		}
		let t5;
		$[20] === mediaProp ? t5 = $[21] : (t5 = resolveBlockImageDimensions(mediaProp), $[20] = mediaProp, $[21] = t5), t4 = t5;
	}
	let resolvedMediaDimensions = t4, t5;
	$[22] !== media || $[23] !== resolvedMediaDimensions || $[24] !== restProps || $[25] !== title ? (t5 = {
		...restProps,
		media,
		mediaDimensions: resolvedMediaDimensions,
		title
	}, $[22] = media, $[23] = resolvedMediaDimensions, $[24] = restProps, $[25] = title, $[26] = t5) : t5 = $[26];
	let previewProps = t5, LayoutComponent = _previewComponents[layout || "default"], t6;
	$[27] !== LayoutComponent || $[28] !== previewProps ? (t6 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(LayoutComponent, { ...previewProps }), $[27] = LayoutComponent, $[28] = previewProps, $[29] = t6) : t6 = $[29];
	let children = t6;
	if (tooltip) {
		let t7 = !tooltip, t8;
		$[30] === Symbol.for("react.memo_cache_sentinel") ? (t8 = ["top-end", "bottom-end"], $[30] = t8) : t8 = $[30];
		let t9;
		$[31] === children ? t9 = $[32] : (t9 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { children }), $[31] = children, $[32] = t9);
		let t10;
		return $[33] !== t7 || $[34] !== t9 || $[35] !== tooltip ? (t10 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Tooltip$1, {
			content: tooltip,
			disabled: t7,
			fallbackPlacements: t8,
			placement: "right",
			children: t9
		}), $[33] = t7, $[34] = t9, $[35] = tooltip, $[36] = t10) : t10 = $[36], t10;
	}
	return children;
});
SanityDefaultPreview.displayName = "Memo(SanityDefaultPreview)";
function defaultResolvePreviewComponent(schemaType) {
	return schemaType.components?.preview ? schemaType.components.preview : SanityDefaultPreview;
}
function pickInputComponent(plugin) {
	return plugin.form?.components?.input;
}
function pickFieldComponent(plugin) {
	return plugin.form?.components?.field;
}
function pickPreviewComponent(plugin) {
	return plugin.form?.components?.preview;
}
function pickItemComponent(plugin) {
	return plugin.form?.components?.item;
}
function pickBlockComponent(plugin) {
	return plugin.form?.components?.block;
}
function pickInlineBlockComponent(plugin) {
	return plugin.form?.components?.inlineBlock;
}
function pickAnnotationComponent(plugin) {
	return plugin.form?.components?.annotation;
}
function pickPortableTextEditorPluginsComponent(plugin) {
	return plugin.form?.components?.portableText?.plugins;
}
/**
* @internal
*/
function useResolveDefaultComponent(props) {
	let $ = (0, import_compiler_runtime.c)(9), { componentResolver, componentProps } = props, { schemaType } = componentProps;
	if (!schemaType) throw Error("the `schemaType` property must be defined");
	let t0;
	$[0] !== componentResolver || $[1] !== schemaType ? (t0 = componentResolver(schemaType), $[0] = componentResolver, $[1] = schemaType, $[2] = t0) : t0 = $[2];
	let DefaultResolvedComponent = t0, t1;
	$[3] === componentResolver ? t1 = $[4] : (t1 = (parentTypeProps) => {
		if (!parentTypeProps.schemaType?.type) throw Error("Attempted to render form component of non-existent parent type");
		let { components, ...restSchemaType } = parentTypeProps.schemaType, ParentTypeResolvedComponent = componentResolver(restSchemaType);
		return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ParentTypeResolvedComponent, { ...parentTypeProps });
	}, $[3] = componentResolver, $[4] = t1);
	let renderDefault = t1, t2;
	return $[5] !== DefaultResolvedComponent || $[6] !== componentProps || $[7] !== renderDefault ? (t2 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(DefaultResolvedComponent, {
		...componentProps,
		renderDefault
	}), $[5] = DefaultResolvedComponent, $[6] = componentProps, $[7] = renderDefault, $[8] = t2) : t2 = $[8], t2;
}
/**
* @internal
*/
function DefaultPreview(props) {
	let $ = (0, import_compiler_runtime.c)(2), t0;
	return $[0] === props ? t0 = $[1] : (t0 = {
		componentProps: props,
		componentResolver: defaultResolvePreviewComponent
	}, $[0] = props, $[1] = t0), useResolveDefaultComponent(t0);
}
/**
* @internal
*/
function usePreviewComponent() {
	let $ = (0, import_compiler_runtime.c)(1), t0;
	return $[0] === Symbol.for("react.memo_cache_sentinel") ? (t0 = {
		defaultComponent: DefaultPreview,
		pick: pickPreviewComponent
	}, $[0] = t0) : t0 = $[0], useMiddlewareComponents(t0);
}
/**
* @beta
*
* React hook that returns the current studio perspective and perspective stack.
*
* This will use the closest PerspectiveContext provider, which is either the global
* PerspectiveContext or the DocumentPerspectiveProvider.
*
* @returns See {@link PerspectiveContextValue}
* @example Reading the current perspective stack
* ```ts
* function MyComponent() {
*  const {perspectiveStack} = usePerspective()
*  // ... do something with the perspective stack , like passing it to the client perspective.
* }
* ```
*/
function usePerspective() {
	let context = (0, import_react.useContext)(PerspectiveContext);
	if (!context) throw Error("usePerspective must be used within a PerspectiveProvider");
	return context;
}
/**
* checks if the document has been set for unpublishing
* @internal
*
* @param document - sanity document
* @returns boolean if the system delete is set
*/
function isGoingToUnpublish(document) {
	return document._system?.delete === !0;
}
/**
* @internal
* @deprecated use useValuePreview instead
*/
var unstable_useValuePreview = useValuePreview;
var INITIAL_STATE$1 = { isLoading: !0 };
var IDLE_STATE = {
	isLoading: !1,
	value: {
		title: void 0,
		description: void 0
	}
};
/**
* @internal
*/
function useValuePreview(props) {
	let $ = (0, import_compiler_runtime.c)(15), t0;
	$[0] === props ? t0 = $[1] : (t0 = props || {}, $[0] = props, $[1] = t0);
	let { enabled: t1, ordering, schemaType, value: previewValue, perspectiveStack: chosenPerspectiveStack, variant: chosenVariant } = t0, enabled = t1 === void 0 || t1, { observeForPreview } = useDocumentPreviewStore(), { perspectiveStack, selectedVariantName } = usePerspective(), t2;
	bb0: {
		if (!enabled || !previewValue || !schemaType) {
			let t3;
			$[2] === Symbol.for("react.memo_cache_sentinel") ? (t3 = of(IDLE_STATE), $[2] = t3) : t3 = $[2], t2 = t3;
			break bb0;
		}
		let t3 = previewValue, t4;
		$[3] === t3 ? t4 = $[4] : (t4 = isGoingToUnpublish(t3), $[3] = t3, $[4] = t4);
		let goingToUnpublish = t4, t5;
		if ($[5] !== chosenPerspectiveStack || $[6] !== chosenVariant || $[7] !== goingToUnpublish || $[8] !== observeForPreview || $[9] !== ordering || $[10] !== perspectiveStack || $[11] !== previewValue || $[12] !== schemaType || $[13] !== selectedVariantName) {
			let updatedStack = goingToUnpublish ? [] : chosenPerspectiveStack ?? perspectiveStack, updatedVariant = goingToUnpublish ? void 0 : chosenVariant ?? (chosenPerspectiveStack ? void 0 : selectedVariantName);
			t5 = observeForPreview({
				_id: goingToUnpublish ? getPublishedId(previewValue._id) : previewValue._id,
				...goingToUnpublish ? {} : { ...previewValue }
			}, schemaType, {
				perspective: updatedStack,
				variant: updatedVariant,
				viewOptions: { ordering }
			}).pipe(map(_temp$2), catchError(_temp2$2)), $[5] = chosenPerspectiveStack, $[6] = chosenVariant, $[7] = goingToUnpublish, $[8] = observeForPreview, $[9] = ordering, $[10] = perspectiveStack, $[11] = previewValue, $[12] = schemaType, $[13] = selectedVariantName, $[14] = t5;
		} else t5 = $[14];
		t2 = t5;
	}
	return useSyncObservable(t2, INITIAL_STATE$1);
}
function _temp2$2(error) {
	return of({
		isLoading: !1,
		error
	});
}
function _temp$2(event) {
	return {
		isLoading: !1,
		value: event.snapshot || void 0
	};
}
var orientationChange$ = typeof window > "u" ? EMPTY : fromEvent(window, "orientationchange").pipe(shareReplay(1));
var resize$ = typeof window > "u" ? EMPTY : fromEvent(window, "resize").pipe(shareReplay(1));
var scroll$ = typeof window > "u" ? EMPTY : fromEvent(window, "scroll", {
	passive: !0,
	capture: !0
}).pipe(shareReplay(1));
function isIntersectionObserverSupported() {
	return typeof window < "u" && "IntersectionObserver" in window && "IntersectionObserverEntry" in window && "intersectionRatio" in IntersectionObserverEntry.prototype && ("isIntersecting" in IntersectionObserverEntry.prototype || Object.defineProperty(IntersectionObserverEntry.prototype, "isIntersecting", { get() {
		return this.intersectionRatio > 0;
	} }), !0);
}
var intersectionObservableFor = isIntersectionObserverSupported() ? createIntersectionObserverBased() : createLegacyBased();
function createIntersectionObserverBased() {
	let intersectionObserverEntriesSubject = new Subject(), intersectionObserver = new IntersectionObserver((entries) => {
		entries.forEach((entry) => {
			intersectionObserverEntriesSubject.next(entry);
		});
	}, {
		threshold: 0,
		rootMargin: "150px"
	});
	return function intersectionObservableFor(element) {
		return new Observable((observer) => (intersectionObserver.observe(element), observer.next(), () => intersectionObserver.unobserve(element))).pipe(mergeMap(() => intersectionObserverEntriesSubject.asObservable()), filter((entry) => entry.target === element), map((ev) => ({ isIntersecting: ev.isIntersecting })));
	};
}
function createLegacyBased() {
	function getViewport() {
		return {
			left: 0,
			right: window.innerWidth,
			top: 0,
			bottom: window.innerHeight
		};
	}
	function intersects(rect, viewport, margin) {
		return rect.left <= viewport.right + margin && rect.right >= viewport.left - margin && rect.top <= viewport.bottom + margin && rect.bottom >= viewport.top - margin;
	}
	function inViewport(element) {
		return () => intersects(element.getBoundingClientRect(), getViewport(), 150);
	}
	return function intersectionObservableFor(element) {
		let isElementInViewport = inViewport(element);
		return merge(of(isElementInViewport()), resize$, scroll$, orientationChange$).pipe(map(isElementInViewport), map((isIntersecting) => ({ isIntersecting })));
	};
}
var visibilityChange$ = typeof window > "u" ? EMPTY : fromEvent(document, "visibilitychange").pipe(shareReplay(1));
function useVisibility(props) {
	let $ = (0, import_compiler_runtime.c)(6), { element, hideDelay: t0, disabled } = props, hideDelay = t0 === void 0 ? 0 : t0, t1;
	bb0: {
		if (!element || disabled) {
			let t2;
			$[0] === Symbol.for("react.memo_cache_sentinel") ? (t2 = of(!1), $[0] = t2) : t2 = $[0], t1 = t2;
			break bb0;
		}
		let t2;
		if ($[1] !== element || $[2] !== hideDelay) {
			let seed = "checkVisibility" in element && element.checkVisibility(), isDocumentVisible$ = concat$1(of(!document.hidden), visibilityChange$.pipe(map(_temp$1))).pipe(distinctUntilChanged()), inViewport$ = intersectionObservableFor(element).pipe(map(_temp2$1)), t3;
			$[4] === hideDelay ? t3 = $[5] : (t3 = (isVisible) => isVisible ? of(!0) : of(!1).pipe(delay(hideDelay)), $[4] = hideDelay, $[5] = t3), t2 = isDocumentVisible$.pipe(switchMap((isDocumentVisible) => isDocumentVisible ? inViewport$ : of(!1)), switchMap(t3), distinctUntilChanged(), startWith(seed)), $[1] = element, $[2] = hideDelay, $[3] = t2;
		} else t2 = $[3];
		t1 = t2;
	}
	let visible = useObservable(t1, !1);
	return !disabled && visible;
}
function _temp2$1(event_0) {
	return event_0.isIntersecting;
}
function _temp$1(event) {
	return event.target instanceof Document && !event?.target?.hidden;
}
function _extractUploadState(value) {
	return _resolveUploadValue(value?._upload);
}
function getStringOrUndefined(value) {
	return isString$1(value) ? value : void 0;
}
function _resolveUploadValue(value) {
	if (!isRecord$4(value)) return;
	let progress = typeof value.progress == "number" ? value.progress : 0, createdAt = getStringOrUndefined(value.initiated || value.createdAt), updatedAt = getStringOrUndefined(value.updated || value.updatedAt), fileName = getStringOrUndefined(value?.file?.name), fileType = getStringOrUndefined(value?.file?.type), previewImage = getStringOrUndefined(value.previewImage);
	if (createdAt && updatedAt && fileName && fileType) return {
		progress,
		createdAt,
		updatedAt,
		file: {
			name: fileName,
			type: fileType
		},
		previewImage
	};
}
/**
* This component is responsible for converting renderPreview() calls into an element.
* It:
* - subscribes to "prepared" preview value as long as the element is visible on screen
* - resolves the configured preview component for the schema type
* - prepares "preview"-props and passes this to the configured preview component
* @internal
* */
function PreviewLoader(props) {
	let $ = (0, import_compiler_runtime.c)(41), Component, layout, perspectiveStack, restProps, schemaType, skipVisibilityCheck, styleProp, value, variant;
	$[0] === props ? (Component = $[1], layout = $[2], perspectiveStack = $[3], restProps = $[4], schemaType = $[5], skipVisibilityCheck = $[6], styleProp = $[7], value = $[8], variant = $[9]) : ({layout, value, component: Component, style: styleProp, schemaType, skipVisibilityCheck, perspectiveStack, variant, ...restProps} = props, $[0] = props, $[1] = Component, $[2] = layout, $[3] = perspectiveStack, $[4] = restProps, $[5] = schemaType, $[6] = skipVisibilityCheck, $[7] = styleProp, $[8] = value, $[9] = variant);
	let { t } = useTranslation$1(), [element, setElement] = (0, import_react.useState)(null), isVisible = useVisibility({
		disabled: skipVisibilityCheck,
		element,
		hideDelay: 2e3
	}) || skipVisibilityCheck, t0 = skipVisibilityCheck || isVisible, t1;
	$[10] !== perspectiveStack || $[11] !== schemaType || $[12] !== t0 || $[13] !== value || $[14] !== variant ? (t1 = {
		enabled: t0,
		schemaType,
		value,
		perspectiveStack,
		variant
	}, $[10] = perspectiveStack, $[11] = schemaType, $[12] = t0, $[13] = value, $[14] = variant, $[15] = t1) : t1 = $[15];
	let preview = useValuePreview(t1), t2 = styleProp?.minWidth || 1, t3 = styleProp?.minHeight || 1, t4;
	$[16] !== styleProp || $[17] !== t2 || $[18] !== t3 ? (t4 = {
		...styleProp,
		minWidth: t2,
		minHeight: t3
	}, $[16] = styleProp, $[17] = t2, $[18] = t3, $[19] = t4) : t4 = $[19];
	let style = t4, t5;
	$[20] === value ? t5 = $[21] : (t5 = _extractUploadState(value), $[20] = value, $[21] = t5);
	let uploadState = t5, t6;
	bb0: {
		if (uploadState?.previewImage) {
			let t7;
			$[22] === t ? t7 = $[23] : (t7 = t("preview.image.file-is-being-uploaded.alt-text"), $[22] = t, $[23] = t7);
			let t8;
			$[24] !== t7 || $[25] !== uploadState.previewImage ? (t8 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)("img", {
				alt: t7,
				src: uploadState.previewImage
			}), $[24] = t7, $[25] = uploadState.previewImage, $[26] = t8) : t8 = $[26], t6 = t8;
			break bb0;
		}
		if (preview?.value?.media === null || preview?.value?.media === !1) {
			t6 = void 0;
			break bb0;
		}
		if (!preview?.value?.media) {
			t6 = schemaType.icon;
			break bb0;
		}
		t6 = preview?.value?.media;
	}
	let media = t6, t7;
	$[27] === preview?.value ? t7 = $[28] : (t7 = preview?.value || {}, $[27] = preview?.value, $[28] = t7);
	let t8 = preview?.error, t9 = preview?.isLoading, t10;
	$[29] !== Component || $[30] !== layout || $[31] !== media || $[32] !== restProps || $[33] !== schemaType || $[34] !== t7 || $[35] !== t8 || $[36] !== t9 ? (t10 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Component, {
		...restProps,
		...t7,
		media,
		error: t8,
		isPlaceholder: t9,
		layout,
		schemaType
	}), $[29] = Component, $[30] = layout, $[31] = media, $[32] = restProps, $[33] = schemaType, $[34] = t7, $[35] = t8, $[36] = t9, $[37] = t10) : t10 = $[37];
	let t11;
	return $[38] !== style || $[39] !== t10 ? (t11 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
		ref: setElement,
		style,
		children: t10
	}), $[38] = style, $[39] = t10, $[40] = t11) : t11 = $[40], t11;
}
/**
* @internal
*/
function Preview(props) {
	let $ = (0, import_compiler_runtime.c)(3), PreviewComponent = usePreviewComponent(), t0;
	return $[0] !== PreviewComponent || $[1] !== props ? (t0 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(PreviewLoader, {
		...props,
		component: PreviewComponent
	}), $[0] = PreviewComponent, $[1] = props, $[2] = t0) : t0 = $[2], t0;
}
/**
* React hook that returns the name of the current dataset
*
* @public
* @returns The name of the current dataset
* @example Using the `useDataset` hook
* ```ts
* function MyComponent() {
*   const dataset = useDataset()
*   // ... do something with the dataset name ...
* }
* ```
*/
function useDataset() {
	return useSource().dataset;
}
var INITIAL_VALUE = {
	data: [],
	versions: [],
	error: null,
	loading: !0
};
var observableCache = /* @__PURE__ */ new Map();
var withSystemCache = /* @__PURE__ */ new Map();
var swr = createSWR({ maxSize: 100 });
/**
* Fetches the document versions for a given document
*
* @param props - document Id of the document (might include release id)
* @returns - data: document versions, loading, errors
* @hidden
* @beta
*/
function useDocumentVersions(props) {
	let observable = useDocumentVersionsObservable(props);
	return useSyncObservable(observable, INITIAL_VALUE);
}
/**
* Creates an observable that emits the document versions for a given document.
*
* @hidden
* @beta
*/
function useDocumentVersionsObservable(props) {
	let $ = (0, import_compiler_runtime.c)(10), { documentId } = props, t0;
	$[0] === documentId ? t0 = $[1] : (t0 = getPublishedId(documentId), $[0] = documentId, $[1] = t0);
	let publishedId = t0, dataset = useDataset(), projectId = useProjectId(), documentPreviewStore = useDocumentPreviewStore(), { state$: releasesState$ } = useReleasesStore(), t1;
	$[2] === releasesState$ ? t1 = $[3] : (t1 = releasesState$.pipe(map(_temp2$3)), $[2] = releasesState$, $[3] = t1);
	let releases$ = t1, t2;
	return $[4] !== dataset || $[5] !== documentPreviewStore || $[6] !== projectId || $[7] !== publishedId || $[8] !== releases$ ? (t2 = getOrCreateDocumentVersionsWithSystemObservable({
		documentPreviewStore,
		publishedId,
		projectId,
		dataset,
		releases$
	}), $[4] = dataset, $[5] = documentPreviewStore, $[6] = projectId, $[7] = publishedId, $[8] = releases$, $[9] = t2) : t2 = $[9], t2;
}
/**
* Temporarily builds the document _system for a given document id.
* This is used until the documents are migrated to the new _system.
* And only if the documents are not variant documents.
*
* Variants will include the _system field.
*/
function _temp2$3(state) {
	return Array.from(state.releases.values()).filter(_temp$5);
}
function _temp$5(release) {
	return !ARCHIVED_RELEASE_STATES.includes(release.state);
}
var buildDocumentSystem = (id, releases) => {
	let versionId = getVersionFromId(id);
	if (versionId) {
		let releaseDocument = releases.find((release) => getReleaseIdFromReleaseDocumentId(release._id) === versionId);
		return {
			bundleId: versionId,
			release: releaseDocument ? {
				_ref: releaseDocument._id,
				_weak: !0
			} : void 0,
			group: {
				_ref: getPublishedId(id),
				_weak: !0
			},
			scopeId: versionId
		};
	}
	return {
		bundleId: isDraftId(id) ? "drafts" : void 0,
		group: {
			_ref: getPublishedId(id),
			_weak: !0
		}
	};
};
var resolveVersionSystem = (version, releases) => version._system?.group ? version._system : {
	...version._system,
	...buildDocumentSystem(version._id, releases)
};
var DOCUMENT_STUB_PATHS = [
	"_id",
	"_type",
	"_rev",
	"_createdAt",
	"_updatedAt",
	"_system"
];
function getOrCreateCachedObservable(cache, cacheKey, createObservable) {
	let cached = cache.get(cacheKey);
	if (cached) return cached;
	let observable = createObservable().pipe(finalize(() => cache.delete(cacheKey)), share({
		connector: () => new ReplaySubject(1),
		resetOnRefCountZero: () => timer(1e3)
	}));
	return cache.set(cacheKey, observable), observable;
}
function getOrCreateDocumentVersionsWithSystemObservable(options) {
	let { documentPreviewStore, projectId, dataset, publishedId, releases$ } = options;
	return getOrCreateCachedObservable(withSystemCache, `${projectId}-${dataset}-${publishedId}`, () => combineLatest([getOrCreateDocumentVersionsObservable({
		documentPreviewStore,
		publishedId,
		projectId,
		dataset
	}), releases$]).pipe(map(([result, releases]) => ({
		...result,
		versions: result.versions.map((version) => ({
			...version,
			[DOCUMENT_SYSTEM_FIELD]: resolveVersionSystem(version, releases)
		}))
	}))));
}
/**
* Retrieves an observable that emits document IDs matching the document versions that exist for a specific id
*
* @param options - The options for creating or retrieving the observable.
* options.documentPreviewStore - The store used to observe document IDs.
* options.publishedId - The ID of the published document.
* options.projectId - The project ID.
* options.dataset - The dataset name.
* @returns An observable that emits the document versions.
*
* @hidden
* @internal
*/
function getOrCreateDocumentVersionsObservable(options) {
	let { documentPreviewStore, projectId, dataset, publishedId } = options, cacheKey = `${projectId}-${dataset}-${publishedId}`;
	return getOrCreateCachedObservable(observableCache, cacheKey, () => documentPreviewStore.unstable_observeVersionDocumentIds(publishedId).pipe(swr(cacheKey), map(({ value }) => value), switchMap((documentIds, index) => {
		if (documentIds.length === 0) return of({
			data: [],
			versions: [],
			error: null,
			loading: !1
		});
		let versions$ = combineLatest(documentIds.map((id) => documentPreviewStore.observePaths({ _id: id }, DOCUMENT_STUB_PATHS).pipe(map((versionInfo) => isRecord$4(versionInfo) ? versionInfo : void 0), map((versionInfo) => ({
			_id: id,
			_rev: versionInfo?._rev ?? "",
			_createdAt: versionInfo?._createdAt ?? "",
			_updatedAt: versionInfo?._updatedAt ?? "",
			[DOCUMENT_SYSTEM_FIELD]: versionInfo?.[DOCUMENT_SYSTEM_FIELD]
		}))))).pipe(map((versions) => ({
			data: documentIds,
			versions,
			error: null,
			loading: !1
		})));
		if (index > 0) return versions$;
		let loadingState = {
			data: documentIds,
			versions: [],
			error: null,
			loading: !0
		};
		return versions$.pipe(startWith(loadingState));
	}), catchError((error) => of({
		error,
		data: [],
		versions: [],
		loading: !1
	}))));
}
/** @internal */
function useActiveWorkspace() {
	let value = (0, import_react.useContext)(ActiveWorkspaceMatcherContext);
	if (!value) throw Error("Could not find `ActiveWorkspaceMatcher` context");
	return value;
}
/**
* IMPORTANT!
*
* Based on the `useRecordDocumentHistoryEvent.ts` from `@sanity/sdk`.
* This version has been lightly modified to use Studio's Comlink Store.
*
* TODO: Adopt `@sanity/sdk` when a compatible version is available.
*
* ---
*
* Hook for managing document interaction history in Sanity Studio.
* This hook provides functionality to record document interactions.
*
* @param documentHandle - The document handle containing document ID and type, like `{_id: '123', _type: 'book'}`
* @returns An object containing:
* - `recordEvent` - Function to record document interactions
* - `isConnected` - Boolean indicating if connection to Studio is established
*
* @example
* ```tsx
* function MyDocumentAction(props: DocumentActionProps) {
*   const {documentId, documentType, resourceType, resourceId} = props
*   const {recordEvent, isConnected} = useRecordDocumentHistoryEvent({
*     documentId,
*     documentType,
*     resourceType,
*     resourceId,
*   })
*
*   return (
*     <Button
*       disabled={!isConnected}
*       onClick={() => recordEvent('viewed')}
*       text={'Viewed'}
*     />
*   )
* }
* ```
*
* @internal
*/
function useRecordDocumentHistoryEvent(t0) {
	let $ = (0, import_compiler_runtime.c)(9), { documentId, documentType, resourceType, resourceId, schemaName } = t0, { node } = useComlinkStore();
	if (resourceType !== "studio" && !resourceId) throw Error("resourceId is required for media-library and canvas resources");
	let t1;
	$[0] !== documentId || $[1] !== documentType || $[2] !== node || $[3] !== resourceId || $[4] !== resourceType || $[5] !== schemaName ? (t1 = (eventType) => {
		let run = () => {
			let message = {
				type: "dashboard/v1/events/history",
				data: {
					eventType,
					document: {
						id: documentId,
						type: documentType,
						resource: {
							id: resourceId,
							type: resourceType,
							schemaName
						}
					}
				}
			};
			node?.post?.(message.type, message.data);
		};
		try {
			run();
		} catch (t2) {
			let error = t2;
			throw console.error("Failed to record history event:", error), error;
		}
	}, $[0] = documentId, $[1] = documentType, $[2] = node, $[3] = resourceId, $[4] = resourceType, $[5] = schemaName, $[6] = t1) : t1 = $[6];
	let recordEvent = t1, t2;
	return $[7] === recordEvent ? t2 = $[8] : (t2 = { recordEvent }, $[7] = recordEvent, $[8] = t2), t2;
}
/**
* Decorate operations API methods with Comlink history capture when relevant operations are executed.
*
* Event capture only occurs when Comlink is available.
*
* @internal
*/
function useDocumentOperationWithComlinkHistory(t0) {
	let $ = (0, import_compiler_runtime.c)(28), { api, publishedDocId, docTypeName, version } = t0, { activeWorkspace } = useActiveWorkspace(), t1;
	$[0] !== publishedDocId || $[1] !== version ? (t1 = version ? getVersionId(publishedDocId, version) : publishedDocId, $[0] = publishedDocId, $[1] = version, $[2] = t1) : t1 = $[2];
	let t2;
	$[3] !== activeWorkspace.dataset || $[4] !== activeWorkspace.projectId ? (t2 = [activeWorkspace.projectId, activeWorkspace.dataset], $[3] = activeWorkspace.dataset, $[4] = activeWorkspace.projectId, $[5] = t2) : t2 = $[5];
	let t3 = t2.join("."), t4;
	$[6] !== activeWorkspace.name || $[7] !== docTypeName || $[8] !== t1 || $[9] !== t3 ? (t4 = {
		resourceType: "studio",
		documentId: t1,
		documentType: docTypeName,
		resourceId: t3,
		schemaName: activeWorkspace.name
	}, $[6] = activeWorkspace.name, $[7] = docTypeName, $[8] = t1, $[9] = t3, $[10] = t4) : t4 = $[10];
	let { recordEvent } = useRecordDocumentHistoryEvent(t4), renderingContextStore = useRenderingContextStore(), capabilities = useSyncObservable(renderingContextStore.capabilities), [hasRecordedEdit, setHasRecordedEdit] = (0, import_react.useState)(!1), t5;
	$[11] === hasRecordedEdit ? t5 = $[12] : (t5 = (next) => {
		hasRecordedEdit || (next(), setHasRecordedEdit(!0));
	}, $[11] = hasRecordedEdit, $[12] = t5);
	let preRecordPatch = t5;
	capabilities?.comlink;
	let t6 = capabilities?.comlink, t7;
	$[13] !== recordEvent || $[14] !== t6 ? (t7 = {
		hasComlink: t6,
		recordEvent
	}, $[13] = recordEvent, $[14] = t6, $[15] = t7) : t7 = $[15];
	let comlinkContext = t7, t8;
	$[16] === comlinkContext ? t8 = $[17] : (t8 = withComlinkEvent({
		...comlinkContext,
		operationName: "delete",
		comlinkEventType: "deleted"
	}), $[16] = comlinkContext, $[17] = t8);
	let withComlinkDelete = t8, t9;
	$[18] === comlinkContext ? t9 = $[19] : (t9 = withComlinkEvent({
		...comlinkContext,
		operationName: "del",
		comlinkEventType: "deleted"
	}), $[18] = comlinkContext, $[19] = t9);
	let withComlinkDel = t9, t10;
	$[20] !== comlinkContext || $[21] !== preRecordPatch ? (t10 = withComlinkEvent({
		...comlinkContext,
		operationName: "patch",
		comlinkEventType: "edited",
		preRecordEvent: preRecordPatch
	}), $[20] = comlinkContext, $[21] = preRecordPatch, $[22] = t10) : t10 = $[22];
	let withComlinkPatch = t10, t11;
	return $[23] !== api || $[24] !== withComlinkDel || $[25] !== withComlinkDelete || $[26] !== withComlinkPatch ? (t11 = withComlinkDelete(withComlinkDel(withComlinkPatch(api))), $[23] = api, $[24] = withComlinkDel, $[25] = withComlinkDelete, $[26] = withComlinkPatch, $[27] = t11) : t11 = $[27], t11;
}
/**
* Decorate the provided API method with Comlink history capture.
*/
function withComlinkEvent({ operationName, comlinkEventType, hasComlink, recordEvent, preRecordEvent = (next) => next() }) {
	return function(api) {
		return hasComlink ? {
			...api,
			[operationName]: {
				...api[operationName],
				execute: (...args) => {
					let next = () => recordEvent(comlinkEventType);
					preRecordEvent(next), api[operationName].execute(...args);
				}
			}
		} : api;
	};
}
/**
* Returns a referentially stable {@link DocumentPairTarget} for the document pair APIs.
*
* Callers commonly build the target inline on every render (e.g. via `getPairTarget(...)`), so
* keying a memo on the target object directly would recreate it every time — and resubscribing
* `pair.editOperations` re-checks out the pair. This hook destructures the target into its
* primitives and memoizes the object on them, so the returned reference only changes when the
* target's contents change.
*
* A bare string is back-compat shorthand for a plain version target (its scope id, i.e. the
* `<scopeId>` segment of `versions.<scopeId>.<groupId>`); `undefined` means the base
* draft/published pair.
*
* @internal
*/
function useMemoizedDocumentPairTarget(version) {
	let $ = (0, import_compiler_runtime.c)(11), t0;
	$[0] === version ? t0 = $[1] : (t0 = normalizeDocumentPairTarget(version), $[0] = version, $[1] = t0);
	let target = t0, kind = target?.kind, scopeId = target?.kind === "version" || target?.kind === "variant" ? target.scopeId : void 0, variantId = target?.kind === "variant" || target?.kind === "target-missing" ? target.variantId : void 0, allowCreate = target?.kind === "variant" ? target.allowCreate : void 0, t1;
	bb0: switch (kind) {
		case "version": {
			let t2;
			$[2] === scopeId ? t2 = $[3] : (t2 = {
				kind: "version",
				scopeId
			}, $[2] = scopeId, $[3] = t2), t1 = t2;
			break bb0;
		}
		case "variant": {
			let t2;
			$[4] !== allowCreate || $[5] !== scopeId || $[6] !== variantId ? (t2 = allowCreate === void 0 ? {
				kind: "variant",
				scopeId,
				variantId
			} : {
				kind: "variant",
				scopeId,
				variantId,
				allowCreate
			}, $[4] = allowCreate, $[5] = scopeId, $[6] = variantId, $[7] = t2) : t2 = $[7], t1 = t2;
			break bb0;
		}
		case "target-missing": {
			let t2;
			$[8] === variantId ? t2 = $[9] : (t2 = variantId === void 0 ? { kind: "target-missing" } : {
				kind: "target-missing",
				variantId
			}, $[8] = variantId, $[9] = t2), t1 = t2;
			break bb0;
		}
		case "unresolved": {
			let t2;
			$[10] === Symbol.for("react.memo_cache_sentinel") ? (t2 = { kind: "unresolved" }, $[10] = t2) : t2 = $[10], t1 = t2;
			break bb0;
		}
		default: t1 = void 0;
	}
	return t1;
}
/**
* @internal
* `version` accepts either a plain version name (release/bundle) or a {@link DocumentPairTarget}
* declaring the resolved target of the selected perspective/variant. With the guarded target
* kinds (`unresolved`, `target-missing`) the returned operations are disabled and throw if
* executed, instead of silently operating on the base draft/published pair.
*/
function useDocumentOperation(publishedDocId, docTypeName, version) {
	let $ = (0, import_compiler_runtime.c)(9), documentStore = useDocumentStore(), target = useMemoizedDocumentPairTarget(version), t0;
	$[0] !== docTypeName || $[1] !== documentStore.pair || $[2] !== publishedDocId || $[3] !== target ? (t0 = documentStore.pair.editOperations(publishedDocId, docTypeName, target), $[0] = docTypeName, $[1] = documentStore.pair, $[2] = publishedDocId, $[3] = target, $[4] = t0) : t0 = $[4];
	let api = useSyncObservable(t0), t1;
	return $[5] !== api || $[6] !== docTypeName || $[7] !== publishedDocId ? (t1 = {
		api,
		docTypeName,
		publishedDocId
	}, $[5] = api, $[6] = docTypeName, $[7] = publishedDocId, $[8] = t1) : t1 = $[8], useDocumentOperationWithComlinkHistory(t1);
}
/**
* Finds the document version whose _system metadata matches the selected bundle and variant.
*
* `bundle` must be the canonical bundle id (`published`, `drafts` or a release id), matching
* `_system.bundleId` exactly. For example, `bundle: 'published'` and `variant: undefined` matches
* a version with no `bundleId` and `variant: null`.
*
* @internal
*/
function getTargetDocument({ bundle, documentVersions, variant }) {
	return documentVersions.find((version) => {
		let inBundle = bundle === "published" ? !version._system.bundleId : version._system.bundleId === bundle, inVariant = variant ? version._system.variant?._ref === variant : !version._system.variant;
		return inBundle && inVariant;
	});
}
/**
* Finds the variant-of-published sibling for a variant: the version stub carrying the same
* `_system.variant` reference but no `_system.bundleId` (i.e. the published variant document).
*
* This is the document that publish-state gating must read for variant targets — the base
* `published` document says nothing about whether the *variant* is published. `undefined` means
* the variant has never been published.
*
* @internal
*/
function getVariantPublishedSibling({ documentVersions, variant }) {
	return getTargetDocument({
		bundle: "published",
		variant,
		documentVersions
	});
}
function createVariantsSet(variants) {
	return (variants ?? []).reduce((acc, variant) => (acc.set(variant._id, variant), acc), /* @__PURE__ */ new Map());
}
function variantStoreReducer(state, action) {
	switch (action.type) {
		case "LOADING_STATE_CHANGED": return {
			...state,
			state: action.payload.error ? "error" : action.payload.loading ? "loading" : "loaded",
			error: action.payload.error
		};
		case "VARIANTS_SET": {
			let variantsById = createVariantsSet(action.payload);
			return {
				...state,
				variants: variantsById
			};
		}
		case "FETCH_SUCCEEDED": {
			let variantsById = createVariantsSet(action.payload);
			return {
				...state,
				variants: variantsById,
				state: "loaded",
				error: void 0
			};
		}
		case "VARIANT_DELETED": {
			let { id } = action.payload, restVariants = new Map(state.variants);
			return restVariants.delete(id), {
				...state,
				variants: restVariants
			};
		}
		default: return state;
	}
}
var QUERY = `*[${`_type=="${VARIANT_DOCUMENT_TYPE}" && _id in path("${VARIANT_DOCUMENTS_PATH}.*")`}] {
  _id,
  _type,
  _rev,
  _createdAt,
  _updatedAt,
  name,
  conditions,
  "priority": coalesce(priority, 0),
  metadata,
} | order(_createdAt desc)`;
var INITIAL_STATE = {
	variants: /* @__PURE__ */ new Map(),
	state: "initialising"
};
/**
* The variants store is initialised lazily when first subscribed to. Upon subscription, it will
* fetch a list of variants and create a listener to keep the locally held state fresh.
*
* The store is not disposed of when all subscriptions are closed. After it has been initialised,
* it will keep listening for the duration of the app's lifecycle. Subsequent subscriptions will be
* given the latest state upon subscription.
*/
function createVariantsStore(context) {
	let { client, enabled } = context;
	if (!enabled) return {
		state$: of({
			variants: /* @__PURE__ */ new Map(),
			state: "loaded"
		}),
		dispatch: () => {}
	};
	let dispatch$ = new Subject();
	function dispatch(action) {
		dispatch$.next(action);
	}
	let listFetch$ = of({
		type: "LOADING_STATE_CHANGED",
		payload: {
			loading: !0,
			error: void 0
		}
	}).pipe(concatWith(listenQuery(client, QUERY, {}, { tag: "variants.listen" }).pipe(map((variants) => ({
		type: "FETCH_SUCCEEDED",
		payload: variants
	})))), catchError((error) => of({
		type: "LOADING_STATE_CHANGED",
		payload: {
			loading: !1,
			error
		}
	})));
	return {
		state$: merge(listFetch$, dispatch$).pipe(scan((state, action) => variantStoreReducer(state, action), INITIAL_STATE), startWith(INITIAL_STATE), shareReplay(1)),
		dispatch
	};
}
/** @internal */
function useVariantsStore() {
	let $ = (0, import_compiler_runtime.c)(5), workspace = useWorkspace(), variantsEnabled = !!workspace.beta?.variants?.enabled, resourceCache = useResourceCache(), studioClient = useClient(VARIANTS_STUDIO_CLIENT_OPTIONS), variantStore;
	return $[0] !== resourceCache || $[1] !== studioClient || $[2] !== variantsEnabled || $[3] !== workspace ? (variantStore = resourceCache.get({
		dependencies: [workspace],
		namespace: "VariantsStore"
	}) || createVariantsStore({
		client: studioClient,
		enabled: variantsEnabled
	}), resourceCache.set({
		dependencies: [workspace],
		namespace: "VariantsStore",
		value: variantStore
	}), $[0] = resourceCache, $[1] = studioClient, $[2] = variantsEnabled, $[3] = workspace, $[4] = variantStore) : variantStore = $[4], variantStore;
}
/**
* Gets all variants.
* @internal
*/
function useAllVariants() {
	let $ = (0, import_compiler_runtime.c)(8), { state$ } = useVariantsStore(), { variants, error, state } = useSyncObservable(state$), t0;
	$[0] === variants ? t0 = $[1] : (t0 = Array.from(variants.values()), $[0] = variants, $[1] = t0);
	let t1;
	$[2] === Symbol.for("react.memo_cache_sentinel") ? (t1 = ["loading", "initialising"], $[2] = t1) : t1 = $[2];
	let t2 = t1.includes(state), t3;
	return $[3] !== error || $[4] !== t0 || $[5] !== t2 || $[6] !== variants ? (t3 = {
		data: t0,
		byId: variants,
		error,
		loading: t2
	}, $[3] = error, $[4] = t0, $[5] = t2, $[6] = variants, $[7] = t3) : t3 = $[7], t3;
}
var RESOLVING = { status: "resolving" };
/**
* Resolves the creatable draft variant for a missing target: only the drafts bundle qualifies
* (release-scoped variants have no advertised draft refs yet, and the published bundle's missing
* target *is* the sibling), and only when the variant-of-published sibling advertises
* `_system.draft`. The id is server-advertised, never computed — this is what keeps the "never
* guess a variant scope id client-side" invariant intact.
*/
function getCreatableTarget(bundle, publishedSibling) {
	if (bundle !== "drafts" || !publishedSibling) return;
	let draftId = publishedSibling._system.draft?._ref;
	if (!draftId) return;
	let scopeId = getVersionFromId(draftId);
	if (scopeId) return {
		id: draftId,
		scopeId
	};
}
/**
* Returns the scope id to thread into version-aware hooks (`useEditState`,
* `useDocumentOperation`, etc.) for a resolved target, or `undefined` when the target is not
* ready or the base draft/published pair applies.
*
* A `variant-missing` state with a {@link CreatableTargetDocument} yields the creatable draft's
* scope id: the pair is checked out at the advertised id from the beginning, so typing (and every
* version-aware read: edit state, sync state, connection) addresses the document being created —
* never the base pair, and never the published sibling.
*
* @internal
* @beta
*/
function getTargetScopeId(state) {
	if (state.status === "ready") return state.scopeId;
	if (state.status === "variant-missing") return state.creatableTarget?.scopeId;
}
/**
* The creatable draft variant of a `variant-missing` state, or `undefined` for every other
* state. Present when the missing target can be created by typing at a server-advertised id
* (see {@link CreatableTargetDocument}); consumers use it to exempt the state from the
* read-only/banner/footer gating that otherwise applies to unresolved variant targets.
*
* @internal
* @beta
*/
function getCreatableVariantTarget(state) {
	return state.status === "variant-missing" ? state.creatableTarget : void 0;
}
/**
* Maps a {@link TargetDocumentState} to the `target` parameter of `useDocumentOperation` /
* `pair.editOperations`, preserving the guarded states: `resolving` yields `unresolved`
* (operations stay `NOT_READY`), and `variant-missing` / `variant-definition-document-not-found`
* yield `target-missing` (operations disabled with `TARGET_NOT_FOUND`). Both throw if executed,
* so operations can never silently fall back to the base draft/published pair.
*
* A `variant-missing` state with a {@link CreatableTargetDocument} yields a `variant` target with
* `allowCreate`: the store keeps `patch`/`commit` enabled so typing creates the draft variant at
* the advertised id, while publish/unpublish/discard stay disabled until the document exists.
*
* @internal
* @beta
*/
function getPairTarget(state) {
	switch (state.status) {
		case "resolving": return { kind: "unresolved" };
		case "variant-missing": return state.creatableTarget ? {
			kind: "variant",
			scopeId: state.creatableTarget.scopeId,
			variantId: state.variant._id,
			allowCreate: !0
		} : {
			kind: "target-missing",
			variantId: state.variant._id
		};
		case "variant-definition-document-not-found": return { kind: "target-missing" };
		case "ready": return state.variant ? state.scopeId ? {
			kind: "variant",
			scopeId: state.scopeId,
			variantId: state.variant._id
		} : {
			kind: "target-missing",
			variantId: state.variant._id
		} : state.scopeId;
		default: return;
	}
}
/**
* Pure resolution logic for {@link useTargetDocumentState}, extracted for testability.
*
* @internal
*/
function getTargetDocumentState(options) {
	let { bundle, selectedVariant, selectedVariantName, variantsLoading, versions, versionsLoading } = options;
	if (!selectedVariantName) {
		if (versionsLoading) return RESOLVING;
		let targetDocument = getTargetDocument({
			bundle,
			variant: void 0,
			documentVersions: versions
		});
		return {
			status: "ready",
			targetDocument,
			scopeId: targetDocument?._system.scopeId ?? void 0,
			variant: void 0,
			publishedSibling: void 0
		};
	}
	if (variantsLoading) return RESOLVING;
	if (!selectedVariant) return {
		status: "variant-definition-document-not-found",
		requestedVariantName: selectedVariantName
	};
	if (versionsLoading) return RESOLVING;
	let targetDocument = getTargetDocument({
		bundle,
		variant: selectedVariant._id,
		documentVersions: versions
	}), publishedSibling = getVariantPublishedSibling({
		variant: selectedVariant._id,
		documentVersions: versions
	});
	return targetDocument ? {
		status: "ready",
		targetDocument,
		scopeId: targetDocument._system.scopeId ?? void 0,
		variant: selectedVariant,
		publishedSibling
	} : {
		status: "variant-missing",
		variant: selectedVariant,
		bundle,
		publishedSibling,
		creatableTarget: getCreatableTarget(bundle, publishedSibling)
	};
}
/**
* Resolves the document targeted by the selected perspective and variant for a document group,
* as an explicit {@link TargetDocumentState}.
*
* Unlike a plain "target document or undefined" lookup, the returned state distinguishes the
* in-flight lookups (`resolving`), the definitive absence of a variant-scoped version
* (`variant-missing`), and an invalid variant selection
* (`variant-definition-document-not-found`) from the legitimate base draft/published fallback
* (`ready` without a target document). Consumers must switch on `status` instead of treating
* `undefined` as "no variant".
*
* @internal
* @beta
*/
function useTargetDocumentState(documentGroupId) {
	let $ = (0, import_compiler_runtime.c)(9), t0;
	$[0] === documentGroupId ? t0 = $[1] : (t0 = { documentId: documentGroupId }, $[0] = documentGroupId, $[1] = t0);
	let { versions, loading: versionsLoading } = useDocumentVersions(t0), { bundle, selectedVariant, selectedVariantName } = usePerspective(), { loading: variantsLoading } = useAllVariants(), t1;
	return $[2] !== bundle || $[3] !== selectedVariant || $[4] !== selectedVariantName || $[5] !== variantsLoading || $[6] !== versions || $[7] !== versionsLoading ? (t1 = getTargetDocumentState({
		bundle,
		selectedVariant,
		selectedVariantName,
		variantsLoading,
		versions,
		versionsLoading
	}), $[2] = bundle, $[3] = selectedVariant, $[4] = selectedVariantName, $[5] = variantsLoading, $[6] = versions, $[7] = versionsLoading, $[8] = t1) : t1 = $[8], t1;
}
function shareLatestWithRefCount() {
	return shareReplay({
		bufferSize: 1,
		refCount: !0
	});
}
function getSchemaType(schema, typeName) {
	let type = schema.get(typeName);
	if (!type) throw Error(`No such schema type: ${typeName}`);
	return type;
}
function getPairPermissions({ grantsStore, permission, draft, version, published, liveEdit }) {
	let effectiveVersion = version || draft || published, effectiveVersionType = effectiveVersion === version ? version : effectiveVersion === draft ? "draft" : "published", { checkDocumentPermission } = grantsStore;
	switch (permission) {
		case "delete": return liveEdit ? [["delete published document (live-edit)", checkDocumentPermission("update", published)]] : [["delete draft document", checkDocumentPermission("update", draft)], ["delete published document", checkDocumentPermission("update", published)]];
		case "discardDraft": return liveEdit ? [] : [["delete draft document", checkDocumentPermission("update", draft)]];
		case "discardVersion": return liveEdit ? [] : [["delete version", checkDocumentPermission("update", version || null)]];
		case "publish": return liveEdit ? [] : [
			["update published document at its current state", checkDocumentPermission("update", published)],
			["delete draft document", checkDocumentPermission("update", draft)],
			["create published document from draft", checkDocumentPermission("create", draft && {
				...draft,
				_id: getPublishedId(draft._id)
			})]
		];
		case "unpublish": return liveEdit ? [] : [
			["update draft document at its current state", checkDocumentPermission("create", draft)],
			["delete published document", checkDocumentPermission("update", published)],
			["create draft document from published version", checkDocumentPermission("create", published && {
				...published,
				_id: getDraftId(published._id)
			})]
		];
		case "update": return liveEdit ? [["update published document (live-edit)", checkDocumentPermission("update", published)]] : [[`update ${effectiveVersionType} document`, checkDocumentPermission("update", effectiveVersion)]];
		case "duplicate": return liveEdit ? [["create new published document from existing document (live-edit)", checkDocumentPermission("create", {
			...published,
			_id: "dummy-id"
		})]] : [[`create new draft document from existing ${effectiveVersionType} document`, checkDocumentPermission("create", {
			...effectiveVersion,
			_id: getDraftId("dummy-id")
		})]];
		default: throw Error(`Could not match permission: ${permission}`);
	}
}
/**
* The observable version of `useDocumentPairPermissions`
*
* @see useDocumentPairPermissions
*
* @internal
*/
function getDocumentPairPermissionsUncached({ client, grantsStore, schema, id, permission, type, version: v, pairListenerOptions }) {
	if (type === "*") return of({
		granted: !1,
		reason: "Type specified was `*`"
	});
	let liveEdit = !!getSchemaType(schema, type).liveEdit;
	return snapshotPair(client, getIdPair(id, { version: v }), type, void 0, pairListenerOptions).pipe(switchMap((pair) => combineLatest([
		pair.draft.snapshots$,
		pair.published.snapshots$,
		pair.version?.snapshots$ || of(null)
	]).pipe(map(([draft, published, version]) => ({
		draft,
		published,
		version
	})))), switchMap(({ draft, published, version }) => {
		let pairPermissions = getPairPermissions({
			grantsStore,
			permission,
			draft,
			version,
			published,
			liveEdit
		}).map(([label, observable]) => observable.pipe(map(({ granted, reason }) => ({
			granted,
			reason: granted ? "" : `not allowed to ${label}: ${reason}`,
			label,
			permission
		}))));
		return pairPermissions.length ? combineLatest(pairPermissions).pipe(map((permissionResults) => {
			let granted = permissionResults.every((permissionResult) => permissionResult.granted);
			return {
				granted,
				reason: granted ? "" : `Unable to ${permission}:\n\t${permissionResults.filter((permissionResult) => !permissionResult.granted).map((permissionResult) => permissionResult.reason).join("\n	")}`
			};
		})) : of({
			granted: !0,
			reason: ""
		});
	}));
}
var getDocumentPairPermissions = memoize$1((options) => getDocumentPairPermissionsUncached(options).pipe(shareLatestWithRefCount()), ({ client, schema, id, type, version, permission, userId }) => {
	let { dataset = "", projectId = "" } = client.config(), liveEdit = type !== "*" && !!schema.get(type)?.liveEdit;
	return [
		dataset,
		projectId,
		getPublishedId(id),
		version ?? "",
		userId ?? "",
		type,
		permission,
		liveEdit
	].join("-");
});
var useDocumentPairPermissionsFromHookFactory = createHookFromObservableFactory(getDocumentPairPermissions);
/** @internal */
function useDocumentPairPermissions(t0) {
	let $ = (0, import_compiler_runtime.c)(10), { id, type, version, permission, client: overrideClient, schema: overrideSchema, grantsStore: overrideGrantsStore, pairListenerOptions } = t0, defaultClient = useClient(DEFAULT_STUDIO_CLIENT_OPTIONS), defaultSchema = useSchema(), defaultGrantsStore = useGrantsStore(), currentUser = useCurrentUser(), client = overrideClient || defaultClient, schema = overrideSchema || defaultSchema, grantsStore = overrideGrantsStore || defaultGrantsStore, userId = currentUser?.id, t1;
	return $[0] !== client || $[1] !== grantsStore || $[2] !== id || $[3] !== pairListenerOptions || $[4] !== permission || $[5] !== schema || $[6] !== type || $[7] !== userId || $[8] !== version ? (t1 = {
		client,
		schema,
		grantsStore,
		id,
		permission,
		type,
		pairListenerOptions,
		version,
		userId
	}, $[0] = client, $[1] = grantsStore, $[2] = id, $[3] = pairListenerOptions, $[4] = permission, $[5] = schema, $[6] = type, $[7] = userId, $[8] = version, $[9] = t1) : t1 = $[9], useDocumentPairPermissionsFromHookFactory(t1);
}
function getReleaseDocumentIdFromReleaseId(name) {
	return `${RELEASE_DOCUMENTS_PATH}.${name}`;
}
/**
* @beta
* @hidden
*/
function useReleasesUpsell() {
	return (0, import_react.useContext)(ReleasesUpsellContext) || FALLBACK_CONTEXT_VALUE;
}
var FALLBACK_CONTEXT_VALUE = {
	upsellDialogOpen: !1,
	mode: "default",
	onReleaseLimitReached: () => null,
	guardWithReleaseLimitUpsell: async () => void 0,
	handleOpenDialog: () => null,
	upsellData: null,
	telemetryLogs: {
		dialogSecondaryClicked: () => null,
		dialogPrimaryClicked: () => null,
		panelViewed: () => null,
		panelDismissed: () => null,
		panelPrimaryClicked: () => null,
		panelSecondaryClicked: () => null
	}
};
var isErrorWithDetails = (error) => typeof error == "object" && !!error && "details" in error && typeof error.details == "object";
var isReleaseLimitError = (error) => isErrorWithDetails(error) && error.details?.type === "releaseLimitExceededError";
function createReleaseOperationsStore(options) {
	let { client, onReleaseLimitReached } = options, handleReleaseLimitError = (action, opts) => action.catch((error) => {
		throw !opts?.dryRun && isReleaseLimitError(error) && onReleaseLimitReached(error.details.limit || 0), error;
	}), handleCreateRelease = (release, opts) => handleReleaseLimitError(client.releases.create({
		releaseId: getReleaseIdFromReleaseDocumentId(release._id),
		metadata: release.metadata
	}, opts), opts), handleUpdateRelease = async (release, opts) => {
		let releaseId = getReleaseIdFromReleaseDocumentId(release._id), unsetKeys = Object.entries(release).filter(([_, value]) => value === void 0).map(([key]) => `metadata.${key}`);
		return handleReleaseLimitError(client.releases.edit({
			releaseId,
			patch: {
				set: { metadata: release.metadata },
				unset: unsetKeys
			}
		}, opts), opts);
	}, handlePublishRelease = (releaseId, opts) => handleReleaseLimitError(client.releases.publish({ releaseId: getReleaseIdFromReleaseDocumentId(releaseId) }, opts), opts), handleDiscardDrafts = async (documentIds, opts) => {
		let draftIds = [...new Set(documentIds.map((documentId) => getDraftId(documentId)))];
		if (draftIds.length === 0) return;
		let existingDraftIds = await client.fetch("*[_id in $draftIds]._id", { draftIds }, {
			perspective: "raw",
			tag: "release.discard-drafts"
		});
		if (existingDraftIds.length === 0) return;
		let discardDraftActions = existingDraftIds.map((draftId) => ({
			actionType: "sanity.action.document.version.discard",
			versionId: draftId
		}));
		return client.action(discardDraftActions, {
			tag: "release.discard-drafts",
			...opts
		});
	}, handleScheduleRelease = (releaseId, publishAt, opts) => handleReleaseLimitError(client.releases.schedule({
		releaseId: getReleaseIdFromReleaseDocumentId(releaseId),
		publishAt: publishAt.toISOString()
	}, opts), opts), handleUnscheduleRelease = (releaseId, opts) => handleReleaseLimitError(client.releases.unschedule({ releaseId: getReleaseIdFromReleaseDocumentId(releaseId) }, opts), opts), handleArchiveRelease = (releaseId, opts) => client.releases.archive({ releaseId: getReleaseIdFromReleaseDocumentId(releaseId) }, opts), handleUnarchiveRelease = (releaseId, opts) => handleReleaseLimitError(client.releases.unarchive({ releaseId: getReleaseIdFromReleaseDocumentId(releaseId) }, opts), opts), handleDeleteRelease = (releaseId, opts) => client.releases.delete({ releaseId: getReleaseIdFromReleaseDocumentId(releaseId) }, opts), handleCreateVersion = async (releaseId, documentId, opts) => {
		let document = await client.getDocument(documentId);
		if (!document) throw Error(`Document with id ${documentId} not found`);
		return client.createVersion({
			baseId: documentId,
			ifBaseRevisionId: document?._rev,
			publishedId: getPublishedId(documentId),
			releaseId
		}, opts);
	}, handleDiscardVersion = (releaseId, documentId, opts) => client.discardVersion({
		releaseId,
		publishedId: getPublishedId(documentId)
	}, !1, opts), handleUnpublishVersion = (documentId, opts) => {
		let releaseId = getVersionFromId(documentId);
		if (!releaseId) throw Error(`Release ID not found for document ${documentId}`);
		return client.unpublishVersion({
			releaseId,
			publishedId: getPublishedId(documentId)
		}, opts);
	}, handleRevertRelease = async (revertReleaseId, releaseDocuments, releaseMetadata, revertType) => {
		await handleCreateRelease({
			_id: revertReleaseId,
			metadata: {
				title: releaseMetadata.title,
				description: releaseMetadata.description,
				releaseType: "asap",
				cardinality: "many"
			}
		});
		let versionId = getReleaseIdFromReleaseDocumentId(revertReleaseId), newVersionDocumentActions = releaseDocuments.map((releaseDocument) => ({
			actionType: "sanity.action.document.version.create",
			document: {
				...releaseDocument,
				_id: getVersionId(releaseDocument._id, versionId)
			},
			publishedId: getPublishedId(releaseDocument._id)
		}));
		await client.action(newVersionDocumentActions), revertType === "immediate" && await handlePublishRelease(revertReleaseId);
	}, handleDuplicateRelease = async (releaseDocumentId, releaseMetadata, releaseDocuments) => {
		if (await handleCreateRelease({
			_id: releaseDocumentId,
			metadata: releaseMetadata
		}), releaseDocuments) {
			let versionId = getReleaseIdFromReleaseDocumentId(releaseDocumentId), duplicateVersionDocumentActions = releaseDocuments.map((releaseDocument) => ({
				actionType: "sanity.action.document.version.create",
				baseId: releaseDocument._id,
				publishedId: getPublishedId(releaseDocument._id),
				versionId: getVersionId(releaseDocument._id, versionId)
			}));
			await client.action(duplicateVersionDocumentActions, { tag: "duplicate-release.create-members" });
		}
	}, handleRevertUnpublishVersion = async (documentId, opts) => await client.action({
		actionType: "sanity.action.document.edit",
		draftId: documentId,
		publishedId: getPublishedId(documentId),
		patch: { unset: ["_system.delete"] }
	}, opts);
	return {
		archive: handleArchiveRelease,
		unarchive: handleUnarchiveRelease,
		schedule: handleScheduleRelease,
		unschedule: handleUnscheduleRelease,
		createRelease: handleCreateRelease,
		updateRelease: handleUpdateRelease,
		publishRelease: handlePublishRelease,
		discardDrafts: handleDiscardDrafts,
		deleteRelease: handleDeleteRelease,
		revertRelease: handleRevertRelease,
		duplicateRelease: handleDuplicateRelease,
		createVersion: handleCreateVersion,
		discardVersion: handleDiscardVersion,
		unpublishVersion: handleUnpublishVersion,
		revertUnpublishVersion: handleRevertUnpublishVersion
	};
}
/**
* @internal
*/
function useReleaseOperations() {
	let $ = (0, import_compiler_runtime.c)(3), studioClient = useClient(RELEASES_STUDIO_CLIENT_OPTIONS), { onReleaseLimitReached } = useReleasesUpsell(), t0;
	return $[0] !== onReleaseLimitReached || $[1] !== studioClient ? (t0 = createReleaseOperationsStore({
		client: studioClient,
		onReleaseLimitReached
	}), $[0] = onReleaseLimitReached, $[1] = studioClient, $[2] = t0) : t0 = $[2], t0;
}
/**
* Gets all releases including archived and published releases
* @internal
*/
function useAllReleases() {
	let $ = (0, import_compiler_runtime.c)(8), { state$ } = useReleasesStore(), { releases, error, state } = useSyncObservable(state$), t0;
	$[0] === releases ? t0 = $[1] : (t0 = sortReleases(Array.from(releases.values())), $[0] = releases, $[1] = t0);
	let t1;
	$[2] === Symbol.for("react.memo_cache_sentinel") ? (t1 = ["loading", "initialising"], $[2] = t1) : t1 = $[2];
	let t2 = t1.includes(state), t3;
	return $[3] !== error || $[4] !== releases || $[5] !== t0 || $[6] !== t2 ? (t3 = {
		data: t0,
		map: releases,
		error,
		loading: t2
	}, $[3] = error, $[4] = releases, $[5] = t0, $[6] = t2, $[7] = t3) : t3 = $[7], t3;
}
/**
* Schedule the provided callback using `scheduler.yield`, if it's available.
* This allows long-running work to be broken up so the browser stays responsive.
* @see https://developer.mozilla.org/en-US/docs/Web/API/Scheduler/yield
* Otherwise, call it immediately.
*/
function schedulerYield(promise) {
	return "scheduler" in window && typeof window.scheduler?.yield == "function" ? window.scheduler.yield() : promise();
}
var bundleDocumentsCache = Object.create(null);
var buildBundleDocumentsObservable = ({ schema, documentPreviewStore, i18n, getClient, currentUser, groqFilter, params, skipValidation }) => {
	let createValidationObservable = (ctx, document) => skipValidation?.(document) ? of({
		isValidating: !1,
		validation: [],
		revision: document._rev,
		hasError: !1
	}) : from(schedulerYield(() => Promise.resolve())).pipe(switchMap(() => validateDocumentWithReferences(ctx, of(document), !1).pipe(map((validationStatus) => ({
		...validationStatus,
		hasError: validationStatus.validation.some((marker) => isValidationErrorMarker(marker))
	}))))), processDocument = (id) => {
		let ctx = {
			observeDocument: documentPreviewStore.unstable_observeDocument,
			observeDocumentPairAvailability: documentPreviewStore.unstable_observeDocumentPairAvailability,
			i18n,
			getClient,
			schema,
			currentUser
		}, document$ = documentPreviewStore.unstable_observeDocument(id, { apiVersion: RELEASES_STUDIO_CLIENT_OPTIONS.apiVersion }).pipe(filter(Boolean), switchMap((doc) => documentPreviewStore.unstable_observeDocumentPairAvailability(id).pipe(map((availability) => ({
			...doc,
			publishedDocumentExists: availability.published.available,
			draftDocumentExists: availability.draft.available
		}))))), validation$ = document$.pipe(switchMap((document) => createValidationObservable(ctx, document)));
		return combineLatest([document$, validation$]).pipe(map(([document, validation]) => ({
			document,
			validation,
			memoKey: v4()
		})));
	}, processDocumentIdsGroup = (documentIdsGroup, groupIndex) => {
		let batchDelay = groupIndex === 0 ? 0 : 100;
		return of(documentIdsGroup).pipe(delay(batchDelay), mergeMapArray((id) => processDocument(id)));
	};
	return documentPreviewStore.unstable_observeDocumentIdSet(groqFilter, params, { apiVersion: RELEASES_STUDIO_CLIENT_OPTIONS.apiVersion }).pipe(map((state) => state.documentIds || []), switchMap((documentIds) => {
		if (documentIds.length === 0) return of([]);
		let documentIdsGroups = [];
		for (let i = 0; i < documentIds.length; i += 5) documentIdsGroups.push(documentIds.slice(i, i + 5));
		return combineLatest(documentIdsGroups.map((batch, batchIndex) => processDocumentIdsGroup(batch, batchIndex).pipe(delay(batchIndex * 100)))).pipe(map((groupResults) => groupResults.flat()));
	}), map((results) => ({
		loading: !1,
		results,
		error: null
	})), catchError((error) => of({
		loading: !1,
		results: [],
		error
	})));
};
var getBundleDocumentsObservable = ({ cacheKey, ...options }) => (bundleDocumentsCache[cacheKey] || (bundleDocumentsCache[cacheKey] = buildBundleDocumentsObservable(options).pipe(finalize(() => {
	delete bundleDocumentsCache[cacheKey];
}), shareReplay(1))), bundleDocumentsCache[cacheKey]);
var BUNDLE_DOCUMENTS_EMPTY = {
	loading: !1,
	results: [],
	error: null
};
var BUNDLE_DOCUMENTS_LOADING = {
	loading: !0,
	results: [],
	error: null
};
/**
* Generic hook to fetch the documents matching a GROQ filter, along with their validation and
* published availability. Used by release- and variant-specific hooks.
*
* @internal
*/
function useBundleDocuments(options) {
	let $ = (0, import_compiler_runtime.c)(11), { groqFilter, params, cacheKey, skipValidation, enabled: t0 } = options, enabled = t0 === void 0 || t0, documentPreviewStore = useDocumentPreviewStore(), { getClient, i18n, currentUser } = useSource(), schema = useSchema(), t1;
	return $[0] !== cacheKey || $[1] !== currentUser || $[2] !== documentPreviewStore || $[3] !== enabled || $[4] !== getClient || $[5] !== groqFilter || $[6] !== i18n || $[7] !== params || $[8] !== schema || $[9] !== skipValidation ? (t1 = enabled ? getBundleDocumentsObservable({
		schema,
		documentPreviewStore,
		getClient,
		i18n,
		currentUser,
		groqFilter,
		params,
		cacheKey,
		skipValidation
	}) : of(BUNDLE_DOCUMENTS_EMPTY), $[0] = cacheKey, $[1] = currentUser, $[2] = documentPreviewStore, $[3] = enabled, $[4] = getClient, $[5] = groqFilter, $[6] = i18n, $[7] = params, $[8] = schema, $[9] = skipValidation, $[10] = t1) : t1 = $[10], useObservable(t1, enabled ? BUNDLE_DOCUMENTS_LOADING : BUNDLE_DOCUMENTS_EMPTY);
}
//#endregion
//#region node_modules/.pnpm/@sanity+ui@4.0.3_react-dom@19.2.8_react@19.2.8__react@19.2.8_styled-components@6.5.3_re_84e50c1ed67c9b277dbd65efc234bba4/node_modules/@sanity/ui/dist/autocomplete.js
/**
* @internal
*/
var StyledAutocomplete = gt.div.withConfig({
	displayName: "StyledAutocomplete",
	componentId: "sc-gz724-0"
})`line-height:0;`;
var ListBox = gt(Box).withConfig({
	displayName: "ListBox",
	componentId: "sc-gz724-1"
})`& > ul{list-style:none;padding:0;margin:0;}`;
function AutocompleteOption(props) {
	let $ = (0, import_compiler_runtime.c)(11), { children, id, onSelect, selected, value } = props, t0;
	$[0] !== onSelect || $[1] !== value ? (t0 = () => {
		setTimeout(() => {
			onSelect(value);
		}, 0);
	}, $[0] = onSelect, $[1] = value, $[2] = t0) : t0 = $[2];
	let handleClick = t0, t1;
	$[3] === handleClick ? t1 = $[4] : (t1 = (event) => {
		event.key === "Enter" && !_isEnterToClickElement(event.currentTarget) && handleClick();
	}, $[3] = handleClick, $[4] = t1);
	let handleKeyDown = t1, t2;
	return $[5] !== children || $[6] !== handleClick || $[7] !== handleKeyDown || $[8] !== id || $[9] !== selected ? (t2 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)("li", {
		"aria-selected": selected,
		"data-ui": "AutocompleteOption",
		id,
		role: "option",
		onClick: handleClick,
		onKeyDown: handleKeyDown,
		children
	}), $[5] = children, $[6] = handleClick, $[7] = handleKeyDown, $[8] = id, $[9] = selected, $[10] = t2) : t2 = $[10], t2;
}
/**
* @internal
*/
function autocompleteReducer(state, msg) {
	return msg.type === "input/change" ? {
		...state,
		activeValue: null,
		focused: !0,
		query: msg.query
	} : msg.type === "input/focus" ? {
		...state,
		focused: !0
	} : msg.type === "root/blur" ? {
		...state,
		focused: !1,
		query: null
	} : msg.type === "root/clear" ? {
		...state,
		activeValue: null,
		query: null,
		value: null
	} : msg.type === "root/escape" ? {
		...state,
		focused: !1,
		query: null
	} : msg.type === "root/open" ? {
		...state,
		query: state.query || msg.query
	} : msg.type === "root/setActiveValue" ? {
		...state,
		activeValue: msg.value,
		listFocused: msg.listFocused || state.listFocused
	} : msg.type === "root/setListFocused" ? {
		...state,
		listFocused: msg.listFocused
	} : msg.type === "value/change" ? {
		...state,
		activeValue: msg.value,
		query: null,
		value: msg.value
	} : state;
}
/**
* @internal
*/
var AUTOCOMPLETE_LISTBOX_IGNORE_KEYS = [
	"Control",
	"Shift",
	"Alt",
	"Enter",
	"Home",
	"End",
	"PageUp",
	"PageDown",
	"Meta",
	"Tab",
	"CapsLock"
];
var AUTOCOMPLETE_POPOVER_FALLBACK_PLACEMENTS = ["bottom-start", "top-start"];
var DEFAULT_RENDER_VALUE = (value, option) => option ? option.value : value;
var DEFAULT_FILTER_OPTION = (query, option) => option.value.toLowerCase().indexOf(query.toLowerCase()) > -1;
/**
* The Autocomplete component is typically used for search components.
* It consists of a text input for writing a query, and properties for rendering suggestions.
*
* @public
*/
function Autocomplete(props) {
	let $ = (0, import_compiler_runtime.c)(184), customValidity, disabled, filterOptionProp, forwardedRef, icon, id, loading, onBlur, onChange, onFocus, onQueryChange, onSelect, openButton, openOnFocus, optionsProp, prefix, readOnly, relatedElements, renderOptionProp, renderPopover, restProps, suffix, t0, t1, t2, t3, t4, t5, t6, valueProp;
	$[0] === props ? (customValidity = $[1], disabled = $[2], filterOptionProp = $[3], forwardedRef = $[4], icon = $[5], id = $[6], loading = $[7], onBlur = $[8], onChange = $[9], onFocus = $[10], onQueryChange = $[11], onSelect = $[12], openButton = $[13], openOnFocus = $[14], optionsProp = $[15], prefix = $[16], readOnly = $[17], relatedElements = $[18], renderOptionProp = $[19], renderPopover = $[20], restProps = $[21], suffix = $[22], t0 = $[23], t1 = $[24], t2 = $[25], t3 = $[26], t4 = $[27], t5 = $[28], t6 = $[29], valueProp = $[30]) : ({border: t0, customValidity, disabled, filterOption: filterOptionProp, fontSize: t1, icon, id, listBox: t2, loading, onBlur, onChange, onFocus, onQueryChange, onSelect, openButton, openOnFocus, options: optionsProp, padding: t3, popover: t4, prefix, radius: t5, readOnly, ref: forwardedRef, relatedElements, renderOption: renderOptionProp, renderPopover, renderValue: t6, suffix, value: valueProp, ...restProps} = props, $[0] = props, $[1] = customValidity, $[2] = disabled, $[3] = filterOptionProp, $[4] = forwardedRef, $[5] = icon, $[6] = id, $[7] = loading, $[8] = onBlur, $[9] = onChange, $[10] = onFocus, $[11] = onQueryChange, $[12] = onSelect, $[13] = openButton, $[14] = openOnFocus, $[15] = optionsProp, $[16] = prefix, $[17] = readOnly, $[18] = relatedElements, $[19] = renderOptionProp, $[20] = renderPopover, $[21] = restProps, $[22] = suffix, $[23] = t0, $[24] = t1, $[25] = t2, $[26] = t3, $[27] = t4, $[28] = t5, $[29] = t6, $[30] = valueProp);
	let border = t0 === void 0 || t0, fontSize = t1 === void 0 ? 2 : t1, listBox = t2 === void 0 ? EMPTY_RECORD : t2, paddingProp = t3 === void 0 ? 3 : t3, popover = t4 === void 0 ? EMPTY_RECORD : t4, radius = t5 === void 0 ? 2 : t5, renderValue = t6 === void 0 ? DEFAULT_RENDER_VALUE : t6, t7 = valueProp || null, t8 = valueProp || null, t9;
	$[31] !== t7 || $[32] !== t8 ? (t9 = {
		activeValue: t7,
		focused: !1,
		listFocused: !1,
		query: null,
		value: t8
	}, $[31] = t7, $[32] = t8, $[33] = t9) : t9 = $[33];
	let [state, dispatch] = (0, import_react.useReducer)(autocompleteReducer, t9), { activeValue, focused, listFocused, query, value } = state, t10;
	$[34] !== fontSize || $[35] !== paddingProp ? (t10 = (t11) => {
		let { value: value_0 } = t11;
		return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Card, {
			"data-as": "button",
			padding: paddingProp,
			radius: 2,
			tone: "inherit",
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Text, {
				size: fontSize,
				textOverflow: "ellipsis",
				children: value_0
			})
		});
	}, $[34] = fontSize, $[35] = paddingProp, $[36] = t10) : t10 = $[36];
	let renderOption = typeof renderOptionProp == "function" ? renderOptionProp : t10, filterOption = typeof filterOptionProp == "function" ? filterOptionProp : DEFAULT_FILTER_OPTION, rootElementRef = (0, import_react.useRef)(null), resultsPopoverElementRef = (0, import_react.useRef)(null), inputElementRef = (0, import_react.useRef)(null), listBoxElementRef = (0, import_react.useRef)(null), [inputElement, _setInputElement] = (0, import_react.useState)(null), t11;
	$[37] === Symbol.for("react.memo_cache_sentinel") ? (t11 = (node) => {
		(0, import_react.startTransition)(() => _setInputElement(node));
	}, $[37] = t11) : t11 = $[37];
	let setInputElement = t11, listFocusedRef = (0, import_react.useRef)(!1), valueRef = (0, import_react.useRef)(value), valuePropRef = (0, import_react.useRef)(valueProp), popoverMouseWithinRef = (0, import_react.useRef)(!1), t12, t13;
	$[38] === inputElement ? (t12 = $[39], t13 = $[40]) : (t12 = () => inputElement, t13 = [inputElement], $[38] = inputElement, $[39] = t12, $[40] = t13), (0, import_react.useImperativeHandle)(inputElementRef, t12, t13);
	let t14, t15;
	$[41] === inputElement ? (t14 = $[42], t15 = $[43]) : (t14 = () => inputElement, t15 = [inputElement], $[41] = inputElement, $[42] = t14, $[43] = t15), (0, import_react.useImperativeHandle)(forwardedRef, t14, t15);
	let listBoxId = `${id}-listbox`, options = Array.isArray(optionsProp) ? optionsProp : EMPTY_ARRAY$1, padding = _getArrayProp(paddingProp), t16;
	$[44] !== options || $[45] !== value ? (t16 = value === null ? void 0 : options.find((o) => o.value === value), $[44] = options, $[45] = value, $[46] = t16) : t16 = $[46];
	let currentOption = t16, t17;
	if ($[47] !== filterOption || $[48] !== options || $[49] !== query) {
		let t18;
		$[51] !== filterOption || $[52] !== query ? (t18 = (option) => !query || filterOption(query, option), $[51] = filterOption, $[52] = query, $[53] = t18) : t18 = $[53], t17 = options.filter(t18), $[47] = filterOption, $[48] = options, $[49] = query, $[50] = t17;
	} else t17 = $[50];
	let filteredOptions = t17, filteredOptionsLen = filteredOptions.length, activeItemId = activeValue ? `${id}-option-${activeValue}` : void 0, expanded = query !== null && loading || focused && query !== null, t18;
	$[54] !== onBlur || $[55] !== onQueryChange || $[56] !== relatedElements ? (t18 = (event) => {
		setTimeout(() => {
			if (popoverMouseWithinRef.current) return;
			let elements = (relatedElements || []).concat(rootElementRef.current ? [rootElementRef.current] : [], resultsPopoverElementRef.current ? [resultsPopoverElementRef.current] : []), focusInside = !1;
			if (document.activeElement) {
				for (let e of elements) if (e === document.activeElement || e.contains(document.activeElement)) {
					focusInside = !0;
					break;
				}
			}
			focusInside === !1 && (dispatch({ type: "root/blur" }), popoverMouseWithinRef.current = !1, onQueryChange && onQueryChange(null), onBlur && onBlur(event));
		}, 0);
	}, $[54] = onBlur, $[55] = onQueryChange, $[56] = relatedElements, $[57] = t18) : t18 = $[57];
	let handleRootBlur = t18, t19;
	$[58] === Symbol.for("react.memo_cache_sentinel") ? (t19 = (event_0) => {
		let listBoxElement = listBoxElementRef.current, focusedElement = event_0.target instanceof HTMLElement ? event_0.target : null, listFocused_0 = listBoxElement?.contains(focusedElement) || !1;
		listFocused_0 !== listFocusedRef.current && (listFocusedRef.current = listFocused_0, dispatch({
			type: "root/setListFocused",
			listFocused: listFocused_0
		}));
	}, $[58] = t19) : t19 = $[58];
	let handleRootFocus = t19, t20;
	$[59] !== onChange || $[60] !== onQueryChange || $[61] !== onSelect ? (t20 = (v) => {
		dispatch({
			type: "value/change",
			value: v
		}), popoverMouseWithinRef.current = !1, onSelect && onSelect(v), valueRef.current = v, onChange && onChange(v), onQueryChange && onQueryChange(null), inputElementRef.current?.focus();
	}, $[59] = onChange, $[60] = onQueryChange, $[61] = onSelect, $[62] = t20) : t20 = $[62];
	let handleOptionSelect = t20, t21;
	$[63] !== activeValue || $[64] !== filteredOptions || $[65] !== filteredOptionsLen || $[66] !== onQueryChange ? (t21 = (event_1) => {
		if (event_1.key === "ArrowDown") {
			if (event_1.preventDefault(), !filteredOptionsLen) return;
			let activeOption = filteredOptions.find((o_0) => o_0.value === activeValue), activeIndex = activeOption ? filteredOptions.indexOf(activeOption) : -1, nextActiveOption = filteredOptions[(activeIndex + 1) % filteredOptionsLen];
			nextActiveOption && dispatch({
				type: "root/setActiveValue",
				value: nextActiveOption.value,
				listFocused: !0
			});
			return;
		}
		if (event_1.key === "ArrowUp") {
			if (event_1.preventDefault(), !filteredOptionsLen) return;
			let activeOption_0 = filteredOptions.find((o_1) => o_1.value === activeValue), activeIndex_0 = activeOption_0 ? filteredOptions.indexOf(activeOption_0) : -1, nextActiveOption_0 = filteredOptions[activeIndex_0 === -1 ? filteredOptionsLen - 1 : (filteredOptionsLen + activeIndex_0 - 1) % filteredOptionsLen];
			nextActiveOption_0 && dispatch({
				type: "root/setActiveValue",
				value: nextActiveOption_0.value,
				listFocused: !0
			});
			return;
		}
		if (event_1.key === "Escape") {
			dispatch({ type: "root/escape" }), popoverMouseWithinRef.current = !1, onQueryChange && onQueryChange(null), inputElementRef.current?.focus();
			return;
		}
		let target = event_1.target, listEl = listBoxElementRef.current;
		if ((listEl === target || listEl?.contains(target)) && !AUTOCOMPLETE_LISTBOX_IGNORE_KEYS.includes(event_1.key)) {
			inputElementRef.current?.focus();
			return;
		}
	}, $[63] = activeValue, $[64] = filteredOptions, $[65] = filteredOptionsLen, $[66] = onQueryChange, $[67] = t21) : t21 = $[67];
	let handleRootKeyDown = t21, t22;
	$[68] === onQueryChange ? t22 = $[69] : (t22 = (event_2) => {
		let nextQuery = event_2.currentTarget.value;
		dispatch({
			type: "input/change",
			query: nextQuery
		}), onQueryChange && onQueryChange(nextQuery);
	}, $[68] = onQueryChange, $[69] = t22);
	let handleInputChange = t22, t23;
	$[70] !== currentOption || $[71] !== renderValue || $[72] !== value ? (t23 = () => {
		dispatch({
			type: "root/open",
			query: value ? renderValue(value, currentOption) : ""
		});
	}, $[70] = currentOption, $[71] = renderValue, $[72] = value, $[73] = t23) : t23 = $[73];
	let dispatchOpen = t23, t24;
	$[74] !== dispatchOpen || $[75] !== focused || $[76] !== onFocus || $[77] !== openOnFocus ? (t24 = (event_3) => {
		focused || (dispatch({ type: "input/focus" }), onFocus && onFocus(event_3), openOnFocus && dispatchOpen());
	}, $[74] = dispatchOpen, $[75] = focused, $[76] = onFocus, $[77] = openOnFocus, $[78] = t24) : t24 = $[78];
	let handleInputFocus = t24, t25;
	$[79] === Symbol.for("react.memo_cache_sentinel") ? (t25 = () => {
		popoverMouseWithinRef.current = !0;
	}, $[79] = t25) : t25 = $[79];
	let handlePopoverMouseEnter = t25, t26;
	$[80] === Symbol.for("react.memo_cache_sentinel") ? (t26 = () => {
		popoverMouseWithinRef.current = !1;
	}, $[80] = t26) : t26 = $[80];
	let handlePopoverMouseLeave = t26, t27;
	$[81] !== onChange || $[82] !== onQueryChange ? (t27 = () => {
		dispatch({ type: "root/clear" }), valueRef.current = "", onChange && onChange(""), onQueryChange && onQueryChange(null), inputElementRef.current?.focus();
	}, $[81] = onChange, $[82] = onQueryChange, $[83] = t27) : t27 = $[83];
	let handleClearButtonClick = t27, t28;
	$[84] === Symbol.for("react.memo_cache_sentinel") ? (t28 = () => {
		dispatch({ type: "input/focus" });
	}, $[84] = t28) : t28 = $[84];
	let handleClearButtonFocus = t28, t29, t30;
	$[85] === valueProp ? (t29 = $[86], t30 = $[87]) : (t29 = () => {
		if (valueProp !== valuePropRef.current) {
			valuePropRef.current = valueProp, valueProp !== void 0 && (dispatch({
				type: "value/change",
				value: valueProp
			}), valueRef.current = valueProp);
			return;
		}
		valueProp !== valueRef.current && (valueRef.current = valueProp || null, dispatch({
			type: "value/change",
			value: valueProp || null
		}));
	}, t30 = [valueProp], $[85] = valueProp, $[86] = t29, $[87] = t30), (0, import_react.useEffect)(t29, t30);
	let t31, t32;
	$[88] === focused ? (t31 = $[89], t32 = $[90]) : (t31 = () => {
		!focused && valueRef.current && dispatch({
			type: "root/setActiveValue",
			value: valueRef.current
		});
	}, t32 = [focused], $[88] = focused, $[89] = t31, $[90] = t32), (0, import_react.useEffect)(t31, t32);
	let t33, t34;
	$[91] !== activeValue || $[92] !== filteredOptions ? (t33 = () => {
		let listElement = listBoxElementRef.current;
		if (!listElement) return;
		let activeOption_1 = filteredOptions.find((o_2) => o_2.value === activeValue);
		if (activeOption_1) {
			let activeIndex_1 = filteredOptions.indexOf(activeOption_1), activeItemElement = listElement.childNodes[activeIndex_1];
			if (activeItemElement) {
				if (_hasFocus(activeItemElement)) return;
				focusFirstDescendant(activeItemElement);
			}
		}
	}, t34 = [activeValue, filteredOptions], $[91] = activeValue, $[92] = filteredOptions, $[93] = t33, $[94] = t34) : (t33 = $[93], t34 = $[94]), (0, import_react.useEffect)(t33, t34);
	let t35;
	bb0: {
		if (!loading && !disabled && value) {
			let t36;
			$[95] === Symbol.for("react.memo_cache_sentinel") ? (t36 = {
				"aria-label": "Clear",
				onFocus: handleClearButtonFocus
			}, $[95] = t36) : t36 = $[95], t35 = t36;
			break bb0;
		}
		t35 = void 0;
	}
	let clearButton = t35, openButtonBoxPadding = padding.map(_temp), openButtonPadding = padding.map(_temp2), openButtonProps = typeof openButton == "object" ? openButton : EMPTY_RECORD, t36;
	$[96] !== dispatchOpen || $[97] !== openButtonProps ? (t36 = (event_4) => {
		dispatchOpen(), openButtonProps.onClick && openButtonProps.onClick(event_4), _raf(() => inputElementRef.current?.focus());
	}, $[96] = dispatchOpen, $[97] = openButtonProps, $[98] = t36) : t36 = $[98];
	let handleOpenClick = t36, t37;
	$[99] !== disabled || $[100] !== expanded || $[101] !== fontSize || $[102] !== handleOpenClick || $[103] !== openButton || $[104] !== openButtonBoxPadding || $[105] !== openButtonPadding || $[106] !== openButtonProps || $[107] !== readOnly ? (t37 = !disabled && !readOnly && openButton ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Box, {
		"aria-hidden": expanded,
		padding: openButtonBoxPadding,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
			"aria-label": "Open",
			disabled: expanded,
			fontSize,
			icon: ChevronDownIcon,
			mode: "bleed",
			padding: openButtonPadding,
			...openButtonProps,
			onClick: handleOpenClick
		})
	}) : void 0, $[99] = disabled, $[100] = expanded, $[101] = fontSize, $[102] = handleOpenClick, $[103] = openButton, $[104] = openButtonBoxPadding, $[105] = openButtonPadding, $[106] = openButtonProps, $[107] = readOnly, $[108] = t37) : t37 = $[108];
	let openButtonNode = t37, t38;
	bb1: {
		if (query === null) {
			if (value !== null) {
				let t39;
				$[109] !== currentOption || $[110] !== renderValue || $[111] !== value ? (t39 = renderValue(value, currentOption), $[109] = currentOption, $[110] = renderValue, $[111] = value, $[112] = t39) : t39 = $[112], t38 = t39;
				break bb1;
			}
			t38 = "";
			break bb1;
		}
		t38 = query;
	}
	let inputValue = t38, t39;
	$[113] === listFocused ? t39 = $[114] : (t39 = (event_5) => {
		event_5.key === "Tab" && listFocused && inputElementRef.current?.focus();
	}, $[113] = listFocused, $[114] = t39);
	let handleListBoxKeyDown = t39, t40;
	bb2: {
		if (filteredOptions.length === 0) {
			t40 = null;
			break bb2;
		}
		let t41;
		if ($[115] !== activeValue || $[116] !== currentOption || $[117] !== filteredOptions || $[118] !== handleOptionSelect || $[119] !== id || $[120] !== listFocused || $[121] !== loading || $[122] !== renderOption) {
			let t42;
			$[124] !== activeValue || $[125] !== currentOption || $[126] !== handleOptionSelect || $[127] !== id || $[128] !== listFocused || $[129] !== loading || $[130] !== renderOption ? (t42 = (option_0) => {
				let active = activeValue === null ? currentOption === option_0 : option_0.value === activeValue;
				return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(AutocompleteOption, {
					id: `${id}-option-${option_0.value}`,
					onSelect: handleOptionSelect,
					selected: active,
					value: option_0.value,
					children: (0, import_react.cloneElement)(renderOption(option_0), {
						disabled: loading,
						selected: active,
						tabIndex: listFocused && active ? 0 : -1
					})
				}, option_0.value);
			}, $[124] = activeValue, $[125] = currentOption, $[126] = handleOptionSelect, $[127] = id, $[128] = listFocused, $[129] = loading, $[130] = renderOption, $[131] = t42) : t42 = $[131], t41 = filteredOptions.map(t42), $[115] = activeValue, $[116] = currentOption, $[117] = filteredOptions, $[118] = handleOptionSelect, $[119] = id, $[120] = listFocused, $[121] = loading, $[122] = renderOption, $[123] = t41;
		} else t41 = $[123];
		let t42;
		$[132] !== listBoxId || $[133] !== t41 ? (t42 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Stack, {
			as: "ul",
			"aria-multiselectable": !1,
			"data-ui": "AutoComplete__resultsList",
			id: listBoxId,
			ref: listBoxElementRef,
			role: "listbox",
			gap: 1,
			children: t41
		}), $[132] = listBoxId, $[133] = t41, $[134] = t42) : t42 = $[134];
		let t43;
		$[135] !== handleListBoxKeyDown || $[136] !== listBox || $[137] !== t42 ? (t43 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ListBox, {
			"data-ui": "AutoComplete__results",
			onKeyDown: handleListBoxKeyDown,
			padding: 1,
			...listBox,
			tabIndex: -1,
			children: t42
		}), $[135] = handleListBoxKeyDown, $[136] = listBox, $[137] = t42, $[138] = t43) : t43 = $[138], t40 = t43;
	}
	let content = t40, t41;
	bb3: {
		if (renderPopover) {
			let t42 = !expanded, t43;
			$[139] !== content || $[140] !== handlePopoverMouseEnter || $[141] !== handlePopoverMouseLeave || $[142] !== inputElement || $[143] !== renderPopover || $[144] !== t42 ? (t43 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(RenderPopover, {
				content,
				hidden: t42,
				inputElement,
				onMouseEnter: handlePopoverMouseEnter,
				onMouseLeave: handlePopoverMouseLeave,
				resultsPopoverElementRef,
				renderPopover
			}), $[139] = content, $[140] = handlePopoverMouseEnter, $[141] = handlePopoverMouseLeave, $[142] = inputElement, $[143] = renderPopover, $[144] = t42, $[145] = t43) : t43 = $[145], t41 = t43;
			break bb3;
		}
		if (filteredOptionsLen === 0) {
			t41 = null;
			break bb3;
		}
		let t42;
		$[146] !== content || $[147] !== expanded || $[148] !== handlePopoverMouseEnter || $[149] !== handlePopoverMouseLeave || $[150] !== inputElement || $[151] !== popover || $[152] !== radius ? (t42 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Popover, {
			arrow: !1,
			constrainSize: !0,
			content,
			fallbackPlacements: AUTOCOMPLETE_POPOVER_FALLBACK_PLACEMENTS,
			matchReferenceWidth: !0,
			onMouseEnter: handlePopoverMouseEnter,
			onMouseLeave: handlePopoverMouseLeave,
			open: expanded,
			overflow: "auto",
			placement: "bottom-start",
			portal: !0,
			radius,
			ref: resultsPopoverElementRef,
			referenceElement: inputElement,
			...popover
		}), $[146] = content, $[147] = expanded, $[148] = handlePopoverMouseEnter, $[149] = handlePopoverMouseLeave, $[150] = inputElement, $[151] = popover, $[152] = radius, $[153] = t42) : t42 = $[153], t41 = t42;
	}
	let results = t41, t42;
	$[154] === loading ? t42 = $[155] : (t42 = loading && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(SpinnerIcon, { className: "_1b6o2se1" }), $[154] = loading, $[155] = t42);
	let t43 = suffix || openButtonNode, t44;
	$[156] !== activeItemId || $[157] !== border || $[158] !== clearButton || $[159] !== customValidity || $[160] !== disabled || $[161] !== expanded || $[162] !== fontSize || $[163] !== handleClearButtonClick || $[164] !== handleInputChange || $[165] !== handleInputFocus || $[166] !== icon || $[167] !== id || $[168] !== inputValue || $[169] !== listBoxId || $[170] !== padding || $[171] !== prefix || $[172] !== radius || $[173] !== readOnly || $[174] !== restProps || $[175] !== t42 || $[176] !== t43 ? (t44 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(TextInput, {
		...restProps,
		"aria-activedescendant": activeItemId,
		"aria-autocomplete": "list",
		"aria-expanded": expanded,
		"aria-owns": listBoxId,
		autoCapitalize: "off",
		autoComplete: "off",
		autoCorrect: "off",
		border,
		clearButton,
		customValidity,
		disabled,
		fontSize,
		icon,
		iconRight: t42,
		id,
		inputMode: "search",
		onChange: handleInputChange,
		onClear: handleClearButtonClick,
		onFocus: handleInputFocus,
		padding,
		prefix,
		radius,
		readOnly,
		ref: setInputElement,
		role: "combobox",
		spellCheck: !1,
		suffix: t43,
		value: inputValue
	}), $[156] = activeItemId, $[157] = border, $[158] = clearButton, $[159] = customValidity, $[160] = disabled, $[161] = expanded, $[162] = fontSize, $[163] = handleClearButtonClick, $[164] = handleInputChange, $[165] = handleInputFocus, $[166] = icon, $[167] = id, $[168] = inputValue, $[169] = listBoxId, $[170] = padding, $[171] = prefix, $[172] = radius, $[173] = readOnly, $[174] = restProps, $[175] = t42, $[176] = t43, $[177] = t44) : t44 = $[177];
	let t45;
	return $[178] !== handleRootBlur || $[179] !== handleRootFocus || $[180] !== handleRootKeyDown || $[181] !== results || $[182] !== t44 ? (t45 = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(StyledAutocomplete, {
		"data-ui": "Autocomplete",
		onBlur: handleRootBlur,
		onFocus: handleRootFocus,
		onKeyDown: handleRootKeyDown,
		ref: rootElementRef,
		children: [t44, results]
	}), $[178] = handleRootBlur, $[179] = handleRootFocus, $[180] = handleRootKeyDown, $[181] = results, $[182] = t44, $[183] = t45) : t45 = $[183], t45;
}
function _temp2(v_1) {
	return Math.max(v_1 - 1, 0);
}
function _temp(v_0) {
	return v_0 === 0 ? 0 : v_0 === 1 || v_0 === 2 ? 1 : v_0 - 2;
}
function RenderPopover({ renderPopover, content, hidden, inputElement, onMouseEnter, onMouseLeave, resultsPopoverElementRef }) {
	return renderPopover({
		content,
		hidden,
		inputElement,
		onMouseEnter,
		onMouseLeave
	}, resultsPopoverElementRef);
}
//#endregion
export { isPausedCardinalityOneRelease as $, formatRelative as $t, filterReleasesForOverview as A, usePerspective as At, getReleaseDefaults as B, useVariantsStore as Bt, RELEASE_TYPES_TONES as C, useDocumentOperation as Ct, createPlugin as D, useDocumentVersionsObservable as Dt, createMediaLibraryClient as E, useDocumentVersions as Et, getCreatableVariantTarget as F, useReleasesUpsell as Ft, getTargetScopeId as G, isDefaultCrop as Gt, getReleaseTone as H, isString as Ht, getDocumentIsInPerspective as I, useResolveDefaultComponent as It, isCardinalityOneRelease as J, isFileSource as Jt, getVariantPublishedSibling as K, isDefaultHotspot as Kt, getDocumentPairPermissions as L, useTargetDocumentState as Lt, formatRelativeLocale as M, useRecordDocumentHistoryEvent as Mt, formatRelativeLocalePublishDate as N, useReleaseOperations as Nt, createReleaseId as O, useImageUrl as Ot, getBundleDocumentsObservable as P, useReleasesStore as Pt, isNotArchivedRelease as Q, useDevicePixelRatio as Qt, getPairTarget as R, useToken as Rt, PreviewLoader as S, useDataset as St, TitleSkeleton as T, useDocumentPairPermissionsFromHookFactory as Tt, getReleasesPerspectiveStack as U, getImageDimensions as Ut, getReleaseDocumentIdFromReleaseId as V, createImageUrlBuilder as Vt, getTargetDocument as W, isAssetObjectStub as Wt, isErrorWithDetails as X, isImageSource as Xt, isDraftPerspective as Y, isImageAssetId as Yt, isGoingToUnpublish as Z, getDevicePixelRatio as Zt, Media as _, useActiveReleases as _t, BlockImagePreview as a, delay as an, isString$1 as at, PUBLISHED$1 as b, useAllVariants as bt, CDN_HOST_STAGING as c, concatWith as cn, pickFieldComponent as ct, DEFAULT_RELEASE_TYPE as d, pickItemComponent as dt, mergeMapArray as en, isPublishedPerspective as et, DefaultPreview$1 as f, pickPortableTextEditorPluginsComponent as ft, LinearProgress as g, useAccessPolicy as gt, LATEST as h, unstable_useValuePreview as ht, ARCHIVED_RELEASE_STATES as i, isAgentBundleName as in, isReleaseType as it, flattenConfig as j, usePreviewComponent as jt, definePlugin as k, useMiddlewareComponents as kt, CircularProgress as l, iif as ln, pickInlineBlockComponent as lt, InlinePreview as m, sortReleases as mt, API_HOST_PRODUCTION as n, INITIAL_STATE$4 as nn, isReleaseLimitError as nt, BlockPreview as o, delayWhen as on, pickAnnotationComponent as ot, DetailPreview as p, shouldShowReleaseInView as pt, isCardinalityOnePerspective as q, isFileAssetId as qt, API_HOST_STAGING as r, createAgentBundlesStore as rn, isReleaseScheduledOrScheduling as rt, CDN_HOST_PRODUCTION as s, mapTo as sn, pickBlockComponent as st, Autocomplete as t, clsx as tn, isReleaseDocument as tt, CompactPreview as u, pickInputComponent as ut, MediaPreview as v, useActiveWorkspace as vt, SanityDefaultPreview as w, useDocumentPairPermissions as wt, Preview as x, useBundleDocuments as xt, PREVIEW_SIZES as y, useAllReleases as yt, getPublishDateFromRelease as z, useValuePreview as zt };
